# This legacy module is stored with CRLF in Git; keep byte-level line endings
# stable so a focused edit does not become a whole-file normalization diff.
src/second-panel-ui.ts -text whitespace=cr-at-eol
