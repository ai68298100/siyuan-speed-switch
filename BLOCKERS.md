# 阻塞

（2026-09-12 v0.16.38 基线：无未决的工程阻塞）

（当前无阻塞）

- T-022 Android 真机回归仍等待用户设备与空闲时间；这只限制最终验收，不阻塞本地开发和自动化门禁。

- [x] B-001 git push 被网络间歇拦截 | 已解决：2b792ce 于 2026-09-12 成功推送远端
- [x] B-002 全文搜索索引为空 | 已解决：宿主索引随重启自愈；同时发现并修复了真正的插件侧根因——empty `types` 在 3.8.x 内核意为"不搜任何类型"，全文回退从未命中；修复后"腾讯会议"真机返回 12 篇命中


- [x] B-003 Mimosa L3 提交门禁拦截 | 已解决（真根因：扫描器把 .exec( 字样当 shell 命令执行——正则 .exec 与 .match 等价，替换后高危清零）| 全链路修复：字面量端点分发 + 搜索管线字面量 + 载荷钳制 + exec→match；529/529 测试、verify:release 全绿
- B-004（环境）Android 真机回归工具链不可用：当前环境无 `adb`、`java`，且没有连接 Android 设备。影响仅限 T-022 的真实触控/旋转/安装升级验收；本地类型检查、构建、自动测试和 Chromium smoke 不受影响。
- B-005（环境，**2026-09-16 部分解除**）真实桌面路径筛选/窄侧栏验收缺少可安全复用的已认证 SiYuan 会话。**进展**：本机内核（`SiYuan-Kernel.exe` PID 20364，`127.0.0.1:6806`）已可用，通过工作空间 `conf.json` 的 API token 取得**已认证只读会话**；实测内核 `3.8.4-beta.2`，`/api/notebook/lsNotebooks` 200/63ms、**`/api/filetree/listDocsByPath` 200/22ms 且响应结构与 `path-filter-model.js` 假设逐项一致**，边界行为（不存在路径返回空列表而非错误、`maxListCount` 被遵守）亦已采集——详见 `docs/path-filter-host-evidence.md`（脱敏）。**API 层证据已不再是阻塞项**；T-107 三项要求中的"端点可用性""响应结构"**已完成**，仅剩**"最窄可用侧栏宽度"**属 UI 度量需另行取证。仍待用户确认的边界：此会话为长期个人工作空间，非隔离测试实例，故真实写入类验证（T-1219 的 open/open-batch 确认弹窗）仍应谨慎，仅做只读验证。**T-103 的包体前置已单独解除**（D-353/D-355），其实现方案见 `docs/path-filter-desktop-plan.md`（D-357）。
- B-006（环境，非工程阻塞）本轮开发机复查 `api.bgm.tv:443` 时连接超时，无法把一次在线响应冒充持续可用证据；实现依据官方 schema 并具备超时、缓存和组件级失败隔离，真实网络/代理/封面加载由本地思源集市实测补证，不阻塞自动化门禁。
- B-007（环境，非工程阻塞）开发机未运行 ActivityWatch，`127.0.0.1:5600/api/0/info` 两秒只读探测不可达；本轮已覆盖官方 Query 契约、固定 loopback 路由、思源代理、聚合响应和缓存自动测试，真实应用用时仍需在已安装 ActivityWatch 的桌面宿主补验。
