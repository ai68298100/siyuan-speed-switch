const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const css=fs.readFileSync(path.join(__dirname,'..','src','index.scss'),'utf8');
test('title min width zero',()=>assert.match(css,/strong[\s\S]*?min-width: 0/));
test('title max width full',()=>assert.match(css,/strong[\s\S]*?max-width: 100%/));
test('description min width zero',()=>assert.match(css,/span[\s\S]*?min-width: 0/));
test('description max width full',()=>assert.match(css,/span[\s\S]*?max-width: 100%/));
test('status min width zero',()=>assert.match(css,/\.sw-home-store__status[\s\S]*?min-width: 0/));
test('status max width full',()=>assert.match(css,/\.sw-home-store__status[\s\S]*?max-width: 100%/));
test('group min width zero',()=>assert.match(css,/\.sw-home-store__group[\s\S]*?min-width: 0/));
test('group max width full',()=>assert.match(css,/\.sw-home-store__group[\s\S]*?max-width: 100%/));
test('summary min width zero',()=>assert.match(css,/\.sw-home-store__summary[\s\S]*?min-width: 0/));
test('summary max width full',()=>assert.match(css,/\.sw-home-store__summary[\s\S]*?max-width: 100%/));
test('width rules preserve wrap',()=>assert.match(css,/overflow-wrap: anywhere/));
test('width rules preserve word break',()=>assert.match(css,/word-break: break-word/));
test('width rules preserve hyphens',()=>assert.match(css,/hyphens: auto/));
test('width rules preserve pretty wrap',()=>assert.match(css,/text-wrap: pretty/));
test('width rules preserve balance',()=>assert.match(css,/text-wrap: balance/));
test('width rules preserve mobile',()=>assert.match(css,/max-width: 560px/));
test('width rules preserve print',()=>assert.match(css,/@media print/));
test('width rules preserve forced colors',()=>assert.match(css,/forced-colors: active/));
test('width rules preserve reduced motion',()=>assert.match(css,/prefers-reduced-motion: reduce/));
test('width rules standard min width',()=>assert.match(css,/min-width: 0/));
test('width rules standard max width',()=>assert.match(css,/max-width: 100%/));
test('width rules avoid fixed title width',()=>assert.match(css,/strong[\s\S]*?max-width: 100%/));
test('width rules avoid fixed description width',()=>assert.match(css,/span[\s\S]*?max-width: 100%/));
test('width rules avoid fixed status width',()=>assert.match(css,/\.sw-home-store__status[\s\S]*?max-width: 100%/));
test('width rules avoid fixed group width',()=>assert.match(css,/\.sw-home-store__group[\s\S]*?max-width: 100%/));
test('width rules avoid fixed summary width',()=>assert.match(css,/\.sw-home-store__summary[\s\S]*?max-width: 100%/));
test('width rules preserve card scope',()=>assert.match(css,/sw-home-store__card-head/));
test('width rules preserve status scope',()=>assert.match(css,/sw-home-store__status/));
test('width rules preserve group scope',()=>assert.match(css,/sw-home-store__group/));
test('width rules preserve summary scope',()=>assert.match(css,/sw-home-store__summary/));
test('width contract deterministic',()=>assert.ok(css.includes('min-width: 0')&&css.includes('max-width: 100%')));
