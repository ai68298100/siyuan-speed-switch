# 小驴雷切（LvSpeed Switch）

[![Version](https://img.shields.io/badge/version-0.23.5-blue)](./plugin.json) [![License: MIT](https://img.shields.io/badge/license-MIT-green)](./LICENSE) [![SiYuan](https://img.shields.io/badge/SiYuan-%E6%80%9D%E6%BA%90%E7%AC%94%E8%AE%B0-ff5c67)](https://b3log.org/siyuan)

小驴雷切是思源笔记的轻量导航工作区：以**已打开页签**为第一优先级，通过实时缩略图完成快速预览和切换；需要时再展开到**收藏夹、全库文档搜索、面板、日记和自定义快捷入口**。桌面弹窗、右侧栏和手机端共享同一套数据与命令，但会根据空间和输入方式采用不同布局。

<p align="center"><img src="preview.png" width="720" alt="小驴雷切预览"/></p>

<p align="center"><img src="docs/interface-map.svg" width="860" alt="小驴雷切桌面弹窗、右侧栏与手机端界面分布图"/></p>

> v0.23.5 是稳定性版本：跨设备同步不再整体重载插件（已打开的切换器与第二面板不再闪断、搜索会话不再被销毁），桌面端用 Escape 关掉日记笔记本选择弹窗后不再偶发失灵，打开页签较多时「最近编辑」排序与日历月视图不再静默漏内容，失效数据源也不再在每个刷新周期重复付出最长 10 秒超时。

> 当前开发策略：开发头已通过类型检查、生产构建、6299 项自动测试、移动端与 Chromium UI 烟测；已接入时间、天气、空气质量、节假日日历、Bangumi 每日放送、DailyHotApi 热搜、NewsNow 资讯、Hacker News 榜单、ActivityWatch 使用时长、iCal 订阅日程、RSS/Atom 订阅、GitHub 贡献热力图、小驴打卡组件（需安装小驴打卡）、内核数据组件与可搜索/可选列的数据库表格（见 ADR 0058）。最近打开、数据库表格、随机回顾、收藏、文档集、指定文档、置顶文档、数据库、已存筛选、最近更新、近期编辑、当前文档大纲、文档关系摘要、标签、书签、剪藏待读、往年今日、近期日记、今日日记、本月日记、日历月视图、今日待办、闪卡待复习、快速记录、近期预约、插件命令、收集箱、笔记统计、今日写作、近期写作活跃度和写作打卡、年度进度、数据健康、倒数日、时间与日期、世界时钟、近期天气、空气质量、每日放送、热搜事件、实时资讯、Hacker News 热门和 RSS 订阅共 58 个组件已完成逐组件深度优化（51 个完整评分卡 + 7 个打卡桥接渲染增强，T-6348~T-6454）；页签面板支持刷新与一级路径分组。同步期间组件面板保持稳定；Agent 仍保持既有只读审计与受控动作边界，不开放新的隐式写入；路径筛选真实宿主能力、窄侧栏、ActivityWatch 实机和 Android 真机验收继续作为兼容性补充。

## 目录

- [思源原生 Agent 能力](#思源原生-agent-能力)
- [核心能力](#核心能力)
- [快速上手](#快速上手)
- [快捷键](#快捷键)
- [设置](#设置)
- [安装与升级](#安装与升级)
- [环境要求与兼容边界](#环境要求与兼容边界)
- [发布前检查](#发布前检查)
- [更新日志](#更新日志)
- [架构与测试](#架构与测试)
- [开发](#开发)
- [开发路线](#开发路线)

## 思源原生 Agent 能力

在支持 `addAgentCapability` 的思源版本（当前按 3.8.4 源码契约适配）中，插件会按宿主能力检测注册十一项能力：

| 能力 | 类型 | 说明 |
| --- | --- | --- |
| `navigation-state` | 只读 | 当前页签、最近打开、最近关闭和收藏摘要（均为有界快照） |
| `search-documents` | 只读 | 受限文档搜索（笔记本、路径、内容类型、子类型、搜索方式、结果排序筛选；支持有界 `limit/offset` 分页并返回 `total/truncated`） |
| `get-document-outline` | 只读 | 指定文档的标题大纲（标题文本 + 层级 + 标题块 ID，可配合打开文档定位章节） |
| `home-widget-snapshot` | 只读 | 组件面板数据快照：按 `moduleId` 查询任意已注册组件（含第三方接入组件）的有界条目 |
| `workspace-context` | 只读 | 工作区一次快照：设备端、活动文档、打开页签、最近关闭、文档集、快捷入口、今日日记状态（只探测不创建）与存储演练健康（有界投影） |
| `home-adapter-diagnostics` | 只读 | 组件适配器诊断摘要：最近成功、缓存、超时和失败状态的有界投影（不含异常对象与敏感文本） |
| `open-document` | 受控导航 | 打开指定文档并定位（仅切换页签，不修改任何笔记数据） |
| `open-documents` | 受控导航 | 批量打开最多 5 篇文档组成工作区（弹窗列出全部标题确认，拒绝或超时一篇不开） |
| `update-task-status` | 受控写 | 切换任务块勾选状态（执行前强制弹窗确认，30 秒超时视为拒绝；仅改勾选标记不改写文本） |
| `create-document` | 受控写 | 在指定笔记本下新建文档（可带初始 Markdown；`notebook` 参数支持 ID 或名称），执行前强制弹窗确认 |
| `append-to-journal` | 受控写 | 向今日日记末尾追加一条内容（日记不存在时自动创建），执行前强制弹窗确认 |

只读能力声明 `localRead` 且不声明写入、外传或外部成本；受控导航不声明写入。所有能力的输入、输出和文本长度均有边界；旧版思源会自动跳过注册，不影响页签切换和手机端加载。能力由思源 Agent 的策略和生命周期统一管理，插件卸载时由宿主清理。受控写操作（如修改任务状态）会在只读层完成人工确认、取消和权限拒绝测试后再逐步开放，详见 [ROADMAP.md](./ROADMAP.md) 的 AI 能力专项。

[English README](./README.en-US.md)

## 核心能力

### 页签切换与实时刷新

- **实时缩略图**：每个页签以卡片展示当前文档内容；后台未渲染文档按需通过内核 API 补齐，视口外内容延迟渲染。
- **分栏原生支持**：按思源窗口/分栏分组，切换时激活正确分栏；页签打开、关闭或批量变化会通知所有已打开的插件视图立即刷新。
- **键盘与鼠标**：方向键和 `Tab` 按真实网格移动，`Enter` 打开，`Esc` 关闭；卡片支持置顶、收藏、关闭和右键菜单。
- **六种排序**：最近使用、打开顺序、打开倒序、最近编辑、标题升序、标题降序，修改后持久化。
- **统一历史入口**：桌面弹窗、侧栏和手机工具栏共享时钟入口；最近打开与最近关闭分区展示，已关闭文档可点击重新打开，失效记录可单项清理。
- **桌面全屏**：仅桌面弹窗提供全屏切换；侧栏和手机端不显示无效的全屏操作。

### 收藏夹与顺序管理

- 收藏使用稳定的文档 rootID，页签关闭或思源重启后仍可重新打开。
- 收藏夹显示序号和数量，可折叠、重命名、删除、上移和下移；桌面端还支持拖拽收藏夹排序。
- 收藏内容收纳在所属收藏夹内部，显示组内序号，可移动到其他组、组内上移/下移；桌面端支持组内拖拽。
- 手机设置页禁用整行拖拽，避免与上下滚动冲突，使用明确的移动按钮完成相同操作。
- 收藏夹可一键打开或关闭组内页签，并对重复文档、失败项和无需处理的状态分别反馈。

### 分层卡片搜索

搜索结果保持固定优先级：

1. **已打开页签**：本地即时过滤，继续保留窗口分组、置顶、排序和键盘导航。
2. **已打开文档内容**：在最多 6 个已打开根文档内调用思源搜索，命中时仍只恢复已有页签，不新增重复卡片。
3. **全库文档**：先按标题查询；标题无结果或启用高级筛选时，回退到受限原生全文搜索，按根文档聚合为卡片和少量片段。

搜索请求使用 180ms 防抖、容量受限的内存缓存、请求版本校验和取消机制。桌面弹窗、右侧栏和手机端各自持有独立搜索会话，互不取消或覆盖。当前工作树支持笔记本、内容类型、子类型、搜索方式和结果排序筛选；仅笔记本/路径筛选保留标题快路径，其他筛选会直接使用受限的原生块级全文请求。全库结果一次最多拉取 33 条，首屏渲染 12 条，超出时通过面板内「加载更多」按钮纯客户端增量展开（不换缓存 key、不发新请求），全部展开后才回落到思源原生搜索出口。路径树选择仍未开放 UI，且本功能不替代思源原生搜索页。后续计划见 [ROADMAP.md](./ROADMAP.md)。

### 面板、日记与快捷入口

- **左侧面板栏**：快速打开文件树、大纲、书签、标签、关系图、反链以及插件 Dock；支持完整显示、仅图标和完全隐藏。
- **右侧栏模式**：页签卡片常驻思源右侧 Dock，缩略图随宽度缩放，可选择放大填满或自动增加列数。
- **当日日记**：桌面与手机工具栏均保留独立入口；可设置默认日记本，未设置时首次使用会要求选择。
- **快捷入口工作区**：桌面默认显示在底部，也可切换到更窄的右侧栏；侧栏和手机端按各自配置显示。
- **文档集**：设置页支持保存当前文档、同名覆盖、行内重命名、删除、恢复预览、受控恢复以及 JSON 导入/导出；导入文件限制为 512 KiB 且合并前需要确认，恢复按保存顺序执行，恢复前会有界预检缺失和无法确认项，支持取消并显示成功、失败、已打开跳过和缺失跳过计数，跨端宿主行为仍需最终验证。
- **四类动作来源**：内置动作、思源 Dock 面板、其他插件公开的命令、第三方插件通过 `registerQuickAction()` 注册的运行时适配器。
- **可配置项**：四字以内短标签、图标、桌面/侧栏/手机端开关、启用状态、排序，以及配置导入导出；`+` 按钮直接进入快捷入口设置。
- 外部插件未加载时只保留入口配置并安全跳过，不轮询、不注入 DOM，不影响页签主体性能。

### 三端界面策略

| 界面 | 主要入口与交互 |
| --- | --- |
| 桌面弹窗 | 搜索、收藏、最近打开、排序、全屏、侧栏、日记、设置；左侧面板栏；底部或右侧快捷入口 |
| 右侧栏 | 紧凑搜索和工具栏；响应式页签/搜索卡片；侧栏快捷入口；不显示全屏 |
| 手机端 | 紧凑排序菜单、收藏、日记、设置；单/双/自动列；底部自定义入口和 `+`；不显示全屏 |

手机端首次打开时会等待 WebView 得到稳定尺寸后再显示卡片，并在缩略图容器获得真实宽度后计算缩放，减少首次进入的瞬时错位。手机设置页采用顶部横向标签和单列控件，收藏与快捷入口使用按钮排序，避免拖拽和页面滚动争抢手势。

### 性能、数据与主题

- 页签本地过滤和切换不等待全库 API 或第三方插件；搜索失败仍保留已打开页签，标题搜索 API 不可用时继续尝试已打开文档内容和受限全文层，只有兼容回退全部不可用才显示错误态。
- 缩略图按视口懒渲染，缓存按文档保存并设置单条上限；关闭无关页签后清理失效缓存。
- MRU、收藏、置顶、设置和快捷入口在读取时校验与去重；收藏最多 512 条、置顶最多 64 条、收藏分组最多 64 条，运行时读取也会防御性清洗损坏数据，并可生成只读容量状态摘要，写入采用防抖并在卸载前等待完成。
- UI 使用思源主题变量和原生图标，约束按钮、卡片、开关和文字尺寸，统一空态、载入态、失败态的视觉层级与读屏语义，兼容默认明暗主题及 Neo 等第三方主题。
- 当前未发布的 R7 视觉方案在上述约束之上增加淡紫主色、浅蓝灰背景层级、圆角卡片、柔和阴影和少量暖色强调；桌面、侧栏、第二面板、设置页与移动端共享 token，但保留各端布局差异。
- 对缺少 `AbortController`、`IntersectionObserver`、`ResizeObserver` 或 `MutationObserver` 的旧 WebView，搜索取消、缩略图懒加载和尺寸/收藏监听会自动降级，不阻断页签切换主流程。

## 快速上手

1. **打开**：顶部工具栏右侧布局图标，或快捷键 `Alt+Shift+S`（可在 **设置 → 快捷键** 修改）；手机端点顶栏入口或悬浮按钮。
2. **切换**：点击卡片，或方向键 / `Tab` 移动后 `Enter`；左侧点击面板直达。
3. **管理**：卡片图钉置顶、星标收藏（点击星标弹出分组菜单）；右上角 × 关闭页签，右键卡片还有完整菜单（手机端长按卡片）。
4. **搜索**：在同一输入框中先看匹配页签，再看全库文档标题卡片。
5. **常驻**：桌面端通过侧栏按钮把切换器固定到右侧 Dock。
6. **第二面板**：顶栏「第二面板」按钮或 `Alt+Shift+P` 打开聚合页（最近打开与收藏），底部快捷入口栏也有固定入口。
7. **自定义**：点击底部或右侧入口栏中的 `+`，选择 Dock、其他插件命令或调整三端可见性。
8. **尺寸**：桌面端面板默认按屏幕比例自适应打开；弹窗内全屏按钮可临时铺满，再点或按 `Esc` 恢复。

## 快捷键

| 按键 | 动作 |
| --- | --- |
| `Alt+Shift+S` | 打开 / 关闭切换器（全局，可在设置中改） |
| `Alt+Shift+P` | 打开第二面板（默认，可在设置中改；在思源 设置 → 快捷键 绑定"全局"后可在思源未获焦时唤起） |
| `↑` `↓` `←` `→` | 按网格移动选择 |
| `Tab` / `Shift+Tab` | 下一项 / 上一项 |
| `Enter` | 切换到选中页签 |
| `Shift+F10` 或 `ContextMenu` | 打开选中卡片的动作菜单（免鼠标） |
| `Esc` | 关闭切换器 |

## 设置

**设置 → 插件 → 小驴雷切 → 设置**（切换器内齿轮按钮直达）。桌面端使用左侧标签，手机端使用顶部可横向滚动标签；改动即时保存：

| 标签页 | 可配置项 |
| --- | --- |
| 外观 | 面板尺寸模式（自适应屏幕比例 / 固定尺寸 / 全屏铺满）、自适应比例（50–100）、固定宽高（480–1920 × 360–1280）、缩略图列数（自动 / 2–8）、缩略图高度（72–360） |
| 分组与排序 | 顶栏「分组 · 排序」按钮一体化设定：分组方式（按笔记本 / 按收藏 / 按创建月份 / 不分组，默认按笔记本）+ 组内排序（六种）；分组块按内容宽度并排流动，组头可折叠 |
| 行为 | 默认排序方式、最近打开记录 |
| 面板 | 左侧面板列表显示 / 隐藏、显示方式（完整 / 折叠图标 / 隐藏）、侧边栏缩略图布局（放大 / 自动增列） |
| 收藏 | 收藏夹折叠、序号、新建 / 重命名 / 删除 / 排序，组内收藏排序和分组调整 |
| 快捷入口 | 标签、图标、三端可见性、启用、拖拽或按钮排序、右侧入口栏、配置导入导出 |
| 日记 | 默认日记笔记本（下拉选择，首次点击日记按钮也会弹出选择） |
| 手机端 | 悬浮按钮开关（默认关）、卡片布局（单列 / 双列 / 自动）、缩略图高度 |

## 安装与升级

- **集市安装**：思源 **设置 → 集市 → 插件** 搜索「小驴雷切 / LvSpeed Switch」（待上架社区集市）。
- **手动安装**：从 [Releases](https://github.com/ai68298100/siyuan-speed-switch/releases) 下载 `package.zip`，解压到 `<工作空间>/data/plugins/siyuan-speed-switch/` 后重启思源（文件夹名必须为 `siyuan-speed-switch`）。

从旧版本升级会保留收藏、分组、置顶、MRU 和设置。`v0.16.9` 首次读取快捷入口配置时会执行字段校验；无效项会被忽略，合法配置不会因对应第三方插件暂时未加载而被删除。

## 环境要求与兼容边界

- 思源笔记 **v3.8.0+**（`minAppVersion` 已自 v0.24.0 起抬升，见 ADR 0064；旧版宿主会静默禁用本插件，请先升级思源本体）。
- 桌面客户端 / 浏览器桌面端前端（支持页签与分栏）。
- 内核数据组件（置顶文档、收集箱、最近更新、数据健康、最近文档、数据库导航、已存筛选、数据库表格）按思源 **v3.8.0+** 内核契约适配。收集箱需登录思源账号并同步收集箱数据。
- Agent 能力仅在宿主提供 `addAgentCapability` 时注册；宿主能力缺失时安全跳过，不影响基础切换。
- 路径树筛选暂不开放 UI：当前只保留模型和请求层，避免依赖不稳定的宿主路径树接口。
- 浏览器模拟仅用于结构和样式检查，不能替代 Android 思源真机验收。
- 真实安装、升级、卸载及主题显示检查属于发布前人工步骤。

## 发布前检查

当前候选状态与自动门禁/真实宿主验收边界见 [发布准备矩阵](docs/release-readiness.md)。

自动检查使用一条固定命令完成：

```bash
pnpm verify:release
```

它依次执行类型检查、生产构建、完整自动测试、移动端 UI 烟测和 Chromium 样式烟测。发布工作流还会在构建后强制检查 `package.zip` 的白名单、版本元数据、远程依赖和 512 KiB 体积上限（思源官方对插件包大小没有限制，该上限为项目自律线，2026-09-15 由 320 KiB 上调）。

自动门禁通过后，再在真实思源环境逐项确认：

1. 桌面弹窗：打开、搜索、排序、收藏、最近记录、日记、设置和全屏。
2. 右侧栏：窄宽度下工具栏、卡片缩放、快捷入口和历史浮层。
3. Android：首帧、单/双列卡片、排序/收藏底部面板、设置页滚动和安全区。
4. 主题：默认明暗主题、Neo 等第三方主题，以及窗口缩放和旋转后的布局。
5. 生命周期：安装、升级、卸载、重启后数据迁移，以及 API 失败/取消/权限拒绝。

本版本已正式发布为 `v0.23.5`

## 更新日志

### v0.23.5（2026-09-19）

- **同步不再重载整个插件**：思源在插件存储数据变化时默认整体重载该插件，而本插件有 13 个持久化
  key——每次跨设备同步合并，或另一个窗口写盘，都会把已打开的切换器、第二面板和搜索会话一并销毁
  （表现为图标闪断、弹窗凭空关闭）。现在覆写 `onDataChanged`：只做有界重读 + 惰性刷新，并强制该
  链路内不得写盘（写盘会再次广播数据变更、回到同一个钩子形成回环）。连续广播合并处理，手动刷新与
  强制刷新路径不受影响。
- **桌面端日记入口不再偶发失灵**：「选择日记笔记本」弹窗此前只在「确认/取消」两个按钮上落定返回值，
  用 Escape 或点击遮罩关闭时等待结果的那条链会永久挂起——于是下一次点日记按钮没有反应。关闭事件现在
  统一走宿主 `Dialog` 的 `destroyCallback`，不区分平台都会兑现。
- **打开页签较多时排序与日历不再漏内容**：内核 `/api/query/sql` 在语句没有外层 `LIMIT` 时按
  `search.limit`（默认 64、下限 32）截断，而插件此前从不读取响应里的 `truncated`。已修正「最近编辑」
  排序的更新时间查询（它此前还绕过了同源端点分发、没有超时保护）、日历月视图的上限与稳定排序，以及
  设置中数据库/文档两个选择器的上限与截断告警。
- **失效数据源不再反复拖慢刷新**：生活组件此前每个刷新周期都会对不可达端点重付一次最长 10 秒超时。
  现在按端点记录 20 秒失败抑制窗，窗内直接走组件已有的「陈旧数据 / 空态」分支；任意一次成功（含手动
  刷新）立即解除抑制。
- **长时间运行的稳定性**：弹窗释放全部改挂宿主 `destroyCallback`（此前有 8 处覆写宿主方法并配合轮询
  `isConnected`），插件源码内的 `setInterval` 已归零。思源对插件禁用/卸载只给 5 秒共享拆除预算，而
  轮询定时器正是活在该预算之外的泄漏源。
- **内部**：新增对思源官方 API 契约快照的校验门（25 个内核端点的请求与响应形状、端点分发面一致性、
  契约与真实宿主分歧的显式登记），并把持久化 key 清单收敛为唯一来源。

### v0.23.4（2026-09-19）

- **「打卡摘要」组件改为原生桥接**：此前它以第三方提供方形式登记在组件目录，商店按
  提供方握手判定可用性，即使小驴打卡已安装并启用，也一直显示「需安装小驴打卡插件后
  可用」。现在它与其余六个打卡组件同等实现原生桥接（`checkin-summary` 进入桥接清单，
  capability `items.read`），小驴打卡在位即就绪。
- **摘要内容增强**：新增聚合快照——今日完成、最长连续、本月天数、今日待续行与连击
  Top3；全部在本地计算，只读、无网络请求。
### v0.23.3（2026-09-19）

- **修复集市更新后被同步回退的问题**：安装包的 zip 条目时间戳从此前的固定纪元改为
  发版提交时间。此前固定纪元会让安装出的文件修改时间早于云同步索引，自动同步按
  修改时间判定“云端更新”并把刚更新的版本覆盖回旧版（仅本插件受影响：只有本插件的
  构建管线使用固定纪元时间戳）。
- **修复按笔记本分组出现“未知笔记本”**：页签缺失显式笔记本元数据时，此前会从文档
  路径首段错误地推导笔记本 ID（那是根文档 ID）。现在打开笔记本分组面板时自动用一次
  有界查询从内核恢复这些页签的真实笔记本归属并重排，组标签不再显示“未知笔记本”。
- **组件卡片头部瘦身**：配置按钮改为齿轮图标（悬停提示保留原语义），更新时间收窄，
  首行不再把标题挤成省略号。
- **图标尺寸首帧兜底**：切换器工具栏全部图标带显式宽高，样式未就绪时也不会闪现
  放大的黑色图标；组件配置弹窗纳入图标钳制观察器覆盖。
- **其他**：贡献热力图新增“少 → 多”色阶图例；性能微基准门禁 best-of-3 抗负载抖动。
### v0.23.2（2026-09-19）

- **修复数据库表格对嵌入/镜像数据库显示空表**：3.8.4 内核下 av 块 ID 与数据库 ID 不一致，
  现已两级解析（块 ID → getAttributeView → 库 ID + viewID + pageSize 重试）；配置搜索支持
  直接粘贴库 ID 绑定（独立数据库不产生 av 块，SQL 发现不到）。
- **数据健康**：缺失资源条目可点击跳转到引用该资源的文档块（真正的处理入口）；引用展示
  改读真实响应字段（item），不再读取内核不返回的 path。
- **排序确定性**：用户内容（名称/标题/标签/路径等）排序显式钉定中文拼音 Collator，
  不再随设备环境漂移（CI 镜像升级实证的跨环境差异）。
- **工程**：性能微基准门禁 best-of-3 抗负载抖动；新增 iCal TZID/RRULE/EXDATE/RDATE、
  存储用量分节、动作面板键等（见 v0.23.1）。
### v0.23.1（2026-09-19）

- **修复 Miniflux 组件无法真实取数**：内核代理网关的 URL 门禁缺少 Miniflux 路由，
  生产环境的未读列表请求一直被拦截（单元测试因模拟网络层而未暴露）。现按既有白名单
  模式放行 entries 与 categories 两条路由。
- **未读文章新增分类筛选**：分类选项从你的 Miniflux 实例动态加载（设置表单内完成），
  凭据仅经请求头传递，筛选在服务端执行。
- **其他**：新增存储用量设置分节；切换器动作面板键（Shift+F10 / ContextMenu）；
  用户内容排序显式钉定中文拼音 Collator（跨设备稳定）；iCal 完整支持时区、重复日程、
  取消场次与补场；写作强度分与 12 个月回看；倒数日倒数/累计双模式；年/季/月进度。
### v0.23.0（2026-09-19）

- **58 个组件全部完成逐组件深度优化**：以 8 维评分卡（设置可发现性、数据正确性、信息层级、
  尺寸适配、交互反馈、状态恢复、性能生命周期、三端/无障碍/隐私）逐组件审查增强，账本见
  `docs/component-deep-optimization-plan.md`。亮点：搜索路径筛选在本地页签/远程结果/正文探测
  三层语义一致（300 卡片筛选 p95 约 0.56ms）；数据库表格类型化单元格；数据库/已存筛选/最近更新/
  近期编辑获得检索、排序与准确总量；收藏分组选择器、文档集最近使用排序、随机回顾候选统计；
  世界时钟跨日标记；倒数日每年重复（2/29 安全钳制）；日历周起始日；天气/空气质量显示开关；
  服务状态维护态独立呈现；打卡桥接组件仅做渲染增强，不新增打卡协议。
- **二轮增量（竞品调研消化）**：
  - 倒数日新增**倒数/累计双模式**（`已经 N 天`）；年度进度支持**年/季/月周期**。
  - **display 覆盖试点**：时间与日期、倒数日、每日引言支持数字大小三档（标准/大/特大）。
  - 近期写作活跃度：**写作强度分**（指数平滑半衰期口径，opt-in）与 **12 个月回看窗口**。
  - **iCal 订阅完整支持时区与重复日程**：TZID 时区解析、RRULE 重复展开（DAILY/WEEKLY/MONTHLY、
    COUNT/UNTIL/BYDAY）、EXDATE 剔除取消场次、RDATE 追加补场——过去起点的重复日程此前不可见。
  - 设置页新增**存储用量**分节：合计与各数据 key 的近似占用（UTF-8 字节）。
  - 切换器动作面板键：**Shift+F10 / ContextMenu** 键盘呼出聚焦卡片的动作菜单（免鼠标）。
- **工程**：自动测试 6117 → 6258（207 文件）；资源自律线按 ADR 0059/0062 校准
  （zip 单条目 224 KiB、raw 832 KiB，512 KiB 归档硬上限不变）；生产依赖图保持 52 模块。
### v0.22.0（2026-09-18）

- **修复加载报错（issue #1）**：特定宿主时序下 `window.siyuan.languages` 未就绪，
  注册全局热键命令时内核读取 `_trayMenu` 抛出 TypeError 并中断插件加载（其后注册的
  智能体能力全部被跳过）。现命令注册全程隔离：单条命令失败不影响插件加载，全局热键
  在宿主就绪前自动降级为应用内热键。
- **组件面板新增 11 个组件（总数 48 → 59）**：
  - **内核数据组件（基于思源 v3.8.x 只读端点）**：数据库表格（绑定一个思源数据库块，
    按其当前视图只读渲染数据行，跟随你在思源里设置的筛选与排序，行点击打开所在文档；
    见 ADR 0058）、置顶文档、收集箱（云端速记，未登录时显示确定空态）、最近更新、
    数据健康（缺失资源清点）、最近文档（思源原生最近列表）、数据库导航、已存筛选
    （原生搜索的已存条件清单）。
  - **外部数据组件**：RSS/Atom 订阅（用户填任意 feed 地址，零凭据零实例）、空气质量
    （Open-Meteo 欧洲 AQI 与 PM2.5/PM10，六档分档）、Hacker News 榜单升级（首页/最佳/
    问答/展示四榜切换）。
  - **生态桥接**：小驴打卡·本月统计（本月打卡天数、记录总量与项目排行）。
- **顶栏与命令面板**：两个顶栏图标新增右键菜单（快捷打开设置/切换器/组件面板）；
  命令面板新增"打开设置""打开今日日记"两个命令。
- **兼容性**：对照思源 v3.8.4 内核源码完成兼容性调研，无破坏性变更；新组件全部基于
  只读端点，不新增任何写入或隐式数据流出。
- **工程质量**：自动测试 5872 → 6138（198 个测试文件）；11 个既有模块的边界加固；
  性能门禁与包体预算保持（package.zip 339861 bytes，512 KiB 上限内）。

### v0.21.0（2026-09-17）

- **组件来源成为一等公民（ADR 0057）**：组件协议 v2.4 新增结构化 `source` 字段
  （`pluginId/name/icon/version/homepage/collection/order`），取代按作者自由文本分组；
  商店改为**来源分组优先于功能分组**——同一插件的多个组件收敛进一个来源组，组头显示来源
  图标、**已添加 x/y** 与**全选本组 / 取消本组**，组内卡片按 `source.order` 升序排列，
  待安装区按来源插件聚合而不再平铺，来源名与子系列名一并进入搜索文本。
- **小驴打卡桥接组件（新增 5 个）**：`checkin-today / checkin-streak /
  checkin-year-heatmap / checkin-weekly / checkin-occasions`，直接消费小驴打卡已公开的
  生态 API v4（`window.siyuanCheckin`，只读、纯本地、零网络）；打卡未安装或能力缺失时
  返回确定空态而不是故障态；日后打卡自行注册相同 `moduleId` 时由 token 覆盖自动接管，
  用户配置不漂移。
- **语义搜索（第三种搜索方法）**：查询语法与正则之后补上语义；仅在宿主 AI embedding
  已配置时方法菜单才出现该选项，未配置时残留选中静默降级为关键词搜索。
- **GitHub 贡献升级为格点热力图**：新增第四种 `viewType: heatmap`，条目硬顶由 42 提到
  371，保留周日对齐占位格，阈值由提供方透传、视图不重写。
- **第三方插件接入示例跑通端到端**：提供方注册模板
  `docs/widget-example/siyuan-checkin-home-modules.js` 与 6 项运行时契约，把模板当真实
  第三方插件走完 register → listModules → read → buildHomeModuleView → unregister。
- **工程质量**：`src/index.ts` 由 9226 行降至 8104 行（移动端切换器群、二级面板 UI 拆为
  独立模块）；`src/index.scss` 6517 行按**顺序切片**拆为 tokens + 9 个切片（CSS 顺序即
  层叠顺序，只能顺序切片、不能按域名聚类），并配独占锚点覆盖门禁；根目录三份账本归档并
  加体积预算门禁；性能自检改为自适应标定 + 比值断言。
- 发版门禁：5959 项自动测试（178 个测试文件）、TypeScript、生产构建、移动端 smoke、
  Chromium smoke 与 `verify:release` 全部通过；产物 dist/index.js 653949 bytes、
  package.zip 330184 bytes。

### v0.20.0（2026-09-17）

- **生活信息支线（新组件）**：新增 **iCal 日程订阅**（用户提供 `.ics` 地址，RFC 5545 有界解析——源 256 KiB/事件 500 上限、超限拒绝而非静默截断，未来窗口日程、30 分钟缓存）与 **GitHub 贡献热力图**（官方公开事件流免 Key 读取，可选 Token 仅经请求头传递且不进 URL/缓存/缓存键，按 UTC 日期桶渲染格点热力图（默认 84 天窗口，28~366 可配）、口径诚实标注与官方热力图的差异，60 分钟缓存）；两者均经内核代理抓取，纳入组件商店目录、依赖说明与来源白名单。
- **修复**：iCal 文本抓取缺陷（D-397）——文本抓取变体的 `responseKind` 选项被底层忽略、恒按 JSON 解析，导致 iCal 卡片线上抓取必然失败；由新增的 GitHub 网络门禁在发版前自然暴露并修复，附回归门禁。
- **存储数据完整性**：`sw_thumb_cache` 读取侧归一化——结构损坏与旧版本上限语义残留（手机端 v0.7.0 写入侧硬编码桌面上限）的缓存条目现会被读取侧清理；新增存储迁移只读演练（有界恢复报告）接入启动流程，并经 workspace-context 暴露演练健康度；文档集恢复新增结构化报告导出。
- **移动端修复**：图标尺寸越界、工具栏 chips 裁切、小组件面板高度与裸 svg 尺寸兜底补齐；布局门禁改按真实手机宽度量测。
- **Workspace 运行时**：会话注册表、恢复流程、取消边界与安全退出等 20+ 契约能力补齐（事件管线接入生产）。
- **工程质量**：全仓 365 条窗口断言迁移为块级有界断言（迁移中发现并修复 size tile 缺 `touch-action` 的真实缺陷）；复杂度性能门禁获得边际重测抗噪（天花板语义不变）；新增存储兼容矩阵文档与双向文档契约门禁、协议兼容声明一致性门禁。
- 发版门禁：5872 项自动测试（171 个测试文件）、TypeScript、生产构建、移动端 smoke、Chromium smoke 与 `verify:release` 全部通过；产物 dist/index.js 631610 bytes、package.zip 318095 bytes。

### v0.19.0（2026-09-16）

- **搜索成熟化**：全库文档结果新增面板内「加载更多」增量展开——首屏仍渲染 12 条，点击后纯客户端展开更多结果（不换缓存 key、不发新请求，重渲染后焦点保持），全部展开后回落思源原生搜索出口；取数上限常量化（33 条）并与智能体路径共用；统一缓存 key（对象键排序 + 无序键集合 + v:1 版本化）经核查确认并由既有测试锁定。
- **架构重构（无行为变化）**：搜索方法群 20 个方法 + 1 个模块级函数（约 887 行）外迁至 `doc-search-ui.ts`，`src/index.ts` 降至 9156 行（七轮重构累计约 -28%）；文档搜索实例状态收拢至 `doc-search-state.ts`；生产依赖图 41 个模块且全程门禁复核。
- **修复**：v0.16.9 编码事故遗留的 351 行注释乱码全量恢复——322 行以 v0.16.8 原文自动匹配，29 行人工定源（其中 2 行为新时期新增注释、按上下文语义重建并拆回原始多行）；仅注释文本，无行为变化。
- **文档**：组件协议新增「跨表面布局」章节，说明桌面/侧栏/手机端三份独立布局与全局共享配置的语义（`supportedDevices` 裁剪、移动端单列全宽、布局清理语义）。
- 发版门禁：5703 项自动测试（160 个测试文件）、TypeScript、生产构建、移动端 smoke、Chromium smoke 与 `verify:release` 全部通过；产物 dist/index.js 601607 bytes、package.zip 309233 bytes。

### v0.18.0（2026-09-16）

- **第三方组件生态**：新增世界时钟、Hacker News、Uptime Kuma 状态、Frankfurter 汇率参考、Miniflux 未读、每日一言、设备电量、NewsNow 实时资讯与 ActivityWatch 使用时长等外部来源组件；配套纯模型候选目录与来源审计文档，未配置凭据的来源默认零联网。
- **路径筛选接入桌面**：基于真实宿主取证（内核 3.8.4 实测 `/api/filetree/listDocsByPath`）解除端点门禁，桌面弹窗开放笔记本/路径筛选；代际标记取消语义与只读边界由契约测试锁定，侧栏入口待窄侧栏真实宽度证据。
- **组件商店重组**：内置组件按主要用途收敛为七组并附中英文说明；新增"需前置依赖/可选数据源"独立页签、依赖状态解析与结构化摘要；手机端组件面板单列纵向布局。
- **韧性增强**：接入思源同步生命周期，同步期间冻结面板交互与侧栏重建；新增 120 秒同步 watchdog 覆盖结束事件丢失。
- **体验打磨**：动效语言统一为时长/缓动令牌、可点击元素按压反馈、`prefers-contrast` 无障碍适配。
- **架构重构（无行为变化）**：六轮拆分使 `src/index.ts` 由 12681 行降至 10003 行（-21%），商店 UI、设置页、配置表单、外部组件注册、字素工具与搜索状态宿主各归独立模块；生产依赖图 31→40 且全程门禁复核。
- **修复**：智能体能力 `home-adapter-diagnostics` 的标题/描述编码损坏（v0.17.0 起）已修复；发布宣称口径与实际注册数对齐（组件 41、能力 11）。
- 发版门禁：5693 项自动测试、TypeScript、生产构建、移动端 smoke、Chromium smoke 与 `verify:release` 全部通过；产物 dist/index.js 601563 bytes、package.zip 307859 bytes。

### v0.17.0（2026-09-14）

- 思源智能体只读审计增强：生命周期历史、健康报告、趋势窗口、传输封装、有限队列与恢复协调器均提供固定字段和脱敏输出。
- 联合恢复增强：支持多协调器原子提交、联合快照、checkpoint 窗口、cursor 增量恢复、diagnostics 状态/风险投影和分页消费。
- 安全边界保持不变：不新增 Agent 写入能力，不暴露 handler、实例、文档正文或宿主异常文本；所有输出均有数量和载荷上限。
- 发版门禁：1513 项自动测试、TypeScript、移动端 smoke、Chromium smoke、生产依赖图和包体检查全部通过。

### v0.16.39（2026-09-13）

- 内置组件 **日历月视图** 已升级为完整 6 周 × 7 列月历：以思源日记属性识别日期并兼容日期标题，显示年月、跨月日期、周末、今日高亮和日记圆点，点击已有日记直达并支持前后 24 个月导航；协议扩展 `viewType=calendar`。
- 新增内置组件 **倒数日**：设定目标日期显示剩余或已过天数（纪念日、截止日都适用）。
- 组件商店 UI 深度优化：默认打开尺寸扩大至 960×720；内置组件按功能分 7 组（日记与日程/任务与清单/文档与导航/数据洞察/学习与记忆/采集与速记/系统与工具）、插件组件按来源作者分组；每张卡片带迷你骨架预览与按比例尺寸缩略框（当前档高亮）；新增**预览效果**按钮（按默认尺寸渲染真实组件与真实数据）。
- 组件商店可用性与交互增强：分类、条件和已添加状态可独立筛选；已添加的第三方组件在 provider 卸载时明确标记失效并保留配置，重新注册后即时恢复；分组、搜索条件和无结果状态均支持中英文及动态刷新。
- 配置体验增强：倒数日使用原生日期选择器，指定文档提供当前打开文档建议和 block ID 校验；笔记本筛选保留空选项与失效值提示，配置保存前提供原生有效性反馈。
- 组件面板 UI 全面打磨：去除内层嵌套外壳（单一外框承载全部内容）；折叠/展开改为紧凑箭头图标；空状态前置淡色圆点；日历格悬停反馈；条目悬停左侧强调色条；stat hero 数字加大加粗；进度条圆角端点；折叠体平滑动画；面板平滑滚动。
- 性能优化：组件面板打开时逐个错峰加载（80ms 间隔），不再内核并发风暴。
- 韧性增强：组件读取失败但存在上次好数据时继续展示旧快照并标「缓存」，不再整块报错；错误态附带原因码便于自诊。
- 修复 append-to-journal 智能体能力定义存在但未注册的缺口（README 宣称 10 项实际只有 9 项）；新增"能力注册守卫"测试防再犯。
- 修复全文内容搜索因空类型筛选从未命中的问题（3.8.x 内核把空 types 视为"不搜任何类型"）。
- 内置组件达 27 个；智能体能力包含 `document-context` 在内共 12 项（7 只读 + 2 受控导航 + 3 受控写）；本轮新增契约后自动测试持续增长。

### 历史版本

完整的逐版本更新历史见 [GitHub Releases](https://github.com/ai68298100/siyuan-speed-switch/releases)。

## 架构与测试

<p align="center"><img src="docs/architecture.svg" width="860" alt="LvSpeed Switch 六层运行时架构"/></p>

插件按 6 层组织。三种界面共用导航服务和持久化结构，但每个界面独立持有 DOM、搜索会话与生命周期：

| 层 | 入口文件 / 类 | 职责 |
| --- | --- | --- |
| 界面层 | `showSwitcher` / `renderSidebarPanel` / `showMobileSwitcher` | 桌面弹窗/全屏、右侧栏和手机端的端侧布局与交互 |
| 编排层 | `registerSwitcherRefresh` / SearchSession / 动作执行器 | 视图刷新广播、独立异步状态、共享命令路由 |
| 导航服务层 | 页签 / 收藏 / 搜索 / 快捷入口 / 日记 / 面板 | 排序、去重、批量操作和渐进式功能组合 |
| 思源集成层 | `getAllTabs` / MobileTabs / 内核 API / Dock / 插件命令 | 统一封装宿主能力和第三方插件边界 |
| 持久化层 | `loadData` / `saveDataDebounced` / `storage-migration.js` | 13 个校验后的 storage key（11 个托管迁移 + 2 个检视型）、防抖写入、卸载前 flush、只读迁移演练、配置导入导出 |
| 基础设施层 | `util.js` / `search-session.js` / `quick-actions.js` / 类型与常量 | 零宿主依赖纯函数、类型、边界值、日志和自动化测试 |

**性能隔离**：已开页签切换只依赖本地状态；全库请求、缩略图补全和第三方动作都是可失败的附加层。每个搜索界面拥有独立请求序号、取消控制器、定时器和缓存，销毁时集中释放。

**数据边界**：13 个 storage key 分项保存——`sw_mru`、`sw_open_history`、`sw_closed_history`、`sw_pinned`、`sw_favorites`、`sw_fav_groups`、`sw_fav_collapsed`、`sw_quick_actions`、`sw_quick_actions_defaults`、`sw_document_sets`、`sw_thumb_cache`、`sw_settings`、`sw_home_state`。收藏最多 512 条、置顶最多 64 条、收藏分组最多 64 个；加载和写入都会去重、裁剪并安全回写。可重新查询的搜索结果和临时界面状态不写入插件数据。

**组件面板开放协议**：第三方插件可以通过 `registerHomeModule` 注册只读、按端隔离的组件，出现在组件面板的组件商店里（支持尺寸型号声明、描述、失败跳转回调）。完整接入指南见 [`docs/widget-protocol.md`](docs/widget-protocol.md)。基础桥接（实验性、显式挂载）：可注册数据模块，再由宿主明确调用 `createHomeModuleController` 或 `createHomePanelController` 挂载。模块读取结果会统一归一化为空、载入、缓存或失败状态，并受条目数、文本长度、并发和生命周期上限约束。插件不会自动改变默认切换器首页，也不会保存外部函数引用；调用方卸载时应执行注册返回的清理函数和控制器 `dispose()`。

```ts
const unregister = speedSwitch.registerHomeModule({
    moduleId: "my-readonly-module",
    title: "我的摘要",
    supportedDevices: ["desktop", "sidebar"],
    read: async (config, device) => ({items: await readSummary(config, device)}),
});
// 由调用方在自己的容器中显式创建并管理面板生命周期。
```

**测试矩阵**：`pnpm test` 自动发现 `tests/` 与 `tests/host/` 下的 `*.test.cjs` 文件，当前共 6299 项测试（211 个测试文件），精确计数以命令输出为准；UI 冒烟测试单独执行：

| 文件 | 覆盖范围 |
| --- | --- |
| `tests/util.test.cjs` | `util.js` 纯函数与数据净化边界 |
| `tests/constants.test.cjs` | 真实源码常量的范围与格式自洽 |
| `tests/search-session.test.cjs` | 会话隔离、取消、版本与缓存上限 |
| `tests/quick-actions.test.cjs` | 默认值、可选内置项、清理、命令/适配器与字素边界 |
| `tests/quick-actions-ui.test.cjs` | 快捷入口选择器和图标符号边界 |
| `tests/search-model.test.cjs` | 搜索聚合、请求归一化、范围、高级筛选、缓存 key 与结果分页规划 |
| `tests/agent-capabilities.test.cjs` | Agent 能力 schema、输入归一化、输出边界、注册降级和 JSON Schema 验证 |
| `tests/i18n.test.cjs` | 中英文键完整性、静态引用与格式 |
| `tests/checkin-bridge-model.test.cjs` | 小驴打卡桥接模型的 API 握手、能力协商、连续天数与热力图口径 |
| `tests/checkin-bridge-protocol.test.cjs` | 组件协议 v2.4 的 source 白名单与 5 个打卡组件的来源/依赖一致性 |
| `tests/home-source-model.test.cjs` | 组件来源解析、来源分组聚合与排序的确定性 |
| 其余顶层与 `tests/host/*.test.cjs` | 最近记录、首页运行时、展示契约、第三方 provider、兼容矩阵和发版契约 |

| UI 测试 | 覆盖范围 |
| --- | --- |
| `tests/mobile-card-smoke.cjs` | 移动端卡片、操作按钮、单列布局、缩略图和设置开关 CSS 不变量 |
| `tests/mobile-toolbar-layout.cjs` | 真实 Chromium 中的手机顶栏芯片可读性、组件面板卡片高度带与"样式未就绪时图标不失控"（含裸 svg 对照） |
| `tests/chromium-style-smoke.cjs` | 实际 Chromium 中的移动卡片、开关和全库搜索卡片计算样式，可叠加宿主/主题 CSS |
| `tests/live-siyuan-smoke.cjs` | 连接测试浏览器中的真实思源桌面前端，检查工具栏、快捷入口和响应式设置页 |
| `tests/live-siyuan-mobile-smoke.cjs` | 连接思源移动前端，检查移动分支、单行工具栏、弹窗边界和设置页溢出 |

## 开发

### 快速命令

```bash
pnpm install            # 安装依赖
pnpm dev                # 开发监听（产出 dev 版 dist/）
pnpm build              # 生产构建 → dist/* + package.zip
pnpm test               # 自动运行全部单元、契约与宿主发版测试（当前 6299 项）
pnpm test:smoke         # 移动端 UI 烟雾测试（需先 pnpm build）
pnpm test:smoke:layout  # 手机顶栏/组件面板布局门禁，含裸 svg 对照（需先 pnpm build）
pnpm test:smoke:browser # Chromium/主题兼容测试（可指定 SIYUAN_BASE_CSS、SIYUAN_THEME_CSS）
pnpm verify:release     # 发布候选本地总门禁（类型、双构建复现审计、6299 项测试、发布/质量/集成审计和三套 UI 冒烟）
```

推送 `v*` 标签即会触发 GitHub Actions 自动构建并发布 Release。

### 如何新增一个设置项

1. **`src/types.ts`** — 在 `ISwSettings` 加字段 + 默认值：
   ```ts
   export interface ISwSettings {
       myNewOption: boolean;     // 新字段
       // ...
   }
   ```

2. **`src/constants.ts`** — 若有边界，加常量（`MY_NEW_MIN` / `MY_NEW_MAX`）。

3. **`src/index.ts → DEFAULT_SETTINGS`** — 加默认值：
   ```ts
   const DEFAULT_SETTINGS: ISwSettings = {
       myNewOption: false,
       // ...
   };
   ```

4. **`src/index.ts → buildSettingsXxx`** — 在对应标签页 builder 加输入控件（开关 / 下拉 / 数字），改动即写盘。

5. **i18n** — 在 `src/i18n/zh-CN.json` 与 `src/i18n/en.json` 同步加 key，并保持两边 key 集合完全一致。

### 如何新增一个 dock 面板

1. 在 `renderDockList` 内的 `DOCK_ITEMS` 数组追加 `{key, icon, label}` 三元组。
2. 若面板需要特殊激活逻辑（不是简单 `openTab`），在 `openDockByKey` 中加分支。

### 第三方快捷动作适配

如果第三方插件已经通过思源 `addCommand()` 注册命令，该命令会自动出现在快捷入口设置的“添加”列表中，无需额外适配。需要传递参数或执行自定义流程时，可以获取小驴雷切插件实例并直接注册一个可见入口：

```ts
const speedSwitch = this.app.plugins.find(
    (plugin) => plugin.name === "siyuan-speed-switch",
);

this.unregisterSpeedSwitchAction = speedSwitch?.registerQuickAction({
    id: "xiaolv-checkin",
    label: "打卡",
    icon: "iconCalendar",
    value: "open",
    targets: ["desktop", "sidebar", "mobile"],
    handler: (value) => openClock(value),
});

// 在提供方插件的 onunload() 中调用：
this.unregisterSpeedSwitchAction?.();
```

注册后入口会写入快捷入口设置，并可按端侧单独启用。回调仅保存在内存中，持久化配置不保存函数；提供方卸载后入口配置仍在，但点击时会安全跳过。低层接口 `registerQuickActionAdapter(id, handler)` 可用于接管已经存在的 `adapter` 配置，不建议通过模拟 DOM 点击或伪造全局快捷键接入。

### 如何新增一个排序方式

1. `SORT_BY_LIST` 常量数组追加排序键。
2. `SortBy` 联合类型追加成员。
3. `sortGroupItems` 内追加排序分支（建议抽到 `util.js` 以便单测）。

## 开发路线

后续功能顺序、设计约束和发版门槛见 [ROADMAP.md](./ROADMAP.md)。

## 📜 决策记录

全部架构决策见 [`docs/adr/`](docs/adr/)（ADR-0001 ~ ADR-0062）。近期关键决策：

- [ADR-0048/0052 UI 模块外迁](docs/adr/0048-mobile-switcher-ui-extraction.md) — 移动端切换器与第二面板从 `index.ts` 拆出的边界裁定
- [ADR-0049/0050 样式顺序切片](docs/adr/0049-stylesheet-order-preserving-split.md) — `index.scss` 为什么只能顺序切片、不能按域名聚类
- [ADR-0051 治理文档归档](docs/adr/0051-governance-doc-archive-and-root-budget.md) — 台账归档与根目录体积预算
- [ADR-0057 组件来源一等公民](docs/adr/0057-widget-source-and-store-grouping.md) — 组件协议 v2.4 与商店来源分组
- [ADR-0058 数据库表格投影](docs/adr/0058-av-widget-bounded-list-projection.md) — 数据库组件为什么是只读的有界列表投影
- [ADR-0059/0062 资源自律线校准](docs/adr/0062-raw-bundle-line-recalibration.md) — 体积只防失控增长：zip 单条目 224 KiB、raw 832 KiB、512 KiB 归档硬上限不变

## 许可证

[MIT](./LICENSE)
