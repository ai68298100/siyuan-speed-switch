# 小驴雷切整体开发路线
> 基线：`v0.37.0` 已发布（键盘优先收口：结果行方向键导航 + 常驻预览窗格）；最后更新：2026-09-26（当前工作树 6209 项测试，256 测试文件）
>
> 当前状态：v0.36.0 已发布；搜索体验批完整落地（标题/片段关键词高亮、片段净化、结果行数字直达与 ↑/↓ 行导航、常驻预览窗格——Tab/方向键/数字直达/Enter/Alt 预览/`>` 命令模式键盘链路闭环）；此前关联内容、保存的搜索、命令模式、marks 书签、多目标快速捕获、预览打开、文档集版本历史、动态收藏组、密度档位、可迁移配置包、provider 协议元数据、移动侧滑所有权与悬浮球三层防误触、真实例 E2E 验收通道（桌面/移动/只读 6 条）均已接入，代码与自动化验收完成，真实 Android 证据仍按 B-004/B-005 后置。主线按 [ADR 0076](docs/adr/0076-navigation-context-roadmap.md) 转入维护基调：等待真机反馈与生态回应；剩余 P2 受控智能（T-6832 评估池，已补充内核 /mcp 上游事实）、关联内容锚文本排序（等 3.8.6 stable）等候补随反馈立项；不引入第二套搜索/动作架构。
## 1. 产品定位

组件商店的下一阶段以逐组件深度优化为主，具体执行账本见
`docs/component-deep-optimization-plan.md`；重点是配置选择器、信息密度、交互反馈和失败恢复，
不再以新增组件数量作为主要进度指标。资源取舍遵循 ADR 0059：功能、性能、UI 与交互
优先，体积门禁只防失控增长。

小驴雷切是思源笔记的**统一切换与工作上下文平台**。第一面板是切换器，负责找到、预览、打开内容和执行动作；第二面板是工作台，负责查看、刷新和编排信息组件；第三面板是片段实验室，负责在安全边界内管理、编辑和预览代码片段；悬浮球是从任意位置进入平台的全局触发器。核心价值仍是让用户更快地找到、查看、切换、编辑和恢复当前工作内容。它不替代思源原生搜索、文件树、历史记录或同步服务，而是在这些能力之上提供一套共享的入口、对象、动作和上下文语义。

三个表面共享平台外壳、状态语言和动作管线，但保留搜索、网格和编辑器各自适合的工作方式；“统一”指工作路径和交互合同统一，不指把所有内容压成一种卡片。

开发优先级固定为：

1. 已打开内容的切换速度和可靠性；
2. 收藏、最近、日记等高频入口；
3. 在同一个搜索页内渐进扩展到全文和全库内容；
4. 工作区恢复等增强能力；
5. 低频、重型或难以证明价值的功能暂缓。

## 2. 不可回退的设计约束

### 2.1 极简而非功能堆叠

- 首屏始终服务于“找到并打开内容”，不放说明性文案、统计图表或营销式模块。
- 工具栏只保留高频一级入口；低频动作归入“更多”。
- 卡片只用于页签、文档和搜索结果等真实对象，不把普通页面分区包装成装饰卡片。
- 优先复用思源图标、菜单、主题变量和交互语义，不建立第二套视觉系统。
- 新能力默认渐进展示：没有搜索词时不请求全库，没有结果时不占用大块空间。

### 2.2 共享语义，端侧适配

桌面弹窗、侧栏和手机端共用同一套领域逻辑、排序规则、搜索状态模型和数据结构；每个界面保有独立的搜索会话。工具栏不追求像素级相同，而是由各端根据空间、输入方式和宿主能力选择入口。

三端都保留的核心语义为：

```text
搜索 | 收藏 | 最近 | 日记
```

- **日记是桌面弹窗、侧栏、手机端均独立显示的常用入口，不得收进“更多”。**
- 桌面端空间更大，可直接显示搜索、收藏、最近、日记、排序、全屏、侧栏和设置等高频工具。
- 侧栏优先显示搜索、收藏、最近、日记和排序；全屏等不适用功能不显示。
- 手机端优先显示搜索、收藏、最近、日记；排序、设置等放入“更多”或底部操作面板，全屏不显示。
- 窄屏允许只显示图标、缩短搜索框或调整间距；可以减少入口，但不得隐藏日记或改变核心命令含义。
- 同一个命令只有一个领域实现，桌面按钮、侧栏按钮和手机底部面板只调用共享命令。

### 2.3 稳定性优先

- 打开切换器、过滤已开页签、切换文档、关闭弹窗四条基本路径不得被远程请求阻塞。
- 内核 API 失败、超时或返回结构变化时，保留已开页签结果并给出轻量错误状态。
- 所有异步结果必须验证请求序号和当前查询词，旧请求不得覆盖新结果。
- 公共设置字段、存储 key 和收藏数据结构不做无迁移的破坏性修改。
- 每个阶段单独提交、单独验收、可单独回滚，不把大规模重构和新功能混在同一版本。

## 3. 搜索总体设计

搜索使用同一个输入框和结果滚动区，但按用户意图分为三层。显示优先级也是执行优先级：

```text
已开页签（本地立即过滤）
    ↓
已打开文档中的全文命中（内核搜索，限定文档范围）
    ↓
全库全文结果（按文档聚合为卡片）
```

### 3.1 第一层：已开页签

- 输入发生后立即按标题、路径和当前可用元数据过滤，不发内核请求。
- 保持现有窗口/分栏分组、置顶、MRU 和键盘导航语义。
- 已打开文档在后续两层中去重，避免同一目标重复占位。

### 3.2 第二层：已打开文档内容

- 使用思源原生全文搜索能力，并把范围限制到当前已打开的根文档。
- 结果按根文档聚合；每个文档默认最多展示 2 条高相关片段。
- 点击标题打开文档，点击片段定位到对应块；无法定位时降级为打开根文档。
- 该层先于全库结果展示，但不得等待全库请求完成后才出现。

### 3.3 第三层：全库卡片搜索

- 当前“全库文档标题 + 路径卡片”作为首个稳定增量保留。
- 后续接入思源原生全文搜索和筛选条件，结果按根文档聚合，不直接铺满块级结果。
- 每页默认最多显示 12 个文档卡片，每个文档最多 2 个片段；其余结果按需加载或交给原生搜索页继续查看。
- 卡片采用响应式网格；窄侧栏和竖屏手机自然退化为单列。
- 借鉴 Neo 的紧凑卡片呈现理念，但实现、样式和代码保持本仓库独立，继续使用 MIT 许可。

### 3.4 筛选范围

优先复用思源稳定的原生条件：笔记本、路径、类型、子类型、排序、分组和搜索方式。筛选 UI 分阶段开放，不一次放出全部选项。

- 首批：关键词、笔记本/路径范围、类型、排序；
- 后续：查询语法、正则、语义搜索（仅在当前思源版本明确支持时启用）；
- 不向普通用户暴露 SQL 搜索；
- 筛选条件必须进入缓存 key，不能把不同范围的结果互相复用。

### 3.5 性能预算

- 已开页签本地过滤目标：单次不跨越一帧；超大页签集以 50 ms 为告警线。
- 内核搜索防抖：180-250 ms；每个界面同一时间最多 1 个进行中的请求。
- 新输入立即取消或作废旧请求；桌面弹窗、侧栏、手机端的请求状态彼此隔离。
- 首屏只渲染有限结果，缩略图继续使用可视区懒加载。
- 搜索缓存只保存在内存中并设容量上限；不把全文片段持久化到插件数据。
- 查询为空、界面已销毁或插件卸载时，必须取消计时器与请求并释放监听。

## 4. 分阶段实施

### R0：冻结基线与稳定化

目标：把当前主线整理成可信的下一阶段起点，不引入新的产品面。

- [x] 完成当前全库标题卡片搜索改动的代码审查与三端主题回归。
- [x] 把搜索请求状态改为按界面实例隔离，避免弹窗和侧栏互相取消、覆盖或共享错误结果。
- [x] 页签加载/销毁事件通知所有存活的切换器视图，桌面批量开关后即时重绘。
- [ ] 验证空输入、快速连续输入、关闭弹窗、切换主题、离线和 API 异常路径。
- [ ] 保留旧 `ISSUE.md` 的排查结论：排序/更多菜单无响应和手机端 `settings` 未定义问题只能作为原型风险，未经当前主线运行时复现与修复不得合入。
- [x] 记录当前设置字段、8 个数据 key、思源最低版本和发布产物清单。

退出条件：类型检查、单元测试、结构烟测、Chromium 主题烟测通过；桌面和手机人工主流程通过。

### R1：拆出共享架构

目标：降低 `src/index.ts` 的耦合，为三端统一和后续全文搜索提供边界。

- [x] 提取搜索会话基础层：请求序号、取消、防抖、缓存和销毁；查询、筛选与结果状态随 R2/R3 继续收口。
- [x] 提取领域命令：打开文档、收藏、最近恢复、日记、排序和设置已按边界拆分（`src/document-actions.js`、`src/favorite-actions.js`、`src/recent-closed.js`、`src/journal-actions.js`、`src/util.js`、`src/settings-model.js`）。
- [ ] 建立界面适配层：桌面弹窗、侧栏、手机端只装配 DOM 和事件。
- [x] 把纯排序、去重、聚合和缓存 key 逻辑放入可单测模块（`src/search-model.js`，暂不接入界面）。
- [x] 抽出收藏项稳定 root 文档 ID 解析与桌面/移动端打开文档命令（`src/util.js`、`src/document-actions.js`），由宿主适配层装配，保留旧版 `openTab` 回退；移动烟测已覆盖 MobileTabs 成功、旧宿主回退、桌面打开、搜索命中块失效后回退根文档和非成功结果隔离。
- [x] 抽出今日日记创建 API 动作（`src/journal-actions.js`），由设置/工具栏 UI 提供笔记本选择并复用统一文档打开命令；烟测覆盖请求体、成功响应和离线失败降级。
- [ ] 不进行一次性文件重写；每次提取保持行为不变并附回归测试。

退出条件：三端调用同一套命令与搜索协调器，现有用户数据无需迁移，功能表现不变。

### R2：分层搜索 MVP

目标：先建立稳定的信息顺序，再扩大搜索范围。

- [x] 已开页签保持即时结果并始终置顶；本地匹配在远程层之前完成，并由搜索编排测试覆盖。
- [x] 稳定全库标题卡片：去重、排除已开文档、加载/空/错误状态、键盘和触控打开。
- [x] 桌面、侧栏、手机端共享卡片构造与结果状态。
- [x] 增加“查看全部”出口：首屏超限时把当前安全筛选映射到思源原生 Search 页签，避免在切换器内无限滚动。
- [x] 验证 Neo、默认明暗主题及窄侧栏下的可读性和点击目标尺寸。

退出条件：连续快速输入无旧结果回跳；100+ 页签与大库场景首屏交互不被阻塞。

### R3：全文搜索与原生筛选

目标：在不把插件变成第二个搜索页的前提下，接入全局搜索。

- [x] 接入已打开文档内容搜索并显示在全库结果之前：最多查询 6 个已打开文档，命中只恢复已有页签卡片，不新增重复卡片；失败自动回退全库搜索。
- [x] 增加标题无结果时的受限全库块搜索回退，按根文档聚合卡片与命中片段；高级类型、排序和搜索方式筛选已接入，主动路径范围选择仍待后续接入。
- [x] 桌面端支持命中块定位并在定位失败时打开根文档；手机端当前仅提供根文档打开，命中块定位待真实 Android 宿主 API 验证后补齐。
- [x] 第一批加入按笔记本筛选，三端共用入口并隔离各自状态；筛选同时约束本地页签、已打开内容、标题结果、全文回退和缓存 key。
- [x] 加入内容类型、结果排序和搜索方式筛选；高级条件绕过标题快路径，直接使用受限原生全文请求，并隔离缓存 key。
- [x] 路径筛选桌面 UI 已按 `docs/path-filter-desktop-plan.md` 接入生产（端点白名单 + 字面量请求 + 代际标记取消 + 菜单/筛选摘要/降级提示，见 `src/doc-search-ui.ts`）；标题搜索 API 缺失时降级到已打开内容/受限全文层，语义搜索不可用时回退关键词模式。权限、只读等宿主 API 缺失分支的收口继续跟进。

退出条件：搜索范围和排序与思源原生结果含义一致；性能预算满足；失败不影响页签切换。

### R4：端侧工具栏

目标：共享命令语义，同时按桌面、侧栏、手机端的空间与能力配置入口。

- [x] 建立共享快捷动作注册与执行层，桌面、侧栏、手机端按端侧配置选择入口。
- [x] 桌面端直接显示更多高频工具（包括全屏）；侧栏和手机端保持更少入口。
- [x] 日记在桌面和手机工具栏保持独立，并保留首次选择默认日记本的流程。
- [x] 全屏仅在桌面端显示；手机端不渲染无效入口。
- [x] 手机排序改为紧凑按钮和菜单，设置及快捷入口按端侧空间重新布局。
- [x] 菜单触发元素使用语义化按钮，验证键盘焦点、触控、坐标定位、层级和关闭时机。
- [x] 针对旧原型的“排序/更多点击无反应”增加运行时交互测试，不能只靠 DOM/CSS 静态检查。

退出条件：所有入口在桌面弹窗、侧栏和手机端均可实际点击；无重复命令实现；日记最多一次点击可达。

### R5：收藏与恢复

目标：先完善用户主动保存和误关闭恢复，再增加更复杂的工作区概念。

- [x] 收藏组和组内条目排序，桌面支持拖拽、手机保留移动按钮，持久化顺序并处理损坏数据。
- [x] 最近打开记录使用独立数据结构，按文档根 ID 归一化、去重、限量，并区分仍打开与已关闭状态。
- [x] 最近关闭采用明确的数据来源、去重规则、容量上限和恢复失败反馈。
- [x] 先接入思源 `destroy-protyle` 关闭事件，以事件前后根 ID 差集维护插件侧记录；没有稳定宿主历史 API 时不伪造关闭状态。
- [x] 最近关闭与最近打开在同一历史入口中分区语义展示，不混淆“仍打开”和“已关闭”。
- [x] 桌面右键与手机长按保持等价操作；历史条目共享打开/移除菜单，普通点击行为保持不变。

退出条件：重启后顺序稳定；恢复不会制造重复页签；批量操作有逐项成功/失败结果。

### R6：文档集 / 工作区快照

目标：为固定项目场景提供一键恢复，但不改变普通用户的默认工作流。

- [x] 完成版本化文档集数据层：只记录可稳定恢复的根文档及最小布局信息，限制名称/条目/集合数量，并支持损坏数据清理。
- [x] 接入设置页文档集标签与保存、同名覆盖、删除和恢复预览。
- [x] 接入受控恢复执行：跳过已打开项、按序打开待恢复项并汇总单项失败。
- [x] 增加文档集 JSON 导入/导出，导入复用同一套版本、去重和容量边界；导入文件先限制为 512 KiB，解析期间锁定按钮并暴露 `aria-busy`，避免超大输入和重复写入。
- [x] 补充有界宿主可用性预检、缺失项确认和可取消恢复；预检使用集中管理的 4 路并发和 5 秒单项超时且保持原顺序，设置界面销毁或插件卸载后不会继续操作脱离 DOM 的控件，恢复结果统一显示成功、失败、已打开跳过和缺失跳过计数，跨端真实 API 行为仍待最终验证。
- [x] 恢复前预览已打开与待打开项，默认去重；明确 404 缺失项并允许跳过。
- [x] 提供同名覆盖和删除二次确认。
- [x] 提供独立的文档集重命名操作，支持行内编辑、Enter 保存、Escape 取消和焦点回归。
- [x] 建立版本化数据结构、迁移清理和导入/导出测试。

退出条件：旧快照可迁移；部分文档失效时可继续恢复其余项目；操作可撤销或明确确认。

### R7：现代化 UI 视觉重构（代码已完成，待人工验收）

目标：在不改变核心信息架构和思源主题兼容性的前提下，让第一面板、第二面板、设置页及手机端共享一套更现代、精致且克制的视觉语言。

- [x] 建立淡紫主色、浅蓝灰层级、暖色强调、圆角和阴影 token，并全部保留思源主题变量回退。
- [x] 重构第一面板工具栏、搜索框、页签/文档卡片、空态和状态卡片。
- [x] 重构第二面板模块卡片、标题栏、操作项和错误/加载状态。
- [x] 重构设置页标签栏、设置条目、文档集管理控件及移动端布局。
- [x] 优化手机端卡片、底部快捷入口、排序/收藏 sheet、遮罩和 FAB，同时保留 44px 触控热区与 safe-area。
- [x] 增加 UI 视觉契约烟测，防止后续样式回退；真实桌面/Android 显示验收仍安排在发布候选阶段统一执行。

退出条件：桌面、侧栏和手机端视觉 token 一致；信息密度、触控尺寸、键盘焦点和 reduced-motion 语义不回退；完整自动门禁通过。

## 5. 暂缓与取消进入主路线的方向

以下方向现阶段不承诺版本，也不应提前占用工具栏入口：

- 使用统计和停留时长追踪；
- AI 智能排序；
- 页签关系图；
- 插件自行实现跨设备同步；
- 插件市场更新逻辑；
- 大而全的批量管理模式和常驻复杂筛选面板。

原因是它们会增加数据采集、性能、维护或交互成本，却不直接改善“快速切换”主路径。主题适配、双端一致和思源同步后的数据兼容属于每个阶段的验收标准，不作为独立卖点排期。

## 6. 兼容、数据与回滚

- 新设置使用显式默认值；读取旧数据时做类型校验、去重和容量裁剪。
- 数据结构变更必须包含版本号、向前迁移函数和损坏数据降级策略。
- 不持久化可由内核重新查询的全文片段、临时 loading 状态或 DOM 信息。
- 新 API 先做能力检测；缺失时隐藏对应入口或回退到当前标题搜索。
- 每个阶段通过独立提交和功能开关控制；发生回归时可以关闭新层级而保留页签切换。
- 不直接复制 GPL 项目实现；外部项目只用于理解交互理念和验证兼容性。

## 7. 测试与发版门槛

每个阶段至少满足以下自动化门槛：

```text
pnpm exec tsc --noEmit
pnpm test
pnpm run test:smoke
pnpm run test:smoke:browser
pnpm build
```

人工验收矩阵：

| 场景 | 桌面弹窗 | 侧栏 | 手机端 |
| --- | --- | --- | --- |
| 打开、关闭、切换页签 | 必测 | 必测 | 必测 |
| 搜索空值、快速输入、无结果、错误 | 必测 | 必测 | 必测 |
| 收藏、最近、日记、更多 | 必测 | 必测 | 必测 |
| 键盘导航与焦点 | 必测 | 必测 | 基础验证 |
| 触控、长按、滚动与返回 | 基础验证 | 基础验证 | 必测 |
| 默认明暗主题、Neo、窄宽度 | 必测 | 必测 | 必测 |
| 50/100+ 页签及大库查询 | 必测 | 必测 | 必测 |

发版前还必须确认：

- 中英文 i18n key 集合一致且无硬编码可见文案；
- 无调试弹窗、`console.log`、临时埋点或绝对路径；
- `plugin.json`、`package.json`、README 和压缩包版本一致；
- 安装包在干净插件目录中完成一次安装、升级和卸载测试；
- 只有用户明确确认后才创建 tag、Release 或提交市场更新。

## 8. 当前执行顺序

### 8.0 2026-09-11 起的确认计划

当前 `v0.16.40` 已完成自动化发布，后续继续以开发增量和真实宿主补充验收并行推进。后续工作按以下顺序执行：

1. **持续开发小步增量**：优先处理能由纯函数、协议 schema、适配层和自动化测试验证的功能；每次只推进一个边界清晰的改动，不等待完整发版。
2. **收拢待合并分支**：逐支复核 `feature/icon-fallback`、`feature/mobile-stability`、`feature/open-history` 等分支；只合入能复现、测试覆盖充分且不改变既有数据契约的修复。远端分支和本地工作树中多出的提交必须先完成差异审查，再决定是否合并或关闭。
3. **保持自动化护栏**：新功能必须补协议/模型/回归测试；定期运行类型检查、完整测试和构建，但这些结果只表示开发质量，不表示真实宿主验收完成。
4. **集中记录人工待验项**：所有依赖真实桌面/Android、安装升级卸载、主题和生命周期的事项先记录在路线与测试清单中，等维护者有时间时批量审核；浏览器和截图只作为结构证据。
5. **补充验收后再扩展**：真实桌面/Android 验收结果用于决定路径筛选 UI、侧栏实验和下一大版本范围；新增高风险 Agent 写入能力前仍需独立审批与回执验证。

确认的后续版本方向：

- **v0.16.x 后续开发集**：持续合入协议一致性、兼容性、稳定性、搜索模型和组件生态的小步改动；不承诺每个开发提交都对应可发布版本。
- **v0.17.x**：优先考虑路径筛选 UI、已打开内容搜索体验和第三方组件/快捷入口协议的真实插件适配；每项先做宿主 API 调研和小范围原型，再决定是否进入主线。
- **Agent 专项**：v0.17 以“上下文 → dry-run 计划 → 用户确认 → 固定动作执行 → 结果回执”为主线；当前先完成独立 `document-context` 与 `workspace-plan` 契约/纯模型，再进行真实宿主权限、取消和卸载验证。在验证完成前不新增删除、批量关闭或跨文档写入能力。

明确暂缓：统计追踪、AI 智能排序、页签关系图、插件自建跨设备同步、大而全的批量管理和常驻复杂筛选面板。它们不进入当前开发优先级，也不应以临时入口形式混入主线。

### 8.0.1 2026-09-14 初版的后续五个大版本路线（已被 8.0.2 调整）

`v0.16.40` 已正式发布。后续五个大版本按以下路线推进；每个版本进入主线前仍需宿主 API 调研、小范围原型和包体预算复核，暂缓清单继续有效。

**v0.17 —— Agent 工作区执行落地与路径筛选 UI**

- 把已建成的独立契约层（`document-context`、`workspace-plan`、审批令牌、一次性执行门卫、session registry、diagnostics）择优接入生产 `addAgentCapability` 注册，坚持"上下文 → dry-run 计划 → 用户确认 → 固定动作执行 → 结果回执"主线。
- 硬约束（2026-09-14 第二轮更新，见 D-216/D-219）：预算障碍已解除——D-219 归档内容决策执行后 `package.zip` 余量 54.5 KiB，契约模块生产接入回归常规包体预算评审（每次接入后复核 raw 自律线与 zip 余量即可，无需再走内容决策）。
- 生产接入进度（2026-09-14）：阶段 1 `workspace-runtime-registry-diagnostics` 已完成生产接入（D-220，闭包 25 模块、zip 余量 49.4 KiB）；阶段 2 `document-context` 只读接入已完成（T-368~T-390），真实桌面取消/权限证据仍后置；计划执行链（plan/approval/execution）在 open-documents 真机点击验证后再评估。
- 完成真实宿主权限、取消、卸载与确认弹窗验证（open-documents 真机点击、手机端能力矩阵）后，才评估 Agent 入口是否加入默认工具栏。
- 路径筛选在拿到可信路径树端点证据（T-107）后开放 UI（T-103），复用已有 `path-filter-model`。

**v0.18 —— 第三方组件生态协议落地**

- 以 `docs/widget-protocol.md` 为契约，完成至少一个真实第三方插件的端到端适配验证：注册、失效/恢复、配置表单、跨表面布局。
- 组件面板生命周期（provider 热替换、重渲染回收）按 D-197~D-203 收口为稳定公开协议。
- 商店体验小步打磨：配置入口、筛选与分组已就绪，重点转向第三方开发者文档与最小示例。

**v0.19 —— 搜索第三层成熟化**

- 在宿主能力检测允许时开放第二批筛选：查询语法、正则、语义搜索；不可用时保持现有回退，不暴露 SQL。
- 结果聚合、分页与"查看全部"出口继续对齐思源原生语义；筛选条件全部进入缓存 key。
- 建立搜索性能基准自动化（防抖、首屏、大库场景），把性能预算从告警线升级为门禁。

**v0.20 —— 数据、恢复与跨端连续性**

- 文档集/工作区恢复健壮性：迁移工具、损坏数据降级、恢复报告导出。
- Agent session registry 从契约层转入真实用例：跨端会话状态只读暴露给宿主 Agent。
- 存储版本统一审计：8 个数据 key 的迁移函数、容量边界与跨版本兼容矩阵。

**v0.21 —— 发布工程与真实宿主验收收口**

- 组件真实宿主验收矩阵（T-266）、桌面路径筛选/窄侧栏验收（T-080/T-093/T-103/T-107）在获得合法会话后集中收口。
- 可访问性、i18n、包体预算检查全部自动化并入 `verify:release`。
- 思源市场发布流程演练：干净目录安装/升级/卸载矩阵、可复现构建证明。

以上为方向性路线；v0.18 起的内容在到达时按 8.0 执行顺序复核后再冻结。真实宿主验收（B-005）继续作为独立前置，不阻塞各版本的本地开发增量。

### 8.0.2 2026-09-14 基于当前完成度的路线重排（现行）

组件商店第二轮优化、`document-context` 只读接入和 workspace runtime diagnostics 已经提前完成，原先“先做基础设施、再决定产品顺序”的路线需要调整为“先收口可验证价值，再开放高风险动作”。未来几个大版本不再把路径筛选、第三方生态和 Agent 执行链捆成一个版本，而是按可验收边界拆开：

| 版本 | 主目标 | 必须交付 | 明确不做/准入条件 |
| --- | --- | --- | --- |
| **v0.16.42 稳定化集** | 把本轮商店和首页改动变成可发布质量 | 组件商店真实宿主抽查；分组折叠、窄屏触控、移除/配置流程；修复空查询 p95 边界波动；同步发布矩阵 | 不新增大功能；真实桌面证据未完成前不宣称路径筛选已交付 |
| **v0.17 Agent 只读工作区** | 让思源智能体可靠理解当前工作现场 | 生产接入并稳定维护 `workspace-context`、`document-context`、组件快照/诊断；统一错误/取消/权限回执；Agent 输出脱敏与有界 | 不默认开放写入、删除、批量关闭；执行链只保留契约和灰度开关，必须通过 `open-documents` 真机确认后才进入下一小版本 |
| **v0.18 受控 Agent 动作 + 路径筛选** | 将“理解 → 计划 → 确认 → 执行 → 回执”闭环在最小动作集上跑通 | 仅开放 `open`/`open-batch`/文档集恢复等低风险动作；审批摘要、一次性令牌、取消/超时/卸载回收；桌面路径筛选先行，侧栏仅在真实宽度证据通过后开放 | 不做跨文档写入、删除类动作；无可信路径树 API 时退回笔记本/标题筛选 |
| **v0.19 组件生态与搜索成熟化** | 让商店和搜索从“可用”进入“可扩展” | 至少一个真实第三方插件端到端接入；provider 失效恢复、配置 schema、跨表面布局文档化；路径/笔记本/类型筛选统一缓存 key；搜索分页与查看全部体验 | 不建设常驻复杂筛选面板；语义搜索/正则仅在宿主能力明确支持时启用 |
| **v0.20 数据连续性与性能门禁** | 提升长期使用的迁移、恢复和大库体验 | 存储版本迁移演练、损坏数据恢复报告、文档集/工作区快照增强；搜索和首页性能基准纳入硬门禁；Agent session registry 只读快照 | 不引入云端同步、行为追踪或 AI 智能排序；包体硬上限已于 2026-09-15 提前上调至经审核的 512 KiB（raw 自律线 768 KiB，见 D-353），v0.20 不再需要此项 |
| **v0.21 发布与宿主验收收口** | 把工程质量和真实宿主证据补齐 | 桌面/侧栏/移动验收矩阵、安装升级卸载、主题/可访问性/i18n、可复现归档和市场发布演练 | Android 真机缺失时保留“后置”事实，不用浏览器模拟冒充设备证据 |

#### 路线调整结论

1. **Agent 主线前移，但执行链后置**：只读上下文已经具备真实价值，应先稳定接入；受控执行仍必须等确认弹窗、取消、权限拒绝和卸载回收的真实宿主证据，避免“契约完成”被误认为“用户可安全使用”。
2. **路径筛选与 Agent 解耦**：路径筛选是搜索体验能力，不应等待整个 Agent 执行链完成；先桌面、后侧栏，移动端仅在有真实路径选择交互时再评估。
3. **组件商店从视觉打磨转向生态验证**：基础分组、状态、预览、配置和可用性已经完成，下一阶段重点是第三方插件真实接入、协议示例和失效恢复，而不是继续堆叠装饰层。
4. **性能从“告警”逐步升级为“发布门禁”**：本地空查询 p95 已出现 48ms 边界波动，v0.16.42 先分离环境长尾与产品回归；v0.20 再把稳定基准纳入硬门禁，避免一次偶发抖动阻断正常开发。
5. **每个大版本拆成可独立回滚的 capability/feature flag**：只读能力、低风险导航动作、路径筛选和第三方 provider 分别具备独立准入条件，任何真实宿主未通过的分支都只保留契约和诊断，不进入默认入口。

#### 当前开发顺序（接下来 3 个里程碑）

1. **M1：v0.16.42 稳定化**——完成组件商店真实桌面抽查、修正发布矩阵与性能边界记录。
2. **M2：v0.17 只读 Agent 收口**——补齐 `document-context`/诊断的取消、权限、卸载证据；完善 Agent 输出审计，不增加新写动作。
3. **M3：v0.18 最小执行闭环**——在真实宿主通过后，只接入两个低风险动作和桌面路径筛选；每项单独开关、单独回滚、单独验收。

#### 生活信息组件支线（v0.18~v0.20）

- **v0.18（当前已完成首批）**：独立来源契约、离线时间组件、Open-Meteo 天气与 holiday-cn 日历标记已接入；保持未配置天气/未开启节假日时不联网，按需加载并使用有界内存缓存。下一步补真实思源主题与侧栏验收。
- **v0.19（当前开发头已完成）**：Bangumi 每日放送、商店来源/联网/隐私标识、可配置自建端点的 DailyHot/NewsNow、来源健康和陈旧缓存提示均已落地。
- **v0.20（当前开发头已完成）**：ActivityWatch 桌面/侧栏回环桥接、固定聚合查询、思源本机代理、5 分钟缓存、刷新健康状态与“设备与专注”卡片已落地；商店增加离线/本机服务/外部 API 来源筛选。原生活动窗口标题与采集不进入当前路线。
- **v0.21（下一阶段）**：优先审计并实现无 Key 的 Hacker News / 安全 RSS 只读组件，再评估 iCal 与 GitHub 贡献热力图；TMDB 仅在 Key 私有存储、撤销和 attribution 验证后提供趋势发现，不声称个性化推荐。
- 统一门禁见 `docs/external-widget-source-audit.md`：外部失败保留陈旧缓存，组件不可见时暂停，凭据不进入日志/Agent，远程文本不作为 HTML 渲染，包体仍低于经审核的 320 KiB。

### 8.0.3 2026-09-18 未来三个大版本路线（已被 8.0.5 接替）

8.0.2 的版本号主题（v0.16.42~v0.21）已被实际发布序列消费（v0.18~v0.21 实际承载的是生活信息支线、来源商店与发布工程等内容），本节按 2026-09-18 完成度重排；8.0.2 的编排原则与暂缓清单继续有效，逐项独立准入、证据不齐即降级不顺延。详细依据见 `docs/dev-plan-2026-09-18.md`。

| 版本 | 主目标 | 必须交付 | 准入条件 / 不做 |
| --- | --- | --- | --- |
| **v0.22.0 修复与支线续篇** | 消化用户反馈（issue #1）并补齐无 Key 阅读支线 | T-6301 命令注册守卫（issue #1 `_trayMenu`：languages 门控 + 安全注册降级）；T-6303 RSS/Atom 通用订阅组件（零凭据零实例）；T-6302 本节刷新 | 不夹带任何需要真实宿主证据的能力；发版后回复 issue #1 |
| **v0.23.0 组件体验与订阅增强** | 深度优化与二轮增量成版 | 58 组件深度优化收官 + 二轮增量（countdown 双模式、year-progress 周期、display 三档字号、写作强度分与 12 个月回看、iCal TZID/RRULE/EXDATE/RDATE、存储用量分节、动作面板键）；路径筛选桌面 UI 已提前交付（见 R3）；集市同步与第三方邀请外发续推 | Miniflux 分类发现挂配置表单鉴权通道；真实宿主验收项永久后置等用户时间 |
| **v0.24.0 受控执行闭环** | T-1219 最小执行闭环上线 | 生产图外 14 个契约模块择优接入；仅开放 `open`/`open-batch`（审批摘要、一次性令牌、30s 超时=拒绝、取消/卸载回收）；Agent 默认工具栏入口评估；手机端能力矩阵 | 前置：v0.23 只读收口 + 本版执行证据齐；退出条件沿用"注册、卸载清理、权限拒绝、取消、异常输出、桌面/手机矩阵全过；旧版宿主无报错" |

执行顺序：`T-6301 → T-6302 → T-6303 → 发版 v0.22.0 →（用户就绪时插入 T-6305/T-6306）→ v0.23.0 解锁线 → T-1219 执行链灰度 → v0.24.0 执行闭环`。

生活信息支线状态更新：Hacker News（固定端点）、iCal、GitHub 热力图均已上线；RSS/Atom 通用订阅已于开发头交付（T-6303）；TMDB 维持 Key 管控前置不变。

### 8.0.4 2026-09-19 当前基线校正（现行）

- 当前发布基线为 **v0.23.5**；本地 `main` 仅超前于 `origin/main`、未落后远端，本地开发提交按约定暂不 push。
- GitHub 的 `v0.23.5` Release 标签指向发布提交 `8d2e498`；`459056b` 是随后补录发布产物快照和推送路径的文档提交，不改动运行时代码。
- 当前本地质量收口已完成：T-6492/T-6482 已落地，T-6483/T-6485 仍保留为真实宿主取证项，T-6486 的预算口径已统一；T-6603~T-6659 的集成边界、选择器截断契约、产物快照、本地分支审计和双构建复现批次已通过本地门禁。
- Agent 执行闭环（T-1219）继续后置到真实宿主确认、取消、权限拒绝、卸载回收和移动端矩阵证据齐备之后；Android 真机仍是最终验收环境阻塞，不阻塞本地开发。
- GitHub 当前公开页面显示 2 个 Issue；后续开发账本不再使用“唯一用户反馈”表述，逐项登记 Issue 编号和处理状态。（两 Issue 已于 2026-09-19 回复/确认后关闭，见 §8.0.5 Issue 台账。）

### 8.0.5 2026-09-19 未来版本计划重排（现行）

8.0.3 的 v0.22.0/v0.23.0 两行已被实际发布序列消费（v0.22.0 于 2026-09-18 发布并含 issue #1 修复；v0.23.0~v0.23.5 已发布，58 组件深度优化与二轮增量首批交付完毕），该表仅剩 v0.24.0 行有效。本节按**主题波次**重排：版本号不再绑定主题，每个波次按准入条件独立取下一个版本号；8.0.3 的编排原则（逐项独立准入、证据不齐即降级不顺延）与第 5 节暂缓清单继续有效。

> **（2026-09-20 更新）本节版本波次表已被 §8.0.6 未来三个大版本计划接替**（补丁波/执行闭环波/性能门禁波已随 v0.24.0、v0.25.0 发布消费完毕，生态验证波内容后移至 §8.0.6 v0.28.x）；本节四条专项主线 A~D、前置决断表与 Issue 台账继续有效。

编排原则：

1. 版本号只表示发布顺序；专项主线跨版本滚动，不在单个版本内强行收口。
2. GitHub Issue 逐项登记销号（延续 8.0.4 口径），发版内容与 issue 回复绑定。
3. D-398/D-399/D-401 三项决断作为波次准入门，先决断后排期，不带着未决假设开发。
4. 四条专项主线（性能 / UI / 组件二轮 / 商店架构）持续滚动，各自带明确“不做”边界。

#### 版本波次表

| 波次 | 主目标 | 必须交付 | 准入 / 不做 |
| --- | --- | --- | --- |
| **补丁波（发布通道收口，2026-09-19 完成）** | 消化两个 Issue、恢复发布通道 | ✅ issue #1 确认既有回复（根因 + v0.22.0 修复 + 验证指引）已充分，直接关闭；✅ issue #2 回复 1980 纪元根因与操作指引（修复已随 ≥v0.23.3）后关闭（T-6673）；✅ push 本地领先提交，远端 CI 与双构建门禁全绿 | 本波无代码变更，不发补丁版本；下一版本为 v0.24.0 |
| **执行闭环波 v0.24.x** | T-1219 workspace-plan 执行链最小闭环 | ✅ D-399 落地（ADR 0063）：`actionEffects` 按动作名双通道声明接入注册器，open-document 显式 localRead 免确认、open-documents localWrite 走宿主确认卡；✅ 自建审批管线 6 模块 381 行撤除；✅ D-398 落地（ADR 0064）：minAppVersion 抬到 3.8.0；✅ D-401 落地：`sw_schema_version` 第 14 个持久化 key + onload 落戳 + 降级留证告警；✅ T-6678：生产内自建确认弹窗撤除，四类受控动作确认全交宿主确认卡；✅ T-6680：执行链以 propose（只读出计划）/execute（整单一次宿主确认，localWrite）双能力入图，契约模块 plan/actions/registry/host-actions/write-actions/document-set-actions 六个入生产（图外余 capability-definitions/probe 两个，隔离门同步，图上限 52→58，压缩条目余量 12495 收窄记录在案）；后续：T-6305 宿主确认卡真机取证、桌面/手机能力矩阵 | 前置：T-6305 真机批处理；无宿主确认链的环境不提供执行链，只保留只读能力；降级层简化分批做，每批 3.8.x 行为不变 |
| **性能门禁波 v0.25.x** | 性能从告警线升级为发布门禁 | 四组基准自动化（空查询 p95、首屏渲染、100+ 页签过滤、大库搜索）接入发布主链，先告警记录趋势、连续两个版本稳定后升硬门禁；T-6490 首屏 20 条 + RAF 批量 + IntersectionObserver 每 12 条追加的增量渲染试点；生产依赖图 52 模块启动门保持 | 不做全量虚拟化重写；环境长尾与产品回归分离，单次抖动不阻断正常开发 |
| **生态验证波 v0.26.x** | 商店从可用到可扩展 | 至少一个真实第三方插件端到端接入验证（注册→配置→失效→恢复→跨表面）；F7 收口（`publish.resources` 新规下迁移或声明 `package.zip` 内 `docs/` 4 个子目录文件）；provider 真实宿主升级/重载场景取证 | 不做 collection 二级分组、不换来源分组键（等真实多系列/撞名数据）；集市同步 T-6306 按用户时间插入 |

#### 专项主线 A：性能优化（滚动）

现状：路径筛选 p95 基准 0.56ms（T-6346）；空查询 p95 曾现 48ms 边界波动；T-6489 失败抑制窗已落地；SQL 全量外层 LIMIT + `truncated` 传导（T-6478）。

✅ 专项 A 全部收口（2026-09-20）：A1 基准自动化——大库聚合基准补齐（1200 命中/300 根文档，best-of-3 抗噪，8ms 告警线进全量套件）；既有空查询 p95（48ms）、关键词过滤 300 页签（30ms）、复杂度倍增门继续有效；首屏渲染由 Chromium smoke 覆盖，T-6490 试点时加深。矩阵：空查询/页签过滤/大库聚合/复杂度四类已自动化。
- A2 T-6490 试点：✅ 判定前提不成立关闭（T-6691）——条目长度已被模型层全部有界化（≤12/371/42），数据加载已有 IO 懒读、刷新已有可见性门控，无未界渲染路径可优化。
- A3 定时刷新统一复核：✅ 全仓 setInterval 为 0，长驻定时链全部自续 setTimeout + 可见性/连接性双重门控（T-6691 审计）。
- 不做：Service Worker、虚拟滚动全量替换、为性能引入运行时依赖。

#### 专项主线 B：UI 完善（滚动）

现状：R7 现代化视觉代码完成、真实宿主验收后置；T-107 只差“最窄侧栏宽度”UI 度量。

- B1 T-107 收口：窄侧栏最窄宽度度量 + 最新 UI 生命周期只读验收。
- B2 可访问性自动化：✅ accessibility-gate 门禁落地（T-6692）——五个动画样式面的 prefers-reduced-motion 回退、基控件与卡片 :focus-visible、状态区 aria-live（错误态 assertive）、异步流程 aria-busy，负向验证通过。对比度自动化：✅ Chromium smoke 对比度采样落地（T-6696/T-6697 夹具令牌集 + 5 采样点 AA 断言）。
- B3 热力图色阶补文字图例与色盲替代刻度（§11.2 候选）。
- B4 三态一致性审计：58 组件的加载/空/错误三态统一复核，“单组件失败不拖累面板”的隔离语义逐项过门；商店搜索/筛选/折叠三态并入本项。
- 不做：动态取色主题、第二套视觉系统（2.1 约束不变）。

#### 专项主线 C：组件二轮优化（滚动，每轮集中收口 3–8 个相关子任务）

现状：58/58 一轮收口；§11.2 候选池首批已交付（countdown 双模式、year-progress 周期、display 三档字号、存储用量分节、动作面板键）。

- C1 候选池续：writing-streak n/m 口径与豁免日不断签；recent-writing-activity 12 个月回看 + 逐年分页；note-stats/today-writing 习惯强度分（纯前端半衰期公式，零新增 SQL）。
- C2 组件级“单独立项”清单：✅ 全部收口（2026-09-20）——RSS 已读（T-6685）、DailyHot 基址+路由选择器（T-6686）、ActivityWatch 桶选择（T-6689）、世界时钟离线城市表（T-6690）四项交付；iCal RRULE（T-6460/6462）与 Miniflux 分类发现（T-6466）核实为已交付销项。
- C3 约束：新增持久化 key 前先决断 D-401（版本戳）；每项仍走评分卡验收；不以测试数量为进度指标。
- 不做：checkin-* 的 skip/pause 渲染跟进（等小驴打卡协议补充元数据）；把打卡/倒数日做成速切内置数据域。

#### 专项主线 D：组件商店架构与 UI（滚动）

现状：协议 v2.4（来源分组/`order` 排序/待安装区聚合）、生命周期 v2.3（热替换/token 校验/可用性徽标）、开发者指南（`docs/widget-protocol.md`）与最小示例齐备；真实第三方端到端案例仅小驴打卡一家。

- D1 架构：真实第三方插件端到端接入验证；`source` 撞名时分组键从展示名换 `pluginId`（等真实撞名案例）；collection 二级分组（等真实多系列数据）。
- D2 UI：商店卡片 `version`/`homepage` 呈现决策（当前归一化透传但不显示——宁可少显示，不挂消费不到的字段）；商店搜索/筛选/折叠三态并入 B4 审计；配置表单截断提示的位置与文案（T-6552 后续，等真实大库宿主确认）。
- D3 生态外发：第三方邀请续推（task-horizon#94 / docktomato#4 先例）；widget-example 与协议文档随集市发布同步更新。2026-09-19 集市调研（T-6672）新增邀请候选：SiYuan Copilot（158★，`workspace-context`/`document-context` 只读能力的天然消费方）、HOME（42★，同质面板作者）、Task Note Management（219★）。
- 不做：来源分组键提前切换、二级分组无数据先行。

#### 前置决断与阻塞归属

| 项 | 内容 | 影响范围 |
| --- | --- | --- |
| D-398 | ✅ 已决断（2026-09-19，ADR 0064）：抬到 3.8.0，随 v0.24.0 发布，降级层分批简化 | 全部波次（“旧版宿主”验收范围收窄为 ≥3.8.x） |
| D-399 | ✅ 已决断（2026-09-19，ADR 0063）：宿主 `actionEffects`，撤自建审批三模块 | v0.24.x 范围已冻结 |
| D-401 | ✅ 已决断（2026-09-19）：`STORAGE_SCHEMA_VERSION` 随 v0.24.x 首个开发批落地 | 专项 C 新增任何持久化 key 的前置 |
| T-6305 | 真机与真实宿主验收批处理 | v0.24.x 前置 |
| T-6483/T-6485 | 三处裸 fetch 迁移 / UNION 双 LIMIT 拆分 | 宿主取证批（并入 T-6305 场景） |
| B-004~B-007 | Android/窄侧栏/BGM/ActivityWatch 环境类 | 按环境后置，不阻塞本地开发 |

#### Issue 台账（逐项销号）

| Issue | 内容 | 当前状态 | 计划 |
| --- | --- | --- | --- |
| #1 | `_trayMenu` TypeError | ✅ 已关闭（2026-09-19）：09-18 既有回复已覆盖根因与 v0.22.0 修复，确认充分后直接关闭，未新增回复（T-6673） | 无 |
| #2 | 不断显示需要更新下载 | ✅ 已关闭（2026-09-19）：已回复 1980 纪元根因、修复版本（≥v0.23.3）与三步操作指引（T-6671/T-6673） | 无 |

### 8.0.6 2026-09-20 未来三个大版本计划（现行）

8.0.5 的补丁波、执行闭环波、性能门禁波已随 v0.24.0/v0.25.0 发布消费完毕；仅剩的生态验证波核心内容（真实第三方端到端、集市、邀请外发）全部依赖用户时间或第三方回应，而专项 A 已收口、B/C 基本收口，自主可做的功能积压已见底。本节据此把三个版本重排为"先清工程债与证据，再承接生态"：v0.26.x 全部本地可做，v0.27.x 以性能硬门禁与存储可靠性为主、证据项按准入解锁，v0.28.x 承接生态验证波。编排原则延续 8.0.5（版本号只表发布顺序、逐项独立准入、证据不齐即降级不顺延），四条专项主线的"不做"边界与第 5 节暂缓清单继续有效。

| 版本 | 主目标 | 必须交付 | 准入 / 不做 |
| --- | --- | --- | --- |
| **v0.26.x 发布工程与文档收口** | 清偿 v0.24/v0.25 两轮成版的工程债，恢复集市发布合规 | ①✅ F7 收口（T-6707）：`plugin.json` 新增 `publish.resources` 逐文件声明 4 个 docs 资产 + 包资源契约门禁（负向验证双注入通过；上游原文核实"目录不可声明"系误读，子目录文件路径可声明）；②✅ EN README 章节顺序与 zh 完全对齐（T-6708，15 章节 1:1；EN 历史迁移 docs/CHANGELOG.en-US.md）；③✅ B4 收口（T-6709：商店搜索/筛选/折叠三态审计，补折叠 aria 契约与零匹配空态行为验证；组件 adapter 侧 B4-2 已无缺口）；④✅ 性能基准稳定计数 +1（T-6710：v0.24/v0.25/v0.26 三版稳定，升硬门禁准入达成，v0.27 执行） | 不夹带新功能面；集市同步（T-6306 类）与生态邀请外发按用户时间插入本版窗口；城市表扩容、iCal BYSETPOS 仅作填充任务不承诺交付 |
| **v0.27.x 性能硬门禁 + 数据连续性** | 性能从告警线升级为发布硬门禁；存储与恢复长期可靠性（①②③ 已随 v0.27.0 成版，仅 ④ 后置） | ①✅ 四组基准升硬门禁（T-6711：过滤类 CPU 时间+均值无条件断言、聚合 40ms 病理线去 CI 豁免、倍增门已无条件；已随 v0.27.0 发布并在 CI 强制执行）；②✅ 存储迁移矩阵全面演练（T-6712：12 handled key × 5 类损坏电池 + 消毒不动点 + v0.23.5 老载荷升级模拟，负向验证点名拦截）；③✅ 文档集恢复报告导出（T-6713 核实：v0.20 已交付，构建器+设置页导出按钮+测试齐备，销项）；④T-6483 裸 fetch 迁移 + T-6485 UNION 双 LIMIT 拆分（准入：T-6305 真机取证完成；未完成继续后置） | 不做虚拟化重写、云端同步、行为追踪；环境长尾与产品回归分离，单次抖动不阻断开发 |
| **v0.28.x 生态验证** | 商店从"可用"进入"可扩展"（8.0.5 生态验证波内容后移承接） | ①至少一个真实第三方插件端到端接入验证（注册→配置→失效→恢复→跨表面）；②provider 真实宿主升级/重载场景取证；③D3 邀请跟进落地（SiYuan Copilot 为 `workspace-context`/`document-context` 天然消费方）；④source 撞名分组键切换、collection 二级分组仅当真实案例/数据出现才做 | 无真实第三方回应时本版收缩为协议文档与 widget-example 完善 + 邀请跟进，不虚构生态证据；邀请外发属用户动作，v0.26 窗口启动 |

跨版本用户参与项（不阻塞本地开发，按时间插入）：

| 项 | 内容 | 承接点 |
| --- | --- | --- |
| T-6305 真机取证批 | 执行链 7 项 + 组件二轮 5 项 + 窄侧栏度量（acceptance-runbook 清单已备，T-6702） | 完成后解锁 v0.27 的 T-6483/T-6485、Agent 手机能力矩阵与移动命中块定位评估 |
| 集市同步 | 集市账号操作 | 每次发版窗口插入 |
| 生态邀请外发 | SiYuan Copilot / HOME / Task Note Management 三候选（T-6672 调研） | v0.26 窗口发出 → v0.28 承接端到端验证 |
| Issue 反馈 | 逐项登记销号，发版内容与回复绑定 | 随时插入，优先于当期版本内容 |

三版本走完后的方向由 issue 反馈与生态回应决定：专项 A/B/C 已收口，若无新增反馈或生态回应，项目转入维护基调（依赖升级、上游内核契约跟进、回归修复），不凭空造功能；如需开辟新方向（含从暂缓清单解冻），由用户决断后另立波次。

### 8.0.7 2026-09-21 全插件性能与 UI 逐项优化战役（现行，用户指令立项）

用户指令：暂停城市表扩展；逐项提升各模块的性能与 UI，修订所有瑕疵。本战役与 §8.0.6 的 v0.29.x~v0.31.x 对应，版本号随批发布。

**战役结构**：审计框架（docs/ui-perf-audit.md）按界面面逐项登记审计结果与瑕疵修复状态；每个瑕疵独立修复、独立验证（负向验证按 gate-audit-checklist）、独立可回滚。

| 审计面 | 性能审计项 | UI 审计项 | 状态 |
| --- | --- | --- | --- |
| 第一面板（搜索/页签/缩略图） | 过滤<15ms 门禁✓、缩略图懒加载✓、缓存有界✓ | 空态/错误态/重试✓、焦点管理✓ | ✅ 门禁齐备，抽查进行中 |
| 第二面板时钟类（本地/世界/引言） | **F1：心跳整卡重建 DOM 待修**（每分钟/每秒全卡 teardown+rebuild，焦点/滚动靠补偿机制） | 秒显示/跨日标记/城市行 | 🔧 F1 修复中 |
| 第二面板联网组件（天气/RSS/iCal/HN 等） | 15 分钟心跳✓、TTL 缓存✓、失败抑制窗✓ | 日期容错✓（T-6725）、来源健康标注✓ | ✅ |
| 组件商店 | 分页✓、金丝雀过滤✓ | 搜索/筛选/折叠三态✓（T-6742b）、CORS 伺服器✓ | ✅ |
| 设置页 | 构建器纯函数✓ | 8 标签布局✓、双语✓ | ✅ |
| 移动端 | 首帧稳定尺寸✓、44px 触控✓ | 单/双/自动列✓、safe-area✓ | ✅ |
| 城市表 | 分组结构（单条 ~15 bytes）✓ | 中英双语全补齐（535→已停扩展） | ✅ 扩展暂停 |

F1 修复设计（下批实施）：home-view 渲染纯文本行组件时输出 `data-sw-row` 标记；home-controller 增加补丁快路径——同 moduleId、同状态、同行数且 label 匹配时只补 value 文本，否则整卡重建（结构变化不猜）。

**明确不做**：不做全量虚拟滚动、不为性能引入运行时依赖、不做第二套视觉系统（§2.1 约束不变）。

### 8.0.7 2026-09-21 悬浮球改造立项（用户需求，优先级：高于填充类，仅次于反馈/集市）

用户需求：悬浮窗改造为 FV/AssistiveTouch 式——点击打开切换器，拖动移动并弹出可自定义动作按键。**调研完成**（`docs/floating-ball-research.md`）：可行（窗口内 DOM 方案），关键机制=思源弹层为运行时递增计数器（window.siyuan.zIndex），悬浮球默认低于弹层让位、不搞无条件置顶；交互按市场收敛惯例（touch slop 分流、左右贴边、贴边半隐、闲置降透明度、弹层让位）；按键复用现有快捷动作配置。完整 UI 与功能规格见 `docs/floating-ball-ui-spec.md`，架构决策见 ADR 0066。

**实施计划（吸收 toolbar-customizer 经验后调整为六阶段，当前已开工）**：

| 批次 | 内容 |
| --- | --- |
| B0（T-6757） | ✅ 完成：独立 FloatingBallConfig v1、端侧可用性映射、默认预设、单例 portal、宿主重绘恢复、设置迁移与纯函数门禁 |
| B1（T-6758） | ✅ 完成：Pointer Events 拖动 + 8~10px touch slop + 左右边缘吸附 + yRatio 位置持久化 + 桌面/侧栏/手机独立生命周期与开关接线；旧 `fabEnabled` 保持兼容（设置控件留 B3） |
| B2（T-6759） | ✅ 完成：共享 quick-action catalog、统一 executor、首层最多 6 项与更多面板、未知/不支持能力处理、拖动命中、外部点击/Escape 关闭、桌面/侧栏/手机样式与空配置安全回退 |
| B3（T-6760） | ✅ 完成：悬浮球独立设置标签、桌面/侧栏/手机开关、端侧动作添加/排序/首层（最多 5 个配置动作 + More）、默认恢复、版本化导入/导出、512 KiB 限制、不可用状态与失败原子性 |
| B4（T-6761） | ✅ 代码完成：闲置降透明度、左右边缘半隐藏、全屏/后台让位、suspended/hidden 原因聚合、aria-hidden/inert/焦点清理、门禁与文档已收口；真实桌面/Android 验收按 BLOCKERS 后置 |
| B5（T-6762） | ✅ 本地代码与自动化验收完成：侧栏按宿主矩形定位、窗口/宿主 resize 安全区重算、更多面板焦点恢复与窄侧栏夹具；真实桌面/Android 证据仍按 BLOCKERS 后置 |
| B6（T-6763） | ✅ 完成：滚动显隐纳入悬浮球 controller 的独立 hidden reason；按配置绑定、按端侧宿主方向识别、关闭/销毁完整解绑；替换旧手机全局 touchmove 监听，桌面/侧栏/手机统一语义 |
| B7（T-6764） | ✅ 本地开发收口：完整外观/行为控件、自由停放、稳定半环/竖列/网格与命中算法、组件面板默认动作、更多搜索/分组/开关、键盘等价入口、能力恢复、执行超时、可视视口避让和滚动恢复把手；Chromium 与 100 次拖动回归，真实宿主/Android 验收按 BLOCKERS 后置，取舍见 ADR 0068 |

**新增工程约束**：动作面板首层最多 6 项；悬浮球与原生工具栏布局解耦；所有宿主观察器、定时器和指针监听器必须可枚举清理；配置导入导出必须版本化并限制容量；悬浮球动作不能直接复用切换器场景的 switcher 隐藏规则，必须使用独立 `context="fab"` 过滤并保留安全回退。**风险清单**（8 项及缓解）见调研报告 §5，参考项目审阅与吸收边界见 §9。**明确不做**：系统级跨应用悬浮窗、FV 全功能海、无条件置顶、100% 全透、以自动点击序列作为核心动作机制。

城市表扩展按用户指令**暂停**（535 城已足够；有用户请求时在分组数组追加即可）。

### 8.0.8 2026-09-22 悬浮球 P0 扩展与 P1-P3 路线（现行）

本轮承接 B0~B7 的本地收口，解决悬浮球“能用但不可编排”的三端体验缺口：悬浮球本体点击动作可按端侧设置；内置/插件动作可覆盖名称与图标；移动端对没有能力声明的动作保留未知态，并允许用户显式尝试；图标沿用小驴打卡的安全语义，支持 Emoji/短文字与受限的 HTTPS 图片、`data:image`，始终保留大小、协议和来源约束。实现与取舍见 [ADR 0069](docs/adr/0069-floating-ball-triggers-and-presentation.md)，竞品行为边界见 [调研记录](docs/floating-ball-competitor-research-2026-09-22.md)。

| 批次 | 任务 | 内容 | 状态 |
| --- | --- | --- | --- |
| P0 | T-6766 | `clickAction` 按 desktop/sidebar/mobile 保存；主点击支持切换器、更多面板或共享动作，失效时安全回退；拖动与点击继续分流 | ✅ 本地完成 |
| P0 | T-6767 | 动作行支持有界名称/图标覆盖，覆盖只影响呈现；图标支持 SVG symbol、Emoji/短文字、受限 HTTPS/data URL；导入导出保留字段 | ✅ 本地完成 |
| P0 | T-6768 | 手机端未知能力显式“尝试（未验证）”开关；设置页保留未知提示，提供方缺失仍不可执行 | ✅ 本地完成 |
| P0 | T-6769 | 三端设置页补齐点击动作选择、动作呈现编辑、状态与恢复路径；首层/更多继续使用共享动作目录 | ✅ 本地完成 |
| P0 | T-6770 | 模型、设置模型、面板、宿主和图标回归；补充导入导出、恶意输入清理、完整导出预算拒绝与安全回退验证 | ✅ 本地完成 |
| P0 | T-6771 | 真实桌面/侧栏/Android SiYuan 验收矩阵与截图证据 | ⏸ 受 B-004/B-005 后置 |

后续按用户反馈推进：**P1** 建立移动端通用动作目录（滚动到顶/底、前后页签、快速记录、目录/最近打开等），允许动作声明 `supportedSurfaces` 与 `mobileSafe`；**P2** 增加双击、长按、滑动方向、分组/九宫格、预设与配置备份恢复；**P3** 再评估插件发现、命令适配器、脚本权限、模拟点击兼容层与配置分享。P2/P3 必须继续保持执行能力与展示元数据分离，脚本和跨应用能力不能绕过思源权限。

2026-09-22 评估（用户需求"任意位置停放+外拨展开"）：自由停放已随 B7 交付（关闭吸附开关即可任意位置常驻，位置持久化无回撤）；"外拨展开面板"新触发器经评估可行，分解为 T-6784~T-6787（评估与待办见 `docs/dev-plan-2026-09-22.md`），其中布局层需补方向偏好输入、手势层需新增速度窗口判定的 sticky targeting 态；**手机端外拨依赖 T-6778 宿主手势归属先行**（贴边球的手指起点可能落进 3.8.5 侧滑激活区，见 ADR 0071）。

2026-09-23 用户指令（[ADR 0072](docs/adr/0072-floating-ball-sidebar-withdrawal.md)）：悬浮球撤出侧栏端（T-6788 已完成），只保留桌面主窗口与手机端——侧栏与桌面同窗重复且空间狭窄。挂载与设置收缩为 `FLOATING_BALL_UI_SURFACES=["desktop","mobile"]`；schema 三面保留以兼容旧配置与导入包，纯函数层不删。后续悬浮球触发器（T-6784~T-6787）只需覆盖两端。

### 8.0.11 2026-09-23 悬浮球增量功能路线（现行，基于源码与竞品调研）

调研完成（`docs/floating-ball-next-research-2026-09-23.md`）：思源 3.8.3+ 官方命令系统 `globalCommand`（插件可直接调用桌面 68+移动 27 条宿主命令）是最大增量，配合 `openTab({card})`、`getActiveEditor().protyle.insert`、`/api/template/render`、`/api/sync/performSync`、`openWindow`、`platformUtils.sendNotification` 与移动端原生桥，悬浮球动作空间从"插件自建动作"扩展到"宿主命令 + 系统桥接"。竞品分类学（FV 二维手势/drag-to-target/任务分享、AssistiveTouch 槽位、OPPO 场景化、三星 App Pairs）按单应用笔记软件过滤后确认：截屏录屏/分屏浮窗/应用推荐/背面敲击等不适配，明确不做。

| 批次 | 任务 | 内容 | 状态 |
| --- | --- | --- | --- |
| 近批 | T-6789 | 命令类动作底座：executor 增 `globalCommand` 分支 + 能力检测（3.8.3+，缺失降级不抬 minAppVersion） | ✅ 完成 |
| 近批 | T-6790 | 首批命令动作：闪卡复习、大纲、反链、收集箱、最近文档/关闭、只读切换（桌面 openTab 优先、手机命令分支回退） | ✅ 完成 |
| 近批 | T-6791 | 一键同步动作：performSync 直调 + sync 事件执行态 + 只读/发布模式降级 | ✅ 完成 |
| 近批 | T-6792 | 模板插入/快速记录升级：template/render + protyle.insert 光标写入 | ✅ 完成 |
| 次批 | T-6793~T-6795 | 桌面抛独立窗口；手机系统动作包（通知/收起键盘/回桌面/iOS 震动，裸桥判空降级）；键盘事件避让 | ✅ 完成 |
| 池 | 评估池 | drag-to-target、AI 选区问答、addFloatLayer、配置分享等（见调研报告 §4）；导出为图片/护眼/全屏**不可稳定达，明确不做** | ⏸ 等反馈 |

### 8.0.12 2026-09-23 调研-吸收循环（长期引擎，用户指令立项）

用户指令：当性能、功能、UI 均完善后，全网搜索值得研究的软件（GitHub、同类软件、Obsidian 等生态同类插件、思源集市插件），吸收优秀功能与想法，选出最适合的开发-测试-优化，然后进入下一轮。本节把该要求固化为**常驻循环引擎**：现役批次（§8.0.10/§8.0.11 队列）清空即滚入下一轮调研，调研与开发可并行，互不阻塞。

**每轮流程**：①全网搜索（Obsidian/Logseq 生态、思源集市、浏览器与编辑器导航、启动器/捕捉工具、手机系统交互）→ ②按评分卡筛选（产品定位契合、真实价值、许可纪律：GPL 只借鉴理念绝不复制代码、不碰暂缓清单）→ ③产出轮次报告与吸收排名 → ④最优项立项开发-测试-优化 → ⑤复盘并进入下一轮。

**第 1 轮（2026-09-23 启动）**：范围=Obsidian 同类插件、思源集市同类插件、广域同类软件（浏览器页签管理/编辑器导航/快捷捕捉/手机导航）。报告落 `docs/research-cycle-r1-2026-09-23.md`；吸收项按评分立项，接续 §8.0.11 队列消化。

### 8.0.13 2026-09-23 可选独立皮肤层（用户指令立项，ADR 0073）

用户指令：UI 效果开第二模式——默认继续与思源本体融合；另设完全独立于思源主题的皮肤模式（参考小驴打卡的独立样式），让各面板有更多展示形态。方向性结论（[ADR 0073](docs/adr/0073-optional-independent-skin-layer.md)）：机制已在打卡插件验证——**根作用域重定义 `--b3-*` 变量 + 少量质感补充（blur/圆角/阴影/壁纸），组件选择器零改写、宿主零影响**。设计原型已产出：`docs/design/skin-prototypes.html`（融合基线/苹果风格·液态玻璃 Light/暗夜玻璃·Dark Glass/暖纸墨水·Paper 四栏，真实生产 DOM+生产样式渲染，截图同目录 PNG）。

| 批次 | 任务 | 内容 | 状态 |
| --- | --- | --- | --- |
| S1 | T-6796 皮肤层架构 | 设置项（皮肤选择+外观档位）、`data-skin` 根作用域接线、`_10-skins.scss` token 桥接层、皮肤注册表纯函数+门禁 | ✅ 完成 |
| S2 | T-6797 苹果风格皮肤 | 按原型实现液态玻璃 Light/Dark；WCAG AA 对比度采样（复用 T-6697 夹具思路）+ 负向验证 | ✅ 完成 |
| S3 | T-6798 色板族扩展 | 暗夜玻璃、暖纸墨水等命名色板；皮肤名进配置版本化导入导出 | ✅ 完成 |

边界：不做主题编辑器/用户自定义取色；不做半融合混合态；皮肤变量只定义在速切根容器内（绝不写 body/:root）；无障碍与 44px 触控在皮肤下不降级。

### 8.0.14 2026-09-23 调研-吸收循环 R1 成果与吸收批（已被 R2 接续，条目全部完成）

R1 三路调研完成（Obsidian 生态/思源集市/广域同类软件，报告 `docs/research-cycle-r1-2026-09-23.md`），交叉去重后立项吸收批 **R1-A（T-6799~T-6804）**，排在 §8.0.11 近批之后消化：

| 任务 | 内容 | 价值/成本 |
| --- | --- | --- |
| T-6799 | 切换器统一索引（一框分区搜已开/最近关闭/收藏/文档集）+ 最近"只看有改动"过滤 | 高/小 |
| T-6800 | 收藏数字槽 1–9 直达 + 文档集"当前名指示/循环切换/离开自动快照/锚定文档" | 高/小 |
| T-6801 | 切换后光标/滚动位置恢复 + 闪烁高亮（集市空白点，全切换路径统一） | 高/中 |
| T-6802 | 搜索语法增强（`"精确"`/`-排除`/`@属性`）+ 同 query 上次选择置顶（本地记忆）+ 计分排序调优 | 高/中 |
| T-6803 | 悬浮球场景预设（场景=动作集一键切换，与文档集联动；与 T-6787 合流） | 高/中 |
| T-6804 | 动态收藏组（查询驱动智能分组；先宿主 API 取证再立项实施） | 高/中高 |

未采纳清单与候选池（日记面增强、手机导航栈等）见 R1 报告 §2/§3。R2 触发：R1-A 消化完毕或出现高价值新信号。

### 8.0.15 2026-09-23 调研-吸收循环 R2 成果与吸收批（现行）

R2 三路完成（CJK/拼音搜索专项、导航交互复扫、思源生态增量，报告 `docs/research-cycle-r2-2026-09-23.md`）。**核心发现：思源内核官方拒绝拼音搜索（issue #10489），插件层拼音匹配是差异化空白**——选型 vendor tiny-pinyin（MIT，gzip 3.5KB），L1 首字母/L2 全拼分层接入客户端过滤层，L0 子串兜底多音字。生态信号：番茄工具箱转专有许可（开源差异化窗口）；熊猫导航"跳转进原生历史"低成本可借鉴。

| 任务 | 内容 | 状态 |
| --- | --- | --- |
| T-6805 | 拼音首字母/全拼匹配（vendor tiny-pinyin；L1/L2 分层接入 filterOpenTabs 与标题过滤；会话缓存；设置开关） | ✅ 完成 |
| T-6806 | 速切跳转进入思源原生前进/后退历史 | ✅ 完成（自有跳转栈，原生 forwardStack 不可用） |
| T-6807 | 零词条工作台补全（空查询呈现智能分组与场景预设入口） | ✅ 完成 |
| T-6808 | Frecency 排序扩展（频率×新近加权） | 🧊 候补池（先用上次选择置顶，等待真实数据） |
| 池 | 过滤条 chips、Essentials 常驻层、并排打开、反链驱动组 | 随反馈 |

明确不做：pinyin-pro 主包进 bundle、omnisearch 移植（GPL）、jieba WASM、fuse.js 做 CJK。

### 8.0.16 2026-09-23 工作上下文导航路线（现行，ADR 0076）

上一轮调研报告 `docs/research-cycle-r4-2026-09-23.md` 已把 15 个方向收敛为一条执行路线。现有能力不是待推倒重做的旧版，而是下面各阶段的基础：

| 现有基础 | 下一步吸收 | 落地任务 | 首个可验收证据 |
| --- | --- | --- | --- |
| `search-model.js` 的三层搜索、统一索引、查询语法、拼音和搜索会话 | D1 统一导航与动作中枢、D2 排序诊断 | T-6812～T-6813 | 统一结果模型经过生产渲染；来源、上限、截断计数和降级原因可测 |
| 文档集、Essentials、跳转栈、场景预设、最近关闭 | D4 工作区/现场恢复 2.0 | T-6815～T-6816 | 恢复回执区分 restored/skipped/failed，并保留旧配置 |
| 当前文档关系摘要、Agent 只读能力、宿主 SQL/API 适配 | D3 关联内容、D12 健康中心 | T-6814、T-6817 | 反链/提及只读有界；宿主与动作失败可解释可重试 |
| 快捷动作 registry、`globalCommand`、模板/同步/快速记录桥 | D5/D6/D10/D13 动作生态 | T-6818～T-6821 | 动作描述、能力、输入、权限和恢复路径统一，执行仍走 registry |
| 桌面/侧栏/手机独立生命周期、首帧保护、皮肤层、浮球 | D7/D11/D15 表面与迁移 | T-6822～T-6824 | 真实 Android/桌面矩阵，密度/皮肤/配置包可回滚 |
| Agent capability 与受控执行链 | D14 受控 Agent/语义检索 | 后续评估 | 先满足排序质量、来源解释、权限和取消证据，再进入默认入口 |

执行顺序固定为：

1. **P0 可靠性**：T-6812 统一结果模型、T-6813 排序/诊断快照、T-6778/T-6779 移动宿主手势契约、T-6780 真机验收；
2. **P0 工作上下文**：T-6814 关联内容只读适配、T-6815 恢复回执和分层快照、T-6816 预览基础；
3. **P1 动作生态**：T-6817 动态组、T-6818 捕获流程、T-6819 提供方协议、T-6820 键盘优先、T-6821 深链接；
4. **P1/P2 表面迁移**：T-6822 移动端独立布局、T-6823 密度/皮肤组合、T-6824 可迁移配置；
5. **P2 受控智能**：仅在前四阶段证据充分后评估语义检索和 Agent 推荐。

每个任务都要写入当期开发账本，并按“纯模型 → 生产接线 → UI/端侧 → 宿主/真机验收”推进；缺少真实宿主时记录 `BLOCKERS.md`，不以浏览器模拟冒充 Android 证据。明确暂缓：完整主页/大屏、任意 SQL/JS、GPL 代码移植、系统级跨应用浮窗、复杂语义索引和没有数据支撑的 frecency。

Task Horizon 移动联动（T-6772～T-6775）：提供方复用现有手机管理器/快速新建表单并修复两条命令；雷切识别实时 `getQuickActionCapabilities()` 声明，复用旧动作 ID，执行时重查能力并接收失败结果。实现与作者交付在独立分支推进，设备验收仍归 B-004/B-005；详见 [ADR 0070](docs/adr/0070-task-horizon-mobile-commands.md) 和 [补丁交付说明](docs/integrations/task-horizon-mobile.md)。本次未将主工作区未提交的 P1 通用动作记为完成。

### 8.0.17 2026-09-23 调研-吸收循环 R5 成果与吸收批（现行，补充 §8.0.16）

R5 三路机制层调研完成（Obsidian+Notion/Craft、启动器/浏览器/编辑器/手机广域、思源生态+新兴笔记软件，报告 `docs/research-cycle-r5-2026-09-23.md`）。结论：**R4 骨架（ADR 0076 五阶段）不变，注入机制级吸收批 R5-A**；关键宿主 API（插件 RPC 网关、`/api/history/` 域、`openTab` 新选项、`addBreadcrumbButton`）已对上游源码逐项核实。定位两处强化：①生态出口——速切只读能力可经 `/api/plugin/rpc` 被外部 Agent/MCP 消费（T-6832 评估池）；②开源窗口——sy-quickswitch 停更闭源 + 番茄工具箱转专有，导航品类开源真空确认。竞品动态：Panda Navigation 功能面窄于速切（其前进后退/滚动收起/预设均已由 T-6806/B6/T-6803 先行交付），保持差异化迭代不响应。

| 任务 | 内容 | 来源 | 归位 |
| --- | --- | --- | --- |
| T-6825 | 搜索结果身份锚定：异步层返回/分区刷新时选中行与视口钉住（fzf --track 机制） | fzf v0.71+ | P0 可靠性，紧跟 T-6813 |
| T-6826 | openTab 新选项接入：`keepCursor`/`removeCurrentTab`/`afterOpen`/`doc.mode`，能力检测+旧版降级；恢复链不抢焦点 | 上游 API.ts | P0，支撑 T-6815/T-6816 |
| T-6827 | 保存的搜索命令化：查询+筛选存为命名命令，进全局命令注册表/悬浮球/数字槽 | Another Quick Switcher | P1 动作生态 |
| T-6828 | `/api/history/` 域接入：官方文档历史替换"只看有改动"SQL 数据源 + 历史回滚入口 | 上游 router.go | P0 工作上下文 |
| T-6829 | 文档集/场景预设版本历史：覆盖保存自动留 N 版可回滚 + Snapshot 手动档 | Workspace++/Context Workspaces | P1 |
| T-6830 | 打开策略层：按来源配置复用 vs 新开页签，防重复（含临时页签标记） | Open Tab Settings | P1 |
| T-6831 | 面包屑入口按钮：`addBreadcrumbButton` 一键进速切/关联内容 | 上游 Plugin 类 | P1 动作生态 |
| T-6832（评估池） | 插件 RPC 网关：只读能力注册为 RPC 方法供外部 Agent/MCP 消费 | 上游 router.go | 依赖 D6/D14 进入条件 |

执行归位：T-6825 排 T-6813 后同批收口；T-6826/T-6828 并入 P0 工作上下文批；其余按表归位。候补池与"不值得吸收"清单见 R5 报告 §5/§6。

### 8.0.18 2026-09-25 全景盘点与下一程规划（现行；盘点对话=2026-09-25 审查轮后）

**现状基线**：v0.36.0 已发布 + 审查轮修复（c2edd56）待随 v0.37.0 攒版。ADR 0076 五阶段中 P0 可靠性/P0 工作上下文/P1 动作生态/P1-P2 表面迁移**主体全部落地**；R5-A 吸收批 T-6825~T-6831 全部完成；候补池用户反馈 P1~P7 全部消化（P8 数据库视图用户明示后置）。键盘优先链路（Tab→↑/↓→1-9/Enter→Alt 预览→`>` 命令）、搜索体验批（高亮/净化/直达/预览窗格）、审查工具（SWSS_AUDIT 截图、内核探针）已入库。基线 6133/6133。

**未开发项全清单**（按证据状态分类）：

| 类别 | 项 | 进入条件 |
| --- | --- | --- |
| 等外部证据 | T-6780 Android 真机零误触验收（B-004/B-005）；T-6305 夹具真机批 | 设备/会话 |
| 等上游 stable | 关联内容锚文本排序（3.8.6-alpha 已做反链排序） | 3.8.6 stable |
| 等生态回应 | T-6832 RPC/MCP 出口（内核原生 /mcp 事实已入档）；Task Horizon 联动验收 | 第三方接入意愿 |
| 用户明示后置 | P8 数据库视图形态渲染（方向已获上游确认） | 用户指令 |
| 有规格待实现 | 预览窗格 sticky 吸顶/悬停触发；命令模式分组折叠（复用 P5 模式）；合成名去重；P-F 底栏动作标签 i18n | 反馈驱动 |

**竞品与模式调研结论**（2026-09-25 广域检索）：思源集市内无同位竞品（sy-quickswitch 停更闭源、Panda Navigation 功能面窄），**导航品类开源真空持续**——差异化窗口仍在，无需响应性开发。外部模式吸收方向：①Arc/Dia 的 spaces+命令栏模式已被工作台停靠坞+文档集+场景预设先期覆盖（我们的"工作上下文"定位与其同构且更早落地）；②[Raycast 2026 登陆 Windows](https://apps.microsoft.com)后命令栏品类进入大众视野——其 Quicklinks/AI Commands/Notes 模式对速切的映射=已落地的保存的搜索/命令模式/快速捕获，**剩余可吸收=窗格布局持久化与剪贴板历史的"启动器纵深"**（后者与思源剪贴板面板重叠，不值得）；③Kairo 等 2026 新浏览器按 workflow 组织会话——对应我们的文档集，验证方向正确。

**下一程三批**（P0→P2，全部有规格，不等反馈即可开工）：

| 批次 | 任务 | 内容 | 验收 |
| --- | --- | --- | --- |
| **C1 恢复纵深（P0）** | T-6840 关联内容 SWR 持久化 | 工作台关联投影写入插件存储（版本戳 key 走 D-401 仪式、有界 ≤8、60s TTL 语义不变），重启后冷启动消除；先显缓存标注"缓存"再后台刷新（P2 反馈的剩余半截） | 契约测试+负向验证；重启后首屏有缓存数据 |
| **C1 恢复纵深（P0）** | T-6841 恢复报告导出增强 | 文档集恢复报告（已有 buildDocumentSetRestoreReport）增补 Essentials 回执明细（opened/failed/skipped 逐项），Markdown 导出含失败原因码 | 导出文件结构单测 |
| **C2 预览深化（P1）** | T-6842 预览窗格 sticky 吸顶 | 结果区滚动时窗格 position:sticky 贴顶（现 absolute 随区滚动）；移动端维持不提供 | E2E 截图对比 |
| **C2 预览深化（P1）** | T-6843 预览窗格悬停触发 | focusin 之外补 mouseenter 委托（同一 debounce 管线）；触摸设备忽略 | 契约+E2E |
| **C2 预览深化（P1）** | T-6844 合成名去重 | 预览大纲首项（合成文档名）与文档自带 h1 同名时跳过，防重复行 | 纯模型单测 |
| **C3 一致性（P1）** | T-6845 快速动作标签 i18n | 内建动作目录 label 走 i18n key（P-F），底栏/命令模式/悬浮球全消费面统一 | i18n key 一致性门禁 |
| **C3 一致性（P1）** | T-6846 命令模式分组折叠 | `>` 列表按内建/宿主命令分组，复用 P5 折叠交互（会话内记忆） | 契约+E2E |

**执行口径**：三批为"规格已齐的消化开发"，C1 最先（用户反馈价值最高）；每批完成后按调研-吸收循环进入 R8（方向=命令栏品类 2026 新模式 + 思源 3.8.6 stable 跟踪）。P8 数据库视图与 Agent 受控写入仍按原约束不自动开工。

### 8.0.19 2026-09-25 三大支柱规划（现行；用户指令定调，任务号 T-6847~T-6858）

用户定调三大支柱：**①页签面板=核心，性能/UI/速度/显示内容都要快和全；②组件面板=整体显示、每组件显示与设置、商店显示与分组重新优化；③悬浮球=性能与稳定性保障，然后逐步扩充可选按钮**。§8.0.18 的 C1~C3（T-6840~T-6846）按支柱重新归位，新增任务 T-6847~T-6858。

**支柱一：页签面板（核心）**

已固化：三层搜索、键盘链路闭环（Tab→↑/↓→1-9/Enter→Alt 预览→`>` 命令）、预览窗格、视口锚定、缩略图可视区懒加载、数字直达角标、工作台停靠坞。

| 任务 | 内容 | 优先级 | 验收 |
| --- | --- | --- | --- |
| T-6840 | 关联内容 SWR 持久化（§8.0.18 C1 归位）：投影写入插件存储（D-401 版本戳仪式、≤8 有界），重启冷启动消除；先显缓存标注"缓存"再后台刷新 | P0 | 契约+负向验证；重启首屏有缓存 |
| T-6841 | 恢复报告导出增强（§8.0.18 C1 归位）：Essentials 回执逐项明细+失败原因码入 Markdown 导出 | P1 | 导出结构单测 |
| T-6847 | 大页签集性能基准门禁：100+ 页签合成夹具下本地过滤 ≤50ms（§3.5 告警线→自动化门禁）、缩略图滚动帧预算；复用 best-of-3 进程内计时先例（注意满载假红教训） | P0 | 基准测试+负向注入验证 |
| T-6848 | 卡片信息层级增强：页签卡更新时间徽标（updated map 已有）+改动标记（与"只看有改动"同源）；设置可控显示 | P1 | 契约+E2E 截图 |
| T-6842~T-6844 | 预览窗格深化（§8.0.18 C2 归位）：sticky 吸顶、悬停触发、合成名去重 | P1 | E2E+纯模型单测 |
| T-6849 | 首帧骨架评估：先测切换器打开→可交互耗时，超标才做骨架态（防过度工程） | P2 | 耗时数据先行 |

**支柱二：组件面板**

已固化：58/58 组件评分卡、商店分组折叠/密度/视图模式/四排序、秒开 fragment 装配、第二面板尺寸模式（follow/adaptive/custom/fullscreen）、刷新全部。

| 任务 | 内容 | 优先级 | 验收 |
| --- | --- | --- | --- |
| T-6850 | P8 数据库组件视图形态渲染（原后置项随本支柱纳入）：消费 columns+rows 渲染有界表格、按当前视图渲染、其他视图降级+视图名标注、行上限+点击跳源库；av 端点结构化取数无 SQL 拼接面 | P1 | 模型单测+E2E（真实 av 夹具已有） |
| T-6851 | 面板状态持久化：商店/面板的分组折叠（collapsedGroups 会话 Set→存储）、密度、视图模式跨会话记忆（版本戳 key） | P1 | 归一化测试+负向验证 |
| T-6852 | 组件设置直达：组件卡片 hover/长按菜单加"设置"项直跳设置页对应段，减少层级 | P1 | 契约+E2E |
| T-6853 | 商店发现性：recommended 置顶标识、分组内计数徽标、新组件"新"标（基于 installedAt） | P2 | 模型单测 |
| T-6854 | 组件卡统一骨架与内联重试：加载骨架统一组件化；失败卡片内联重试按钮（不依赖工具栏"刷新全部"） | P1 | 契约+负向验证 |

**支柱三：悬浮球**

已固化：三层防误触（L1 契约/L2 capture 拦截/L3 滚动链阻断）、离边停靠+上滑呼出、外拨手势速度窗口、场景预设、动作面板、P5 选择器分组折叠、宿主命令目录（9 项 MOBILE 分支源码核对）。

| 任务 | 内容 | 优先级 | 验收 |
| --- | --- | --- | --- |
| T-6855 | 球面板性能回归夹具：面板开/关 10 循环监听泄漏审计（addEventListener 计数）+面板首帧耗时基准；**扩充动作前先立护栏** | P0 | 夹具测试+泄漏负向注入 |
| T-6845 | 快速动作标签 i18n（§8.0.18 C3 归位）：内建目录 label 走 i18n key，底栏/命令模式/悬浮球全消费面统一（P-F 收口） | P1 | i18n key 一致性门禁 |
| T-6856 | 动作目录扩充至 25+ 内建：候选=第二面板开关、组件面板、文档集列表、marks 跳回、剪贴板入口、快速捕获变体；每项 targets 三端声明+能力检测+缺失隐藏 | P1 | 契约+目录计数断言 |
| T-6857 | 数字槽绑定保存的搜索（候补池既有项）：球面板 1-9 绑定保存的搜索/动作，长按配置 | P2 | 契约 |
| T-6858 | 径向快捷菜单：外拨 4 方向各绑一动作（默认 切换器/快速捕获/上一页签/下一页签），复用 T-6784 速度窗口判定；设置页可视化绑定 | P2 | 触控夹具+负向验证 |

**执行顺序**（五波，每波单独提交可回滚）：
1. ✅ **第一波=护城**（2026-09-25 完成，3cee820）：T-6847 性能门禁 + T-6855 球泄漏夹具 + T-6840 关联 SWR；
2. ✅ **第二波=恢复纵深**（2026-09-25 完成，f153a9b）：T-6841 + T-6842~T-6844；
3. ✅ **第三波=悬浮球扩充**（2026-09-25 完成，0ccb359/f627b16）：T-6845 + T-6856（目录 34 项）；
4. ✅ **第四波=组件面板**（2026-09-25 完成）：T-6850 + T-6851 完成；T-6852/T-6854/T-6853 核查后收敛（能力已覆盖：内联重试/卡头配置/组计数徽标均已存在）；
5. ⏸ **第五波=交互升级**（未开工，下一会话首选）：T-6857 + T-6858 + T-6848 + T-6849；或先走 v0.37.0 攒版发布（四波增量已攒够，交接快照见 dev-plan-2026-09-22.md 末尾）。

约束不变：每任务"纯模型→生产接线→UI→门禁（含负向验证）"；包体线 1088 KiB raw（ADR 0081）/ 512 KiB zip（当前 `dist/index.js` 1025822 B，余 6754 B）；不引入第二套搜索/动作架构；真机项仍归 B-004/B-005；**push 前必须 `pnpm exec tsc --noEmit`（CI 会拦，本地 build 不查类型）**。

### 8.0.20 2026-09-25 实验代码片段工作室（T-6859～T-6862）

用户明确要求继续推进实验代码片段工作室。本方向优先于第五波交互升级，但当前不进入发布承诺；初始入口只在桌面页签切换器工具栏，T-6867～T-6868 已将它收敛到共享平台外壳，侧栏和移动切换器/工作台只呈现已支持表面，移动端仍不开放片段实验室。

| 任务 | 内容 | 状态 | 证据/下一步 |
| --- | --- | --- | --- |
| T-6859 | 固定 SiYuan v3.8.5 片段/AI 契约、权限、无 CAS 限制、字体和包体分期 | ✅ 完成 | `.research/siyuan-v3.8.5`、schema 生成 fixture、ADR 0078；无原子保存能力已明确记录 |
| T-6860 | 原生片段列表管理、严格请求投影、目录、禁用导入、导出和本地投稿包 | ✅ 完成（实验） | `/api/snippet/*` + `/api/setting/setSnippet`；保存后回读核对；模型/宿主专项测试；并发读写窗口仍受上游整表接口限制 |
| T-6861 | 隔离预览与宽屏工作室 UI | ⏳ 进行中 | CSS opaque-origin sandbox、4:1 布局、响应式和焦点清理已接入；JS 同步执行按钮暂禁用；Worker 无 DOM，后续只能研究受限模拟预览协议和真实桌面截图 |
| T-6862 | 思源 AI 草稿生成、优化、解释和迭代 | ⏳ 进行中 | 当前支持生成/优化/解释/迭代、taskID、history 边界、SSE 取消/超时/过期回包、手动接受；官方没有无副作用的编辑器 AI 能力预探测，真实宿主 SSE 和更完整多轮体验待后续 |
| T-6864 | 二次功能/交互/UI 调研 | ✅ 完成（研究） | `docs/research-snippet-studio-2026-09-25.md`；下一批先补发布禁用、冲突恢复、备份、顺序和编辑器效率；本轮无生产代码变更 |
| T-6865 | 三大面板与统一切换平台再梳理 | ✅ 完成（研究） | `docs/research-unified-switch-platform-2026-09-25.md`；定位升级为“统一切换与工作上下文平台”；下一批先补统一结果/能力语义、第一面板对象动作分区、悬浮球平台回退和组件健康回执；本轮无生产代码变更 |
| T-6866 | 统一平台 UI、功能与交互设计 | ✅ 完成（设计） | `docs/design-unified-platform-ui-2026-09-25.md` + ADR 0079；定义 `切换器/工作台/片段实验室` 三表面和“悬浮球=全局触发器”，补齐共享外壳、token、状态、对象/动作、路由栈、跨表面回执、片段安全边界和端侧策略；本轮无生产代码变更 |
| T-6867 | P1 统一视觉基础层与表面标记 | ✅ 完成（P1-a） | 新增 `src/styles/_platform-shell.scss` 与 `--sw-platform-*` token/状态/动作/预览原语；切换器、工作台、片段实验室和对应 Dialog 增加静态 surface/platform 标记，保留现有业务 DOM、动态 chunk、脏稿守卫和悬浮球生命周期；`pnpm exec tsc --noEmit`、Sass、完整测试 **6184/6184**、生产构建和 quality audit 50/50 通过 | P1-b 已在 T-6868 接入 |
| T-6868 | P1-b 统一 SurfaceNav、ContextBar 与表面入口动作 | ✅ 完成 | `src/index.ts` 提供共享平台文案、外壳挂载和 `openPlatformSurface(surface, returnTo)`；桌面/移动切换器、工作台、片段实验室和侧栏使用同一表面导航，导航先销毁当前 Dialog；片段实验室保留 `canClose()` 脏稿守卫并在自身生命周期挂起/恢复悬浮球；移动端只开放切换器/工作台。修正 `returnTo` 贯穿桌面/移动切换器，工作台→切换器→片段实验室返回链保持上下文；完整测试 **6184/6184**、构建、`quality:audit` 50/50、`repro:audit` 3/3、release-check、生产图 66/66、四套 Chromium/浮球冒烟通过 | P1-c 统一对象/状态回执与更完整路由上下文；悬浮球恢复上次表面仍后置，真实桌面/Android 按 B-004/B-005 |
| T-6869 | P1-c 统一平台路由上下文（SurfaceContext）、表面 Dialog 单例保护与悬浮球恢复上次表面 | ✅ 完成 | 新增 `src/platform-surface-model.js` 纯模型（表面白名单、`normalizeSurfaceContext` 有界上下文、`resolveSurfaceReturnTarget` 恢复回退、`buildSurfaceContextCaption` ContextBar 投影），生产图 66→67 按先例校准；`index.ts` 增加会话级 `lastPlatformSurface` 记录与 `openPlatformSurface(surface, returnTo, context)` 上下文透传（surface-nav/fab/back/toolbar entry），ContextBar 经 caption 投影渲染；桌面/移动切换器与工作台三个 Dialog 增加单例守卫（destroy-before-open，重复热键/悬浮球连点不再叠窗），片段实验室既有守卫与 `canClose()` 脏稿守卫不变；悬浮球轻触改为恢复上次表面（`resolveSurfaceReturnTarget`，移动端桌面专属实验室安全回退切换器）；工作台编辑现场经表面导航离开时记录、重开时恢复且焦点回到布局开关。新 `tests/platform-surface-model.test.cjs` 4 项 + wiring 契约 22 断言，负向验证 5 注入（FAB 恢复接线/工作台单例守卫/编辑现场记录/生产图上限回退/模型回退失效）均精确红后字节还原；`pnpm exec tsc --noEmit`、完整测试 **6189/6189**、构建（index.js 1028649 B，raw 线内余 ~19.9 KiB）、`quality:audit` 50/50、`integration:audit` 50/50、`repro:audit` 3/3、release-check、四套冒烟与 `verify:release` 全链通过 | P1 收口；下一批=P2 跨表面对象/动作（统一对象描述、片段结果"在实验室打开"）与 objectId 级焦点恢复；真实桌面/Android 按 B-004/B-005 |
| T-6870 | 全插件统一 UI 原型与细节优化设计（用户指令） | ✅ 完成（设计，无生产代码变更） | 盘点结论：平台外壳与三表面设计已齐（ADR 0079，P1 已实现），**设置页 11 标签/对话框浮层/移动端此前无逐页原型**——本轮补齐 `docs/design/platform-ui-redesign-2026-09-26.html` **26 屏**（表面 4+设置 11+对话框 6+移动 3+暗色 2，真实生产 DOM+dist CSS+提案层渲染），截图 `docs/design/redesign-2026-09-26/`（重放 `node scripts/design-screenshots.cjs`）；设计规格 `docs/design-unified-platform-ui-round2-2026-09-26.md`（参考 Raycast/Linear/Things/macOS Settings/Geist，六条应用模式；细节优化清单立项 RZ-1~RZ-6 = T-6871~T-6876） | 用户过目原型后按 RZ-1→RZ-6 分批实现；与 P2 跨表面对象并行不冲突 |
| T-6871 | RZ-1 平台 UI 原语组件化 | ✅ 完成 | `_platform-shell.scss` 补齐 kbd 芯片/上下文栏 kbd 槽/分段控件/胶囊（--pill/--soft）/徽标语义色点，coarse 指针 44px 命中区；新模块 `platform-dom.js`（六态徽标/kbd/分段控件/胶囊的 DOM 助手，document 注入，生产图 67→68）+ jsdom 行为测试 5 项；首个消费点=切换器上下文栏键位提示组（Tab/1-9/Enter/Alt 预览，i18n 双语）；Chromium 冒烟新增 5 个平台原语对比度采样点，**首跑抓到真实违规**（宿主 warning/error 原色做徽标小字 light 2.9 / dark 2.99 未达 3.0）→ 徽标文字与节假日红改语义色×正文混合后 dark/light 全 PASS；wiring 契约 16 断言（块级 declaresIn）+ 4 次注入负向验证全精确红；完整测试 **6195/6195**，包体 1030578 B（raw 线内余 ~18 KiB），四套冒烟全绿 | RZ-2（T-6872 设置页分组卡片+行描述+枚举分段化） |
| T-6872 | RZ-2 设置页分组卡片与枚举分段化 | ✅ 完成 | 宿主新增 `settingGroupTitle/settingGroupCard/settingSegmented` 三方法（Host 接口同步）；五个标签（外观/行为/面板/组件面板/移动端）重组为语义分组卡片；**8 处枚举改分段控件**（皮肤/面板尺寸/dock 显示/侧栏布局/组件强调色/工作台尺寸/移动列数/密度），列数(9)与排序(6)保留下拉；密度开关升级为舒适·紧凑分段（body 标记语义不变）；i18n 新增 15 键双语、删除被替换的 densityCompactLabel；T-6872 契约（分段/组标题/SCSS 块级/双语键）+T-6823 密度契约更新；**2 次注入精确红后还原**；完整测试 **6196/6196**；包体 1031963 B（raw 线内余 ~16.2 KiB）；四套冒烟全绿 | 设置页真实宿主逐标签截图对比归审计截图扩展批；下一批=RZ-3（T-6873 切换器细节） |
| T-6873 | RZ-3 切换器细节：角标 kbd 化与预览状态徽标 | ✅ 完成 | 卡片/结果行数字角标（attr() 机制不变）改 kbd 芯片皮肤（不透明 surface 底+边框+代码字体，缩略图上可读性不回退）；预览窗格头部接六态徽标 loading→ready 两态（`setDocPreviewStatus` + docSearchPreviewStatusLoading/Ready 双语键），头部 flex 徽标右置；"行图标容器"经核查已被现有 `.sw__doc-icon` 32px 瓦片覆盖记收敛；T-6873 契约（attr 机制/surface 底/代码字体块级断言+两态调用+flex 头部）+ 2 次注入精确红；完整测试 **6197/6197**；包体 1032318 B（raw 线内余 ~16.3 KiB）；四套冒烟全绿 | 下一批=RZ-4（T-6874 工作台编辑横幅与商店动作胶囊） |
| T-6874 | RZ-4 工作台编辑横幅与商店动作胶囊 | ✅ 完成 | renderPanel 编辑态分支插入 `sw-home__edit-banner` 横幅（homeEditingHint 双语 + 完成按钮复用 editing 状态源）；主色软底+10px 圆角横幅样式；商店 `__add` 主色实底胶囊、`__configure` 主色软底胶囊（既有 store 契约 min-width/字重/焦点环/nowrap/touch-action 全保留继续通过）；T-6874 契约 + 2 次注入精确红后还原；完整测试 **6198/6198**；包体 1032712 B（raw 线内余 ~15.9 KiB）；四套冒烟全绿 | 健康徽标/回执条拆 T-6874b；下一批=RZ-5（T-6875 快速捕获对话框统一） |
| T-6875 | RZ-5 快速捕获对话框统一 | ✅ 完成 | 目标段选补分段控件外观并**修复真实缺陷（sw__target--active 类切换从无 CSS 规则，激活目标不可辨识）**；保存按钮主色胶囊（专用类覆盖 outline）；footer 键位提示组只陈述真实绑定（Ctrl+Enter 保存/Esc 关闭，createPlatformKbd 产出）；T-6875 契约 + 2 注入逐一精确红；完整测试 **6199/6199**；包体 1033069 B（raw 线内余 ~15.5 KiB）；四套冒烟全绿 | 下一批=RZ-6（T-6876 移动端底栏堆叠与 sheet 审计） |
| T-6876 | RZ-6 移动端底栏堆叠与 sheet 审计 | ✅ 完成（RZ 收口） | 移动作用域快捷动作改图标上/标签下堆叠（44px 命中区，仅 .sw__mobile 作用域桌面不变）；sheet 抓手/16px 顶圆角/safe-area 经审计既有实现已覆盖设计目标记收敛并以契约锁定；T-6876 契约 + 1 注入精确红；完整测试 **6200/6200**；包体 1033069 B（raw 线内余 ~15.5 KiB）；四套冒烟全绿 | **RZ-1~RZ-6 全部收口**；下一批=用户过目 R2 原型后转 v0.37.0 攒版 |
| T-6877 | 三表面原型 R2 反馈迭代与默认全屏（ADR 0080） | ✅ 完成 | 用户三点反馈落地：①切换器默认全屏+已开页签 4 列大卡占主视野（收藏/最近压缩保留、预览常驻）②工作台默认全屏+组件完整渲染（商店压缩为回执条入口）③片段实验室**吸收原始开发框架**（预览主区 4fr+下排 属性:编辑器 2fr:3fr+AI 右栏+目录浮层选择器）；**采纳默认全屏**（panelSizeMode/homeSizeMode 默认 fullscreen、工作室视口全屏；updateSettings 全量落盘→仅影响新装；尺寸选项全保留；ADR 0080）；细节抛光=控件 28px/分段 26px 统一高度+三级文字刻度+唯一主色胶囊；R2 原型 3 屏（platform-ui-redesign-r2）+截图；T-6877 契约+1 注入精确红；完整测试 **6201/6201**；包体 1033010 B；四套冒烟全绿 | 已随 v0.37.0 发布；其后=P2 跨表面对象第二批 |
| T-6878 | P2 跨表面对象第一批：片段对象进工作台与 objectId 定位 | ✅ 完成 | `platform-surface-model` 新增对象种类白名单与 `projectSnippetObjects` 有界投影（id 去重/类型白名单/查询过滤/上限 8）；getSnippet 入端点白名单+字面量分发；零态工作台新增片段实验室行（惰性取数+60s 缓存+竞态丢弃+骨架占位）；chip 携带 objectId 打开工作室并 id 精确/名称回退定位（经脏稿守卫）；模型 3 测试+wiring 契约+2 注入精确红；完整测试 **6205/6205**；包体 1035567 B（余 ~13.0 KiB）；四套冒烟全绿 | 随 v0.38.0 攒版 |
| T-6879 | T-6874b 工作台组件健康回执 | ✅ 完成 | refresh 包装器把结果写回单元 data-sw-health（ok/failed）并聚合底部回执条（sw-home__receipt：ok/total 正常 · 失败 n，i18n 模板插值，无单元移除）；失败单元错误色描边；T-6879 契约+1 注入精确红；完整测试 **6206/6206**；包体 1036195 B（余 ~12.1 KiB） | 随 v0.38.0 攒版 |
| T-6880 | P2 跨表面对象第二批：组件对象描述与失败两通道 | ✅ 完成 | 工作台单元 aria 对象语义三段描述（组件·模块名·健康）随健康重写；失败单元错误色边框+attr() 文字 chip 双通道（OK 清空）；T-6880 契约+1 注入精确红；完整测试 **6207/6207**；包体 1036531 B（余 ~12.0 KiB） | 随 v0.38.0 攒版 |
| T-6881 | P2 跨表面对象第三批：查询态片段分区 | ✅ 完成 | 搜索关键词命中原生片段时结果区呈现片段实验室分区（sw__snippet-results，insertBefore doc-results）；过滤抽为 filterSnippetObjects（projectSnippetObjects 复用、二次过滤保留 lines）；取数收敛 ensureSnippetObjects（60s 缓存+单飞共享+代际丢弃，杜绝逐键打内核）；陈旧关键词守卫丢弃迟到渲染；命令模式先行清理；过滤条 chips 可见性语义接入；模型 8 测试+T-6878 契约演进+T-6881 契约+1 注入精确红；完整测试 **6209/6209**；包体 1038623 B（**余 ~9.7 KiB 告警水位**） | 包体复核已执行（T-6882） |
| T-6882 | 包体复核与 raw 线校准 1024→1088 KiB（ADR 0081） | ✅ 完成 | 减法审计：raw 线只度量 index.js（CSS 不在内）、JS tree-shaking 后无可观死重量、增长可归因已审计批次（1024 线设定以来 +53.7 KB）→ 按先例校准 1088 KiB（+64 步进）；硬上限不变；压缩条目线 288 KiB 逼近（~285 KiB）越线需新 ADR；metrics 常量+release-quality dated 注释+readiness 两行+second-round-widgets 钉死断言同步；**1 注入精确红**；完整测试 **6209/6209**（两次满载偶发假红隔离复测恒过） | 随 v0.38.0 攒版；压缩条目线越线需新 ADR |

边界：原生片段是启用状态唯一来源；预览不写生产文档；导入保持禁用；JS 关闭不能撤销既有副作用；无社区服务端时只生成本地未审核投稿包；不读取模型密钥、不自动请求付费模型。工作室代码使用动态 chunk，保持主入口包体在线内；完整门禁、生产构建和浏览器截图仍是本方向退出条件。

### 8.0.9 2026-09-22 思源 3.8.5 侧滑与悬浮球输入边界复核（研究完成，暂不开发）

用户反馈思源手机端 3.8.5 更新左右滑后，左右边缘悬浮球容易触发侧栏。源码复核已完成（`docs/mobile-swipe-floating-ball-research-2026-09-22.md`、[ADR 0071](docs/adr/0071-mobile-swipe-ownership-for-floating-ball.md)）：3.8.5 在 `touch.ts` 中把普通页面侧滑方向激活距离设为 12 px，并按屏宽三分之一或 32 px+快速甩动提交；document 级 touch listeners 会继续看到悬浮球触摸。现有 `touch-action:none`、Pointer Capture 和 8~12 px plugin slop 不能表达对宿主 JavaScript 手势的所有权。思源已有 `data-prevent-swipe` 整轮手势契约，悬浮球当前 portal 尚未声明它。

本轮结论：B0~B7 的悬浮球架构、动作目录、几何和位置方案继续可用；下一次实现只补移动端宿主手势契约，不先调阈值、不修改用户 `sidebarSwipe`、不把球简单移离边缘。推荐方案 A 是给移动 portal 根、触发按钮和恢复按钮设置 `data-prevent-swipe`；方案 B（局部 touch 冒泡隔离）只在 3.8.0 或真实设备仍有冲突时评估。3.8.1~3.8.5 已从源码确认 marker 契约，3.8.0 未发现，最低版本暂不抬升。

| 批次 | 任务 | 内容 | 状态 |
| --- | --- | --- | --- |
| 研究 | T-6777 | 锁定 v3.8.5/v3.8.4 源码、还原侧滑阈值/提交条件、核对现有 portal 事件链、记录替代方案和验收矩阵 | ✅ 完成；本轮无生产代码变更 |
| P0 | T-6778 | 移动 portal 根/触发按钮/恢复按钮加入 `data-prevent-swipe`；保留 Pointer Events、CSS 和配置 schema | ⏸ 待用户确认后开发 |
| P0 | T-6779 | v3.8.5 touch 判定夹具：有 marker 时整轮不触发侧栏，无 marker 时保留宿主行为；负向注入与生命周期清理 | ⏸ 待 T-6778 |
| 真机 | T-6780 | 3.8.5 Android/桌面验收：左右拖动、更多面板竖向滚动、旋转、键盘、侧栏开关与连续拖动 | ⏸ 受 B-004/B-005 后置 |
| 条件 | T-6781 | 仅当 3.8.0 兼容或真机证据证明 marker 不足时，评估局部 touch firewall 或独立抬升最低版本 | ⏸ 依赖 T-6780，不预先实现 |

退出条件：左右两侧悬浮球连续拖动零次意外侧栏提交、零次重复动作；更多面板竖向滚动和长按语义不回归；契约测试按 `docs/gate-audit-checklist.md` 完成负向验证；真实 Android 证据与浏览器夹具分开记录。

### 8.0.10 2026-09-22 设置页 UI 优化立项（用户指令，悬浮球优先）

用户指令：优化设置页的 UI 与实际效果，尤其悬浮球相关设置要更加美化。本专项只调呈现层（SCSS 与少量装饰性 DOM），不动配置 schema、不改 `settingItem`/`data-control` 语义与既有 class 钩子；视觉延续 R7 token 语言与思源主题变量（`--sw-accent-soft`、`color-mix`、浮层阴影），不建第二套视觉系统。预览区目标是"所见即所得"：预览球与动作卡对齐真实浮层视觉（主色球体+双层阴影、12px 圆角浮层卡），舞台提供贴边感应区提示与端侧徽标。

| 批次 | 任务 | 内容 | 状态 |
| --- | --- | --- | --- |
| U1 | T-6782 | 悬浮球设置深度美化：预览舞台"设备屏幕"质感与 snap 感应区提示、端侧 chip 徽标、预览球/目标卡对齐真实浮层视觉、预览/外观/动作三区统一卡片层级、控件网格输出值胶囊与 range 主题化、动作行序号徽标与 hover 反馈 | ✅ 完成 |
| U2 | T-6783 | 设置页其余标签页复用同一卡片语言逐页跟进（存储用量、文档集、导入导出等），只调样式不改数据契约 | ✅ 完成 |

约束：若契约测试需覆盖新增装饰性结构，按 `docs/gate-audit-checklist.md` 做负向验证；美化不改变任何设置项的持久化行为与无障碍语义（44px 触控、focus-visible、aria 保持不回退）。

### 8.1 2026-09-09 重整基线（历史记录）

- 以下内容记录 2026-09-09 的阶段性基线，仅用于追溯，不代表当前 GitHub 状态；当前状态以本文顶部和 8.0 确认计划为准。
- 当时 GitHub 状态：公开仓库无开放 Issue/PR，`origin/main` 停在 `8992978`，最新 Release 为 `v0.16.11`，对应远端 CI 已通过。
- 本地状态：`main` 在远端之后有 149 个提交，其中 101 个为测试提交；这些提交已形成连续历史，但尚未推送或发布。
- 已接入产品路径：受限全文搜索编排、最近打开、快捷入口 registry、只读 Agent 导航/搜索能力，以及移动排序与首帧保护。
- 第二面板与首页运行时已完成生产接入：`home-runtime.js`、`home-controller.js`、`home-panel.js`、`home-view.js` 和 `home-adapters.js` 已接入只读注册、按端读取、状态渲染、折叠/焦点恢复、刷新隔离与卸载清理；`recent-closed.js` 已接入最近关闭基础流程，仍需真实宿主事件时序验收。
- 自动门禁：`pnpm test` 已改为自动发现全部 72 个单元/契约/宿主测试文件，避免新增测试未进入本地与 CI 默认入口；CI 使用项目锁定的 `pnpm exec tsc` 工具链、声明只读仓库权限并限制重复运行，CI/Release 均设有 15 分钟超时。

### 历史大项目归档

大项目一至六的详细设计、退出条件与质量门已在上方 R0–R7、测试门槛和当前执行顺序中收敛；此处不再重复维护，避免路线文档膨胀造成归档包体浪费。发布仍以 `package.zip` 为唯一归档，使用 `v*` 标签并先执行 `dry-run`。

### AI 能力专项：接入思源原生 Agent

这不是另起一个聊天面板，而是把小驴雷切已有的导航、搜索和恢复能力声明为思源 Agent 可调用的受控能力。当前思源插件 API 已提供 `addAgentCapability`，同时 Agent 运行时支持能力声明、参数/输出校验、权限策略、审批策略、宿主取消状态和副作用标记；插件 handler 暂未收到 `AbortSignal`，因此接入必须以能力检测、最小权限、内部超时和卸载清理为前提。

- [x] 第一层候选：只读导航能力。提供“列出当前页签、读取最近打开/收藏摘要、返回可打开的根文档 ID”等能力；默认声明 `localRead`，不写入笔记。当前已通过 `addAgentCapability` 做运行时注册，桌面/手机使用各自已有数据源。
- [x] 第一层候选：只读搜索能力。优先匹配当前已开页签，再探测已打开文档内容，最后调用标题搜索；标题无结果或启用高级筛选时使用容量受限的原生块搜索回退，来源、笔记本和结果数量保持有界。
- [x] 第一层增量（v0.16.17）：组件面板数据快照 `home-widget-snapshot`。支持发现模式（省略 moduleId 列出全部可查询组件）与按 moduleId 查询，第三方接入组件自动可查。
- [x] 第二层（部分完成，v0.16.18/0.16.20）：受控执行能力已开放 `open-document`（受控导航，打开并定位文档）与 `update-task-status`（受控写，切换任务勾选，执行前强制弹窗确认、30 秒超时视为拒绝、effects 如实声明 `localWrite`）。v0.16.25 增补 `append-to-journal`（向今日日记末尾追加内容，日记缺失自动创建）。v0.16.37 增补 `open-documents`（批量打开最多 5 篇文档，弹窗列标题确认）。剩余受控动作（批量关闭、跨文档操作、删除类写入）仍不作为默认能力。
- [x] 第三层（v0.16.37）：开发进度上下文 `workspace-context`。将设备端、活动文档、打开页签、文档集清单、快捷入口和今日日记状态收敛为一次只读快照供思源 Agent 读取；今日日记只按日期前缀探测，绝不调用会创建文档的 createDailyNote；不把全文片段、密钥或外部服务凭据写入插件持久化数据。
- 第四层：与其他插件协作。为“小驴打卡”等插件保留稳定的入口/能力协议，优先通过 Agent capability 或现有 adapter 调用，不扫描 DOM、不保存函数引用、不绕过思源权限。
- 移动端边界：Agent 能力是否可用由宿主能力检测决定；手机端只暴露真实可执行的根文档打开、日记和导航动作，不把桌面专属全屏、块定位或不可用插件命令标记为可用。
- 安全边界：任何写入、删除、批量关闭或跨文档操作都不作为默认能力；若未来开放，必须提供明确输入 schema、输出 schema、`localWrite`/`dataEgress` 等副作用声明和人工确认路径。

专项退出条件：在至少一个支持 Agent capability 的思源版本上完成能力注册、卸载清理、权限拒绝、取消、异常输出和桌面/手机能力矩阵测试；旧版宿主必须无报错并保持现有页签切换功能不变。真实宿主验证进展（2026-09-12，本机 v3.8.2）：能力注册与调用全链路已实测通过（workspace-context 经模型→frontend 工具分发→插件 handler→内核，返回真实工作区快照，只读探测正确）；桌面端常规回归与旧版兼容（3.8.2 页签切换无报错）已确认。尚未覆盖：卸载清理、权限拒绝与取消的宿主级行为、open-documents 确认弹窗的真机点击、手机端能力矩阵。未覆盖项完成前，Agent 入口仍不加入默认工具栏，最低思源版本保持不变。

当前主线已完成大项目一至六的代码、测试与文档收口：搜索筛选、历史恢复、快捷入口/第二面板、三端响应式、R7 UI 现代化和发布工程均已接入；排序领域逻辑已从 `src/index.ts` 抽取至 `src/util.js` 并由桌面/侧栏/手机共用。下一阶段继续开发不依赖真实设备的本地只读组件、Agent 协议与兼容边界；真实桌面/Android 宿主验收集中后置，若出现回归则先修复并补齐测试。

### 长周期支线任务包（30 项）

原 30 项支线的测试、fixture 和 ADR 已进入本地主线并通过统一测试入口，本轮不再继续追加同类碎片任务。它们作为快捷入口、搜索、历史恢复、第二面板和发版工程的契约库存保留；主线只有在对应生产功能接入并完成真实宿主验收后，才把相关能力标记为交付。后续新增测试必须随同生产缺口、明确回归或发版门槛进入，不再以测试数量本身作为进度指标。

任何阶段发现主流程回归，立即停止叠加新功能，先修复并补测试，再恢复路线。
## 当前执行口径

- 自动门禁优先：类型检查、生产构建、全量测试及两类 smoke 必须保持全绿；不 push、不发布。
- 下一阶段优先推进本地只读组件、Agent/搜索兼容和可访问性/性能细节；真实 Android 验收按 D-042 后置。
- 缺少稳定宿主契约时记录阻塞并转向其他任务；不引入任意 SQL/JS、第三方私有存储或大体积依赖。
