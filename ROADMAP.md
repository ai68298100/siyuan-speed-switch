# 小驴速切整体开发路线
> 基线：`v0.16.41`（已发布）；开发头：`main`（本地领先 `origin/main` 2 个提交，未推送）；最后更新：2026-09-14
>
> 当前状态：组件商店第二轮视觉/交互优化、配置体验、Agent 组件发现、`document-context` 只读接入和只读审计生命周期历史/回放/健康报告/趋势窗口/合并恢复/传输封装/传输队列指标/维护/回放/协调器健康/联合恢复契约已完成；生产构建 `dist/index.js` 442624 bytes，`package.zip` 273945 bytes（300 KiB 硬上限余量 33255 bytes）。全量测试 1375/1375 通过，空查询性能 p95 已降至约 0.6ms；真实桌面/Android 仍按策略后置。
## 1. 产品定位

小驴速切首先是思源笔记的**页签与文档导航器**，核心价值是让用户更快地找到、预览、切换和恢复工作内容。它不替代思源原生搜索、文件树、历史记录或同步服务，而是在这些能力之上提供一个轻量、统一的入口。

开发优先级固定为：

1. 已打开内容的切换速度和可靠性；
2. 收藏、最近、日记等高频入口；
3. 在同一个搜索页内渐进扩展到全文和全库内容；
4. 工作区恢复等增强能力；
5. 低频、重型或难以证明价值的功能暂缓。

## 2. 不可回退的设计约束

### 2.1 极简而非功能堆叠

- 首屏始终服务于“找到并打开内容”，不放说明性文案、统计图表或营销式模块。
- 工具栏只保留高频一级入口；低频动作归入“更多”。
- 卡片只用于页签、文档和搜索结果等真实对象，不把普通页面分区包装成装饰卡片。
- 优先复用思源图标、菜单、主题变量和交互语义，不建立第二套视觉系统。
- 新能力默认渐进展示：没有搜索词时不请求全库，没有结果时不占用大块空间。

### 2.2 共享语义，端侧适配

桌面弹窗、侧栏和手机端共用同一套领域逻辑、排序规则、搜索状态模型和数据结构；每个界面保有独立的搜索会话。工具栏不追求像素级相同，而是由各端根据空间、输入方式和宿主能力选择入口。

三端都保留的核心语义为：

```text
搜索 | 收藏 | 最近 | 日记
```

- **日记是桌面弹窗、侧栏、手机端均独立显示的常用入口，不得收进“更多”。**
- 桌面端空间更大，可直接显示搜索、收藏、最近、日记、排序、全屏、侧栏和设置等高频工具。
- 侧栏优先显示搜索、收藏、最近、日记和排序；全屏等不适用功能不显示。
- 手机端优先显示搜索、收藏、最近、日记；排序、设置等放入“更多”或底部操作面板，全屏不显示。
- 窄屏允许只显示图标、缩短搜索框或调整间距；可以减少入口，但不得隐藏日记或改变核心命令含义。
- 同一个命令只有一个领域实现，桌面按钮、侧栏按钮和手机底部面板只调用共享命令。

### 2.3 稳定性优先

- 打开切换器、过滤已开页签、切换文档、关闭弹窗四条基本路径不得被远程请求阻塞。
- 内核 API 失败、超时或返回结构变化时，保留已开页签结果并给出轻量错误状态。
- 所有异步结果必须验证请求序号和当前查询词，旧请求不得覆盖新结果。
- 公共设置字段、存储 key 和收藏数据结构不做无迁移的破坏性修改。
- 每个阶段单独提交、单独验收、可单独回滚，不把大规模重构和新功能混在同一版本。

## 3. 搜索总体设计

搜索使用同一个输入框和结果滚动区，但按用户意图分为三层。显示优先级也是执行优先级：

```text
已开页签（本地立即过滤）
    ↓
已打开文档中的全文命中（内核搜索，限定文档范围）
    ↓
全库全文结果（按文档聚合为卡片）
```

### 3.1 第一层：已开页签

- 输入发生后立即按标题、路径和当前可用元数据过滤，不发内核请求。
- 保持现有窗口/分栏分组、置顶、MRU 和键盘导航语义。
- 已打开文档在后续两层中去重，避免同一目标重复占位。

### 3.2 第二层：已打开文档内容

- 使用思源原生全文搜索能力，并把范围限制到当前已打开的根文档。
- 结果按根文档聚合；每个文档默认最多展示 2 条高相关片段。
- 点击标题打开文档，点击片段定位到对应块；无法定位时降级为打开根文档。
- 该层先于全库结果展示，但不得等待全库请求完成后才出现。

### 3.3 第三层：全库卡片搜索

- 当前“全库文档标题 + 路径卡片”作为首个稳定增量保留。
- 后续接入思源原生全文搜索和筛选条件，结果按根文档聚合，不直接铺满块级结果。
- 每页默认最多显示 12 个文档卡片，每个文档最多 2 个片段；其余结果按需加载或交给原生搜索页继续查看。
- 卡片采用响应式网格；窄侧栏和竖屏手机自然退化为单列。
- 借鉴 Neo 的紧凑卡片呈现理念，但实现、样式和代码保持本仓库独立，继续使用 MIT 许可。

### 3.4 筛选范围

优先复用思源稳定的原生条件：笔记本、路径、类型、子类型、排序、分组和搜索方式。筛选 UI 分阶段开放，不一次放出全部选项。

- 首批：关键词、笔记本/路径范围、类型、排序；
- 后续：查询语法、正则、语义搜索（仅在当前思源版本明确支持时启用）；
- 不向普通用户暴露 SQL 搜索；
- 筛选条件必须进入缓存 key，不能把不同范围的结果互相复用。

### 3.5 性能预算

- 已开页签本地过滤目标：单次不跨越一帧；超大页签集以 50 ms 为告警线。
- 内核搜索防抖：180-250 ms；每个界面同一时间最多 1 个进行中的请求。
- 新输入立即取消或作废旧请求；桌面弹窗、侧栏、手机端的请求状态彼此隔离。
- 首屏只渲染有限结果，缩略图继续使用可视区懒加载。
- 搜索缓存只保存在内存中并设容量上限；不把全文片段持久化到插件数据。
- 查询为空、界面已销毁或插件卸载时，必须取消计时器与请求并释放监听。

## 4. 分阶段实施

### R0：冻结基线与稳定化

目标：把当前主线整理成可信的下一阶段起点，不引入新的产品面。

- [x] 完成当前全库标题卡片搜索改动的代码审查与三端主题回归。
- [x] 把搜索请求状态改为按界面实例隔离，避免弹窗和侧栏互相取消、覆盖或共享错误结果。
- [x] 页签加载/销毁事件通知所有存活的切换器视图，桌面批量开关后即时重绘。
- [ ] 验证空输入、快速连续输入、关闭弹窗、切换主题、离线和 API 异常路径。
- [ ] 保留旧 `ISSUE.md` 的排查结论：排序/更多菜单无响应和手机端 `settings` 未定义问题只能作为原型风险，未经当前主线运行时复现与修复不得合入。
- [x] 记录当前设置字段、8 个数据 key、思源最低版本和发布产物清单。

退出条件：类型检查、单元测试、结构烟测、Chromium 主题烟测通过；桌面和手机人工主流程通过。

### R1：拆出共享架构

目标：降低 `src/index.ts` 的耦合，为三端统一和后续全文搜索提供边界。

- [x] 提取搜索会话基础层：请求序号、取消、防抖、缓存和销毁；查询、筛选与结果状态随 R2/R3 继续收口。
- [x] 提取领域命令：打开文档、收藏、最近恢复、日记、排序和设置已按边界拆分（`src/document-actions.js`、`src/favorite-actions.js`、`src/recent-closed.js`、`src/journal-actions.js`、`src/util.js`、`src/settings-model.js`）。
- [ ] 建立界面适配层：桌面弹窗、侧栏、手机端只装配 DOM 和事件。
- [x] 把纯排序、去重、聚合和缓存 key 逻辑放入可单测模块（`src/search-model.js`，暂不接入界面）。
- [x] 抽出收藏项稳定 root 文档 ID 解析与桌面/移动端打开文档命令（`src/util.js`、`src/document-actions.js`），由宿主适配层装配，保留旧版 `openTab` 回退；移动烟测已覆盖 MobileTabs 成功、旧宿主回退、桌面打开、搜索命中块失效后回退根文档和非成功结果隔离。
- [x] 抽出今日日记创建 API 动作（`src/journal-actions.js`），由设置/工具栏 UI 提供笔记本选择并复用统一文档打开命令；烟测覆盖请求体、成功响应和离线失败降级。
- [ ] 不进行一次性文件重写；每次提取保持行为不变并附回归测试。

退出条件：三端调用同一套命令与搜索协调器，现有用户数据无需迁移，功能表现不变。

### R2：分层搜索 MVP

目标：先建立稳定的信息顺序，再扩大搜索范围。

- [x] 已开页签保持即时结果并始终置顶；本地匹配在远程层之前完成，并由搜索编排测试覆盖。
- [x] 稳定全库标题卡片：去重、排除已开文档、加载/空/错误状态、键盘和触控打开。
- [x] 桌面、侧栏、手机端共享卡片构造与结果状态。
- [x] 增加“查看全部”出口：首屏超限时把当前安全筛选映射到思源原生 Search 页签，避免在切换器内无限滚动。
- [x] 验证 Neo、默认明暗主题及窄侧栏下的可读性和点击目标尺寸。

退出条件：连续快速输入无旧结果回跳；100+ 页签与大库场景首屏交互不被阻塞。

### R3：全文搜索与原生筛选

目标：在不把插件变成第二个搜索页的前提下，接入全局搜索。

- [x] 接入已打开文档内容搜索并显示在全库结果之前：最多查询 6 个已打开文档，命中只恢复已有页签卡片，不新增重复卡片；失败自动回退全库搜索。
- [x] 增加标题无结果时的受限全库块搜索回退，按根文档聚合卡片与命中片段；高级类型、排序和搜索方式筛选已接入，主动路径范围选择仍待后续接入。
- [x] 桌面端支持命中块定位并在定位失败时打开根文档；手机端当前仅提供根文档打开，命中块定位待真实 Android 宿主 API 验证后补齐。
- [x] 第一批加入按笔记本筛选，三端共用入口并隔离各自状态；筛选同时约束本地页签、已打开内容、标题结果、全文回退和缓存 key。
- [x] 加入内容类型、结果排序和搜索方式筛选；高级条件绕过标题快路径，直接使用受限原生全文请求，并隔离缓存 key。
- [ ] 路径筛选仍只保留模型和请求层，待获得可信路径树后再加入 UI；标题搜索 API 缺失时已降级到已打开内容/受限全文层，语义搜索不可用时已回退关键词模式，仍需继续收口权限、只读和其他宿主 API 缺失分支。

退出条件：搜索范围和排序与思源原生结果含义一致；性能预算满足；失败不影响页签切换。

### R4：端侧工具栏

目标：共享命令语义，同时按桌面、侧栏、手机端的空间与能力配置入口。

- [x] 建立共享快捷动作注册与执行层，桌面、侧栏、手机端按端侧配置选择入口。
- [x] 桌面端直接显示更多高频工具（包括全屏）；侧栏和手机端保持更少入口。
- [x] 日记在桌面和手机工具栏保持独立，并保留首次选择默认日记本的流程。
- [x] 全屏仅在桌面端显示；手机端不渲染无效入口。
- [x] 手机排序改为紧凑按钮和菜单，设置及快捷入口按端侧空间重新布局。
- [x] 菜单触发元素使用语义化按钮，验证键盘焦点、触控、坐标定位、层级和关闭时机。
- [x] 针对旧原型的“排序/更多点击无反应”增加运行时交互测试，不能只靠 DOM/CSS 静态检查。

退出条件：所有入口在桌面弹窗、侧栏和手机端均可实际点击；无重复命令实现；日记最多一次点击可达。

### R5：收藏与恢复

目标：先完善用户主动保存和误关闭恢复，再增加更复杂的工作区概念。

- [x] 收藏组和组内条目排序，桌面支持拖拽、手机保留移动按钮，持久化顺序并处理损坏数据。
- [x] 最近打开记录使用独立数据结构，按文档根 ID 归一化、去重、限量，并区分仍打开与已关闭状态。
- [x] 最近关闭采用明确的数据来源、去重规则、容量上限和恢复失败反馈。
- [x] 先接入思源 `destroy-protyle` 关闭事件，以事件前后根 ID 差集维护插件侧记录；没有稳定宿主历史 API 时不伪造关闭状态。
- [x] 最近关闭与最近打开在同一历史入口中分区语义展示，不混淆“仍打开”和“已关闭”。
- [x] 桌面右键与手机长按保持等价操作；历史条目共享打开/移除菜单，普通点击行为保持不变。

退出条件：重启后顺序稳定；恢复不会制造重复页签；批量操作有逐项成功/失败结果。

### R6：文档集 / 工作区快照

目标：为固定项目场景提供一键恢复，但不改变普通用户的默认工作流。

- [x] 完成版本化文档集数据层：只记录可稳定恢复的根文档及最小布局信息，限制名称/条目/集合数量，并支持损坏数据清理。
- [x] 接入设置页文档集标签与保存、同名覆盖、删除和恢复预览。
- [x] 接入受控恢复执行：跳过已打开项、按序打开待恢复项并汇总单项失败。
- [x] 增加文档集 JSON 导入/导出，导入复用同一套版本、去重和容量边界；导入文件先限制为 512 KiB，解析期间锁定按钮并暴露 `aria-busy`，避免超大输入和重复写入。
- [x] 补充有界宿主可用性预检、缺失项确认和可取消恢复；预检使用集中管理的 4 路并发和 5 秒单项超时且保持原顺序，设置界面销毁或插件卸载后不会继续操作脱离 DOM 的控件，恢复结果统一显示成功、失败、已打开跳过和缺失跳过计数，跨端真实 API 行为仍待最终验证。
- [x] 恢复前预览已打开与待打开项，默认去重；明确 404 缺失项并允许跳过。
- [x] 提供同名覆盖和删除二次确认。
- [x] 提供独立的文档集重命名操作，支持行内编辑、Enter 保存、Escape 取消和焦点回归。
- [x] 建立版本化数据结构、迁移清理和导入/导出测试。

退出条件：旧快照可迁移；部分文档失效时可继续恢复其余项目；操作可撤销或明确确认。

### R7：现代化 UI 视觉重构（代码已完成，待人工验收）

目标：在不改变核心信息架构和思源主题兼容性的前提下，让第一面板、第二面板、设置页及手机端共享一套更现代、精致且克制的视觉语言。

- [x] 建立淡紫主色、浅蓝灰层级、暖色强调、圆角和阴影 token，并全部保留思源主题变量回退。
- [x] 重构第一面板工具栏、搜索框、页签/文档卡片、空态和状态卡片。
- [x] 重构第二面板模块卡片、标题栏、操作项和错误/加载状态。
- [x] 重构设置页标签栏、设置条目、文档集管理控件及移动端布局。
- [x] 优化手机端卡片、底部快捷入口、排序/收藏 sheet、遮罩和 FAB，同时保留 44px 触控热区与 safe-area。
- [x] 增加 UI 视觉契约烟测，防止后续样式回退；真实桌面/Android 显示验收仍安排在发布候选阶段统一执行。

退出条件：桌面、侧栏和手机端视觉 token 一致；信息密度、触控尺寸、键盘焦点和 reduced-motion 语义不回退；完整自动门禁通过。

## 5. 暂缓与取消进入主路线的方向

以下方向现阶段不承诺版本，也不应提前占用工具栏入口：

- 使用统计和停留时长追踪；
- AI 智能排序；
- 页签关系图；
- 插件自行实现跨设备同步；
- 插件市场更新逻辑；
- 大而全的批量管理模式和常驻复杂筛选面板。

原因是它们会增加数据采集、性能、维护或交互成本，却不直接改善“快速切换”主路径。主题适配、双端一致和思源同步后的数据兼容属于每个阶段的验收标准，不作为独立卖点排期。

## 6. 兼容、数据与回滚

- 新设置使用显式默认值；读取旧数据时做类型校验、去重和容量裁剪。
- 数据结构变更必须包含版本号、向前迁移函数和损坏数据降级策略。
- 不持久化可由内核重新查询的全文片段、临时 loading 状态或 DOM 信息。
- 新 API 先做能力检测；缺失时隐藏对应入口或回退到当前标题搜索。
- 每个阶段通过独立提交和功能开关控制；发生回归时可以关闭新层级而保留页签切换。
- 不直接复制 GPL 项目实现；外部项目只用于理解交互理念和验证兼容性。

## 7. 测试与发版门槛

每个阶段至少满足以下自动化门槛：

```text
pnpm exec tsc --noEmit
pnpm test
pnpm run test:smoke
pnpm run test:smoke:browser
pnpm build
```

人工验收矩阵：

| 场景 | 桌面弹窗 | 侧栏 | 手机端 |
| --- | --- | --- | --- |
| 打开、关闭、切换页签 | 必测 | 必测 | 必测 |
| 搜索空值、快速输入、无结果、错误 | 必测 | 必测 | 必测 |
| 收藏、最近、日记、更多 | 必测 | 必测 | 必测 |
| 键盘导航与焦点 | 必测 | 必测 | 基础验证 |
| 触控、长按、滚动与返回 | 基础验证 | 基础验证 | 必测 |
| 默认明暗主题、Neo、窄宽度 | 必测 | 必测 | 必测 |
| 50/100+ 页签及大库查询 | 必测 | 必测 | 必测 |

发版前还必须确认：

- 中英文 i18n key 集合一致且无硬编码可见文案；
- 无调试弹窗、`console.log`、临时埋点或绝对路径；
- `plugin.json`、`package.json`、README 和压缩包版本一致；
- 安装包在干净插件目录中完成一次安装、升级和卸载测试；
- 只有用户明确确认后才创建 tag、Release 或提交市场更新。

## 8. 当前执行顺序

### 8.0 2026-09-11 起的确认计划

当前 `v0.16.40` 已完成自动化发布，后续继续以开发增量和真实宿主补充验收并行推进。后续工作按以下顺序执行：

1. **持续开发小步增量**：优先处理能由纯函数、协议 schema、适配层和自动化测试验证的功能；每次只推进一个边界清晰的改动，不等待完整发版。
2. **收拢待合并分支**：逐支复核 `feature/icon-fallback`、`feature/mobile-stability`、`feature/open-history` 等分支；只合入能复现、测试覆盖充分且不改变既有数据契约的修复。远端分支和本地工作树中多出的提交必须先完成差异审查，再决定是否合并或关闭。
3. **保持自动化护栏**：新功能必须补协议/模型/回归测试；定期运行类型检查、完整测试和构建，但这些结果只表示开发质量，不表示真实宿主验收完成。
4. **集中记录人工待验项**：所有依赖真实桌面/Android、安装升级卸载、主题和生命周期的事项先记录在路线与测试清单中，等维护者有时间时批量审核；浏览器和截图只作为结构证据。
5. **补充验收后再扩展**：真实桌面/Android 验收结果用于决定路径筛选 UI、侧栏实验和下一大版本范围；新增高风险 Agent 写入能力前仍需独立审批与回执验证。

确认的后续版本方向：

- **v0.16.x 后续开发集**：持续合入协议一致性、兼容性、稳定性、搜索模型和组件生态的小步改动；不承诺每个开发提交都对应可发布版本。
- **v0.17.x**：优先考虑路径筛选 UI、已打开内容搜索体验和第三方组件/快捷入口协议的真实插件适配；每项先做宿主 API 调研和小范围原型，再决定是否进入主线。
- **Agent 专项**：v0.17 以“上下文 → dry-run 计划 → 用户确认 → 固定动作执行 → 结果回执”为主线；当前先完成独立 `document-context` 与 `workspace-plan` 契约/纯模型，再进行真实宿主权限、取消和卸载验证。在验证完成前不新增删除、批量关闭或跨文档写入能力。

明确暂缓：统计追踪、AI 智能排序、页签关系图、插件自建跨设备同步、大而全的批量管理和常驻复杂筛选面板。它们不进入当前开发优先级，也不应以临时入口形式混入主线。

### 8.0.1 2026-09-14 初版的后续五个大版本路线（已被 8.0.2 调整）

`v0.16.40` 已正式发布。后续五个大版本按以下路线推进；每个版本进入主线前仍需宿主 API 调研、小范围原型和包体预算复核，暂缓清单继续有效。

**v0.17 —— Agent 工作区执行落地与路径筛选 UI**

- 把已建成的独立契约层（`document-context`、`workspace-plan`、审批令牌、一次性执行门卫、session registry、diagnostics）择优接入生产 `addAgentCapability` 注册，坚持"上下文 → dry-run 计划 → 用户确认 → 固定动作执行 → 结果回执"主线。
- 硬约束（2026-09-14 第二轮更新，见 D-216/D-219）：预算障碍已解除——D-219 归档内容决策执行后 `package.zip` 余量 54.5 KiB，契约模块生产接入回归常规包体预算评审（每次接入后复核 raw 自律线与 zip 余量即可，无需再走内容决策）。
- 生产接入进度（2026-09-14）：阶段 1 `workspace-runtime-registry-diagnostics` 已完成生产接入（D-220，闭包 25 模块、zip 余量 49.4 KiB）；阶段 2 `document-context` 只读接入已完成（T-368~T-390），真实桌面取消/权限证据仍后置；计划执行链（plan/approval/execution）在 open-documents 真机点击验证后再评估。
- 完成真实宿主权限、取消、卸载与确认弹窗验证（open-documents 真机点击、手机端能力矩阵）后，才评估 Agent 入口是否加入默认工具栏。
- 路径筛选在拿到可信路径树端点证据（T-107）后开放 UI（T-103），复用已有 `path-filter-model`。

**v0.18 —— 第三方组件生态协议落地**

- 以 `docs/widget-protocol.md` 为契约，完成至少一个真实第三方插件的端到端适配验证：注册、失效/恢复、配置表单、跨表面布局。
- 组件面板生命周期（provider 热替换、重渲染回收）按 D-197~D-203 收口为稳定公开协议。
- 商店体验小步打磨：配置入口、筛选与分组已就绪，重点转向第三方开发者文档与最小示例。

**v0.19 —— 搜索第三层成熟化**

- 在宿主能力检测允许时开放第二批筛选：查询语法、正则、语义搜索；不可用时保持现有回退，不暴露 SQL。
- 结果聚合、分页与"查看全部"出口继续对齐思源原生语义；筛选条件全部进入缓存 key。
- 建立搜索性能基准自动化（防抖、首屏、大库场景），把性能预算从告警线升级为门禁。

**v0.20 —— 数据、恢复与跨端连续性**

- 文档集/工作区恢复健壮性：迁移工具、损坏数据降级、恢复报告导出。
- Agent session registry 从契约层转入真实用例：跨端会话状态只读暴露给宿主 Agent。
- 存储版本统一审计：8 个数据 key 的迁移函数、容量边界与跨版本兼容矩阵。

**v0.21 —— 发布工程与真实宿主验收收口**

- 组件真实宿主验收矩阵（T-266）、桌面路径筛选/窄侧栏验收（T-080/T-093/T-103/T-107）在获得合法会话后集中收口。
- 可访问性、i18n、包体预算检查全部自动化并入 `verify:release`。
- 思源市场发布流程演练：干净目录安装/升级/卸载矩阵、可复现构建证明。

以上为方向性路线；v0.18 起的内容在到达时按 8.0 执行顺序复核后再冻结。真实宿主验收（B-005）继续作为独立前置，不阻塞各版本的本地开发增量。

### 8.0.2 2026-09-14 基于当前完成度的路线重排（现行）

组件商店第二轮优化、`document-context` 只读接入和 workspace runtime diagnostics 已经提前完成，原先“先做基础设施、再决定产品顺序”的路线需要调整为“先收口可验证价值，再开放高风险动作”。未来几个大版本不再把路径筛选、第三方生态和 Agent 执行链捆成一个版本，而是按可验收边界拆开：

| 版本 | 主目标 | 必须交付 | 明确不做/准入条件 |
| --- | --- | --- | --- |
| **v0.16.42 稳定化集** | 把本轮商店和首页改动变成可发布质量 | 组件商店真实宿主抽查；分组折叠、窄屏触控、移除/配置流程；修复空查询 p95 边界波动；同步发布矩阵 | 不新增大功能；真实桌面证据未完成前不宣称路径筛选已交付 |
| **v0.17 Agent 只读工作区** | 让思源智能体可靠理解当前工作现场 | 生产接入并稳定维护 `workspace-context`、`document-context`、组件快照/诊断；统一错误/取消/权限回执；Agent 输出脱敏与有界 | 不默认开放写入、删除、批量关闭；执行链只保留契约和灰度开关，必须通过 `open-documents` 真机确认后才进入下一小版本 |
| **v0.18 受控 Agent 动作 + 路径筛选** | 将“理解 → 计划 → 确认 → 执行 → 回执”闭环在最小动作集上跑通 | 仅开放 `open`/`open-batch`/文档集恢复等低风险动作；审批摘要、一次性令牌、取消/超时/卸载回收；桌面路径筛选先行，侧栏仅在真实宽度证据通过后开放 | 不做跨文档写入、删除类动作；无可信路径树 API 时退回笔记本/标题筛选 |
| **v0.19 组件生态与搜索成熟化** | 让商店和搜索从“可用”进入“可扩展” | 至少一个真实第三方插件端到端接入；provider 失效恢复、配置 schema、跨表面布局文档化；路径/笔记本/类型筛选统一缓存 key；搜索分页与查看全部体验 | 不建设常驻复杂筛选面板；语义搜索/正则仅在宿主能力明确支持时启用 |
| **v0.20 数据连续性与性能门禁** | 提升长期使用的迁移、恢复和大库体验 | 存储版本迁移演练、损坏数据恢复报告、文档集/工作区快照增强；搜索和首页性能基准纳入硬门禁；Agent session registry 只读快照 | 不引入云端同步、行为追踪或 AI 智能排序；包体硬上限仍为 300 KiB |
| **v0.21 发布与宿主验收收口** | 把工程质量和真实宿主证据补齐 | 桌面/侧栏/移动验收矩阵、安装升级卸载、主题/可访问性/i18n、可复现归档和市场发布演练 | Android 真机缺失时保留“后置”事实，不用浏览器模拟冒充设备证据 |

#### 路线调整结论

1. **Agent 主线前移，但执行链后置**：只读上下文已经具备真实价值，应先稳定接入；受控执行仍必须等确认弹窗、取消、权限拒绝和卸载回收的真实宿主证据，避免“契约完成”被误认为“用户可安全使用”。
2. **路径筛选与 Agent 解耦**：路径筛选是搜索体验能力，不应等待整个 Agent 执行链完成；先桌面、后侧栏，移动端仅在有真实路径选择交互时再评估。
3. **组件商店从视觉打磨转向生态验证**：基础分组、状态、预览、配置和可用性已经完成，下一阶段重点是第三方插件真实接入、协议示例和失效恢复，而不是继续堆叠装饰层。
4. **性能从“告警”逐步升级为“发布门禁”**：本地空查询 p95 已出现 48ms 边界波动，v0.16.42 先分离环境长尾与产品回归；v0.20 再把稳定基准纳入硬门禁，避免一次偶发抖动阻断正常开发。
5. **每个大版本拆成可独立回滚的 capability/feature flag**：只读能力、低风险导航动作、路径筛选和第三方 provider 分别具备独立准入条件，任何真实宿主未通过的分支都只保留契约和诊断，不进入默认入口。

#### 当前开发顺序（接下来 3 个里程碑）

1. **M1：v0.16.42 稳定化**——完成组件商店真实桌面抽查、修正发布矩阵与性能边界记录。
2. **M2：v0.17 只读 Agent 收口**——补齐 `document-context`/诊断的取消、权限、卸载证据；完善 Agent 输出审计，不增加新写动作。
3. **M3：v0.18 最小执行闭环**——在真实宿主通过后，只接入两个低风险动作和桌面路径筛选；每项单独开关、单独回滚、单独验收。

### 8.1 2026-09-09 重整基线（历史记录）

- 以下内容记录 2026-09-09 的阶段性基线，仅用于追溯，不代表当前 GitHub 状态；当前状态以本文顶部和 8.0 确认计划为准。
- 当时 GitHub 状态：公开仓库无开放 Issue/PR，`origin/main` 停在 `8992978`，最新 Release 为 `v0.16.11`，对应远端 CI 已通过。
- 本地状态：`main` 在远端之后有 149 个提交，其中 101 个为测试提交；这些提交已形成连续历史，但尚未推送或发布。
- 已接入产品路径：受限全文搜索编排、最近打开、快捷入口 registry、只读 Agent 导航/搜索能力，以及移动排序与首帧保护。
- 第二面板与首页运行时已完成生产接入：`home-runtime.js`、`home-controller.js`、`home-panel.js`、`home-view.js` 和 `home-adapters.js` 已接入只读注册、按端读取、状态渲染、折叠/焦点恢复、刷新隔离与卸载清理；`recent-closed.js` 已接入最近关闭基础流程，仍需真实宿主事件时序验收。
- 自动门禁：`pnpm test` 已改为自动发现全部 72 个单元/契约/宿主测试文件，避免新增测试未进入本地与 CI 默认入口；CI 使用项目锁定的 `pnpm exec tsc` 工具链、声明只读仓库权限并限制重复运行，CI/Release 均设有 15 分钟超时。

### 历史大项目归档

大项目一至六的详细设计、退出条件与质量门已在上方 R0–R7、测试门槛和当前执行顺序中收敛；此处不再重复维护，避免路线文档膨胀造成归档包体浪费。发布仍以 `package.zip` 为唯一归档，使用 `v*` 标签并先执行 `dry-run`。

### AI 能力专项：接入思源原生 Agent

这不是另起一个聊天面板，而是把小驴速切已有的导航、搜索和恢复能力声明为思源 Agent 可调用的受控能力。当前思源插件 API 已提供 `addAgentCapability`，同时 Agent 运行时支持能力声明、参数/输出校验、权限策略、审批策略、宿主取消状态和副作用标记；插件 handler 暂未收到 `AbortSignal`，因此接入必须以能力检测、最小权限、内部超时和卸载清理为前提。

- [x] 第一层候选：只读导航能力。提供“列出当前页签、读取最近打开/收藏摘要、返回可打开的根文档 ID”等能力；默认声明 `localRead`，不写入笔记。当前已通过 `addAgentCapability` 做运行时注册，桌面/手机使用各自已有数据源。
- [x] 第一层候选：只读搜索能力。优先匹配当前已开页签，再探测已打开文档内容，最后调用标题搜索；标题无结果或启用高级筛选时使用容量受限的原生块搜索回退，来源、笔记本和结果数量保持有界。
- [x] 第一层增量（v0.16.17）：组件面板数据快照 `home-widget-snapshot`。支持发现模式（省略 moduleId 列出全部可查询组件）与按 moduleId 查询，第三方接入组件自动可查。
- [x] 第二层（部分完成，v0.16.18/0.16.20）：受控执行能力已开放 `open-document`（受控导航，打开并定位文档）与 `update-task-status`（受控写，切换任务勾选，执行前强制弹窗确认、30 秒超时视为拒绝、effects 如实声明 `localWrite`）。v0.16.25 增补 `append-to-journal`（向今日日记末尾追加内容，日记缺失自动创建）。v0.16.37 增补 `open-documents`（批量打开最多 5 篇文档，弹窗列标题确认）。剩余受控动作（批量关闭、跨文档操作、删除类写入）仍不作为默认能力。
- [x] 第三层（v0.16.37）：开发进度上下文 `workspace-context`。将设备端、活动文档、打开页签、文档集清单、快捷入口和今日日记状态收敛为一次只读快照供思源 Agent 读取；今日日记只按日期前缀探测，绝不调用会创建文档的 createDailyNote；不把全文片段、密钥或外部服务凭据写入插件持久化数据。
- 第四层：与其他插件协作。为“小驴打卡”等插件保留稳定的入口/能力协议，优先通过 Agent capability 或现有 adapter 调用，不扫描 DOM、不保存函数引用、不绕过思源权限。
- 移动端边界：Agent 能力是否可用由宿主能力检测决定；手机端只暴露真实可执行的根文档打开、日记和导航动作，不把桌面专属全屏、块定位或不可用插件命令标记为可用。
- 安全边界：任何写入、删除、批量关闭或跨文档操作都不作为默认能力；若未来开放，必须提供明确输入 schema、输出 schema、`localWrite`/`dataEgress` 等副作用声明和人工确认路径。

专项退出条件：在至少一个支持 Agent capability 的思源版本上完成能力注册、卸载清理、权限拒绝、取消、异常输出和桌面/手机能力矩阵测试；旧版宿主必须无报错并保持现有页签切换功能不变。真实宿主验证进展（2026-09-12，本机 v3.8.2）：能力注册与调用全链路已实测通过（workspace-context 经模型→frontend 工具分发→插件 handler→内核，返回真实工作区快照，只读探测正确）；桌面端常规回归与旧版兼容（3.8.2 页签切换无报错）已确认。尚未覆盖：卸载清理、权限拒绝与取消的宿主级行为、open-documents 确认弹窗的真机点击、手机端能力矩阵。未覆盖项完成前，Agent 入口仍不加入默认工具栏，最低思源版本保持不变。

当前主线已完成大项目一至六的代码、测试与文档收口：搜索筛选、历史恢复、快捷入口/第二面板、三端响应式、R7 UI 现代化和发布工程均已接入；排序领域逻辑已从 `src/index.ts` 抽取至 `src/util.js` 并由桌面/侧栏/手机共用。下一阶段继续开发不依赖真实设备的本地只读组件、Agent 协议与兼容边界；真实桌面/Android 宿主验收集中后置，若出现回归则先修复并补齐测试。

### 长周期支线任务包（30 项）

原 30 项支线的测试、fixture 和 ADR 已进入本地主线并通过统一测试入口，本轮不再继续追加同类碎片任务。它们作为快捷入口、搜索、历史恢复、第二面板和发版工程的契约库存保留；主线只有在对应生产功能接入并完成真实宿主验收后，才把相关能力标记为交付。后续新增测试必须随同生产缺口、明确回归或发版门槛进入，不再以测试数量本身作为进度指标。

任何阶段发现主流程回归，立即停止叠加新功能，先修复并补测试，再恢复路线。
## 当前执行口径

- 自动门禁优先：类型检查、生产构建、全量测试及两类 smoke 必须保持全绿；不 push、不发布。
- 下一阶段优先推进本地只读组件、Agent/搜索兼容和可访问性/性能细节；真实 Android 验收按 D-042 后置。
- 缺少稳定宿主契约时记录阻塞并转向其他任务；不引入任意 SQL/JS、第三方私有存储或大体积依赖。
