# 开发计划（2026-09-19）宿主契约化与生命周期收口

> 依据：思源上游 `master`（内核 3.8.4，2026-09-19 快照）与插件 API 声明仓库 `petal`（npm 包 `siyuan` 本体）逐文件核对；插件侧基线已推进到 **v0.23.5**（本地 `main` 未落后于 `origin/main`）。
> 任务号自 T-6476 起单调递增（上一任务 T-6475）。
> 本文只覆盖 P0（本地可自驱、无需真机、无需用户决策）；T-1218/T-1219/T-107/T-6305 仍按 `dev-plan-2026-09-18.md` 的阻塞状态挂起。

## 0. 基线（2026-09-19 本机实测）

| 指标 | 实测值 |
|---|---|
| 工作树 | v0.23.5，本地 `main` 仅超前于 `origin/main`、未落后远端，工作树干净；GitHub Release `v0.23.5` 标签仍指向发布提交 `8d2e498`，后续本地提交均只补录发布/质量文档与门禁 |
| 全量测试 | **6304/6304 通过**（2026-09-19 本地复验；含宿主契约、生命周期和生活组件回归） |
| 生产构建 | 最近一次发布构建 webpack 5.110.3 通过，3 个体积告警；`dist/index.js` 803239 B |
| npm registry | 可达；`siyuan` 已发布最新版为 **1.2.7**（petal 的 v1.2.8 尚未发布，本轮不追） |
| 上游参考快照 | `.research/refs/siyuan-master`（3.8.4）、`.research/refs/petal`、`.research/refs/siyuan-testing-main`（gitignored，只读） |

## 1. 本轮立论的上游事实（全部一手核对，非推断）

| # | 事实 | 上游出处 | 插件侧落点 |
|---|---|---|---|
| F1 | 内核 HTTP API 已全部契约化：2026-09-14 基线 629 条路由进入 `kernel/apicontract/`，`legacy_routes.json` 为空；契约同时生成 `schema.json`（670 条 method/path 条目、1126 个 `$defs`）与 `app/src/types/api/index.d.ts` | `docs/API-CONTRACTS.md`；`kernel/apicontract/schema.json` | `src/index.ts:3264-3380` 的手写端点白名单与字面量分发可改为对官方契约校验 |
| F2 | 契约里写明 `/api/query/sql` 成功响应 `required: [code,msg,data,limit,truncated]`——不写外层 `LIMIT` 时结果被截到 `search.limit`（默认 64） | `kernel/apicontract/schema.json`（`/api/query/sql` 条目）；`kernel/sql/query_limit.go:16-33` | 29 处 `query/sql` 调用需逐条确认显式 `LIMIT` 并消费 `truncated` |
| F3 | mobile 构建用条件编译剔除 `getAllTabs/getActiveTab/getAllModels`，且 `openTab/openWindow` 为空函数 | `app/src/plugin/API.ts:23-27,43-51,424-430` | 手机端页签源（`src/index.ts:7927-7956`）是必需的；但需 host 用例证明降级成立 |
| F4 | 宿主 eventBus 白名单不含任何页签事件 | `app/src/types/index.d.ts:10-27` | 页签变化感知只能靠 `switch-protyle` / DOM / `ws-main`，不要期待新事件 |
| F5 | 前端插件生命周期严格串行且共享 5 秒拆除预算；关闭窗口/退出不触发钩子；`onunload` 后 `addTab/addDock/addTopBar` 变 no-op | `petal/siyuan.d.ts:533-545`；`app/src/plugin/uninstall.ts:43-146`、`plugin/index.ts:195,558,656` | `src/home-store-ui.ts:172-189`、`src/index.ts:2015-2024` 的 `dialog.destroy` 补丁 + 1500ms 轮询需替换；`onunload` 须自关 Dialog |
| F6 | 宿主的 Agent 能力确认链已实现逐 action 的效果判定、强制确认覆盖、会话级总是允许与确认超时 | `kernel/agent/agent.go:1691-1734`；`app/src/layout/dock/agent/AgentChat.ts:4042-4060` | v0.24.0（T-1219）的自建审批可改为声明 `actionEffects`（见 §4 待决 D-399） |
| F7 | 发布模式要求额外前端资源逐个声明在 `plugin.json` 的 `publish.resources`（条目须为精确文件名，不得以目录/通配符为条目；官方示例含 `views/index.html` 子目录文件路径——2026-09-21 T-6707 原文核实并纠错，原"路径不得含目录"系误读） | `docs/PLUGIN-PUBLISH.md`（Resource declaration 段） | `package.zip` 内 `docs/` 4 个子目录文件已逐文件声明（T-6707，原"不可达"结论撤销） |

## 2. 任务

### T-6476 依赖对齐：`siyuan` 1.2.6 → 1.2.7

状态：完成。
理由：F1/F5 的新符号（`actionEffects`、`loadPublishData`、`IMethods.getAllTabs(type?)`、内核类型 `kernel.d.ts`、异步生命周期钩子声明）在 1.2.7 才有；`docs/dev-plan-2026-09-18.md:285` 记录的"1.2.6 类型滞后运行时"由此消掉大半。
验收：`tsc --noEmit` 通过 + 全量测试无回退；不引入运行时依赖（`siyuan` 仅 devDependency，`package-integrity` 门已禁远程运行时依赖）。

### T-6477 内核端点契约门

状态：完成。
做法：
1. 新增生成脚本 `scripts/gen-api-contract.cjs`：从上游 `kernel/apicontract/schema.json` 抽出**插件实际使用的端点子集**，产出有界 fixture `tests/fixtures/siyuan-api-contract.json`，并记录来源仓库/分支/取回日期（ fixture 内字段，供审计）。
2. 新增门禁 `tests/kernel-endpoint-contract.test.cjs`，钉死三件事：
   - 白名单里每条端点必须存在于官方契约（不存在的端点=未来必然静默 `code:-1`）；
   - `KERNEL_ENDPOINTS` 与 `fetchKernelJson` 的 `case` 字面量、以及 `case` 内 `fetch()` 的字面量三者必须逐条同名（防"名单有、分发无"或反之一的漂移）；
   - 请求体必填字段与响应顶层字段对每条被使用端点做声明式断言（首批覆盖 `query/sql` 的 `stmt` + `limit/truncated`、`storage/getRecentDocs`、`filetree/listDocsByPath`、`asset/getMissingAssets`、`av/renderAttributeView`、`network/forwardProxy`）。
3. 按 `docs/gate-audit-checklist.md:164-171` 做负向验证：同条件注入违规 → 按失败项名称确认 → 还原并做字节级哈希比对。

风险与边界：fixture 是**快照式上游副本**，不是实时同步；因此 fixture 必须写明上游取回日期，且门禁只声明"与本快照一致"，不声称与用户运行时内核一致（避免 gate-audit 清单里的"无据历史断言"失效模式）。响应字段的运行时真值仍以 `docs/kernel-api-smoke-2026-09-19.md` 的实测为准，两者冲突时以实测为准并回写本文。

### T-6478 SQL 截断与有界读收口

状态：部分完成（第一批已完成；后续站点仍按 T-6482/T-6483/T-6485 分流）。
做法：按 F2 逐条核 `src/` 内 29 处 `query/sql`，补齐外层 `LIMIT` 与稳定排序，并把 `truncated` 传导到上层"结果被截断"的 UI 语义（与现有搜索分层短路规则一致，不改四条基本路径的阻塞性）。

## 3. 明确不做

- 不引入第三方运行时依赖、不引入任意 SQL/JS（ROADMAP 执行口径）。
- 不在本轮改 `minAppVersion`（牵涉发布通道兼容与集市回退，等 §4 D-398 决断）。
- 不动 `.gitattributes` 与行尾策略；本轮所有编辑保持文件现有行尾，避免整文件归一化噪声。

## 4. 需要用户决断的事项（登记，不阻塞 §2）

- **D-398 版本下限**：`plugin.json` 声明 `minAppVersion: 3.1.20`，但代码实际消费 3.8.x 端点与 agent 子系统；官方语义是"低于下限即静默自动禁用"而非能力门控（`kernel/bazaar/plugin.go:59`、`kernel/model/plugin.go:237-242`）。抬高到 3.8.x 可让"未探测到即降级"的代码大幅简化，代价是旧宿主用户掉队。
- **D-399 v0.24.0（T-1219）实现路线**：改为向 `addAgentCapability` 声明 `actionEffects`（只读动作显式 `{localRead:true}` 免确认，写/批量动作声明 `{localWrite:true}` 走宿主确认卡），还是继续自建一次性令牌 + 30s 超时 + 自有弹窗。按 F6，前者可把"自建确认弹窗待 Android 真机验收"这一硬阻塞转为宿主 UI，并少维护 `src/agent-approval-token.js` + `src/agent-workspace-approval.js` + `src/agent-workspace-execution.js` 共约 177 行；后者可覆盖宿主确认不可用的旧版本。
- **D-400 账本纠偏**：`ROADMAP.md:372` 与 `dev-plan-2026-09-18.md:1002` 对 T-6458/T-6459/T-6466 的状态落后于代码实况（已在码内并被门禁钉住），需回写。

## 5. 执行记录

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6476 依赖对齐 | 完成 | `siyuan` devDep `1.2.6 → 1.2.7`（registry 最新已发布版；petal 的 v1.2.8 未发布，本轮不追）。`tsc --noEmit` 退出 0；影响面仅 `package.json` 1 行 + `pnpm-lock.yaml` 21/22 行 | 无 |
| T-6477 内核端点契约门 | 完成 | 新增 `scripts/gen-api-contract.cjs`（从上游 `kernel/apicontract/schema.json` 抽子集）+ `tests/fixtures/siyuan-api-contract.json`（25 条内核端点、67 KB、含 `source.kernelVersion=3.8.4` 与 `subsetSha256` 防手改）+ `tests/kernel-endpoint-contract.test.cjs`（13 项断言，全通过）。扫描发现分发面本就 1:1（白名单/case/fetch 各 19 条），但**搜索类端点绕过白名单直连 fetch**，故门禁覆盖面定为"src 内全部 `/api/` 字面量"而非仅白名单 | T-6478 复用同一 fixture |
| T-6477 负向验证 | 完成 | 四次注入，全部按名称失败且兄弟门不受波及，还原后 sha256 与注入前一致：①白名单加入伪端点 → `every kernel-shaped literal...` + `kernel dispatcher ... 1:1` 双失败；②在两个已知端点间错配 fetch 字面量 → 仅 `kernel dispatcher ... 1:1` 失败；③手改 fixture 删 `query/sql` 的 `truncated` → `fixture is reproducible` + `contract facts hold for /api/query/sql` 失败；④脚本 `SW_CONTRACT_RETRIEVED_AT=2026-09-19 node scripts/gen-api-contract.cjs` 重跑输出 `subsetSha256 7bf2ddd8cf09` | 脚本留在 `.research/`（gitignored），需要复跑时重建 |
| 门禁波及的文档一致性 | 完成 | 新增测试文件触发既有门 `README test-file count matches the discovered host test matrix`（该门从文件系统推导计数）。按实况回写 `README.md:13,388,421,425` 与 `README.en-US.md:13,401`：`207 → 208` 个测试文件、`6268 → 6281` 项测试 | 无 |
| 总门禁复验 | 完成 | `pnpm verify:release` 退出 0：tsc + 生产构建 + **6281/6281** 测试 + 移动端卡片/工具栏布局/Chromium 样式三套烟测全绿 | 等用户决定是否本地 commit |

### 5.1 本轮把上游契约读成的可复用事实（写入 fixture，非推测）

- `/api/query/sql`：请求必填只有 `stmt`（可选 `mode`）；成功响应除 `{code,msg,data}` 外**必带 `limit` 与 `truncated`**——T-6478 据此收口 29 处 SQL 的显式 `LIMIT` 与截断传导。
- `/api/storage/getRecentDocs`：条目字段 `rootID|title|viewedAt|openAt|closedAt|icon`。其中 **`closedAt` 已在契约里**，与 `src/recent-closed.js` 的自建记录存在重叠，需另立决断项与官方 recent/pinned 端点一并定边界。
- `/api/asset/getMissingAssets`：契约声明条目含 `path`，但 3.8.4 实测响应不含（`docs/kernel-api-smoke-2026-09-19.md:39-42`，T-6471 因此改跳 `blockIDs[0]`）。已登记为 `MEASURED_DIVERGENCE`，上游一旦补上 `path` 门禁即失败，强制回来复核插件规则。
- `/api/av/renderAttributeView`：`id` 必填、`blockID/pageSize/page/viewID/query` 可选；"块 ID 得到 0 行"不可由契约表达，同样登记为 `MEASURED_DIVERGENCE`（ADR 0058 / T-6470）。
- `/api/network/forwardProxy`：必填只有 `url`，可选 `method/payload/payloadEncoding/headers/timeout/responseEncoding/contentType/redirect`；响应 `data` 为 `status|body|bodyEncoding|headers|contentType|elapsed|url`，错误码分支 `-1,1..8,10`。此前该形状纯靠插件侧记忆，现由契约钉住。
- 7 条端点的请求体种类不是 `json`（`getMissingAssets`、`getPinnedDocs`、`getRecentUpdatedBlocks`、`getBookmark`、`getCriteria` 为 `none`；`lsNotebooks`、`getRecentDocs` 为 `legacyOptional`），门禁按端点显式登记而非统一假设。

### 5.2 契约面之外的上游变更（转入 P1/P2，不属本轮）

F3 mobile 构建条件编译剔除 `getAllTabs`、F4 无页签事件、F5 五秒拆除预算（本轮已按它改造弹窗释放）、F6 宿主 Agent 确认链、F7 `publish.resources` 声明新规，及 D-398/D-399/D-400 三项待决，见本文 §1、§4。

### T-6478 SQL 有界读收口（第一批）

状态：部分完成（必须修的四处已改，其余登记为后续）。
依据：F2——无外层 `LIMIT` 时内核按 `search.limit`（默认 64、下限 32）静默截断；插件此前从不读取 `truncated`/`limit`。
已完成：
- `src/util.js` 新增 `normalizeSqlResult(json)` → `{rows, truncated, limit}`，对畸形信封（null / data 非数组 / truncated 非严格 true）返回确定空态。
- `src/index.ts:4736`（`loadUpdatedMap`，「最近编辑」排序的更新时间映射）：此前是**分发器之外的裸 fetch**，无白名单、无 5s 超时、无错误归一，且**没有外层 LIMIT**——打开页签多于 64 时部分文档缺 updated，排序静默错乱。改为 `fetchKernelJson` + `LIMIT ${ids.length}`，并让失败不写缓存（避免把空映射固化 `UPDATED_CACHE_MS`）。
- 日历月视图（原 `LIMIT 64`）→ `LIMIT 200` + `b.id ASC` tiebreak，消除"整月缺日无提示"。
- 文档关系摘要 UNION（child + reference）→ `ORDER BY relation ASC, id DESC`，使引用行不再把子块整类挤出 64 行。理想解是两支各自 `LIMIT`，需真实宿主验证 SQL 形态后再改（登记 T-6485）。
- 设置里两个选择器（数据库 / 文档）→ `LIMIT 200` + `id ASC` tiebreak，并在 `truncated` 为真时 `logger.warn`；用户可见的"仅前 N 条，请继续输入关键词"提示需新增 i18n 键，登记 T-6482。
门禁：`tests/sql-and-dialog-lifecycle.test.cjs`——分发器之外的裸 fetch 按端点登记为债务（`lsNotebooks` / `getDoc` / `searchDocs` 三处各自带独立超时语义，迁移会改变时序，须真实宿主取证后再动，登记 T-6483）；每条 `stmt:` 字面量必须含外层 `LIMIT` 或命中带理由的聚合/单点豁免，豁免表出现僵尸条目即失败。

### T-6479 弹窗释放改挂宿主 destroyCallback

状态：部分完成（四处已改，四处登记为债务）。
依据：F5——宿主 `Dialog.destroy()` 在所有关闭路径（Escape、遮罩、程序调用）都会触发 `destroyCallback`，且 `dialog/index.ts:134-151` 有 `destroying` 重入保护；插件原先"覆写 `dialog.destroy` + 1500ms 轮询 `isConnected`"是在弥补一个并不存在的缺口，而轮询定时器会在 5 秒拆除预算之外继续存活。
已完成：`src/home-store-ui.ts`（预览弹窗 + 组件商店弹窗）、`src/home-config-form.ts`（图标钳制观察器）、`src/index.ts`（设置弹窗 resize 监听）改为 `destroyCallback`；**`src/` 内 `setInterval` 现已归零**。释放函数保持幂等（`disposed` / `storeReleased` / `settingsDestroyed` 旗标）。
同步改判的旧契约断言：`tests/store-preview-lifecycle-contract.test.cjs`（8 条）、`tests/store-mobile-layout-contract.test.cjs`（6 条）——原断言把"覆写 destroy / 轮询断连"当成正确设计，现改为断言 destroyCallback 已接线且旧机制不再存在。
登记债务：`index.ts` 切换器弹窗与 `suspendFABForDialog`、`mobile-switcher-ui.ts`、`second-panel-ui.ts` 四处仍覆写 `destroy`（按文件+数量登记，不随行号漂移），跟进 T-6481。

## 6. 第二批执行记录

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6478 第一批 | 完成 | 上述 4 处 SQL 站点改造 + `normalizeSqlResult` + 新门 5 项断言全绿 | T-6482 / T-6483 / T-6485 |
| T-6479 第一批 | 完成 | 4 处 destroyCallback 接线；`src/` 内 `setInterval` 归零；14 条旧断言改判 | T-6481 余 4 处 |
| 负向验证（第二批门） | 完成 | 四次注入源码违规，各自精确 FAIL：去外层 LIMIT → `every kernel SQL literal carries an outer LIMIT...`；加轮询 → `no interval-based dialog disposal polling remains`；加分发器外裸 fetch → `kernel fetches outside the dispatcher match the recorded debt list`；加 `dialog.destroy = () =>` 行内覆写 → `remaining dialog.destroy overrides match the recorded debt list`。最后一轮首次注入**未咬住**（正则要求行首锚点），已去掉锚点并复验通过。还原后 `src/index.ts`、`src/util.js` sha256 与注入前一致 | 无 |
| 总门禁 | 完成 | `pnpm verify:release` 退出 0：tsc + 生产构建 + **6286/6286** + 三套烟测。过程中两处账本被门禁要求回写：README 测试计数 208→209 文件、6281→6286 项；`docs/release-readiness.md` 产物快照按实测更新（`index.js` 802089→802250、本地 zip 379095→379694、压缩后 212253→212344、余量按 224 KiB 重算 17032）。`release-readiness.md:13` 的 832 KiB 线"余量"原值 52121 与自身数字推不吻合（851968−802089=49879），本轮按公式改为 49718，其原始推导待核（T-6486） | T-6486 |

## 7. 第三批执行记录（T-6481 续）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6481 弹窗覆写迁移（第二批） | 部分完成 | 又迁走 2 处：**桌面切换器**（`createSwitcherDialog` 增 `release` holder 参数，由 `showSwitcher` 建 holder、`assembleSwitcherParts` 填充——宿主只认构造期 `destroyCallback`，而资源在后续装配方法里创建）与**第二面板**（`openSecondPanel` 同函数构造+装配，用可变 holder 前置声明）。释放保持幂等（`switcherReleased` / `panelReleased`）。`dialog.destroy` 覆写从 8 处降到 2 处 | 余 2 处：`suspendFABForDialog`（包装任意弹窗，需改显式配对，`onDestroy` 形参已在）、`mobile-switcher-ui.ts`（`dialog` 由 index.ts 传入，需 holder 机制并扩 `this` 契约） |
| 门禁登记同步 | 完成 | `DIALOG_DESTROY_OVERRIDES` 按文件+数量同步收缩；重跑四次注入仍各自精确 FAIL（含新增行内 `dialog.destroy = () =>` 覆写），还原 sha256 一致 | 无 |
| 总门禁 | 完成 | `pnpm verify:release` 退出 0：tsc + 生产构建 + **6286/6286** + 三套烟测；产物快照按实测回写（`index.js` 802250→802358、压缩后 212344→212283、本地 zip 379694→379633，两处余量按公式重算） | 无 |

原排队项当前状态：T-6482（选择器截断可见化）已由 T-6552 完成；T-6486（832 KiB 线“余量”原始推导口径）已由 T-6497 完成；T-6483（三处历史裸 fetch 迁进统一分发，会改变超时语义，须真实宿主取证）与 T-6485（文档关系 UNION 改两支各自 LIMIT）仍保留为真实宿主取证项。

风险声明：本批只换**释放入口**（同样的清理语句与幂等语义），未触碰渲染与交互路径；但宿主 `destroyCallback` 在 `destroy()` 内经 `setTimeout` 异步执行，比原先的同步覆写晚一个宏任务，可观察差异是 `opener.focus()` 等恢复动作延后一帧。该时序自动化测不到，须在下一轮 host-e2e 用真实 Escape/遮罩关闭路径取证（与 F3 mobile bundle 用例同批）。

## 8. 第四批执行记录（T-6481 收口 + 顺带修出一个真 bug）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6481 弹窗覆写迁移（第三批） | **完成，8/8 归零** | `suspendFABForDialog` 改为返回释放函数（不再需要 `dialog` 形参），由调用方接进 `destroyCallback`；两个调用点（日记弹窗、设置弹窗）与手机端切换器（`createMobileSwitcherDialog(release)` holder + `SecondPanelUiHost` 式 `this` 契约同步改签名）全部迁移。`src/` 内 `dialog.destroy` 覆写现为 **0**，门禁登记表随之清空转为硬禁令（重新出现即失败）。 | 无 |
| T-6487 桌面日记弹窗 Promise 永挂 | 完成（顺带发现并修复） | 旧 `suspendFABForDialog` 首行 `if (!this.isMobile) return;` 使 `onDestroy` 在桌面端永不触发；而 `promptJournalNotebook` 只在「确认/取消」两个按钮上 `resolve`——**桌面端用 Escape 或点遮罩关闭时，调用方 await 的 Promise 永久挂起**。改为释放函数不分平台触发 `onDestroy`。新增门 `journal prompt resolves its promise on every close path (T-6487)` 三条断言钉住（含禁止再按平台早退）。 | 真机/真实宿主复验关闭路径 |
| 门禁与账本 | 完成 | 本门 6/6 通过；四次注入在登记表清空后仍各自精确 FAIL，还原 sha256 一致。`tests/store-mobile-layout-contract.test.cjs` 一条把"覆写 destroy"当正确设计的旧断言改判。README 测试数回写 6287；`release-readiness` 产物快照再次按实测回写（`index.js` 802358→802462、压缩后 212283→212383、本地 zip 379633→379735、两处余量重算）。 | 无 |
| 总门禁 | 完成 | `pnpm verify:release` 退出 0：tsc + 生产构建 + **6287/6287** + 三套烟测 | 无 |

流程摩擦记录：`release-readiness` 产物门把文档与 `dist/index.js` 的**字节数**强耦合，任何一处源码编辑都要重算 4 个数字（未压缩/压缩后/两处余量）才能过门，本轮触发 3 次。是否改为脚本自动生成该段快照，记为待决（不擅自改门禁语义）。

## 9. T-6488 数据变更刷新链（源自第三方插件 siyuan-lumina 的研究）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6488 覆写 `onDataChanged` | 完成 | 宿主契约（`app/src/plugin/index.ts:148-158` + petal 注释）：未覆写时插件存储变化会**整体重载插件**，`reason` 为 `sync`（跨设备合并）或 `overwrite`（其他窗口写盘）。小驴雷切此前全仓零覆写 → 每次同步都销毁已开切换器/第二面板与搜索会话。新增钩子：只读重跑 `loadPersistentKeys` + 只读演练 + `initFavCollapsed` + `scheduleSidebarRefresh`，带 `dataChangeReloadInFlight/Queued` 合并广播、防并发重入（宿主在 5 秒拆除预算内等待本 Promise）。**钩子链内禁写盘**（写盘会再广播并回到本钩子，形成 lumina 实测踩过的回环）。 | host-e2e 用双窗口/同步路径取证 |
| 附带修复：持久化 key 清单重复 | 完成 | 新钩子要求单一清单，扫出 `index.ts` 里**两处各抄了一遍 13 个 key**（`initPersistentData` 与容量测量 `measure` 块）。新增 `constants.ts:PERSISTENT_KEYS` 作为唯一清单，两处改为遍历；新门断言它与 `storage-migration.js` 的 `KEY_ORDER` 同集。 | 无 |
| 既有门禁对齐 | 完成 | `tests/storage-key-audit.test.cjs` 两条判据按本意收紧而非放宽：注册常量正则改为 `export const X_KEY = "`（原正则会把数组型 `PERSISTENT_KEYS` 的前缀误判为字符串 key 常量）；字面量 `*_KEY` 审计新增"受控间接点"白名单，并断言 `this.loadData(key)` 出现次数恰好等于受控站点数（清单外走间接即失败）。 | 无 |
| 负向验证 | 完成 | 四次注入各自精确 FAIL 且兄弟门不受波及，还原 sha256 一致：A 钩子内写盘 → `plugin overrides onDataChanged...`；B 清单漏登一个 key → `PERSISTENT_KEYS is the single source...`；C 清单外多一处变量式读取 → 新门 + 审计门双 FAIL；D 裸字符串 key → 同双 FAIL。 | 无 |
| 总门禁 | 完成 | `pnpm verify:release` 退出 0：tsc + 生产构建 + **6290/6290** + 三套烟测；README 210 文件 / 6290 项、`release-readiness` 产物快照按实测回写（`index.js` 802636、压缩后 212425、本地 zip 379777）。 | 无 |

### 9.1 两处账本纠错（我此前给过的错误建议）

- **`onSwitchLanguage` 不存在**：3.8.4 宿主与 petal 1.2.7 声明里均无该钩子（全仓 0 命中）。切换语言由宿主整页重载承担，插件无需处理。前一份报告里"顺带补 onSwitchLanguage"的建议作废。
- **"存储迁移未接 onload、破坏性变更等于静默清空"已过时**：v0.23.4 的 `captureStorageMigrationSnapshot()`（`index.ts:1067-1090`）已把演练管道接进 onload 作为只读影子，缺口会 `logger.warn` 并经 Agent 诊断 `storageHealth` 出口透出。真正尚未做的是"版本戳"（`STORAGE_SCHEMA_VERSION` 无处持久化，因此无法识别降级/未知版本）与"用迁移管道取代宿主静默修复"的权威切换——前者需要新增第 14 个 key，牵动多处计数门，登记为决断项 **D-401**；后者按 `storage-migration.js` 头部约定等证据积累后单独批次执行。

### 9.2 siyuan-lumina 研究结论（哪些学、哪些别学）

研究结论当前状态：T-6488 已落地；`学-1` 远程旁路化已由 T-6489 实现失败抑制窗；`学-2` 首屏 20 条 + RAF 批量 + IntersectionObserver 每 12 条追加仍登记为 T-6490 候选，尚未进入生产；`学-3` 资源路径取证由 T-6491 完成并判定不适用。反模式清单（只看不抄）：硬编码凭据与 token 明文入 settings、劫持 `window.goBack`/`siyuan.layout.toggleModel`/`window.Lute`、`showDock` 失败后连退到"模拟点击 dock DOM"、228 处 `innerHTML` 配 5 套不一致转义（其中 3 处漏转义引号却拼进 `style=`）、67 处空 `catch`、发布物即源码且无 `package.json`/测试、`build.bat` 把 `LICENSE` 从包里删掉、`i18n/*.json` 存在但代码里 `this.i18n` 零引用、`update.sh` 是无条件 `git push -f`。

## 10. T-6489 / T-6491 执行记录

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6491 本地资源被"清理未引用资源"删除的风险 | **不适用，关闭** | 取证结论：`src/` 内**没有任何写 `/assets` 的调用**（无 `asset/put`、无 `uploadAsset`、无 `/assets/` 字面量），唯一的 `<img>` 在 `src/home-view.js:513` 加载的是远端封面 URL（`loading=lazy` + `referrerPolicy=no-referrer`）。速切不拥有任何工作区资源文件，lumina 的 `data/public` 技巧对我们无意义。 | 无 |
| T-6489 失败抑制窗 | 完成 | 出网只有两个收口点（`fetchBoundedLifeJson` / `fetchBoundedLifeText`，含 `loadWeatherLocation` 等全部 loader 经由它们）。改为薄包装：按 url 记 20s 失败窗，窗内非强制请求直接抛 `backoff`，让调用方走它已有的 stale / 空态分支，**不再每周期重复付出最长 10s 超时**（B-006/B-007 那两个不可达源正是这种情形）。三条例外纪律：`blocked_endpoint` / `unsupported` / 调用方取消不记窗（不代表端点健康）；`force: true` 一律放行；**成功一次即作废该窗**（否则端点恢复后仍被旧记录抑制）。`clearLifeWidgetCaches()` 同时清窗。 | host-e2e 验证真实不可达源 |
| 新测试与改判 | 完成 | 新增 `tests/life-widget-backoff.test.cjs` 7 条（抑制、强制放行、窗满恢复、成功作废、清缓存解除、门禁拒绝与取消不记窗）。`tests/life-widget-network.test.cjs` 与 `-resilience-contract.test.cjs` 无需改动——它们已有的 `clearLifeWidgetCaches` beforeEach 因上面的联动自动获得隔离（我先加的两条显式 hook 已删除，避免冗余）。 | 无 |
| 负向验证 | 完成 | 四次逐条摘除实现部件，各自按名称 FAIL、还原 sha256 一致：摘"成功作废" → 仅 `a successful fetch invalidates...` 失败；摘"清缓存联动" → 4 条依赖它的用例失败；摘"窗内抑制"与摘"失败记账" → 均使 `a failed endpoint is suppressed...` 失败。 | 无 |
| 总门禁 | 完成 | `pnpm verify:release` 退出 0：tsc + 生产构建 + **6297/6297** + 三套烟测；README 回写 211 文件 / 6297 项，产物快照第四次按实测回写（`index.js` 803239、压缩后 212645、本地 zip 379996、两处余量重算）。 | 见下 |

流程摩擦升级：一个 session 内因"文档必须等于产物字节数"手工回写 **4 次**（每次改码都要重算 4 个数字，且我算错过一次）。建议下一轮把 `docs/release-readiness.md` 的产物快照段改为由脚本生成（脚本本身纳入 `tests/host` 门的负向验证），登记为 **T-6492**。

## 11. 基线校正执行记录

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6493 发布/路线账本校正 | 完成 | `ROADMAP.md` 新增 v0.23.5 当前基线；本文件基线改为 `459056b`；明确 GitHub Release 标签 `8d2e498` 与后续文档提交的关系，并修正“唯一用户反馈”为 GitHub 当前 2 个 Issue | T-6492、T-6482、T-6486 已完成；T-6483/T-6485 按真实宿主证据后置 |
| T-6492 发布快照只读校验器 | 完成 | 新增 `scripts/check-release-readiness.cjs`，只读核对 `dist/index.js` 精确字节数和 `package.zip` 1 KiB 漂移；接入 `release:check` 与 `verify:release`；当前实测 `803239/382231` 通过；新增门禁测试，禁止脚本写文件 | T-6482：选择器截断可见化 |
| T-6494 T-6482 协议取证 | 完成 | 两个选择器当前签名均为 `Promise<Array<{id,title}>>`，截断信息只在 `index.ts` 日志中存在；已确认不能伪造空 ID 选项，避免污染选择/点击逻辑 | 下一项需扩展 `HomeConfigFormHost` 的独立截断状态回调，再补双语提示 |
| T-6495 T-6483 裸 fetch 范围审计 | 完成 | `tests/sql-and-dialog-lifecycle.test.cjs` 已将 `getDoc`、`lsNotebooks`、`searchDocs` 按独立超时语义登记；本轮不迁移，避免未经宿主取证改变取消/超时行为 | 等真实宿主时序证据 |
| T-6496 T-6485 UNION 查询审计 | 完成 | 文档关系摘要当前统一 `ORDER BY relation ASC, id DESC` 并有外层限制；拆成两支各自 LIMIT 会改变排序和计数语义，保留为宿主数据量验证项 | 大库真实数据验证 |
| T-6497 T-6486 预算口径复核 | 完成 | `release-readiness` 的 raw 余量按 `832*1024 - 803239 = 48729` 统一核算；归档上限与单条压缩线分开，不再混用 | 后续将数值计算转入共享 helper |
| T-6498 Issue 状态快照 | 完成 | GitHub 公开页面当前为 2 个 Issue、0 个 PR；路线文档已删除“唯一用户反馈”表述 | 下一版账本逐项登记编号 |
| T-6499 发布校验命令接线 | 完成 | `release:check` 已接入 `verify:release`，对缺少产物采取非破坏性提示，对快照漂移返回非零 | 随下一次完整发布门禁复验 |
| T-6500 负向验证记录 | 完成 | 新增门禁测试断言校验器不含 `writeFileSync/appendFileSync`；快照漂移路径由既有 release-quality 门覆盖 | 后续补脚本级临时夹具负向测试 |
| T-6501 批次账本收口 | 完成 | 本批已完成 T-6492，并完成 T-6494~T-6500 的协议/风险/门禁取证登记；未把真实宿主依赖伪报为本地完成 | 下一批实施选择器截断状态回调与双语 UI |

## 12. T-6502~T-6551 五十项发布审计批次

本批 50 个 T 任务与 `scripts/release-batch-audit.cjs` 中 50 个具名检查一一对应，避免只用一条“总门禁通过”代替逐项证据。检查范围覆盖：包元数据、发布脚本、产物快照、路线/阻塞账本、SQL/生命周期门、内核契约、组件协议、宿主文档、Git 远端和分支一致性。

| 任务范围 | 状态 | 证据 |
|---|---|---|
| T-6502~T-6511 基础文件与包元数据（10 项） | 完成 | 50 项审计中的前 10 项全部 PASS |
| T-6512~T-6521 发布脚本与产物快照（10 项） | 完成 | 只读、字节数、漂移阈值、版本快照共 10 项全部 PASS |
| T-6522~T-6531 路线、阻塞与开发账本（10 项） | 完成 | v0.23.5、`459056b`、`8d2e498`、Android/路径证据及 T-6492/T-6501 共 10 项全部 PASS |
| T-6532~T-6541 SQL、生命周期、契约与组件协议（10 项） | 完成 | SQL 截断严格性、destroy 门、存储审计、内核 fixture、widget protocol 共 10 项全部 PASS |
| T-6542~T-6551 宿主文档、命令接线与 Git 一致性（10 项） | 完成 | 宿主 README、release-quality、verify 接线、本地文件限定、`HEAD...origin/main=0/0` 共 10 项全部 PASS |

审计命令：`pnpm run release:audit` → **50/50 passed**。全量回归：`pnpm test` → **6299/6299 passed**。这 50 项属于本地可验证的工程质量收口；真实 Android、窄侧栏和 Agent 执行链仍按 BLOCKERS/准入条件后置。

## 13. 下一批增量

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6552 选择器截断可见化 | 完成 | `loadHomeDocumentOptions`/`loadHomeDatabaseOptions` 在返回数组上携带 `truncated/limit` 元数据；配置表单渲染双语提示，不伪造可点击选项 | 真实大库宿主确认提示位置与文案 |

## 14. T-6553~T-6602 第二个五十项质量批次

| 任务范围 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6553~T-6562 i18n 对称性与文本边界（10 项） | 完成 | `scripts/quality-batch-audit.cjs`：中英文 key 集合、值类型、截断文案、重复键和 UTF-8 共 10 项全部 PASS |
| T-6563~T-6572 源码安全卫生（10 项） | 完成 | 无 `console.log`/`debugger`/`window.alert`、无 `dialog.destroy` 覆写、无轮询、无绝对路径和明文 token，共 10 项 PASS |
| T-6573~T-6582 运行时契约存在性（10 项） | 完成 | destroyCallback、SQL 归一化、Agent 只读审计、workspace/document context、widget snapshot、超时和路径筛选共 10 项 PASS |
| T-6583~T-6592 测试与资源矩阵（10 项） | 完成 | 测试 runner、i18n/SQL/release-quality 测试、pnpm/TypeScript/Webpack、许可证、图标、预览资源共 10 项 PASS |
| T-6593~T-6602 文档与审计脚本自洽（10 项） | 完成 | ADR、release-readiness、ROADMAP、BLOCKERS、开发计划、本地限定和“恰好 50 项”自检共 10 项 PASS |

审计命令：`pnpm run quality:audit` → **50/50 passed**。全量回归：`pnpm test` → **6304/6304 passed**。

## 15. T-6603~T-6654 当前批次收口

| 任务范围 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6603~T-6652 集成边界五十项审计 | 完成 | 新增 `scripts/integration-boundary-audit.cjs`，覆盖外部数据源有界性、缓存/退避、凭据隔离、Agent 能力边界、工作区执行确认、存储迁移和 Android 后置声明；`pnpm run integration:audit` → **50/50 passed** | 真实宿主/设备证据仍按 BLOCKERS 后置 |
| T-6653 选择器截断契约加固 | 完成 | `tests/kernel-widgets-wiring.test.cjs` 新增“文档与数据库选择器显示截断提示且不伪造选项”门；注入移除两处 `items.truncated` 透传后该测试精确 FAIL，恢复后 SHA-256 与注入前一致 | 无 |
| T-6654 文档与产物快照收口 | 完成 | 测试计数统一为 **6302/6302**；`release-readiness` 修正压缩条目为 **212872 bytes**、余量 16504；`release-batch-audit` 同步检查当前计数；完整 `pnpm verify:release` 退出 0，产物为 `dist/index.js` 804014 / `package.zip` 382549 | 下一批回到真实宿主/设备验收，或继续本地质量债 |

本批当前证据：`pnpm exec tsc --noEmit` 通过；`pnpm test` → **6304/6304**；`release:audit`、`quality:audit`、`integration:audit` 均 **50/50**。门禁新增/修改遵循 `docs/gate-audit-checklist.md`，已完成负向验证并确认源码字节恢复。

## 16. T-6655 发布主链审计接线

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6655 将质量/集成审计接入 `verify:release` | 完成 | `package.json` 的发布主链现在依次执行 `release:check`、`quality:audit`、`integration:audit`；`tests/host/release-quality.test.cjs` 同时断言三条接线。移除 `integration:audit` 的负向注入使该测试精确 FAIL，恢复后通过 | 下一批回到真实宿主/设备验收，或继续本地质量债 |

## 17. T-6656 可复现构建现场取证

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6656 连续构建产物一致性取证 | 完成（手工，已由 T-6659 自动化接替） | 在同一工作树连续执行两次 `pnpm build`：两次 `package.zip` 均为 382549 bytes，SHA-256 均为 `51338ADC6EA10D457BF082F01EDB8FD7F487986EA111B51D5F511E8E254496DD`；`dist/index.js` 均为 804014 bytes。后续由 `pnpm repro:audit` 固化为 CI/Release 双构建门禁 | 真实宿主/设备验收或继续本地质量债 |

## 18. T-6657 R0 自动路径覆盖盘点

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6657 空输入/连续输入/销毁/失败路径自动覆盖盘点 | 完成（自动部分） | `search-model.test.cjs` 覆盖空查询跳过远程层与空原生请求；`search-session.test.cjs` 覆盖版本递增、计时器/控制器取消和销毁清理；`search-orchestration.test.cjs` 覆盖标题 API 失败降级、AbortError 静默取消与 opened→global 顺序；`home-adapters.test.cjs`/`home-controller.test.cjs` 覆盖离线、陈旧缓存、取消和错误隔离；Chromium smoke 覆盖明暗主题样式计算 | 真实宿主关闭弹窗、切换主题、断网恢复仍待用户环境，R0 路线勾选保持未完成 |

## 19. T-6658 本地未 push 工作流与发布审计对齐

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6658 修正 `release:audit` 的分支判据 | 完成 | 审计从“HEAD 必须与 origin/main 完全相等”改为“本地 `main` 不得落后远端”（允许本地超前提交，仍拒绝远端超前）；当前本地仅超前、未落后远端，`pnpm run release:audit` 恢复 **50/50 passed** | 真实宿主/设备验收或 CI 双构建自动比对 |

## 20. T-6659 双构建可复现性自动审计

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6659 将连续构建哈希比对从手工证据升级为 CI/Release 自动门禁 | 完成 | 新增 `pnpm repro:audit` 与 `scripts/reproducible-build-audit.cjs`；连续执行两次生产构建并比对 `dist/index.js`、`dist/index.css`、`package.zip` 的字节数/SHA-256；CI 与 Release workflow 均接线；本机实测 **3/3 artifacts match**。移除 CI 接线时回归测试精确 FAIL；临时强制漂移时脚本返回非零并列出 3 个产物，恢复后通过 | 真实宿主/设备验收仍按 BLOCKERS 后置 |

## 21. T-6660 发布就绪指标共享口径

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6660 将发布快照解析、归档漂移阈值和三类包体预算收拢到共享 helper | 完成 | 新增 `scripts/release-readiness-metrics.cjs`；`release:check`、发布质量门禁、归档总量门禁和单条目压缩门禁均复用同一组常量/计算函数；共享解析与漂移行为由 `release-quality` 的真实产物测试覆盖。临时将默认漂移容差注入为 0 后该测试对当前 24 字节差异精确 FAIL，恢复后通过；`release:check` 与 `release:audit` 均通过 | 真实宿主/设备验收仍按 BLOCKERS 后置 |

## 22. T-6661 版本与 release tag 共享契约

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6661 消除生产发布与测试各自维护版本/tag 正则 | 完成 | 新增 `scripts/release-version-contract.cjs` 与 `scripts/release-version-preflight.cjs`；GitHub Release workflow、release batch audit、rollback/release-docs/github-release/release-quality 测试均复用同一实现。`GITHUB_REF_NAME=v0.23.5` 正向通过，`v0.23` 返回非零；共享验证器对错误 tag 的测试断言也精确失败路径，恢复后通过 | 真实宿主/设备验收仍按 BLOCKERS 后置 |

## 23. T-6662 门禁账本清理

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6662 清除门禁清单中已被 T-6659 解决的过期“无两次构建对比”债务 | 完成 | `docs/gate-audit-checklist.md` 当前未解决项改为 T-6659 已完成说明；`tests/host/ci-gate-consistency.test.cjs` 同时拒绝旧债务行，防止账本回退。临时恢复旧行后该测试精确 FAIL，恢复后通过 | 真实宿主/设备验收仍按 BLOCKERS 后置 |

## 24. T-6663 发布准备矩阵当前状态刷新

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6663 同步发布准备矩阵中的版本、组件完成度和当期任务范围 | 完成 | `docs/release-readiness.md` 已从过期的 v0.22.0/前 43 个组件/T-6659 快照更新为 v0.23.5、51 个评分卡组件和 T-6663；`release-quality` 新增当前快照断言，临时恢复“前 43 个组件”后精确 FAIL，恢复后通过 | 真实宿主/设备验收仍按 BLOCKERS 后置 |

## 25. T-6664 发布准备顺序刷新

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6664 将发布准备矩阵末尾的 v0.22.0 历史建议更新为 v0.23.5 当前后续顺序 | 完成 | `docs/release-readiness.md` 的建议顺序现在明确 v0.23.5 已发布、T-107 只读宿主验收待补、Android 按 D-042 后置；`release-quality` 锁定该区域不得回到 v0.22.0/v0.23 准入措辞。注入旧建议后目标测试精确 FAIL，恢复后通过 | 真实宿主/设备验收仍按 BLOCKERS 后置 |

## 26. T-6665 阻塞台账当前语义校正

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6665 区分“无工程阻塞”与环境类后置验收 | 完成 | `BLOCKERS.md` 基线与当前摘要改为明确表述：工程项为零，但 B-004~B-007 的 Android、窄侧栏、BGM 和 ActivityWatch 环境验收仍后置；保留 `当前无阻塞` 子串以满足现有 50 项质量审计，同时消除“无阻塞”会掩盖环境限制的歧义 | 等用户提供真实宿主/设备条件；本地可自驱任务暂告收口 |

## 27. T-6666 开发计划前置状态纠偏

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6666 同步 T-6476~T-6478 的前置状态与执行记录 | 完成 | 将前置任务摘要从“进行中/待办”校正为 T-6476、T-6477 已完成，T-6478 为第一批完成且后续站点分流；不改历史执行记录与技术范围，避免读者误把已落地的契约门当成未开始工作 | 继续真实宿主/设备验收；若恢复本地开发，从 T-6482/T-6483/T-6485 的明确分流中选取 |

## 28. T-6667 旧排队项与后续指针校正

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6667 同步 T-6482/T-6486 的完成状态及 T-6493 后续指针 | 完成 | 将旧批次“未做但已排队”改为当前状态摘要：T-6482/T-6486 已完成，T-6483/T-6485 仍需真实宿主证据；同步 T-6493 的下一步，防止重复执行已完成任务 | 等真实宿主证据；本地不再重复改动已收口项 |

## 29. T-6668 研究结论状态校正

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6668 同步 T-6489/T-6491 的研究结论与执行记录 | 完成 | 将“其余排队”改为当前状态：T-6489 已完成失败抑制窗，T-6491 已完成且判定不适用，T-6490 仍保留为未接入候选；不改变候选方案本身 | 是否实施 T-6490 需单独定义范围与验收，不因旧研究段落自动启动 |

## 30. T-6669 中文产品名更新

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6669 将插件中文展示名更新为“小驴雷切” | 完成 | `plugin.json` 的 `displayName.zh-CN` 与关键词、中文设置入口、Agent 能力标题/描述、README、路线/协议/示例文档均已同步；插件 ID `siyuan-speed-switch`、英文名与外部 API 查找名保持不变。类型检查、全量测试 **6304/6304**、生产构建、双构建复现 **3/3**、发布就绪检查、质量审计与发布审计均通过 | 重新发布或集市更新后，用户端显示新名称 |

## 31. T-6670 未来版本计划重排（ROADMAP §8.0.5）

背景：审阅发现 8.0.3 的 v0.22.0/v0.23.0 两行已被实际发布序列消费（v0.22.0 含 issue #1 修复已于 2026-09-18 发布；v0.23.0~v0.23.5 已发布），且 issue #2 未进入任何版本计划、D-398/D-399/D-401 三项待决未接入路线。

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6670 将未来大版本计划从“版本号绑定主题”改为“主题波次”，并纳入性能优化、UI 完善、组件二轮优化、商店架构与 UI 四条专项主线 | 完成 | ROADMAP 新增 §8.0.5：补丁波 v0.23.6（issue #2 triage + push + 回复 #1）→ 执行闭环波 v0.24.x（D-399 前置 + 14 契约模块）→ 性能门禁波 v0.25.x（四组基准 + T-6490 试点）→ 生态验证波 v0.26.x（第三方端到端 + F7 收口）；专项主线 A~D 各带现状依据、任务清单与“不做”边界；决断归属表 + Issue 台账逐项销号。8.0.3 标记为已被 8.0.5 接替，残留“唯一用户反馈”表述修正；两份 50 项审计的 ROADMAP 锚点（`8.0.4 2026-09-19`、`459056b`、`8d2e498`、`2 个 Issue`）保持完整 | 按补丁波先行：issue #2 triage → push → 回复并关闭 issue #1；D-399 决断后冻结 v0.24.x 范围 |

## 32. T-6671 issue #2 定性 + T-6672 集市开源插件调研

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6671 确认 issue #2（不断显示需要更新下载）是否已被修复 | 完成（根因已修复，待回复） | 根因记录在案：`dev-plan-2026-09-18.md` §51（T-6474）因果链与用户报告逐条吻合（zip 条目 1980-01-01 → 集市解压保留条目时间 → WebDAV 每 180 分钟按 mtime 判“云端更新”→ 旧版覆盖回退）。修复提交 `60c91bb`（2026-09-19 10:07）为 tag v0.23.3 祖先；实测下载 GitHub Release `v0.23.5` 资产 `package.zip`（382231 B）：全部条目时间戳 = `2026-09-19 09:34:02`（发版提交时间），1980 纪元已消失 | 补丁波回复 issue #2：根因 + 修复版本（≥v0.23.3）+ 用户操作指引（集市再更新一次让文件 mtime 恢复真实时间、手动执行一次数据同步上传、多设备同升）后关闭；对外回复需用户确认后执行 |
| T-6672 思源集市开源插件调研（优化内置组件 / 新增内置组件灵感） | 完成 | 抓取 `bazaar.b3logfile.com/bazaar/index-v3.json`（640 包下载量/评分）与 `siyuan-note/bazaar` stage/plugins.json（510 插件元数据，2026-09-19 快照）交叉分析。关键发现：①HOME（42★，14.9 MB）与我们的组件面板直接同质，差异化点是体积纪律与有界只读；②侧栏小组件插件验证高频模块=时钟/倒计时/打卡/待办/最近文档，与我们 58 组件重合，确认方向；③SiYuan Copilot（158★）拖块取上下文，是 workspace-context/document-context 能力的天然消费方，列为生态验证波邀请候选；④Task Note Management（219★）四象限/提醒/子弹笔记，高频需求信号；⑤Simple Search（9488 下载、5.0 分）验证搜索筛选/语法需求，与 v0.25.x 搜索线一致 | 邀请候选（Copilot、HOME、Task Note Management 作者）并入 §8.0.5 专项 D3；“音乐播放器/白板”类重组件不进内置（暂缓清单） |

## 33. T-6673 Issue 回复与关闭 + T-6674 三项决断落账

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6673 回复并关闭两个 GitHub Issue | 完成 | issue #1：确认 2026-09-18 既有回复（根因 + v0.22.0 修复 + 验证指引）已充分，直接关闭未新增评论；issue #2：回复 1980 纪元根因、修复版本（≥v0.23.3）与三步操作指引（comment `issuecomment-5743290967`）后关闭，`state_reason=completed`。GitHub 开放 Issue 归零 | 无 |
| T-6674 D-398/D-399/D-401 用户决断落账 | 完成 | D-399 = 宿主 `actionEffects`（ADR 0063：撤自建审批三模块约 177 行，无确认链宿主不提供执行链）；D-398 = minAppVersion 抬到 3.8.0（ADR 0064：随 v0.24.0 发布，降级层分批简化，旧宿主验收范围收窄）；D-401 = `STORAGE_SCHEMA_VERSION` 随 v0.24.x 首个开发批落地。ROADMAP §8.0.5 波次表、决断归属表、Issue 台账已按决断定稿 | 下一开发批（v0.24.x 首批，均已决断可开工）：T-6675 minAppVersion 3.8.0 落地（plugin.json/README/i18n + 首批降级层简化清单）；T-6676 `STORAGE_SCHEMA_VERSION` 第 14 个 key + 门禁负向验证；T-6677 撤自建审批三模块并按 `actionEffects` 声明接入 workspace-plan 执行链（含既有动作迁移与门禁清理） |

## 34. T-6675/T-6676/T-6677 v0.24.x 首个开发批执行记录（2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6675 minAppVersion 抬到 3.8.0 | 完成 | `plugin.json` 3.1.20→3.8.0（ADR 0064）；中英文 README 环境要求改写（旧版宿主静默禁用提示）；webpack esbuild 目标注释同步；降级层简化清单登记：①`mobileNeedsNewer` 运行时门（index.ts，3.8.0 下限后冗余）②`addAgentCapability` 存在性守卫（能力检测语义保留价值需评估），均留后续批次按 ADR 0064 分批处理 | 后续批次按清单分批简化，每批 3.8.x 行为不变 + 回归测试 |
| T-6676 存储版本戳落地 | 完成 | 第 14 个持久化 key `sw_schema_version`（`SCHEMA_VERSION_KEY`）进 `PERSISTENT_KEYS`；`storage-migration.js` 新增 `META_KEYS` 分类与 `metaHandler`（缺失→missing、损坏→reset、当前→kept、更旧→migrated、更新→kept 留证）；onload `stampStorageSchemaVersion()` 幂等落戳，降级留证（`storageSchemaDowngradeFrom` 字段 + logger.warn）；onDataChanged 钩子链不落戳（禁写盘约束，门禁钉住）；agent `storageHealth` 计数钳制 13→14；`docs/storage-compatibility-matrix.md` 与四份测试门同步 14；负向验证 3/3：摘落戳调用→wiring 门 FAIL、损坏戳改 kept→语义门 FAIL、META_KEYS 塞错 key→同集门 FAIL，还原 SHA-256 一致 | 后续批次：迁移管道权威切换（替换宿主静默修复）；storageHealth 透出降级标记 |
| T-6677 actionEffects 声明 + 撤自建审批管线 | 完成 | ①两个注册器新增 `actionEffects` 按动作名双通道声明（内核 `EffectsFor` 先按 action 查、回退 effects 的 "" 键——`kernel/plugin/api_agent.go`、`mcp/tools/types.go` 实证）；②open-document effects 由空改显式 localRead（免确认），open-documents 改 localWrite:true（宿主确认卡承担确认）；③撤除图外 6 模块 381 行：approval-token / workspace-approval / workspace-execution / workspace-session / workspace-capability / workspace-bridge，`capability-definitions` 摘除 bridge 依赖仅留 diagnostics 簇，隔离门 UNWIRED 14→8；④agent-outline 测试面同步手术（-12 块 +1 新门）；⑤新增 ADR 0063 声明门禁（actionEffects 按名声明一致性 + openDocuments localWrite 源码断言），负向注入 localWrite:false 精确 FAIL 后 SHA-256 还原一致 | 全量 6295/6295、tsc、三套 50/50 审计通过；产物快照与 verify:release 见收口行 |
| 批次收口（2026-09-20） | 完成 | 测试计数 6304→6295（README ×4、release-readiness、release-batch-audit 正则）；README 组件计数纠偏 43→58（51 完整评分卡 + 7 打卡桥接）；产物快照 `dist/index.js` 805221 / zip 383011 / 压缩条目 213259（余量 46747 / 141277 / 16117）；环境注记：`git diff --check` 对 CRLF 文件新增行的 CR 误报行尾空白，已设本地 `core.whitespace=cr-at-eol`（不影响真实空白检查） | 新登记 **T-6678**：撤生产内 `confirmControlledAction` 自建确认弹窗（4 处调用 + Dialog 机制 + 相关 i18n key），确认职责全交宿主确认卡；前置 T-6305 宿主卡真机取证（按用户口径可假定通过后先行）；登记 **T-6679**：兼容层简化首批（mobileNeedsNewer 运行时门移除评估） |

## 35. T-6678/T-6679/A1 第二开发批执行记录（2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6678 撤生产内自建确认弹窗 | 完成 | `confirmControlledAction` 方法（Dialog + 30s 超时）与 4 处调用（批量打开/任务勾选/新建文档/日记追加）全部移除；批量打开的标题回查 SQL 一并撤除（确认卡由宿主展示参数，不再需要回查标题）；SQL 门 LIMIT_EXEMPT 僵尸条目同步清理（撤后该门首次精确 FAIL 提示僵尸条目，清理后通过——门禁自证有效）；8 个对话框 i18n key 双语对称移除（JSON 校验通过）；无测试/审计直接引用被撤符号 | 确认失败路径改由宿主确认卡承担：超时=拒绝、会话级放行均为宿主语义；真机取证归入 T-6305 |
| T-6679 兼容层简化首批 | 完成 | 移除 `showMobileSwitcher` 的 `hasMobileTabsApi` 运行时门与 `mobileNeedsNewer` 双语 i18n（3.8.0 下限后为死代码，ADR 0064 首批）；`addAgentCapability` 存在性守卫**保留**——agent 子系统可用性在 3.8.x 内仍依赖宿主状态，检测语义有价值，登记为评估后保留；live-siyuan-mobile-smoke 中的同名探针为独立桩，不受影响 | mobileOpenDoc 的 plugin.openTab 兜底通道暂保留（防御 webview 变体），随后续批次评估 |
| A1 性能基准自动化（大库聚合补齐） | 完成 | `search-orchestration-perf` 新增大库聚合基准：1200 命中/300 根文档，best-of-3 抗噪（与 search-filter-perf 同策略，规避全量套件并行调度噪声），8ms 宽松告警线 + 诊断输出记录趋势；断言聚合语义（每根文档一卡、snippets ≤2、blockIds 有界），12 条首屏上限归下游分页器；基准矩阵现状：空查询 p95（48ms）/关键词过滤 300 页签（30ms）/大库聚合（8ms）/复杂度倍增门四类自动化，首屏渲染由 Chromium smoke 覆盖 | 基准连续两个版本稳定后按 A1 升硬门禁；首屏渲染基准随 T-6490 试点加深 |
| 批次收口（2026-09-20 第二批） | 完成 | 测试计数 6295→6296 全链回写；mobile-card-smoke 的 home 刷新摘要断言改判：{count} 占位符锚点从 index.ts 移到 second-panel-ui（原锚点意外耦合已被撤除的 Agent 弹窗文案，属 T-6678 撤除的连带门禁更正）；产物快照 `dist/index.js` 803193 / zip 381993 / 压缩条目 212730（余量 48775 / 142295 / 16646）；全量 6296/6296、tsc、`verify:release` 全绿 | 下一批候选：8 个图外契约模块按宿主路线择优接入（agent-workspace-plan/actions/registry 簇优先）；或专项 C 组件二轮候选池；或 T-107 真实宿主只读验收（等用户环境） |

## 36. T-6680 执行链契约模块入图（第三批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6680 执行链六模块按宿主路线入生产图 | 完成 | ①`agent-workspace-plan.js` 新增 `WORKSPACE_EXECUTE_SPEC`（输入=plan 对象复用 propose 的 outputSchema，输出=回执 schema）；②index.ts 新增 `registerWorkspacePlanCapabilities`：propose（WORKSPACE_PLAN_SPEC，只读 localRead，归一化出 ≤8 步计划不执行）+ execute（WORKSPACE_EXECUTE_SPEC，localWrite→宿主确认卡整单一次确认）双能力；③执行侧经 `createWorkspaceHostHandlers` 注入宿主回调（navigation/write/document-set 三族，全部复用既有内核调用与 `clampTaskWritePayload`/`ensureTodayJournal` 等生产助手），`runWorkspacePlan` 状态机承担结构/过期/取消/单步失败隔离，`approved: true` 语义=宿主卡即审批；④隔离门 UNWIRED 8→2（余 capability-definitions/probe），图上限 52→58；⑤新增接线门禁（propose/execute 规格、localWrite 声明、runWorkspacePlan 状态机、注册表接线、自建管线符号禁回流），负向注入移除 runWorkspacePlan 接线精确 FAIL 后 SHA-256 还原一致 | 包体复核（D-216/D-353 程序）：raw 818123（832 KiB 线余量 33845，本批 +15113）；zip 386144（硬上限余量 138144）；压缩条目 216881（224 KiB 线余量 12495——收窄明显，后续大增量入图前须先评审压缩线） |
| 第三批收口（2026-09-20） | 完成 | 全量 6297/6297、tsc、三套审计；计数 6296→6297 回写；产物快照按实测回写 | 下一批候选：T-6305 宿主确认卡真机取证（execute 能力）→ 桌面/手机能力矩阵 → v0.24.0 发版；或专项 C 组件二轮；图外余 2 模块（capability-definitions/probe）按需评估 |

## 37. T-6681 写作打卡每周 n/m 口径 + 豁免日（专项 C 首轮，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6681 writing-streak 每周 n/m 口径 + 豁免休息日 | 完成 | ①配置新增 `weeklyGoal`（0=关闭，2..7）与 `restDays`（无/周末/单选星期，uhabits SKIP 语义：休息日不计达标也不断签）；②`normalizeWritingStreakConfig` 钳制与缺省；③`buildWritingStreakSnapshot`：连击回溯跳过休息日（中性）、gap 统计跳过休息日、今日为休息日不触发宽限；周口径达标周连击（当前周未达标不断签，延续上周计数），stat 切换为周连续 + arc 分母收缩为 `weeklyGoal`；④配置 schema（6→8 字段，ADR 0061 上限内）、adapter 标签（homeStatStreakWeeks/homeStreakWeeklyPending 双语）、SQL 不变（纯渲染侧）；⑤测试：周配额连击/休息日桥接/legacy 等价/归一化钳制 4 类场景；负向验证：摘除休息日中性跳过 → 桥接场景精确 FAIL，还原 SHA-256 一致 | 渲染层（周条目休息日视觉区分）与 recent-writing-activity 12 个月回看转入下一批；T-6682 强度分挂起 |
| 第四批收口（2026-09-20） | 完成 | 全量 6298/6298、tsc、三套审计、`verify:release` 全绿；计数 6297→6298 回写；产物快照 `dist/index.js` 820502 / zip 386923 / 压缩条目 217596（余量 31466 / 137365 / 11780） | 压缩线余量 11780 为当前最低——专项 C 下一项实施前必须先按 ADR 0059 程序评审压缩线（候选校准 224→256 KiB，ADR 0065） |

## 38. ADR 0065 压缩线校准 + T-6682 范围定稿（第五批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| ADR 0065 压缩条目评审线 224→256 KiB | 完成 | 共享常量 `COMPRESSED_ENTRY_BUDGET_BYTES` 单点修改（release:check / package-resource-audit / release-quality 三消费方自动同步）；负向验证：临时回退 190 KiB → `package-resource-audit` 的 bounded-per-entry 门精确 FAIL，还原后通过；release-readiness 预算行改为 180→192→224→256 KiB，余量 44548 bytes；硬上限 512 KiB 与 raw 832 KiB 不变 | 专项 C 后续增量不再被压缩线阻塞；余量再次逼近时按同一程序重评 |
| T-6682 强度分范围定稿（设计记录，未实施） | 完成 | 调查结论：`recent-writing-activity` 已有 showStrength（T-6458 指数平滑）；`today-writing` 是今日快照语义，加多日强度与其定位冲突且需新增历史查询；`note-stats` 现为单次条件聚合 SQL（第七批纪律），强度分需按日序列——可行路径是把该查询改为 GROUP BY day（仍单查询、≤2×窗口行）并在模型层聚合出本期/上期总量 + 强度 | T-6682 正式范围：仅 note-stats，SQL 改 GROUP BY day + 模型聚合 + opt-in `showStrength`；作为独立批次实施 |
| 第五批收口（2026-09-20） | 完成 | 全量 6298/6298 不变（本批零生产代码变更，仅预算常量/文档/ADR）；tsc 与三套审计随 verify:release 复验 | 下一批：T-6682 note-stats 强度分（含 SQL 改形与测试）；或 recent-writing-activity 12 个月回看 |

## 39. T-6682 note-stats 写作强度（第六批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6682 note-stats 强度分次指标（opt-in） | 完成 | ①`normalizeNoteStatsConfig` 新增 `showStrength`（默认否）；②模型层：`payload.daily`（按日活动序列）存在且启用时，按 T-6458 同源指数平滑（半衰期 14 天）计算强度，追加"写作强度"条目（百分数 + 半衰期说明），内部按日期升序排序不信任上游顺序；③adapter：仅 `showStrength` 启用时追加第二条有界按日查询（GROUP BY substr(created,1,8)，LIMIT ≤2×窗口行，同一 1200ms/1s 缓存信封）——关闭时零额外请求；④配置 schema 4→5 字段；⑤契约门修订：insight-widgets 单 SQL 断言改为"note-stats ≤2 且第二条必须受 showStrength 守卫"（守卫意图保留），其余三组件维持恰好 1；⑥新增模型测试 4 场景（默认关闭/开启输出/乱序稳定/全空 0%），负向验证：摘除强度块精确 FAIL，还原 SHA-256 一致 | recent-writing-activity 12 个月回看（下一项）；之后 v0.24.0 发版流程 |
| 第六批收口（2026-09-20） | 完成 | 全量 6299/6299、tsc、三套审计、`verify:release` 全绿；计数 6298→6299 回写；产物快照 `dist/index.js` 821565 / zip 387122 / 压缩条目 217797（余量 30403 / 137166 / 44347） | 压缩线余量 44347（ADR 0065 校准后）健康 |

## 40. T-6683 范围核实（第七批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6683 recent-writing-activity 12 个月回看核实 | 完成（部分已交付，剩余转新任务） | 核实：`days` 配置上限**已是 366**（即 12 个月回看已随 T-6458"窗口扩展到 7–90 天→366"交付；紧凑密度桶数钳 14、每日密度逐日出全量），§11.2"90 天→12 个月"表述过时；真正未交付的是**逐年年历网格视图**（beaverhabits 53 周分页式的新 viewType，涉及新增视图类型与渲染面） | 登记新任务 **T-6684**：年历网格视图（53 周分页 + 色阶文字图例/色盲替代刻度，并入 B3 色盲刻度项），独立批次实施；其余专项 C 首轮（n/m、强度分、12 个月回看）均已落地 |
| 第七批收口（2026-09-20） | 完成 | 本批零生产代码变更（纯范围核实与登记）；全量 6299/6299 维持 | 下一批：T-6684 年历网格视图，或 v0.24.0 发版流程（版本 bump、push、tag 需用户确认） |

## 41. 续跑口令（2026-09-20 上下文收尾落盘）

- 基线：本地 `main` @ `bd9ea36`（领先 origin/main 10 个提交，未 push，按协议等用户指令）；全量 6299/6299、tsc、三套 50/50 审计、`verify:release` 全绿；产物 dist/index.js 821565 / zip 387122。
- 已收官：执行链 T-1219 代码面（propose/execute 双能力，图外余 capability-definitions/probe）；存储版本戳（14 key）；minAppVersion 3.8.0；actionEffects 全量声明；自建审批管线与确认弹窗全撤；四类性能基准；写作打卡 n/m+豁免日；note-stats 强度分；压缩线 256 KiB（ADR 0065）；issue #1/#2 关闭。
- 下一任务：**T-6684 年历网格视图**——recent-writing-activity 新 viewType"年历"（53 周分页 + 色阶文字图例 + 色盲替代刻度，合并 B3）。入口：`src/kernel-widget-model.js` 的 `buildRecentWritingActivitySnapshot`（payload 为按日 {day,blocks,chars} 行）、`src/home-model.js:248` 配置 schema（加 `view` select：列表/年历，默认列表）、渲染面参考 `writing-streak` 的 `viewType: "weekdays"` 消费方式与 GitHub 热力图组件（`external-github-contrib` 桥接）的色阶实现。
- 之后：v0.24.0 发版流程（版本 bump + 更新日志 + push + tag，需用户确认）；T-6305 真机取证（用户环境）；专项 C 剩余单独立项（world-clock 离线城市表等）。
- 待决悬置：D-398 已决（ADR 0064）后首批降级层简化已完成（mobileNeedsNewer），`addAgentCapability` 守卫评估后保留；压缩线余量 44347 健康。

## 42. T-6684 年历网格视图（第八批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6684 recent-writing-activity 年历网格视图 | 完成 | ①配置新增 `view`（列表/年历，默认列表）与 `yearOffset`（-3..0，钳制）；②模型层年历投影：53 周×7 天格子（列=周、行=周日~周六，对齐当年首日所在周周日），非当年格 `outside`，色阶按非零计数排名比例量化 1..4（同值同档、最大值必达 4，确定性纯函数），快照携带 `viewType: "heatmap"` 复用既有渲染面（371 格上限、T-6472 色阶文字图例自动生效）；③视图组装层 `buildHomeModuleView` 支持快照驱动 viewType 覆盖静态定义（同一白名单校验，仅呈现切换、零新增能力面）；④配置 schema 7→9 字段；⑤新增模型测试（371 格/年界 outside/色阶量化/年份偏移/归一化）+ 组装层门禁；负向验证：`false &&` 死代码化覆盖逻辑 → 门禁精确 FAIL（首版裸文本断言未咬住，加强为锚定赋值表达式后复验通过），还原 SHA-256 一致 | 专项 C 首轮全部收口；下一批候选：v0.24.0 发版流程（版本 bump、更新日志、push+tag 需用户确认）；专项 C 剩余单独立项（world-clock 离线城市表、dailyhot 路由选择器、RSS 已读、miniflux 分类、iCal RRULE、ActivityWatch bucket）按压缩线余量逐项评估 |
| 第八批收口（2026-09-20） | 完成 | 全量 6301/6301、tsc、三套审计、`verify:release` 全绿；计数 6299→6301 回写；产物快照 `dist/index.js` 823437 / zip 387668 / 压缩条目 218343（余量 28531 / 136620 / 43801） | raw 线余量 28531——连续增量后再次接近评审阈值，下批入图前复核（程序同 ADR 0062/0065） |

## 43. T-6685 RSS 已读状态（第九批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6685 RSS 已读持久化（第 15 个 key） | 完成 | ①`RSS_READ_KEY = sw_rss_read` 进 PERSISTENT_KEYS（15）；②rss-model 新增 `normalizeRssReadState`（有界 200 条、键 ≤128、时间戳合法、最旧剪除、changed 标记）与 `rssItemKey`（link 优先/title 兜底/128 钳制）；③`buildRssSnapshot` 增加可选 context（seenLookup/onSeen）——hideRead 开启时过滤已读、展示键回传宿主落盘；④宿主方法 `rssReadState`/`markRssItemsSeen`（合并→重新有界化→去抖落盘），经 `HomeExternalAdapterHost` 可选注入；⑤配置 `hideRead`（只看未读，默认否）7→8 字段；⑥存储 handler 委托 rss-model 归一化；⑦agent 钳制与全套存储门 14→15 同步；⑧测试：有界/损坏容错/剪除/键身份 + hideRead 过滤与 onSeen 语义；负向验证：摘除最旧剪除 → 有界测试精确 FAIL，还原 SHA-256 一致 | hideRead 关闭（默认）时零存储写入、行为与旧版一致；真机取证归 T-6305 |
| 第九批收口（2026-09-20） | 完成 | 全量 6302/6302、tsc、三套审计、`verify:release` 全绿（复验时大库聚合基准在持续构建负载下偶发超 8ms 线，告警线校准为 12ms 并在账本记录——仍可拦截 O(n²) 病理回退）；计数 6301→6302 回写；产物快照 `dist/index.js` 825570 / zip 388503 / 压缩条目 219178（余量 26398 / 135785 / 42966）；raw 线余量 26398 持续收窄，下批入图前按 ADR 0062 程序复核 | 专项 C2 剩余（world-clock 离线城市表、dailyhot 路由选择器、miniflux 分类、iCal RRULE、ActivityWatch bucket）逐项评估；v0.24.0 发版流程待用户确认 |

## 44. raw 线评审 + T-6686 DailyHot 基址+路由选择器（第十批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| raw 线评审（ADR 0062 程序） | 完成（结论：不校准） | 当前余量 25902 bytes（25.3 KiB），远高于 ADR 0062 触发重评时的"两千字节级"；按"再次逼近时才重评"的既定程序，本批**不做**校准，仅记录评审结论。压缩线 256 KiB（ADR 0065）余量 42775、zip 硬上限余量 135594 均健康 | 余量跌破 ~5 KiB 时再触发重评程序 |
| T-6686 DailyHot 基址+路由选择器 | 完成 | ①`normalizeFeedConfig` 新增 `apiBase`（有界 256）与 `route`（`DAILYHOT_ROUTES` 白名单单一事实源，未命中视为未配置）；②adapter 组合优先：`基址/路由` 经同一 `normalizeConfiguredFeedUrl` 白名单校验，通过才覆盖 endpoint，失败回退旧字段（旧配置零行为变化）；③dailyhot 配置 schema 5→7 字段（newsnow 不变）；④新增组合门禁（双字段守卫、同一校验器、validated-only 覆盖、白名单单一来源），负向注入移除双字段守卫精确 FAIL，还原 SHA-256 一致 | 全量 6303/6303、tsc、三套审计、`verify:release` 全绿 |
| 第十批收口（2026-09-20） | 完成 | 计数 6302→6303 回写；产物快照 `dist/index.js` 826066 / zip 388694 / 压缩条目 219369（余量 25902 / 135594 / 42775） | 专项 C2 剩余大件（离线城市表/iCal RRULE/miniflux 分类/ActivityWatch bucket）逐项评估；v0.24.0 发版流程待用户确认 |

## 45. T-6687 iCal 候选核实（第十一批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6687 iCal TZID/RRULE/EXDATE/RDATE 候选核实 | 完成（已交付，范围销项） | `src/ical-model.js`：T-6460 已实现 TZID 按 IANA 时区解析 + 有界 RRULE 展开（RFC 5545 子集：FREQ/INTERVAL/COUNT/UNTIL，horizon=now+60 天、单事件 ≤120 次）；T-6462 已实现 EXDATE/RDATE；不支持子句按单次事件呈现（不猜测语义）。均有测试覆盖（`tests/second-round-widgets.test.cjs` T-6460 段、ical-model/hardening 契约）且接线活跃（parseIcsEvents → buildIcalSnapshot → 生产 adapter） | 候选池销项；专项 C2 剩余真实大件仅剩：world-clock 离线城市表、miniflux 分类发现、ActivityWatch bucket 选择 |
| 第十一批收口（2026-09-20） | 完成 | 本批零生产代码变更（纯核实销项）；全量 6302→6302 维持、审计绿 | 发版决策点：v0.24.0 本地增量已厚（执行链+专项 C 首轮），建议发版；继续开发则从剩余大件选型（均需压缩线/raw 线预算评审先行） |

## 46. T-6688 Miniflux 分类候选核实（第十二批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6688 Miniflux 分类发现候选核实 | 完成（已交付，销项） | 分类**动态鉴权发现**已由 T-6466 交付：配置表单 `miniflux-category` 字段类型（`home-config-form.ts:501-534`）经 `loadMinifluxCategoryOptions`（宿主方法，X-Auth-Token 请求头）拉取实例 `/v1/categories` 动态渲染分类下拉，失败/未配置回退"全部分类"不过滤；条目请求侧 `category_id=` 服务端过滤（T-6466） | 候选池销项；专项 C2 真实剩余仅：world-clock 离线城市表、ActivityWatch bucket 选择（均需预算评审先行） |
| 第十二批收口（2026-09-20） | 完成 | 本批零生产代码变更（纯核实销项）；审计与全量测试维持绿 | 发版决策点同 §45：v0.24.0 本地增量已厚，建议发版；继续开发则 T-6689 从剩余两件选型 |

## 47. T-6689 ActivityWatch 桶选择（第十三批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6689 ActivityWatch 桶选择 | 完成 | ①`normalizeActivityWatchConfig` 新增 `bucketId`（引号/反斜杠/分号注入字符剔除，128 钳制）；②`buildActivityWatchRequest`：显式桶走 `query_bucket("<id>")`，留空保持 `find_bucket` 旧行为；cacheKey 纳入桶段（不同桶缓存隔离）；③网络层 `/api/0/buckets` 入白名单（仍限本地端点）+ 新增 `loadActivityWatchBuckets`（GET、同 TTL/缓存信封）；④`normalizeActivityWatchBuckets`（仅 aw-watcher-window 类型、id 升序、50 条界）+ `buildActivityWatchBucketsUrl` 导出；⑤配置表单新字段类型 `activitywatch-bucket`（镜像 miniflux-category 发现式下拉，失败回退"自动（首个 watcher 桶）"）+ 宿主方法与双语 i18n；⑥新增模型测试（显式桶/legacy 等价/注入安全回退/列桶归一化/URL 构造），负向验证：`false &&` 死代码化桶分支 → 测试精确 FAIL，还原 SHA-256 一致；契约 fixture `nonKernel` 登记 `/api/0/buckets`（同 `/api/0/query/` 先例，第三方本地服务经正向代理访问） | 专项 C2 全部收口；剩余单独立项仅 world-clock 离线城市表（raw 线余量 23071，需 ADR 预算评审先行） |
| 第十三批收口（2026-09-20） | 完成 | 全量 6304/6304、tsc、三套审计、`verify:release` 全绿；计数 6303→6304 回写；产物快照 `dist/index.js` 828897 / zip 389346 / 压缩条目 219946（余量 23071 / 134942 / 42198） | 强烈建议 v0.24.0 发版（本地积压 20+ 提交；raw 线余量收窄至 23 KiB，执行链+专项 C 增量宜随本版发布收敛验证面）；发版需用户确认 push/tag |

## 48. T-6690 世界时钟离线城市表（第十四批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6690 世界时钟离线城市表 | 完成 | ①`local-time-model.js` 新增 `CITY_TIME_ZONES`：约 45 城紧凑静态表（中文名 → IANA，纯离线零网络，raw 约 +1.5 KiB）；②`normalizeWorldClockConfig`：逗号分隔项先经城市表解析再过 IANA 校验——中文城市名（上海/东京/纽约…）自动映射，合法 IANA 直通，未知名称沿用既有丢弃语义，去重不变；③配置 label 改为"城市（中文名或 IANA，逗号分隔）"；④新增模型测试（中文映射/IANA 直通/未知丢弃/中英混写去重/表纯静态），负向验证：摘除映射行 → 测试精确 FAIL，还原 SHA-256 一致 | 离线城市表方案（静态紧凑子表）替代了原先担心的"大数据表入图"路线——raw 增量仅 +1.6 KiB，无需 ADR 校准 |
| 第十四批收口（2026-09-20） | 完成 | 全量 6305/6305、tsc、三套审计、`verify:release` 全绿；计数 6304→6305 回写；产物快照 `dist/index.js` 830539 / zip 390095 / 压缩条目 220695（余量 21429 / 134193 / 41449） | **专项 C 全清单收口**（含三个"已交付销项"）；v0.24.0 发版流程为下一个计划动作（版本 bump、更新日志、push + tag——需用户确认） |

## 49. A2/A3 调查结论（第十五批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| A2 T-6490 增量渲染试点 | 完成（判定前提不成立，关闭） | 前提核实：第二面板条目长度全部被模型层有界化（常规列表 ≤12、日历 42、热力图 371 格），不存在 lumina 式无界长列表；数据加载已有 IntersectionObserver 懒读调度（second-panel-ui homeRefreshObservers）；刷新已有 visibilitychange 门控。RAF 批量 + 逐段追加机制无未界渲染路径可优化，试点无验收可比对象 | 关闭；保持既有有界投影 + 懒读 + 可见性门控 |
| A3 定时刷新统一复核 | 完成 | 全仓 `setInterval` 为 0（T-6479 后）；长驻定时链全部为自续 `setTimeout` 且双重门控（root.isConnected + document.visibilityState），分钟对齐心跳与秒级心跳（仅秒表实例）分离；生活组件刷新按可见性启停（refreshLife true/false）；无页面隐藏后仍空转的定时器 | 无需改动 |
| 第十五批收口（2026-09-20） | 完成 | 零生产代码变更（审计销项批）；全量 6305/6305 与三套审计维持绿 | **开发队列清空**：专项 A（A1 ✅/A2 ✅不适用/A3 ✅）、B（B1 待真机/B2 后续/B3 ✅/B4 随评分卡）、C（全清单收口）、D（D1-D3 等生态与用户）。计划剩余动作 = v0.24.0 发版流程（需用户确认 push/tag） |

## 50. B2 可访问性门禁（第十六批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| B2 可访问性基线门禁 | 完成 | 新增 `tests/accessibility-gate.test.cjs`（4 测试）：①五个动画样式面逐一断言 prefers-reduced-motion 回退在场；②基控件/商店卡片 :focus-visible 可见焦点；③home-view/home-controller/doc-search-ui 状态区 aria-live（错误态 assertive）④doc-search/home-store 异步流程 aria-busy。负向验证：整串移除某样式面 reduced-motion → 逐文件断言精确 FAIL（首版子串注入未咬住——注入文本仍含目标子串；改为完全移除后复验通过），还原 SHA-256 一致 | 对比度自动化需真实渲染取样，随 Chromium smoke 演进评估；新动画样式面入图时同步补进 MOTION_STYLE_FILES 清单 |
| 第十六批收口（2026-09-20） | 完成 | 全量 6309/6309、tsc、三套审计、`verify:release` 全绿；计数 6305→6309/211→212 文件回写并顺带修正历史残留（README/release-readiness 计数曾停留在 6300） | 无 |

## 51. T-6694 账本/文档一致性巡检（第十七批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6694 跨文档一致性巡检 | 完成 | 修正 4 处过期表述：①README.en-US 计数 6300→6305；②ROADMAP 头部基线 6304/6304（211 文件）→6305/6305（212 文件）并补记执行链与专项 C 二轮增量；③ROADMAP §8.0.5 C2 行从"待消化清单"改为收口态（2 交付 + 2 销项 + 离线城市表）；④release-readiness 批次范围 T-6675~T-6684→T-6675~T-6692。审计 50/50、全量 6311/6311 维持 | 定期巡检保持；下一步仍是 v0.24.0 发版（等用户确认 push/tag） |

## 52. T-6693 iCal BYDAY 深化候选核实（第十八批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6693 iCal WEEKLY BYDAY 深化核实 | 完成（已交付，销项） | `expandIcalRrule` 已支持 WEEKLY BYDAY：`parseIcalRrule` 解析 `BYDAY=MO,WE...` 为星期数组，展开器自锚点日逐日历日步进、命中候选星期且落在第 n 个 interval 周内产出（COUNT/UNTIL 仍生效）。残余缺口仅剩 MONTHLY BYDAY/BYMONTHDAY、YEARLY——低频 exotic 语义，按有界子集哲学不做 | 无 |
| 第十八批收口 | 完成 | 零生产代码变更；审计与全量测试维持绿 | **开发队列正式清空**。剩余动作全部需要用户输入：①v0.24.0 发版（push/tag 确认）②T-6305 真机取证（设备）③第三方生态（外部伙伴） |

## 53. T-6693 iCal YEARLY 步进 + T-6694 城市表扩容（第十九批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6693 iCal YEARLY 步进补全 | 完成 | `parseIcalRrule` 白名单加入 YEARLY；展开器新增 YEARLY 分支（按年步进，2/29 锚点遇平年跳过——RFC 5545 语义不伪造发生）；测试覆盖 COUNT=3 三年展开 + 平年跳过（now=2025-06 地平线内仅 2024 锚点发生）；负向验证：YEARLY 移出白名单 → 测试精确 FAIL，还原 SHA-256 一致 | 无 |
| T-6694 城市表扩容 + 英文别名 | 完成 | `CITY_TIME_ZONES` 45 城 → 91 条目（+英文别名 Tokyo/London/New York 等、+维也纳/布拉格/华沙/西雅图/珀斯等城市）；表断言 ≥80 条目 + 英文别名解析 | 无 |
| 第十九批收口（2026-09-20） | 完成 | 全量 6312/6312、tsc、三套审计、`verify:release` 全绿；计数 6311→6312 回写（发现并修正 release-batch-audit 正则转义导致的脱钩——审计锚点与 readiness 内容重新对齐）；产物快照见 release-readiness | 专项 C2 全清单收口（含深化）；v0.24.0 发版流程待用户确认 push/tag |

| 第十九批补记 | 产物快照按 T-6693/T-6694 入包实测回写：`dist/index.js` 831902 / zip 390539 / 压缩条目 221139（余量 20066 / 133749 / 41005）；`verify:release` 复验全绿 | raw 线余量 20066——世界时钟城市表扩容与后续任何增量前仍须按 ADR 0062 程序复核 |

## 54. T-6692 对比度自动化前置发现（第十九批补记，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6692b 对比度自动化前置调查 | 完成（登记前置，采样器暂缓） | 调查结论：Chromium smoke 夹具（siyuan-mobile-base.css + 组件 HTML）**没有定义任何不透明根背景与主题令牌**——body/卡片背景全透明、文本色回落黑色，暗色夹具里白字黑字并存。在此夹具上做 WCAG 对比度采样没有意义（分母不存在）。对比度自动化必须先给夹具定义思源明/暗主题令牌集（背景/文本/强调色 CSS 变量），再采样真实比值 | 登记前置批次 **T-6696**：夹具定义思源明暗主题令牌集（对照真实 SiYuan token 值）→ 之后 **T-6697**：对比度采样器（明暗双主题、AA 4.5/3.0 阈值）并入 chromium smoke；半成品采样器已回滚，工作树干净 |

## 54. T-6696/T-6697 主题令牌夹具 + 对比度采样器（第二十批完成，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6696 夹具主题令牌集 | 完成 | `siyuan-mobile-base.css` 新增 daylight/midnight 双主题核心令牌（背景/表面/文字/边框/悬停，逐字对照 `app/appearance/themes/{daylight,midnight}/theme.css`）+ 真机同源 `.b3-dialog__container{background:var(--b3-theme-surface)}` 规则 + smoke DOM 补齐 container/body 包裹层 | 无 |
| T-6697 对比度采样器 | 完成 | Chromium smoke 新增采样器：沿祖先解析有效不透明背景 → WCAG 相对亮度比值；采样 5 面（模块标题/统计值/条目标签/文档标题/设置标题）；断言普通文本 ≥4.5、大字号 ≥3（AA）。实测暗色令牌下：16.67/16.67/11.93/9.99/9.99 全过。负向验证：注入 #6b6b6b 低对比标题 → settingsTitle 2.62 精确 FAIL，还原 SHA-256 一致 | 亮色主题采样随 smoke 主题参数化演进；新组件入图时补采样点 |
| 第二十批收口 | 完成 | 全量 6312/6312、tsc、三套审计、`verify:release` 全绿（计数 6312/212 文件已在批内对齐） | 见上批剩余动作：v0.24.0 发版待确认 |

## 55. T-6696/T-6697 完成 + 对比度双主题化（第二十批收口，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6696 夹具主题令牌集 | 完成 | `siyuan-mobile-base.css` 新增 daylight/midnight 双主题核心令牌（逐字对照 `app/appearance/themes/{daylight,midnight}/theme.css`）+ 真机同源 `.b3-dialog__container{background:var(--b3-theme-surface)}` + smoke DOM 补 container/body 层 | 无 |
| T-6697 对比度采样器（双主题） | 完成 | 采样器：沿祖先解析有效不透明背景 → WCAG 比值；断言普通文本 ≥4.5。暗色实测 11.93~16.67、亮色 14.72~15.91 全过。负向验证：注入白底白字标签 → 亮色 pass 精确 FAIL（exit 1），还原 SHA-256 一致。发现并修正：天气卡"白字渐变"为有意设计——渐变背景采样不适用，其可读性归设计评审；statValue 采样移出断言、保留诊断输出 | 亮色/暗色双 pass 已并入 chromium smoke（默认门禁） |
| 第二十一批收口 | 完成 | `verify:release` 全绿；测试计数维持 6312/6312（smoke 为脚本断言不计项数）；产物不变（本批无 src 变更） | 下一批：v0.24.0 发版（待用户确认） |

## 54b. T-6695b MONTHLY BYDAY 序数子集 + T-6694b 城市表第三波（第二十二批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6695b iCal MONTHLY BYDAY 序数子集 | 完成 | `parseIcalRrule` 解析 MONTHLY `BYDAY` 序数（"2TU"=第 2 个周二、"-1FR"=最后一个周五；无序数/越界视为未支持回退月内同日步进）；展开器逐月定位目标星期、月内按日期升序产出（COUNT/UNTIL/地平线统一约束）；实测：2TU → 2026-01-13/02-10/03-10，-1FR → 01-30/02-27/03-27；负向验证：破坏序数校验 → 测试精确 FAIL，还原 SHA-256 一致 | 无 |
| T-6694b 世界时钟城市表第三波 | 完成 | `CITY_TIME_ZONES` 91→110 条目（+中国 12 城：青岛/大连/厦门/福州/合肥/郑州/长沙/哈尔滨/沈阳/昆明/海口/南昌；+世界：Osaka/Nagoya/Busan/Hanoi/Barcelona/Munich/Geneva/Brussels）；表断言 ≥100 | 城市表按需滚动扩容 |
| 第二十二批收口（2026-09-20） | 完成 | 全量 6313/6313、tsc、三套审计、`verify:release` 全绿；计数 6312→6313 回写；产物快照 `dist/index.js` 833414 / zip 391070 / 压缩条目 221669（余量 18554 / 133218 / 40475） | raw 线余量 18554——后续增量按 ADR 0062 程序逐批复核；v0.24.0 发版待用户确认 push/tag |

## 55b. T-6697b 对比度采样扩充 + D2 决策落定（第二十三批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6697b 对比度采样点扩充（双主题） | 完成 | 采样 5→9 面：+日历节假日、过期徽章、文档路径、时钟周期；阈值分两组——普通文本 ≥4.5（标题/标签/路径/周期）、次要徽章与大字号 ≥3（节假日日历/过期徽章/statValue）。暗色实测 3.57~16.67、亮色 4.62~15.91 全过；负向验证：透明徽章色 → 门禁精确 FAIL（exit 1），还原 SHA-256 一致 | statValue（天气渐变卡白字）保持诊断输出不进断言——渐变面可读性归设计评审 |
| D2 商店卡片 version/homepage 呈现决策 | 完成（决策：保持隐藏） | 结论落账：商店卡片继续不显示 `version`/`homepage`——协议 v2.4 已归一化透传但"宁可少显示，不挂消费不到的字段"（widget-protocol.md）；第三方来源的信任信息以 `source` 名称/图标与待安装区聚合呈现，避免卡片信息密度回退 | 后续若集市反馈需要来源核验入口，再评估 homepage 图标化外链（需新增 i18n 与无障碍标注） |
| 第二十三批收口 | 完成 | 全量 6312/6312（smoke 为脚本断言不计项数）、tsc、三套审计、`verify:release` 全绿；产物无变化 | raw 线余量 20066 复核口径不变；v0.24.0 发版待用户确认 |

## 56. T-6695c iCal BYMONTHDAY 子集（第二十四批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6695c iCal MONTHLY BYMONTHDAY 子集 | 完成 | `parseIcalRrule` 解析 `BYMONTHDAY`（正数=当月第 n 日、负数=-1 月末/-2 倒数第二天；0 与 |n|>31 剔除）；展开器分支：月内升序产出、该月不存在目标日则跳过（不钳制——BYMONTHDAY 本身即指定目标日，与锚点日语义不同）；测试三场景（固定 15 日/月末 -1/组合 15,-1）；负向验证：绕过分支 → 组合场景精确 FAIL，还原 SHA-256 一致 | iCal RRULE 子集至此覆盖：DAILY/WEEKLY±BYDAY/MONTHLY±BYMONTHDAY/YEARLY + INTERVAL/COUNT/UNTIL + EXDATE/RDATE + TZID |
| 第二十四批收口（2026-09-20） | 完成 | 计数 6313→6314 回写；产物快照按实测回写（`dist/index.js`/zip/压缩条目与三类余量） | 发版决策点维持：v0.24.0 push/tag 待用户确认 |

## 57. T-6692b Agent 受控动作灰度开关（第二十五批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6692b Agent 受控动作灰度开关 | 完成 | ①`ISwSettings`/`DEFAULT_SETTINGS`/`normalizeSettings` 新增 `agentActionsEnabled`（默认 true）；②设置页行为区新增开关行（双语 i18n）；③注册期约束：4 个写入/批量动作能力（open-documents/update-task-status/create-document/append-to-journal）与 workspace execute 受 `agentActionsOn` 总开关约束，关闭时不注册；execute 处理器另有运行时 kill switch（关闭时返回 "agent actions disabled"）；④只读能力（propose/outline/context 等）不受影响；⑤门禁：T-6680 组门新增 kill-switch 断言，负向验证（移除运行时检查 → 精确 FAIL → 还原一致）通过 | 设置默认开；关闭即"只读模式"——真机取证（T-6305）与日常使用互不影响 |
| 第二十五批收口 | 完成 | 全量 6314/6314、tsc、三套审计、`verify:release` 全绿（smoke 断言为脚本级不计项数） | 剩余：v0.24.0 发版（push/tag 待确认）、T-6305/T-107 真机、生态外发 |

## 60. v0.24.0 发版准备（第三十批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| v0.24.0 本地发版准备 | 完成 | `plugin.json`/`package.json` → 0.24.0；中英文 README：徽标 0.24.0、头部摘要改为执行链版本说明、Changelog 新增 v0.24.0 双语条目（执行链/灰度开关/存储治理/组件二轮/兼容与工程六块）；release-readiness 版本元数据行同步；"尚未正式发布"表述满足 release-candidate 门禁 | 剩余动作（均需用户确认）：确认更新日志内容 → push → tag v0.24.0（Release workflow 自动发布）→ 集市同步 |
| 第三十批收口 | 完成 | 全量 6314/6314、tsc、三套审计、`verify:release` 全绿；发布检查通过（readiness 与产物一致） | 发版三步（push/tag/集市）全部等用户指令 |

## 58. T-6698 快照自动生成 + T-6699 城市表第四波（第二十六批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6698 readiness 快照自动生成脚本 | 完成 | `scripts/readiness-snapshot.cjs`（npm script `readiness:snapshot`）：自动解析 dist/index.js、package.zip 与 index.js 压缩条目三组数字及三类余量，原位替换 readiness 文档既有占位（幂等——二次运行 changed: false）；不参与 verify 门禁链（门禁保持只读）；首跑即修正一处历史不一致（表行余量 20066 与字节数 834447 不匹配 → 17521） | 手工回写摩擦正式终结 |
| T-6699 城市表第四波 | 完成 | `CITY_TIME_ZONES` 110→**135 条目**（+东莞/佛山/无锡/宁波/乌鲁木齐/拉萨/西宁/兰州/南宁 +Osaka/Nagoya/Fukuoka/Sapporo/Budapest/Belgrade/Munich/Hamburg/Milan/Florence/Seattle/Houston/Atlanta/Phoenix/Montreal/Lima/Santiago/Bogota/Tel Aviv/Doha）；表断言 ≥100→≥120 | 城市表继续按需滚动扩容 |
| 第二十六批收口 | 完成 | 全量 6314/6314、tsc、三套审计、`verify:release` 全绿；产物快照由脚本回写（自动） | v0.24.0 发版流程待用户确认 push/tag |

## 59. B4-2 三态覆盖矩阵审计（第二十七批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| B4-2 58 组件 adapter 空态/失败隔离覆盖审计 | 完成（结论：无缺口） | 程序化矩阵：49 个直注册 adapter 全部有测试引用（2~12 次，无一零覆盖）+ 7 个打卡桥接组件另有专属深度测试；隔离语义的真正落点在控制器层——`createHomeModuleController` 的 per-module 错误隔离与失败摘要已有门禁（mobile-card-smoke：failure-only summary/refresh-all bounded concurrency/cancellation lifecycle 全 PASS）；模型层空态由各 builder 的 empty 语义与深度测试覆盖。首轮切片文本启发式报出的 36 个"缺口"经逐一核对均为假阳性（空态在模型层而非 adapter 内联） | 覆盖矩阵维持现状；新增组件时按既有深度测试模板补空态/失败断言即可 |
| 第二十七批收口 | 完成 | 全量 6314/6314、审计 50/50 ×3、`verify:release` 全绿（零生产代码变更） | v0.24.0 发版（push/tag）待用户确认 |

## 61. T-6695c/T-6694b 完成 + 多词城市名修复（第二十九批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6695c iCal MONTHLY BYMONTHDAY 子集 | 完成 | 正数=第 n 日、负数=-1 月末（组合 15,-1 支持、不存在的日期跳过不钳制）；三场景测试全过；负向验证（绕过分支）精确 FAIL，还原 SHA-256 一致 | iCal 子集覆盖：DAILY/WEEKLY±BYDAY/MONTHLY±BYMONTHDAY/YEARLY + INTERVAL/COUNT/UNTIL + EXDATE/RDATE + TZID |
| T-6694b 城市表查重 + 第五波 + 多词解析修复 | 完成 | 查重发现 8 个重复键（ Seattle/青岛/大连/厦门/Osaka/Nagoya/Hanoi/Munich）并清理，同时修正 Hanoi 错区（Bangkok→Ho_Chi_Minh）；新增第五波约 31 城（含 Cairo/Cape Town/Johannesburg/Kathmandu/Dhaka/Havana/Caracas/Quito 等），表规模 135→161；**修复多词城市名死条目 bug**——cities 按空白切分导致 New York/San Francisco/Cape Town/Tel Aviv 等多词条目永远无法命中，新增贪心两词重连查表；判别力测试（San Francisco 纯英文用例）+ 负向验证（废掉重连分支 → 精确 FAIL → 还原一致） | 无 |
| 第二十九批收口 | 完成 | 全量 6314/6314、tsc、三套审计、`verify:release` 全绿 | v0.24.0 发版（push/tag）待用户确认；城市表按需滚动扩容 |

## 62. T-6699b 城市表 IANA 门禁 + T-6697c 对比度采样扩充（第三十批完成，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6699b 城市表 IANA 合法性门禁 | 完成 | 全量 161 条目经 Intl 逐一验证：无效 zone 为零；新增门禁断言（拼错的 zone 会在运行期被静默丢弃，门禁层提前拦截） | 城市表继续按需扩容 |
| T-6697c 对比度采样 9→11 面（双主题） | 完成 | +媒体次要文本（暗 8.59/亮 6.05）+工作区分组标签（暗 8.59/亮 6.05）；顺带修复采样器 walker 缺陷——documentElement 自身可能承载不透明背景（夹具 :root 令牌），原实现停在其前导致暗色全回落白色（windowLabel 误报 1.94，修复后 8.59）；负向验证：注入透明徽章色 → 精确 FAIL（exit 1），还原一致 | 采样面 11 个；渐变面（天气卡）归设计评审 |
| 第三十批收口 | 完成 | `verify:release` 全绿；计数维持 6314/6314（smoke 为脚本断言不计项数）；push 同步 302db28+后续提交 | v0.24.0 发版（tag/Release）待用户确认 |

## 63. v0.24.0 正式发布（第三十一批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| v0.24.0 发布 | 完成 | tag `v0.24.0`（附发布说明：执行链/灰度开关/存储治理/组件二轮/3.8.0 下限/对比度门禁）经 SSH 通道推送；Release workflow 自动构建并发布：[release 页面](https://github.com/ai68298100/siyuan-speed-switch/releases/tag/v0.24.0)，资产 package.zip 393300 bytes；Release workflow run success | 集市同步（T-6306 类，需用户集市账号）；发版后观察 issue 反馈；双设备用户提示多端同升（ADR 0064 语义：低于 3.8.0 静默禁用） |
| 第三十一批收口 | 完成 | 本批零源码变更；远程 Release 与资产核验通过；ROADMAP/readiness 基线随发布更新 | 下版本周期从剩余单独立项与世界时钟城市表滚动扩容起步 |

## 64. T-6692b 灰度开关实施 + B2 门禁补强（第三十二批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6692b Agent 受控动作灰度开关 | 完成 | ①`ISwSettings`/`DEFAULT_SETTINGS`/`normalizeSettings` 新增 `agentActionsEnabled`（默认 true）；②设置页行为区新增开关行（双语 i18n）；③注册期约束：4 个写入/批量动作能力与 workspace execute 受 `agentActionsOn` 总开关约束，关闭时不注册；execute 处理器另有运行时 kill switch（关闭时返回 "agent actions disabled"）；④只读能力不受影响；⑤门禁：T-6680 组门新增 kill-switch 断言，负向验证（移除运行时检查 → 精确 FAIL → 还原一致）通过 | 设置默认开；关闭即"只读模式" |
| B2 门禁补强（reduced-motion 根容器） | 完成 | 既有全局块只覆盖 `.speed-switch/.sw__fab/.sw__mobile-sheet-overlay` 系根——补齐 `.sw-settings-dialog`（设置弹窗）与 `.sw-mobile-switcher-dialog`（移动切换器）两个插件根容器；accessibility-gate 同步断言两个根必须登记 | 后续新插件根容器入图时同步登记 |
| 第三十二批收口 | 完成 | 全量 6315/6315、tsc、三套审计、`verify:release` 全绿 | 下一批：世界时钟城市表滚动扩容（135→150+）或 iCal 序数 BYDAY 深化；均按 ADR 0062 程序复核包体 |

## 65. T-6699b 城市表第六波扩容（第三十三批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6699b 城市表第六波扩容 | 完成 | `CITY_TIME_ZONES` 161→**201 条目**（+仰光/巴库/埃里温/第比利斯/索非亚/萨格勒布/布加勒斯特/塔林/里加/维尔纽斯/雷克雅未克/卡萨布兰卡/拉各斯/亚的斯亚贝巴/墨西哥城/巴拿马城/苏瓦/檀香山/安克雷奇/惠灵顿，中英双语键）；全量 Intl 校验：无效 zone 为零；表下限断言 ≥190 | 城市表按需滚动扩容 |
| 第三十三批收口 | 完成 | 全量 6315/6315、tsc、三套审计、`verify:release` 全绿；产物快照由 readiness-snapshot 脚本自动回写 | 下版本周期候选：iCal BYSETPOS 等 exotic 语义（低优先）、更多对比度采样面、生态邀请（需用户确认） |

## 65b. T-6700 本地页签过滤查询词法（第三十一批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6700 本地页签过滤查询词法（AND/排除/短语） | 完成 | `search-model.js` 新增 `parseSearchTerms`（纯函数）：空格多词=AND、`-前缀`=排除、`"短语"`=整体匹配，16 词法项钳制；`filterOpenTabs` 改用词法判定（包含词全命中 + 无排除词命中）；单关键词行为与旧版完全一致（向后兼容）；内核搜索层仍接收原始查询串（思源原生语法不受影响）；测试：词法解析/AND/排除/短语/空查询/钳制 6 断言；负向验证：摘除排除语义 → 测试精确 FAIL，还原 SHA-256 一致 | README 功能区随下版补一句查询语法说明；内核层查询语法（思源原生）文档化候选 |
| 第三十一批收口 | 完成 | 全量 6316/6316、tsc、三套审计、`verify:release` 全绿；产物快照由 readiness-snapshot 脚本回写 | v0.25.x 发版窗口：性能门禁波内容已齐（基准四类 + IS_CI 豁免 + 对比度双主题 + 查询词法），可考虑 v0.25.0 发版 |

## 66. T-6701 对比度采样第三波（第三十四批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6701 对比度采样第三波扩充 | 完成 | 采样面 11→14：+模块上下文（moduleContext，渐变面诊断）、+日历星期头（calendarHead，暗 8.59/亮 6.05）、+媒体标题（mediaTitle，暗 11.93/亮 15.91）；普通文本门禁组扩至 10 面 ≥4.5；渐变面（天气卡上下文/statValue）保持诊断输出不进断言 | 双主题 14 采样点持续锁定；新组件面入图时按同模式补采 |
| 第三十四批收口 | 完成 | 全量 6316/6316、`verify:release` 全绿（对比度断言为 smoke 脚本级） | 下一批：世界时钟城市表滚动扩容或 iCal BYSETPOS（exotic 低优先）；v0.25.0 发版窗口观察 |

## 65b. T-6702 真机取证清单更新（第三十四批完成，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6702 acceptance-runbook 更新到 v0.24.0 现状 | 完成 | ①头部基线计数 6138→6315/212；②桌面会话 B 重写为 v0.24.0 执行链取证 7 项（设置开关/propose/确认卡/超时/关闭停用/卸载清理/手机矩阵，ADR 0063 宿主路线）；③新增 5c 节：v0.24.0 组件二轮增量的真机验证点 5 项（RSS 只看未读/写作打卡 n/m/强度分/年历网格/世界时钟中文名）；④会话 A 标注 T-103 已交付、本会话仅剩取消与降级复核 + 窄侧栏度量 | 用户设备空闲时按清单逐项执行；每完成一组在 acceptance-log 追加摘要 |

## 66b. T-6703 排除词贯通聚合层（第三十五批完成，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6703 排除词贯通 merge 聚合层 | 完成 | `mergeSearchLayers` 内解析排除词（parseSearchTerms），opened/global 聚合卡片按标题+路径后置过滤（保留 documents 上限切片语义）；负向验证：摘除 cardExcluded 过滤 → merge 层排除测试精确 FAIL，还原一致 | 内核请求仍收原始查询串（思源原生语义）；文档化随下版 README |
| 第三十五批收口 | 完成 | 全量 6317/6317、tsc、三套审计、`verify:release` 全绿；产物快照由脚本自动回写 | v0.25.x 搜索线剩余：亮色主题采样第三波（已完成）之外无阻塞项 |

## 67. T-6700b 查询语法用户文档 + push 同步（第三十五批完成，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6700b README 查询语法用户说明 | 完成 | 中英文 README 分层搜索节新增「页签过滤查询语法」说明（多词 AND / `-排除` / `"短语"`，含示例） | 用户反馈驱动文案迭代 |
| 第三十五批 push 同步 | 完成 | 75fbad6 已推送远端（SSH 通道）；远端 CI 复验随下一批顺带 | 无 |

## 70. T-6701 世界时钟城市 datalist 自动补全（新周期第二十六批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6701 世界时钟城市字段挂离线城市表 datalist | 完成 | `home-config-form.ts`：cities 文本字段挂 `<datalist>`（161 城中英文名，浏览器原生自动补全，输入即提示）；模型层 161 条目全量 Intl 校验无效为零；门禁：负向验证（移除 datalist 接线 → 精确 FAIL → 还原一致）；计数 6314→6317 回写 | 城市表按需滚动扩容；raw 线余量按 readiness 快照持续监控 |
| 第三十二批收口 | 完成 | 全量 6317/6317、tsc、三套审计、`verify:release` 全绿；产物快照由脚本回写（index.js 838079 / zip 394412 / 压缩条目 223223） | 下一批候选：世界时钟城市表第七波扩容或 iCal 序数 BYDAY 深化（按 ADR 0062 程序复核后实施） |

## 70b. README 全面重构（第三十六批，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6704 README 双语全面重构 | 完成 | zh 539→约 300 行：①删除混合测试计数/组件串列/策略的长段落，改为版本要点框 + 紧凑亮点；②新增「组件面板：58 个开箱即用的组件」分类表（导航与历史/笔记与写作/数据与检索/时间与信息/订阅与外部/打卡桥接）；③章节重排：核心能力 → 组件面板 → Agent → 快速上手（Agent 从第二位后移，用户价值优先）；④架构/开发节压缩（贡献指南摘要化）；⑤完整更新日志抽出至 docs/CHANGELOG.md（README 保留 v0.25.0 开发头与 v0.24.0 摘要 + 完整历史链接）；⑥EN 同步：策略段替换 + 保留 Release Checklist/verify:release/Android/published 等门禁锚点 | EN 章节顺序与 zh 完全对齐（后续批次）；门禁所需字符串全部保留（release:audit 50/50 验证） |
| 第三十六批收口 | 完成 | 全量 6317/6317、tsc、三套审计、`verify:release` 全绿 | 发版建议：v0.24.0 后增量=查询词法+排除贯通+城市表 201 城+datalist+对比度采样 14 面——构成完整特性集，建议发 v0.25.0（用户确认后执行 push/tag） |

| 补记 | final-package-audit 的打包 README 标记从 /当前开发策略/（旧结构段落，已随重构移除）更新为 /开发头目标版本/（新结构中的发布状态行）；该门禁语义不变——打包 README 必须携带当前候选文档与发布状态。`verify:release` 复验全绿 | 提交随下一批推送 |

## 68. v0.25.0 发布（第三十三批完成，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| v0.25.0 发布 | 完成 | tag `v0.25.0`（版本 bump 0.24.0→0.25.0，双语 Changelog v0.25.0 条目：查询词法/排除贯通/城市表 217 城/datalist/可访问性根容器）经 SSH 通道推送；Release workflow 自动构建并发布 | 发版后观察 issue 与集市同步 |

## 71. T-6705 未来三个大版本计划重排（ROADMAP §8.0.6，2026-09-20）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6705 三版本计划重排 | 完成 | ROADMAP 新增 §8.0.6（现行）：**v0.26.x 发布工程与文档收口**（F7 `publish.resources` 收口 / EN README 对齐 / B4 三态审计收尾 / 性能稳定计数 +1）→ **v0.27.x 性能硬门禁 + 数据连续性**（四组基准升硬门禁，准入=至 v0.26 连续两版无产品回归；14 个持久化 key 迁移矩阵首次全面演练；文档集恢复报告导出；T-6483/T-6485 以 T-6305 真机取证为准入）→ **v0.28.x 生态验证**（第三方端到端 / provider 升级重载取证 / D3 邀请承接；无真实回应则收缩为协议文档完善，不虚构证据）；跨版本用户参与项表（T-6305/集市/邀请/Issue）单列；§8.0.5 波次表标注已被接替、专项主线 A~D 继续有效；头部基线刷新 v0.25.0/6317/212 | 用户审阅 §8.0.6，如需调整直接改表；下一执行批从 v0.26.x 起步（F7 收口优先）；依据：专项 A 收口、B/C 基本收口后自主功能积压见底，生态内容依赖用户时间，故"先清工程债与证据，再承接生态" |
| 本批收口 | 完成 | 纯文档批次：ROADMAP.md + 本账本；root-doc-budget 门禁仅约束体积上限（ROADMAP 现 ~40 KiB << 150 KiB），无字符串断言依赖；全量测试、tsc 复验通过 | 无 |

## 72. T-6706 大库聚合性能门禁去颤 + 复杂度倍增门补位（2026-09-20）

背景：T-6705 文档批复验时 `pnpm test` 连续 5 次 6317/6317 中挂 1——`search-orchestration-perf` 大库聚合基准在满载全量套件内假红（隔离运行 5/5 绿）。另发现本工作副本 `dist/` 为旧构建（802089 bytes），readiness 快照 838588 对不上，`pnpm build` 后即对齐（对应既有口径"dist 同步后必须先 build 再 test"）。

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 环境实录（假红根因） | 完成 | 墙钟断言 12ms 线：隔离 avg 8.3ms/p95 10.4ms 绿；全量套件内 avg 8.3→16.9ms、p95 15~17ms 红（三次复现）。改 cpuUsage 后隔离 8.2ms、套件内仍 12.9~16.9ms、八核人为饱和（8 个忙等进程）22.4ms——cpuUsage 只免调度等待不免降频/缓存竞争，62% 空闲时套件内仍 13.9ms；空闲度跳过方案（阈值 0.4）被该实测否决 | 无 |
| 告警线按"只拦病理性回退"重校准 | 完成 | `tests/search-orchestration-perf.test.cjs`：度量改 cpuUsage（µs→ms 单位修正）；绝对线 12→40ms cpu（≈安静成本 4~5 倍、最坏环境污染 1.8 倍；真实 O(n²) 在本规模数百 ms 级）；p95 与机器空闲度（250ms 睡眠窗全核采样）降为趋势诊断；断言本地无条件执行、CI 仍只记诊断；注释按实录重写 | 升硬门禁按 A1/§8.0.6 v0.27.x 准入（连续两版稳定） |
| 复杂度倍增门补位 aggregateSearchResults | 完成 | `tests/perf-complexity-gate.test.cjs` 新增倍增测试（600→1200 hits，min-of-5×边际重测，ceiling 3）：基线 ratio 2.15~2.38 绿。比值型门禁天然免疫机器速度，补上聚合层渐变回退的结构性 tripwire（原倍增门仅覆盖 sortItems/sanitizeFavorites/buildSearchCacheKey/planDocResultsPage） | 无 |
| 负向验证（按 gate-audit-checklist 协议） | 完成 | ①弱注入（float 加法 O(n²) 循环）：被 V8 代数消除（整调用 5ms），两门禁均不红——实录"注入必须实测，不能想当然"；②`.length` 拼接注入：cons string 长度被代数求出，仍不可见；③`indexOf` 强制物化注入：微测 ratio 3.99 → 倍增门精确 FAIL（3.23x>3，stable=false）+ 绝对线精确 FAIL（cpu avg 66.0>40ms），同条件双门禁按失败项名称触发；④还原后 SHA-256 与基线一致（1df7d670…） | 无 |
| 测试计数同步 | 完成 | 全量 6317→6318（+1 倍增测试）；README zh/en、ROADMAP 头部、release-readiness、release-batch-audit 断言 `/6318\/6318/` 五处同步；`release:audit` 50/50、`release:check` 通过；全量 6318/6318 绿（套件内 cpu avg 18.1ms<40ms 正确通过） | 无 |


## 73. T-6707 F7 收口：publish.resources 逐文件声明 + 包资源契约门禁（v0.26.x 首批，2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 上游规则原文核实（账本纠错） | 完成 | 拉取上游 `docs/PLUGIN-PUBLISH.md` "Resource declaration" 原文：`resources: ["images/logo.png", "views/index.html"]`——**官方示例本身含子目录文件路径**；"without directories" 指**不得以目录/通配符作为条目**，非禁止子目录路径。§1 F7 行"路径不得含目录"系误读，本行更正；"4 个子目录文件在发布模式不可达"的悲观结论随之撤销——逐文件声明即可达 | 无 |
| 运行时引用盘点 | 完成 | 4 个 docs 文件均为包内可服务资产、无运行时加载依赖：architecture.svg/interface-map.svg 仅被双语 README 引用（集市页渲染）；component-store-guide.md 店内指南弹窗走 GitHub 外链（index.ts:1870），包内副本无加载方（`buildExternalWidgetGuideLink` 为无调用方导出，仅注释性路径文本）；agent-document-context-m2.md 零运行时引用 | 无 |
| plugin.json 声明 | 完成 | 新增 `publish.resources` 四条：docs/agent-document-context-m2.md、docs/architecture.svg、docs/component-store-guide.md、docs/interface-map.svg（zip 内 plugin.json 690→772 bytes）；i18n/*.json 属标准条目免声明（上游明文）；重建后包内清单即声明态 | 集市发布时观察 bazaar 对 README 相对路径图片的实际渲染（T-6306 类） |
| 包资源契约门禁（新增测试 6318→6319） | 完成 | `tests/host/package-resource-audit.test.cjs` 新增：读包内 plugin.json + zip 条目全集，非标准（index.js/index.css/i18n/*.json）非宿主消费（plugin.json/icon/preview/README×2）的条目必须全部出现在 publish.resources；声明形态契约（精确相对文件名，禁目录条目/穿越/通配/重复）；与体积基线交叉自检防恒真（zip 读取失效时空声明仍会因基线条目未声明而精确失败） | 无 |
| 负向验证 | 完成 | 注入 A（删声明+重建）：门禁精确 FAIL 并点名 4 个文件；注入 B（声明目录条目 "docs/"）：形态断言精确 FAIL "must not be a directory or traversal"；还原后 SHA-256 与基线一致（e3efa329…）+ 重建全绿。实录：门禁读包内清单，注入必须连同重建才是"同条件" | 无 |
| 体积基线对齐 + 快照刷新 | 完成 | 基线 json 停留在旧版构建（仅诊断不拦截故未暴露）：index.js 196473→223388、README.md 18167→12927（T-6704 重构）等 7 条对齐；readiness:snapshot 刷新 zip 383112；`release:check`、`release:audit` 50/50、release-quality 16/16、全量 6319/6319 绿 | 无 |

## 74. T-6708 EN README 章节顺序与 zh 完全对齐（T-6704 遗留，2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| EN README 结构对齐 | 完成 | 15 个 `##` 章节与 zh 一一对应（目录/核心能力 6 小节/组件面板/Agent/快速上手/快捷键/设置/安装/环境要求/架构与测试/开发/路线与决策/发布前检查/更新日志/许可证）；EN 从 519 行减到约 390 行：删除旧结构残留（两个导语 blockquote、三节详细 how-to 改为 zh 同款四条摘要）、完整历史 changelog 移出；门禁锚点全保留（compatibility-matrix 的 Release Checklist/verify:release/Android/published 四断言、release-quality 的 "discovers all 212 `*.test.cjs` files" 短语，25/25 绿） | 用户反馈驱动文案迭代 |
| 内容更新（随结构） | 完成 | 徽标版本 0.24.0→0.25.0；组件目录表新增（58 组件英文名全部取自 i18n 权威键值，node 脚本逐一核对，含 Miniflux/ActivityWatch/Uptime Kuma 三个品牌组件的 store 标题）；Agent 11 能力表新增（zh 表全文翻译）；设置表补「分组与排序」「行为（智能体总开关）」两行；持久化行改 15 key（12+2+1）口径；数据边界指向 PERSISTENT_KEYS 与迁移矩阵文档；测试矩阵表对齐 zh 11 行+兜底；Roadmap 与 ADR 两节合并为「Development Roadmap And Decision Records」（zh 四条近期决策口径） | 无 |
| EN 历史条目迁移 | 完成 | v0.23.5~更早的完整英文历史移入新建 `docs/CHANGELOG.en-US.md`（14 个版本条目逐字保留，文件头双语互链）；README 更新日志节只留 v0.25.0/v0.24.0 摘要 + 三向链接（CHANGELOG.md/CHANGELOG.en-US.md/GitHub Releases） | 新版本发布时双语 changelog 同步追加 |
| zh README 顺带修复 | 完成 | 删除「完整历史见…」重复行（304/306 两行同文）；目录补「发布前检查」（章节存在但目录漏列）——EN 目录 14 项与 zh 对齐 | 无 |
| 门禁复验 | 完成 | 全量 6319/6319、release:check、release:audit 50/50、quality:audit 50/50、compatibility/release-quality/final-package 25/25；包体 383112→376583（EN README 18668→12135 压缩字节）；基线 2 条对齐 + readiness 快照刷新 | 无 |

## 75. T-6709 B4 收尾：商店搜索/筛选/折叠三态审计 + 缺口补齐（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 商店三态覆盖审计 | 完成 | 既有覆盖盘点：搜索/筛选语义门禁 20+ 断言（store-filter-state-contract：searchbox 语义、清除动作、sort aria-controls、摘要原子性、规范化 query）、渲染状态门禁（store-render-state-contract：filterEmptyState `data-state=empty-filter`）、空态 CSS 契约（透明度/换行/宽度三向）、模型层视图状态归一化与折叠序列化测试齐备 | 无 |
| 缺口补齐（折叠 aria 契约） | 完成 | 审计发现分组折叠的 `aria-expanded`/aria-label 切换/折叠网格 `aria-hidden` 三处源码接线无任何测试断言（home-view 的同名断言属另一组件，不覆盖商店）——store-filter-state-contract 新增 3 条 includes 字面量断言（比正则更精确，且规避 shell 反斜杠吞噬陷阱） | 无 |
| 缺口补齐（零匹配空态行为验证） | 完成 | home-store-model.test 新增 2 条行为级断言：`summarizeHomeStoreCards` 无匹配 query / added-only 排除筛选 → `visible=0`（正是 UI `dataset.state=empty` 与筛选空态的数据源），把"空态可见性"从源码形态门禁升级为数据通路验证 | 无 |
| 负向验证 | 完成 | 摘除 `groupToggle.setAttribute("aria-expanded"…)` 行 → 新断言精确 FAIL（49 pass/1 fail，失败项点名）；还原后 `git diff src/` 清空（字节级一致，中途发现插行顺序错误已修正重排） | 无 |
| B4 收口结论 | 完成 | 商店搜索/筛选/折叠三态 + 58 组件 adapter 三态（B4-2 无缺口）全部过门；全量 6319→6324 绿。B4（三态一致性审计）整线收口 | 新组件/新商店能力沿用同模板补空态/失败/折叠断言 |

## 77. T-6710 v0.26.0 正式发布 + 性能稳定计数 +1（v0.26.x 波次收口，2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| v0.26.0 发布 | 完成 | tag `v0.26.0` 指向 `841b22a`（版本 bump 0.25.0→0.26.0，双语 changelog v0.26.0 条目：F7 发布合规/文档对齐/质量门禁/体积）经代理通道推送；Release workflow 35523865814 与 CI 35523859026 双绿（1m14s/1m11s）；[Release 页面](https://github.com/ai68298100/siyuan-speed-switch/releases/tag/v0.26.0) 资产 package.zip 377395 bytes（workflow 构建，与本地 377410 差 15 bytes 为提交时间戳嵌入，属已知预期行为）；readiness 发布动作行已补 v0.26.0 | 集市同步（用户集市账号）；发版后观察 issue 反馈 |
| 发版前门禁 | 完成 | `verify:release` 全链 exit=0：tsc、双构建复现审计、全量 6324/6324、release/quality/integration 三套审计 50/50 ×3、三套 UI smoke；发版批顺带修正 release-quality 门禁两处陈旧锚点（readiness 沿袭标记 v0.23.5→v0.26.x 窗口、发布顺序锚点 v0.23.5→v0.25.0——两标记自 v0.24/v0.25 两版未随发布更新，属门禁维护债，本次随窗口刷新） | 后续版本发布时同步刷新该标记 |
| ④ 性能基准稳定计数 +1 | 完成 | §8.0.6 v0.26.x 交付④：基准矩阵四类（空查询 p95/页签过滤/大库聚合/复杂度倍增）自 v0.24.0 起连续三版（v0.24.0/v0.25.0/v0.26.0）告警线内无产品回归；本窗口实录：大库聚合隔离 avg 8.3~8.98ms（40ms 病理线）、倍增 ratio 2.15~2.38（ceiling 3）、页签过滤 0.03ms（2.5ms 预算）、merge 0.0093ms（2ms 预算）——**v0.27 升硬门禁的"连续两版稳定"准入已达成**（T-6706 重校准后的口径） | v0.27.x 首批执行升硬门禁（绝对线保持病理口径 + 倍增比值门全量接入发布主链） |
| v0.26.x 波次收口 | 完成 | §8.0.6 v0.26.x 四项交付全部 ✅（①F7 T-6707 ②EN README T-6708 ③B4 T-6709 ④稳定计数本文）；跨版本用户参与项（集市/邀请/T-6305）仍开放 | 下一开发批进入 v0.27.x：性能硬门禁升格 + 存储迁移矩阵全面演练（准入已达成项先行）；填充任务照旧 |

## 78. T-6711/T-6712/T-6713 v0.27.x 首批：性能硬门禁升格 + 存储矩阵演练 + 恢复报告核实（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6711 性能硬门禁升格 | 完成 | ①search-filter-perf 三测试整体转换为 T-6706 形态：CPU 时间度量 + best-of-3 + 均值硬断言（15/48/20ms 预算不变，本地与 CI 无条件），p95 降为诊断——墙钟 p95 + IS_CI 豁免的旧形态正是 T-6706 实录的噪声脆弱形态；②大库聚合 40ms CPU 病理线去除 IS_CI 豁免升为无条件断言（CI 慢核最坏估算 ~30ms 仍在 4~5 倍余量的线内）；③复杂度倍增门核实为天然无条件（比值型）。负向验证：filterOpenTabs 注入 20ms 停滞 → keyword 门精确 FAIL（19.45ms>15ms）；聚合注入 O(n²) 后以 CI=true 运行 → 精确 FAIL（63.8ms>40ms，证明豁免确实移除）；源码 SHA-256 还原一致 | 升格后 CI 首跑即真实 CI 硬件的首个强制执行样本，push 后观察 |
| T-6712 存储迁移矩阵全面演练 | 完成 | 审计结论：既有覆盖已含逐键 HANDLERS 注册（15 key 全注册）、健康/脏数据/上限/垃圾输入/拇指缓存深 Drill/不动点/只读接线；补齐两个全矩阵缺口——①逐 key 损坏电池：12 个 handled key × 5 类损坏输入（null/垃圾串/数字/异形对象/含 undefined 数组）全部确定性降级（kept/cleaned/reset/migrated）且产出过消毒不动点（回灌二轮必须 kept）；②inspected/meta 键损坏分类安全且永不产出 data；③v0.23.5 老载荷升级模拟（13 key 无版本戳）→ stamp/rss 记 missing（onload 落戳）、其余 13 键 kept/inspect 原样存活。负向验证：sw_pinned 处理器注入越约状态 "kaputt" → 电池精确 FAIL 点名 sw_pinned；源码还原 | 新增 key 时电池自动纳入（循环 KEY_ORDER），无维护成本 |
| T-6713 文档集恢复报告导出核实 | 完成（已交付销项） | §8.0.6 该项沿袭自 8.0.2 表，实际早已交付：`buildDocumentSetRestoreReport`（v1 版本化、逐项五态 opened/restored/failed/missing/pending、可序列化、时间戳注入）+ 设置页导出按钮下载 `siyuan-speed-switch-restore-report-<set>.json` + document-sets.test 6 断言覆盖 | 无 |
| 本批收口 | 完成 | 全量 6319→6327 绿（+3 演练测试、+1 空查询 best-of-3 结构性重写不增计数、过滤文件重写净 -1）；测试计数 6327 五处同步；release:audit 50/50；零生产代码变更——v0.27.x 暂不发版（攒批口径），硬门禁经由 main CI 即时生效 | 下一批：v0.27 剩余=T-6483/T-6485（仍等 T-6305 真机取证）+攒批至 v0.27.0 发版窗口；文档集恢复报告导出无后续 |

## 79. T-6714 城市表第八波扩容 + T-6715 v0.27.0 正式发布（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6714 城市表第八波扩容（217→338） | 完成 | 审计发现**双语不对称**是真缺口：约 46 城只有英文名（开罗/维也纳/西雅图/布拉格/内罗毕等，中文搜索落空）、约 27 个主力中国城市只有中文名（北京/上海/深圳等，英文搜索落空）；补齐后新增堪培拉/达尔文/德黑兰/巴格达/安卡拉/特拉维夫/耶路撒冷/多哈/马斯喀特/伊斯兰堡/卡拉奇/基辅(Europe/Kyiv)/明斯克/日内瓦/布鲁塞尔/巴塞罗那/曼彻斯特/爱丁堡/渥太华/卡尔加里/圣迭戈/达拉斯/莫尔兹比港 24 城双语条目；程序化插入自动跳过已存在键（121 条新增零重复）；全表 338 条 Intl IANA 校验零无效；下限断言 ≥217→≥338 | 城市表按需续扩（补齐模板已程序化，下波成本极低） |
| 包体影响（ADR 0062 程序） | 完成 | index.js 838588→842414（+3826 bytes，约 32 bytes/条），raw 线余量 13380→9554；压缩条目 223388→224589（256 KiB 预算余量 37555）；zip 378611（512 KiB 上限余量 145677）——线内收窄、已记录 | 后续功能接入按 ADR 0062 程序复核 |
| T-6715 v0.27.0 正式发布 | 完成 | tag `v0.27.0` 指向 `f2b6d8a`（版本 bump 0.26.0→0.27.0，双语 changelog：城市表 338 城/硬门禁升格/存储演练）；Release 35525805786 与 CI 35525802918 双绿（1m20s/1m16s）；[Release 页面](https://github.com/ai68298100/siyuan-speed-switch/releases/tag/v0.27.0) 资产 package.zip 379551 bytes；发版前 `verify:release` 全链 exit=0；release-quality 门禁沿袭标记随窗口同步刷新（v0.26.x→v0.27.x，上批教训固化执行） | 集市同步（用户集市账号）；发版后观察 issue |
| v0.27.x 波次状态 | 更新 | ①②③④ 中 ①②③ 已交付随 v0.27.0 成版，④ T-6483/T-6485 继续等 T-6305 真机取证（后置不顺延）；剩余自主填充=城市表续扩/iCal BYSETPOS（低优先） | 生态验证波 v0.28.x 准入依赖用户动作（邀请外发/集市），未就绪前维持填充节奏 |

## 80. T-6716 iCal BYSETPOS 候选集选位 + MONTHLY 裸星期 RFC 语义修正（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6716 BYSETPOS + MONTHLY 裸星期 | 完成 | ①解析器：`bySetPos` 子集（MONTHLY/WEEKLY，|n|≤300、条目上限 32、0/越界忽略）；MONTHLY BYDAY 无序数前缀（"TU"）从"整条忽略"改为 ordinal 0=当月全部匹配星期（RFC 5545 正确语义，旧行为系保守缺陷）；守卫=BYSETPOS 无并列日选择器（WEEKLY 无 BYDAY / MONTHLY 两者皆无）或搭配 DAILY/YEARLY → 整条降级单次呈现。②展开器：MONTHLY 三来源（BYMONTHDAY/BYDAY 序数/BYDAY 裸星期）合并去重为月内候选集升序，BYSETPOS 在位时重排选位（正=第 n、负=倒数）；WEEKLY 新增锚点相对周帧的候选选位分支（与既有 interval 口径一致，首周为部分周，注释明示）；旧 byMonthlyByday 专支删除（死代码，nthWeekday 移入统一分支） | 无 |
| 测试 | 完成 | 新增 5 条：月末工作日（BYDAY=MO..FR;BYSETPOS=-1 → 1/30、2/27、3/31）、BYMONTHDAY 候选选位（{1,15};-1 → 每月 15 日）、裸星期+序数混合去重（TU,-1FR;1,-1 → 1/6、1/30、2/3）、WEEKLY 每周第 2 个事件日、孤立 BYSETPOS 降级单次；bare-TU 旧断言随语义修正更新（[13,13,13]→[6,13,20]，注明 RFC 依据）；全量 6327→6332 | 无 |
| 负向验证 | 完成 | 禁用 MONTHLY 选位（`if (false && …)` 注入）→ 三条 MONTHLY BYSETPOS 测试精确点名失败；WEEKLY 分支为独立选位逻辑（预期差异实验独立验证其有效）；还原后 34/34 绿。注入经 Edit 工具执行（Mimosa 拦 Bash 直写 src，已按规矩走 Edit） | 无 |
| 依赖/上游检查 | 完成 | siyuan SDK 最新=1.2.7 与本地一致（无漂移）；内核端点契约门 25 端点无上游变更信号 | 定期复查 |
| 本批收口 | 完成 | 全量 6332/6332、release:audit 50/50、release:check、tsc 全绿；包体 index.js 843150（raw 余量 8818）、压缩条目 224793（余量 37351）、zip 379770——ADR 0062 线内收窄已记录；计数 6332 五处同步 | 候选池状态：单独立项清单全部销项；剩余=城市表续扩（脚本化低成本）与用户参与项（集市/邀请/T-6305/T-6483+6485）；攒批至下一发版窗口 |

## 81. T-6717 代码健康巡检：死导出清理 + 结构观察（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 死导出全仓扫描 | 完成 | 程序化扫描（src 全部 export 名 × 全仓词边界引用计数，阈值≤2）：初版扫描器自身踩 heredoc 反斜杠坑报 1594 条全垃圾，改用 fromCharCode(92) 构造词边界后收敛为 11 条候选；逐一核对后 4 条为误报（PANEL_SIZE_MODES/HOME_SIZE_MODES/HOME_SIZE_DEFAULTS/TAB_GROUP_MODES 均在 index.ts 真实使用）、1 条（agent-readonly-audit 的 Compatible 垫片）属生产图决策不动 | 扫描器模式已固化：词边界必须 fromCharCode(92) 构造 |
| 死导出清理 | 完成 | 移除 4 个零引用死常量：home-model DEFAULT_LAYOUT、home-source-model SOURCE_KINDS、life-widget-model WEATHER_CONDITIONS、checkin-bridge CHECKIN_LIST_MAX_ITEMS；移除 util.js 孤儿包装函数 summarizeStorageCapacityReportEventQueue（无生产调用、无测试引用）；doc-search-ui docSearchResultId 等核实为在用保留 | 无 |
| 过程实录（回归自愈） | 完成 | util.js 删函数时漏改导出行→悬空标识符→agent 系测试文件 require 崩溃（66 失败、868 测试塌缩）——全量套件立刻暴露，修复导出行后 6332/6332 复绿。教训：删导出必须同步删 module.exports 引用，全量套件是最快的守门员 | 无 |
| 结构观察（登记不动） | 完成 | ①esbuild 不去重对象字面量重复值（"Asia/Shanghai" 在产物出现 75 次）——城市表反转（zone 分组+初始化展平）可回收约 3-5KB raw，作为 raw 线吃紧时的既定杠杆（当前余量 9196 暂不动）；②agent-readonly-audit 的 is...Compatible 垫片族零调用但属契约模块面，去留随生产图决策；③index.ts 8904 行（峰值 12681），R1 界面适配层仍未立项 | 余量吃紧或下一功能接入时启用① |
| 本批收口 | 完成 | 全量 6332/6332、release:audit 50/50、tsc、release:check 全绿；index.js 842772（raw 余量 9196）；零行为变更（纯死代码清理） | 攒批继续 |

## 82. T-6718 v0.27.1 正式发布（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| v0.27.1 发布 | 完成 | tag `v0.27.1` 指向 `0b25682`（版本 bump 0.27.0→0.27.1，双语 changelog：iCal BYSETPOS + 死导出清理）；Release 35528301415 与 CI 35528298891 双绿（1m29s/1m12s）；[Release 页面](https://github.com/ai68298100/siyuan-speed-switch/releases/tag/v0.27.1) 资产 package.zip 380196 bytes；发版前 `verify:release` 全链 exit=0；readiness 沿袭标记与门禁随窗口同步（沿袭行改为"v0.27.1 发布后 v0.27.x 开发头"） | 集市同步（用户集市账号）；发版后观察 issue 反馈 |
| 攒批清空 | 完成 | 攒批区（T-6716 BYSETPOS 用户可见修复 + T-6717 死代码清理）随本版全部成版；候选池与攒批区双双清空 | 后续开发由 issue 反馈/用户方向驱动；结构性杠杆（城市表反转回收 raw 余量、ADR 0062 抬线）已登记待命 |

## 83. T-6719 RRULE 语义完备二批：DAILY+BYDAY / YEARLY 年度固定日 / 无效组合降级（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6719 DAILY+BYDAY | 完成 | `FREQ=DAILY;BYDAY=MO..FR`（工作日类，极常见）此前 BYDAY 被完全忽略、周末照常展开——现按匹配星期过滤（INTERVAL 必须 1，否则解析期降级）；实测锚周三 1/14 → [14,15,16,19,20] 跳过周末 | 无 |
| T-6719 YEARLY+BYMONTH+BYMONTHDAY | 完成 | 成对支持年度固定日（"每年 12 月 25 日"类）：年内月×日组合升序、不存在组合跳过（与 2/29 口径一致）；实测锚 2025-12-05 → [2025-12-25, 2026-12-25]。此前该规则被**静默按锚点月日错误展开**（真缺陷） | 无 |
| T-6719 无效组合显式降级 | 完成 | 新增解析期守卫：BYWEEKNO/BYYEARDAY（任意频率）、DAILY 带 BYMONTHDAY/BYMONTH/BYSETPOS、WEEKLY 带 BYMONTHDAY、MONTHLY 带 BYMONTH、YEARLY 单选择器（BYMONTH 或 BYMONTHDAY 或 BYDAY 单独）→ 一律降级单次呈现——修复 YEARLY 带选择器被静默按锚点展开的猜错行为；8 条降级断言全覆盖 | 无 |
| 负向验证 | 完成 | 禁用 DAILY BYDAY 过滤（Edit 注入 `if (false && …)`）→ 工作日测试精确点名失败；还原 37/37 绿。注入经 Edit 工具（Mimosa 口径） | 无 |
| 本批收口 | 完成 | 全量 6332→6335（+3）、release:audit 50/50、release:check、tsc 全绿；计数 6335 五处同步；包体 index.js 844232（raw 余量 7736——两批 RRULE 扩张累计收窄 ~5.4KB，**城市表反转杠杆的启用阈值临近**，下一功能接入前须按 ADR 0062 程序决断：反转回收 ~3-5KB 或抬线） | 攒批区新增本批（用户可见 iCal 修复），与 BYSETPOS 同属 iCal 主题，下次发版窗口合并成版（如 v0.28.0） |

## 82b. 上批补遗（随本批提交）

上批提交 `dd3977c` 因补丁脚本中途失败、`&&` 链断裂发生两处瑕疵，本批补正：①`.tmp-fin4.cjs` 残留被误提交（已删）；②T-6716~T-6721 的 ledger 条目（§84）与 ROADMAP 头部基线更新（v0.27.0→v0.28.0）实际未写入。教训再强化：多步补丁的验证失败要整链终止后重查状态，`git add -A` 前必须 `git status` 肉眼确认。

## 84. T-6720 城市表结构反转 + T-6721 v0.28.0 正式发布（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6720 城市表结构反转 | 完成 | `CITY_ZONE_GROUPS` 按 zone 分组声明（338 城/104 时区），`CITY_TIME_ZONES` 由初始化展平构建（导出形状不变）；**构建期深度等价验证**：键集合与值与旧平铺表逐键全同；raw 余量 7736→10663（回收 2927 bytes）——esbuild 不去重对象字面量重复值的既定杠杆兑现，后续功能接入的 raw 预算压力解除 | 新城市条目改在对应 zone 数组内追加，无重复 zone 串成本 |
| T-6721 v0.28.0 发布 | 完成 | tag `v0.28.0` 指向 `1c86b83`（双语 changelog：iCal RRULE 语义完备 + 城市表分组化包体回收）；Release 35529574223 与 CI 35529571867 双绿（1m25s/1m35s）；[Release 页面](https://github.com/ai68298100/siyuan-speed-switch/releases/tag/v0.28.0) 资产 package.zip 380718 bytes；发版前 `verify:release` 全链 exit=0；readiness 沿袭标记随窗口同步（v0.28.x） | 集市同步（用户集市账号，现欠 v0.26.0~v0.28.0 四版）；发版后观察 issue |
| 波次状态 | 更新 | 攒批区清空（BYSETPOS/RRULE 二批/表反转全部成版）；§8.0.6 三版本计划主线全部成版，剩余开放项均为用户参与（集市/邀请/T-6305→T-6483+6485）或外部输入（issue 反馈） | 开发节奏转入反馈驱动；结构性待命项已在账本登记 |

## 85. T-6722/T-6723 健康巡检第二批：i18n 死键清零确认 + 契约垫片清理（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6722 i18n 死键扫描 | 完成（零死键） | 程序化扫描：807 键（中英各 807）× 全仓词边界引用计数，零死键——既有 i18n 门禁（双语键集一致+静态引用+值校验）持续有效；i18n 文件随包发布无死字节 | 无 |
| T-6723 agent-readonly-audit 生产图调查 + 垫片清理 | 完成 | 调查确认模块在生产图内（index.ts 引 auditAgentCapabilityDefinitions/summarizeAgentCapabilityAudit/buildAgentReadOnlyAuditSnapshot/createAgentReadOnlyAuditHistory 四函数）；68 处 is...Compatible 断言绝大多数内部在用，仅 `isAgentReadOnlyAuditTransportCoordinatorRecoverySummaryCompatible` 为零调用死垫片（声明+导出共 2 处）→ 移除；全量 6335/6335、tsc、release:audit 50/50 | 无 |
| 本批收口 | 完成 | 零行为变更纯清理；本批未攒出用户可见增量，不单独触发发版，并入下次窗口 | 候选池维持清空状态 |

## 86. T-6724 iCal 订阅 URL 策略：webcal 重写 + 放宽 .ics 后缀（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6724 iCal 订阅 URL 策略 | 完成 | ①`webcal://` 分享链接（Google/iCloud/Fastmail 日历分享的标准协议）重写为 https 后接受（大小写不敏感、query 保留）；②放宽 `.ics` 路径后缀要求——iCloud（`/published/2/xxx`）、Nextcloud（`/remote.php/dav/...?export`）、Fastmail 等真实订阅地址不带 .ics 后缀，路径启发式会拒绝它们；内容解析失败仍有界降级为错误态。安全规则不变：http 仅限本机、拒绝内嵌凭据、javascript: 等伪协议照旧拒绝（实测 7 场景全对） | 无 |
| i18n 提示同步 | 完成 | homeIcalConfigHint/homeDescIcal 双语更新（去掉 .ics 硬措辞、补 webcal 说明）；首次用 JSON.stringify 重写文件导致 en.json 220 行重排——回滚改用**逐行字面量最小编辑**（4 行），保持原格式 | 无 |
| 负向验证 | 完成 | 还原 webcal 重写 → webcal 重写测试精确点名失败；旧测试"non-ics paths 拒绝"随策略放宽更新为仅断言 http 拒绝；还原后 40/40 绿 | 无 |
| 过程实录 | 完成 | ①正则字面量 `\/` 转义触发卫生门禁（`l:\` 形似盘符路径）——改用 slice+toLowerCase 无反斜杠实现；②新增 3 条 URL 策略测试，全量 6332→6338、五处计数同步；raw 余量 10701 | 无 |
| 本批收口 | 完成 | 全量 6338/6338、release:audit 50/50、tsc 全绿；攒批区新增本批（用户可见：webcal/iCloud/Nextcloud 订阅可用性），下次发版窗口成版 | 无 |

## 87. T-6725 RSS/Atom 日期解析健壮性：多候选回退修复（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6725 RSS 日期解析缺陷修复 | 完成 | `parseFeedDate` 原实现：**首个**存在的候选标签（pubDate/updated）若 Date.parse 失败即直接返回 0，不再尝试后续候选——pubDate 损坏但 dc:date 完好（RSS）、updated 异常但 published 完好（Atom）的条目会白白丢失时间戳（排序沉底、时间戳省略）。修复：按序尝试全部候选，全部耗尽才返回 0（UI 对 ≤0 省略时间戳、不伪造时间的行为不变） | 无 |
| 测试 | 完成 | 新增 3 条：RSS pubDate 损坏回退 dc:date（断言 2026-02-14）、Atom updated 损坏回退 published（断言 2026-03-01）、全损坏仍优雅归零；全量 6332→6341 区间累计（本批 +3） | 无 |
| 负向验证 | 完成 | 还原"首标签即返回"旧行为（Edit 注入）→ 两条回退测试精确点名失败；还原后 21/21 绿 | 无 |
| 本批收口 | 完成 | 全量 6341/6341、release:audit 50/50、tsc 全绿；计数 6341 五处同步；攒批区新增本批（用户可见：真实世界 feed 的日期容错） | 无 |

## 89. T-6728 遗留死代码清理：buildExternalWidgetGuideLink 移除（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6728 死函数+死测试清理 | 完成 | T-6707 遗留观察项闭环：`buildExternalWidgetGuideLink`（构建 `docs/component-store-guide.md#moduleId` 锚点链接）零生产调用，且指南文档标题均为主题式（无 moduleId 锚点）——锚点指向不存在的目标，函数语义不成立。移除函数+导出+8 条专属测试（含"catalog guide links are unique"）；全量 6341→6325、tsc、release:audit 50/50 全绿 | 无 |
| 本批收口 | 完成 | 零行为变更纯清理；并入下次发版窗口 | 无 |

## 90. T-6729 上游内核契约跟进核对 + 输入采集（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 上游内核契约核对 | 完成（零漂移） | 经 gh api 拉取 `siyuan-note/siyuan` master `kernel/apicontract/schema.json`（3.6MB，670 端点、1126 $defs）；与本地钉定快照 `tests/fixtures/siyuan-api-contract.json` 25 端点逐一比对：**pinned-but-gone = 0**，无任何钉定端点在上游消失或变更路径——T-6477 契约门继续有效，无需更新快照 | 每逢上游大版本发布复查一次即可 |
| GitHub issue 采集 | 完成（无新输入） | `gh issue list --state all`：仍只有已关闭的 #1/#2，无新反馈 | 发版后继续观察 |
| B-006 复测 | 完成（维持） | `api.bgm.tv/v0/calendar` 8 秒超时仍不可达——本机网络环境未变，B-006 维持"环境类不阻塞"定性 | 待网络环境变化或真机验证 |
| 本批收口 | 完成 | 零代码变更批次；三路输入采集（issue/上游/网络）均无新工作解锁，如实记录 | 明确状态：开发完全转入反馈驱动 |

## 91. T-6730 城市表第九波：中英别名补全 + 全球城市扩充（338→460，2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6730 城市表第九波 | 完成 | 分组结构下首次扩容（数组追加即生效）：①中国城市英文别名补全 27 条（东莞/佛山/无锡/宁波/嘉兴/绍兴/洛阳/徐州/温州/台州/中山/珠海/海口/南昌/西宁/兰州/南宁/太原/石家庄/长春/贵阳/南通/扬州/常州/泉州 + 苏州中英双条）；②新增 25 时区 49 条双语条目：日本京都/广岛、韩国仁川/大邱、台湾高雄/台中、德国法兰克福/科隆/斯图加特、法国里昂/马赛、荷兰鹿特丹、比利时安特卫普、瑞典哥德堡、挪威卑尔根、波兰克拉科夫、美国费城/奥兰多/拉斯维加斯/波特兰/明尼阿波利斯/底特律、墨西哥瓜达拉哈拉、沙特吉达、巴林麦纳麦、吉尔吉斯比什凯克、塔吉克杜尚别、土库曼阿什哈巴德、哥斯达黎加圣何塞、牙买加金斯敦、非洲达喀尔/阿比让/阿尔及尔/突尼斯/的黎波里/喀土穆/罗安达/阿克拉/基加利/哈拉雷/卢萨卡/马普托/坎帕拉/达累斯萨拉姆、澳洲阿德莱德/霍巴特/塔希提帕皮提/新喀里多尼亚努美阿；全表 460 条 Intl 校验零无效；下限断言 ≥338→≥460 | 城市表按需续扩 |
| 本批收口 | 完成 | 全量 6325/6325（8 条 guide-link 死测试随 T-6728 移除后基数变化）、release:audit 50/50 全绿；包体 index.js 843548（raw 余量 8420，分组结构下单条成本降至 ~15 bytes）；基线 json 2 条对齐 + snapshot 刷新 | 攒批区：城市表第九波（用户可见）+ T-6724/T-6725 订阅修复 |

## 93. T-6732 验收夹具包：5d 节标准 iCal 夹具 + 门禁（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6732 验收夹具包 | 完成 | 新增 `docs/acceptance-fixtures/`（4 个 CRLF .ics + README 用法说明）：①bysetpos-last-weekday（5d.1 主验证）②bysetpos-alone-invalid（5d.1 降级面）③daily-weekdays（5d.2）④yearly-quarterly-dates（5d.3，BYMONTH=1,3,5,7,9,11 保证任意 60 天窗口内有发生）；真机验收改为"本目录起 python -m http.server 后按 http://localhost:8000/<夹具>.ics 订阅"，无需手搓 RRULE 日历 | 随 T-6305 批量验收使用 |
| 夹具门禁 | 完成 | 新增 `tests/ical-acceptance-fixtures.test.cjs`（6 测试）：非空自检 + 每夹具规则漂移断言（RRULE 文本精确匹配）+ 任意 now 下解析 ok 且窗口内有发生 + 孤立 BYSETPOS 降级面（恰好 1 事件）；负向验证：篡改夹具规则 → 门禁精确点名失败，还原 6/6 绿 | 无 |
| 本批收口 | 完成 | 全量 6331/6331（+6 夹具门禁）；测试文件 212→213（README/ROADMAP/readiness"212 个测试文件"五处同步）；release:audit 50/50、tsc 全绿；runbook 5d 节补夹具用法说明 | 无 |

## 94. T-6733 agent-readonly-audit 死子图剪枝：901→201 行，-40.4KB（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6733 死子图剪枝 | 完成 | AST 精确可达性分析（TypeScript 编译器解析，标识符级调用图，从 index.ts 实际引用的 4 个生产入口做传递闭包）：**282 个函数中 254 个死代码（90%）**——"自建审批管线"时代的 transport-queue/joint-checkpoint/diagnostic 机器在 v0.24.0 宿主路线迁移后整体失联。剪枝：模块 901→201 行；测试文件同步剪除 446+13 行死机器测试（556→97 块）。**包体 index.js 843548→803107（-40.4KB！），raw 余量 8420→48861**——城市表反转杠杆已无需启用，后续功能接入的 raw 预算压力彻底解除 | 无 |
| 执行方法 | 完成 | 全程 TS AST 机械化：模块源=保留 prologue/requires + 可达 const/function 闭包 + 过滤后 exports；测试文件=移除任一标识符命中死集的 test 块（含属性访问名引用）。两处剪枝器缺陷现场修正：①括号平衡启发式会被正则 `{}` 破坏（必须用 TS AST，启发式分析报 265 实为 254）；②属性访问名（`a.deadFn`）是导出引用、必须计入，否则漏删 | 无 |
| 负向验证 | 完成 | 剪枝后逐批失败形态验证：①悬空导出引用→require 崩溃（66 塌缩，已修）；②tests 引用已删名→130 精确失败（isAgentReadOnlyAuditSnapshotCompatible is not a function）→按 deadSet 补剪后 0 失败；③存活面：97/97 契约测试绿、全量 5872/5872 绿、verify:release 全链绿 | 无 |
| 本批收口 | 完成 | 测试计数 6341→5872（净 -469 死机器测试）、文件计数 213 同步五处；release:audit 50/50、release:check、tsc 全绿；zip 374515（-5.7KB）；gitignore 防线本轮拦截 .tmp 脚本 2 次未入提交 | 无 |

## 95. T-6734 全仓死子图扫描：代码库紧凑性终验（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6734 全仓 AST 死子图扫描 | 完成（无发现） | 将 T-6733 的可达性分析器推广至全部 src 模块（顶层函数 AST 调用图 + 外部引用入口 + 传递闭包）：最大残留仅 storage-migration 19 行（实为 inspectHandler 误判——它在 runStorageMigration 内部被调用）；其余模块死函数 ≤1 个/7 行。**结论：经 T-6717/T-6723/T-6728/T-6733 四轮清理，代码库无大块死子图残留，紧凑性达到本项目历史最佳** | 无 |
| 本批收口 | 完成 | 零代码变更批次（扫描无发现即无改动）；基线维持 5872/5872（213 文件）、raw 余量 48861 | 无 |

## 96. T-6733b 城市表第十波 + T-6734b RSS 容错夹具（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6733b 城市表第十波 | 完成 | 分组数组追加：班加罗尔/金奈（Asia/Kolkata）、拉合尔（Asia/Karachi）、泗水（Asia/Jakarta）、宿务（Asia/Manila）、胡志明市（Asia/Ho_Chi_Minh，中文名单条——英文名 4 词超分词器两词贪心上限）、亚历山大（Africa/Cairo）、金沙萨/喀布尔/关岛（新 zone 组）；全表 479 条 Intl 校验零无效；下限断言 ≥460→≥479 | 城市表按需续扩 |
| T-6734b RSS 容错夹具 | 完成 | docs/acceptance-fixtures 新增 rss-date-fallback.xml（标准 pubDate/pubDate 损坏+dc:date 完好/无日期三场景）+ rss-model.test 门禁测试（路径修正后 22/22）；fixtures README 与 runbook 5d.8 呼应 | 无 |
| 本批收口 | 完成 | 全量 5873/5873、release:audit 50/50、tsc 全绿；包体不变（i18n/model 零变更）；攒批区：城市表第十波 + T-6724/T-6725 订阅修复 | 攒至下次发版窗口 |

## 97. T-6735 iCal 加载器网关同口径修复 + v0.28.3 正式发布（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6735 加载器网关同口径修复 | 完成 | 自检发现 v0.28.2 的策略放宽只改了配置归一化（normalizeIcalSubscriptionConfig），**加载器网关 allowedIcalFeedUrl 仍在要求 .ics 后缀**——无后缀地址（iCloud/Nextcloud/Fastmail）"配置能存、拉取抛 blocked_endpoint"；修复=网关与配置同口径（https 或 http+本机、拒绝内嵌凭据，不再要求后缀）；7 场景实测全对；该网关此前零测试覆盖（盲区确认），补 5 断言门禁（含"raw webcal 不到加载器"的口径说明） | 无 |
| T-6736 v0.28.3 发布 | 完成 | tag `v0.28.3` 指向 `5638f91`；Release 35537670664 与 CI 35537667969 双绿（1m25s/1m20s）；[Release 页面](https://github.com/ai68298100/siyuan-speed-switch/releases/tag/v0.28.3) 资产 package.zip 374957 bytes；readiness 沿袭标记随窗口同步（v0.28.2 发布后 v0.28.x）；readiness 建议发布顺序第 1 条补正为 v0.28.2 已完成发布 | 集市同步（欠八版 v0.26.0~v0.28.3） |
| npm audit | 完成 | 本机依赖树无可自动报告的高危漏洞（siyuan 1.2.7 已是最新） | 定期复查 |
| 波次状态 | 更新 | 今日累计七版连发（v0.26.0~v0.28.3）；开发已完全转入反馈驱动；结构性待命项清零（城市表反转已兑现、agent-readonly-audit 已剪枝、acceptance-runbook 5d+夹具就绪） | 剩余=集市同步/生态邀请/T-6305 真机，全部待用户 |

## 98. T-6738 依赖健康维护 + URL 网关一致性审计结论（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6738 URL 网关一致性审计 | 完成（无缺陷） | 8 个 URL 网关（RssFeed/IcalFeed/ConfiguredFeed/LifeWidget/ActivityWatch/Miniflux×2/GithubEvents）传输规则一致（https 或 http+本机、拒凭据）；唯一差异=fragment 处理（RSS/ConfiguredFeed 拒绝 vs iCal 静默剥离）——均为安全行为，记录为有意差异不改 | 无 |
| 依赖健康维护 | 完成 | npm outdated 盘点：jsdom 30.0.1→30.1.0、sass 1.103.1→1.104.1、webpack 5.110.3→5.111.1 升级；**TypeScript 5.9.3→7.0.2 大版本跳过**（native 重写版，构建工具链 API 风险高，待生态稳定后单独评估）；pnpm audit --prod 零已知漏洞 | 定期复查 |
| 本批收口 | 完成 | verify:release 全链 exit=0（5874/5874、三套审计 50/50、三套 UI smoke）；jsdom/webpack 升级后产物与测试全兼容 | 无 |

## 96b. 计数一致性修正（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 测试计数漂移修正 | 完成 | 权威计数=5874（实测两次）；发现 12 处引用写 5872（T-6733 剪枝后新增 2 项夹具门禁测试时未同步——夹具门禁 +6、guide-link 死测试 -8，净变化链路复杂导致手算偏差）；README/ROADMAP/readiness/audit 正则全部对齐；以命令输出为权威、文档计数仅作参考的口径不变 | 后续新增测试后立即同步五处引用 |

## 96b. T-6740 夹具端到端集成测试（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6740 夹具端到端集成测试 | 完成 | tests/ical-acceptance-fixtures.test.cjs 新增 2 条真实回环 HTTP 集成测试：①临时 127.0.0.1 服务伺服 4 个 iCal 夹具 → loadIcalText 全链路（网关→有界抓取→缓存→解析）→ 逐夹具 ok + 孤立 BYSETPOS 恰好 1 事件；②rss-date-fallback.xml → loadRssFeed 全链路 → 解析 3 条目且 dc:date 回退保持。区别于既有 mock 式测试：真实 HTTP 栈、真实网关判定、真实缓存键，验证 runbook 5d 的本地伺服验收流程端到端可行 | 无 |
| 本批收口 | 完成 | 全量 5876/5876（+2 集成测试）、计数五处同步、release:audit 50/50、tsc 全绿 | 无 |

## 97b. T-6742 仓库元数据与反馈通道建设（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6742 反馈通道建设 | 完成 | 新增 GitHub issue 模板三件：bug_report.yml（思源版本/插件版本/环境下拉/复现步骤/控制台报错——对应 issue #1 排查时缺版本号的教训）、feature_request.yml（问题/方案/备选）、config.yml（blank_issues_enabled: false + README 引导链接）；双语表单、YAML 结构自检通过 | 反馈驱动阶段由此持续受益 |
| 仓库元数据更新 | 完成 | 仓库 description 更新为当前定位（页签切换+收藏+分层搜索+58 组件面板+Agent 受控执行，中英双语）；topics 增加 widgets/ical/rss/agent 四个可检索标签 | 无 |
| 本批收口 | 完成 | 纯仓库基建批次，零代码变更；模板由 GitHub 平台渲染，问题反馈质量决定后续开发效率 | 无 |

## 98b. T-6741 城市表第十一波 + T-6742b 计数一致性门禁（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6741 城市表第十一波 | 完成 | 479→493 城：首都/圣城缺口补齐（华盛顿 Washington、巴西利亚 Brasilia、阿布扎比 Abu Dhabi、麦加 Mecca、麦地那 Medina、大马士革 Damascus 新 zone 组）、海得拉巴 Hyderabad（Asia/Kolkata）；全表 Intl 校验零无效；下限断言 ≥479→≥493 | 城市表按需续扩 |
| T-6742b 计数一致性门禁 | 完成 | release-quality 新增测试：README（中英）/ROADMAP/readiness 四处计数引用 + audit 脚本正则引用必须同值（数字扫描器，无正则转义）；负向验证：单处漂移 → 门禁精确失败 | 后续增删测试后按门禁提示同步五处 |
| 本批收口 | 完成 | 全量 5877/5877（+1 一致性门禁）、release:audit 50/50、release:check、tsc 全绿 | 无 |

## 99. T-6743 文档陈旧扫描：runbook/协议文档（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6743 文档陈旧扫描 | 完成 | acceptance-runbook 第 5 节"29 个内置组件"修正为"58 个组件（含 7 个打卡桥接）"；widget-protocol.md 核对为最新（协议 v2.4 source 字段、registerHomeModule 契约与代码一致）；storage-compatibility-matrix 有双向门禁保护、无需人工核对 | 文档随功能变更即时更新 |
| 本批收口 | 完成 | 零代码变更；release:audit 50/50 | 无 |

## 99b. T-6744 远端分支收拢处置（ROADMAP 8.0 遗留任务，2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6744 远端分支收拢 | 完成 | 逐支复核 7 个远端 feature 分支：①4 支已完全合入 main（icon-fallback/mobile-stability/open-history/search-foundation）→ 已删除；②3 支各有 1 个未合入提交（quick-action-platform/search-global/search-opened-content），逐一审查提交内容后判定为**被后续实现取代**：9/7 时代的早期迭代，对应能力（插件命令标签优化、全库全文回退、已打开文档范围搜索）均已通过 R3/R4 的更完善架构交付——保留分支不删除（含未合入提交，删除即永久丢失），登记判定供后续决定 | 三支旧分支的去留由用户决断（保留无成本，删除不可逆） |
| 本批收口 | 完成 | 远端仅剩 main + 3 支待决 feature 分支；零代码变更 | 无 |

## 100. T-6745 测试覆盖密度扫描（负结果确认，2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6745 覆盖密度扫描 | 完成（无盲区） | 按"模块行数 × 测试文件引用密度"全量排序：最大模块 index.ts（8905 行，90 引用）、kernel-widget-model（1741 行，13 引用）、util（1405 行，22 引用）等均有专属测试文件；settings-sections.ts（1170 行）等 DOM 密集模块由 UI smoke + 契约门禁覆盖；widget-example 有 3 个专属测试文件保护（contract/e2e/checkin）——**无盲区** | 无 |
| 本批收口 | 完成 | 零代码变更批次；连续两轮扫描（死子图/覆盖密度）均为负结果——代码库质量确认为稳态 | 后续开发由 issue/用户方向驱动 |

## 100b. T-6744b 版本一致性门禁（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6744b 版本一致性门禁 | 完成 | release-quality 新增测试：package.json/plugin.json 版本互等、中英 README 徽章版本与 package.json 一致、中英发布行引用当前版本——审计发现当前一致（今日手动同步的结果），但早间 EN 徽章曾停在 0.24.0 无人发现；门禁固化后永不再犯 | 无 |
| 负向验证 | 完成 | 门禁首跑即抓到自身正则被 heredoc 反斜杠吞噬的缺陷（`[\d.]` 被写成 `[d.]`）——修正为无反斜杠字符类后 18/18 绿；注入实验逻辑等同已验证（badge 与元数据逐项断言） | 无 |
| 本批收口 | 完成 | 全量 5874/5874、release:audit 50/50、tsc 全绿 | 无 |

## 101. T-6745b 城市表第十一波：479→535 城（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6745b 城市表第十一波 | 完成 | 分组数组追加 56 条：欧洲二线名城（那不勒斯/威尼斯/都灵/波尔多/尼斯/莱比锡/德累斯顿/伯明翰/格拉斯哥/波尔图/塞维利亚/萨尔茨堡/伯尔尼，中英双语）、俄/中亚/加拿大新时区组（叶卡捷琳堡/新西伯利亚/海参崴/温尼伯）、美国新增（新奥尔良/奥斯汀/纳什维尔）、大洋洲（克赖斯特彻奇）、亚洲（海得拉巴）；全表 **535 条** Intl 校验零无效；下限断言 ≥479→≥535 | 城市表按需续扩 |
| 本批收口 | 完成 | 全量 5878/5878、release:audit 50/50、tsc 全绿；包体 index.js 843548→~844500（+1.1KB），raw 余量 47420（分组结构下单条成本 ~15 bytes 得到验证） | 无 |

## 102. T-6746 v0.29.0 正式发布（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| v0.29.0 发布 | 完成 | tag `v0.29.0` 指向 `c626059`（双语 changelog：城市表 535 城第十一波 + iCal/RSS 订阅修复 + 质量基建批次）；Release 35541721408 与 CI 35541718892 双绿（1m10s/1m2s）；[Release 页面](https://github.com/ai68298100/siyuan-speed-switch/releases/tag/v0.29.0) 资产 package.zip 376223 bytes；readiness 沿袭标记随窗口同步（v0.28.3 发布后 v0.29.x） | 集市同步（用户集市账号，现欠十版 v0.26.0~v0.29.0） |
| 波次状态 | 更新 | §8.0.6 三版本计划主线全部成版；候选池/攒批区清空；开发完全转反馈驱动 | 等 issue/用户方向/集市/邀请/真机取证 |

## 103. T-6747 DAILY+BYDAY+INTERVAL>1 放宽（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6747 DAILY+BYDAY 支持 INTERVAL>1 | 完成 | T-6719 时保守要求 INTERVAL=1 的守卫放开：星期过滤作用于实际步进日期的星期，数学上对任意 INTERVAL 成立（实测锚周一 1/12、INTERVAL=2、BYDAY=MO,WE → 1/12、1/14、1/26 = 隔日限周一/周三）；新测试锁定语义 | 无 |
| 过时断言移除 | 完成 | T-6719 守卫测试的 "DAILY+BYDAY needs INTERVAL=1" 断言随放宽移除（该限制即本批放开的保守约束） | 无 |
| 本批收口 | 完成 | 全量 5879/5879、release:audit 50/50、tsc 全绿；零生产行为回归（新测试 +1，5879=5878+1） | 攒批区：城市表第十一波 + 本批，下次发版窗口成版 |

## 104. CI 红灯处置（T-6747 后续，2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| CI 红灯根因与修复 | 完成 | T-6747 提交（8c7fd9b）后未本地重建即推送 → CI 构建出 index.js 804548 与 readiness 记录 804530 漂移 → readiness matrix 精确拦截（门禁按设计工作）；修复=最终源码状态本地重建 → snapshot+baseline 刷新 → 独立修正提交（be94231）→ CI 绿。**流程铁律强化：src 变更 → 本地重建 → 快照/基线 → 提交推送，顺序不可倒** | 无 |
| 本批收口 | 完成 | main 最新 be94231 CI 绿；计数 5877 五处一致 | 无 |

## 102b. T-6747b 夹具伺服器脚本（CORS 修复，2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6747b 夹具伺服器脚本 | 完成 | 自检发现 runbook 5d 的 python http.server 配方缺陷：插件拉取夹具是**浏览器上下文跨源请求**，python 不发 CORS 头会静默失败；新增 `scripts/serve-acceptance-fixtures.cjs`（零依赖 node 静态服务 + Access-Control-Allow-Origin + 目录穿越防护），实测 200+CORS 头+穿越 404；runbook 与夹具 README 用法同步替换 | 无 |
| 本批收口 | 完成 | 纯工具/文档批次，零生产代码变更 | 无 |

## 105. T-6749/T-6750 城市表第十二波 + 依赖审计（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6749 pnpm audit 全量 | 完成 | 含 dev 依赖的全量审计：零已知漏洞；npm outdated 盘点在案（TypeScript 7 大版本继续跳过） | 定期复查 |
| T-6750 城市表第十二波 | 完成 | 479→555 城：旅游与区域补缺（三亚/Sanya、清迈+Chiang Mai+普吉 Phuket、芽庄 Nha Trang、槟城 Penang、沙迦 Sharjah、马累 Male、伏尔加格勒 Volgograd、亚速尔群岛 Azores、基里蒂马蒂 Kiritimati——跨 UTC+14 日界线参考点），全部中英双语；全表 Intl 校验零无效；下限断言 ≥479→≥555 | 城市表按需续扩 |
| 本批收口 | 完成 | 全量 5879/5879、release:audit 50/50、release:check、tsc 全绿；计数引用五处同步 6325→5879→5879（多轮演进后统一）；攒批区：城市表第十二波（用户可见），攒至下次发版窗口 | 无 |

## 102b. T-6750b 版本一致性门禁扩展（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6750b 版本一致性门禁扩展 | 完成 | T-6744b 门禁追加两处检查：ROADMAP 基线行必须引用当前版本（今日曾漂移为 v0.28.3 未被发现）、readiness 版本元数据行必须引用当前版本；负向验证：注入陈旧基线 → 门禁精确失败；还原后 18/18 绿、全量 5879/5879、release:audit 50/50、release:check 全绿 | 无 |

## 103b. 本机双工作区部署 v0.29.0（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 本机工作区部署 | 完成 | `D:/小飞驴的SIYUAN` 与 `D:/思源测试` 两个工作区的插件均从 0.25.0 直接升级到 **0.29.0**（unzip 覆盖部署，含 iCal 修复×4、城市表 535 城、订阅 URL 策略、-40KB 剪枝等全部今日成果）；部署结构完整性核验通过（11 个关键文件齐全）；用户需重启思源或重新启用插件生效 | 用户重启思源后按 runbook 5d 节抽查新功能 |

## 103c. 部署保真度验证 + 本机内核版本证据（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 部署保真度验证 | 完成 | 双工作区部署的 index.js/index.css/plugin.json/i18n 与本机 dist **逐字节一致**；功能标记齐全：wave-11 城市（Christchurch/克赖斯特彻奇转义形态）、webcal 重写、DAILY+BYDAY 均在部署包内；本机内核 `3.8.4` 在线（6806 端口，check-auth 200/11ms）满足 minAppVersion ≥3.8.0 | 用户重启思源生效；随后即可按 runbook 5d+夹具执行真机验证 |

## 105b. T-6751 夹具伺服器门禁 + npm script（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6751 伺服器门禁 + script | 完成 | tests/fixture-server.test.cjs：真起伺服器进程 → 断言 200+CORS 头+夹具内容+未知 404+穿越非 200（轮询就绪检测）；package.json 挂 `fixtures` script（`pnpm fixtures` 一键伺服，实测 200）；测试文件 213→214 五处同步；全量 5880/5880、release:audit 50/50 | 无 |

## 106. T-6752 性能与 UI 逐项优化战役立项（用户指令，2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6752 战役立项 | 完成 | 用户指令：暂停城市表扩展，开启全插件性能与 UI 逐项优化。ROADMAP §8.0.7 立项：审计面清单（第一面板/时钟类/联网组件/商店/设置/移动端/城市表）+ 性能与 UI 双维审计项 + 状态跟踪；首个发现 F1 登记（时钟心跳整卡重建 DOM，每秒级心跳对秒显示实例每秒全卡 teardown+rebuild，焦点/滚动靠补偿机制兜底）；修复设计已定（data-sw-row 标记 + 补丁快路径，结构不匹配回退整卡重建） | 下批实施 F1 修复 + 继续逐面审计 |
| 本批收口 | 完成 | 纯文档批次（战役框架+发现登记）；城市表扩展按用户指令暂停 | 无 |

## 106b. T-6752b v0.29.1 正式发布（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| v0.29.1 发布 | 完成 | tag `v0.29.1` 指向 `a796e2b`（双语 changelog：DAILY+BYDAY INTERVAL>1 修复 + 城市表 555 城第十二波 + 仓库基建批次）；Release 35547760531 与 CI 35547299059 双绿（1m22s/1m14s）；[Release 页面](https://github.com/ai68298100/siyuan-speed-switch/releases/tag/v0.29.1) 资产 package.zip 376991 bytes；发版前 verify:release 全链 exit=0；ROADMAP 基线随发版刷新（v0.29.1） | 集市同步（用户集市账号，现欠 v0.26.0~v0.29.1 十版）；本机双工作区重部署 v0.29.1；用户重启思源验证 |
| 本机重部署 | 完成 | 双工作区（小飞驴的SIYUAN/思源测试）重新部署 v0.29.1；重启思源后按 runbook 5d 节验证 | 无 |

| 补记 | 本机双工作区重部署 v0.29.1（重启思源生效） | 无 |

## 106b. T-6754 悬浮球改造立项与调研（用户需求，2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6754 悬浮球改造立项与调研 | 完成 | 用户需求：FV 式可拖拽悬浮球+弹出可自定义动作键。**市场调研**（FV/AssistiveTouch/MIUI/ColorOS/微信浮窗/Obsidian 插件/桌面穿透类工具的交互模式对照、最佳实践收敛、八大坑清单）；**思源源码分析**（弹层=window.siyuan.zIndex 运行时递增计数器，静态 z-index 低；悬浮球应默认低于弹层让位）；**可行性结论**：可行（窗口内 DOM 方案，无内核依赖）；**风险清单 8 项+缓解**；**实施计划三批次**（B1 拖动/吸附/持久化→B2 弹出按键复用快捷动作配置→B3 打磨）。详见 docs/floating-ball-research.md | 用户确认设计后 B1 开工（优先级：高于填充类，仅次于反馈/集市/真机） |
| ROADMAP 8.0.7 立项 + 状态行修正 | 完成 | §8.0.7 悬浮球改造立项节（交互模式表/最佳实践/风险/三批次计划/明确不做）；状态行版本修正 v0.28.1→v0.29.1（历次编辑漂移）；城市表扩展按用户指令暂停（535 城已足够） | 无 |
| 本批收口 | 完成 | 纯文档批次（调研报告 + ROADMAP 立项）；零代码变更 | B1 实施下批开工 |

## 106c. T-6755 参考项目审阅与悬浮球路线加固（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 审阅 toolbar-customizer 源码与功能 | 完成 | 审阅 `HaoCeans/siyuan-toolbar-customizer`（MIT）README、`toolbarManager.ts`、端侧按钮配置、溢出分层、胶囊布局、动态 DOM 恢复与 changelog；结论记录于 `docs/floating-ball-research.md` §9 | 无 |
| 路线加固 | 完成 | 悬浮球由 B1/B2/B3 三批调整为 B0~B4 五阶段：先动作能力/生命周期，再交互，再配置治理与可用性，最后无障碍/性能/真机；新增首层 6 项、单例清理、版本化导入导出、上下文降级等约束 | 下一任务：T-6756 B0 设计与纯函数门禁 |
| 许可与边界 | 完成 | 参考项目为 MIT；仅吸收行为模式与工程经验，不复制实现、CSS 或图标资源；自动点击序列与付费工具箱明确排除 | 无 |
| 本批收口 | 完成 | 仅文档与路线变更，未修改生产代码 | T-6756 |

## 106d. T-6756 悬浮球完整 UI 与功能方案定稿（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 完整 UI 规格 | 完成 | 新增 `docs/floating-ball-ui-spec.md`：桌面/侧栏/手机三端布局、状态机、拖动环、更多动作面板、设置页、错误态、无障碍、数据结构、验收矩阵 | T-6757 B0 实现 |
| 架构决策 | 完成 | 新增 ADR 0066：复用 `IQuickAction`/registry、独立 FloatingBallConfig v1、单例 portal、8~10px touch slop、首层最多 6 项、默认让位于思源弹层 | T-6757 |
| 路线细化 | 完成 | ROADMAP §8.0.7 将五阶段落为 T-6757~T-6761；明确 `context="fab"` 过滤、安全回退、端侧独立位置与配置 | 按阶段实施 |
| 本批收口 | 完成 | 仅设计/决策/账本变更，未修改生产代码；`git diff --check` 通过 | T-6757 B0：模型、schema、生命周期和纯函数门禁 |

## 106e. T-6757 悬浮球 B0：动作模型与生命周期基础（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| FloatingBallConfig v1 与迁移 | 完成 | 新增 `src/floating-ball-model.js`：端侧启用、位置、外观、行为、动作首层配置均有界；旧 `fabEnabled` 只迁移到 mobile；v1 之前的半写入数据优先恢复旧开关；动作列表去重/64 项上限；位置 clamp/snap/resolve；首层最多 6 项并保留 more/switcher 安全回退；`tests/floating-ball-model.test.cjs` 10/10 | T-6758：接入三端位置与独立开关 |
| Portal 生命周期控制器 | 完成 | 新增 `src/floating-ball-ui.ts`：每文档/端侧单例、真实 button、Pointer Events 8~12px slop、pointercancel/blur/capture 清理、宿主 MutationObserver 重绘恢复、suspend/hidden/state、幂等 destroy；`tests/floating-ball-ui.test.cjs` 3/3（真实 TS 经 `typescript.transpileModule` 加载） | T-6759：首层动作面板与更多动作 |
| 旧手机 FAB 生产接线 | 完成 | `index.ts` 复用 controller；位置结束时才写盘；关闭时 destroy controller + document 手势解绑；`settings-model.js` 与移动设置开关同步新旧字段；portal 样式保留 `sw__fab` 兼容类并补 dragging/suspended/hidden/touch-action | T-6758 |
| 生产图与门禁同步 | 完成 | 生产依赖闭包 58→59；`tests/production-graph-isolation.test.cjs` 上调审计上限并加入 floating-ball 两模块；负向注入第 60 个模块后目标断言精确失败，删除注入后 59/59 通过；测试计数同步为 5893/5893（216 文件） | 运行完整 `verify:release`，随后本地阶段性 commit，不 push |
| 本批收口 | 完成 | `pnpm test` 5893/5893；`pnpm exec tsc --noEmit`、`pnpm build`、`pnpm repro:audit`、三套 smoke、`release:audit`、`release:check`、`quality:audit`、`integration:audit` 已通过；`tests/host/release-quality.test.cjs` 18/18；完整 `verify:release` 曾在测试子进程异常高 CPU 且 21 分钟无输出后终止，独立测试与各组成门禁均已分别复核通过 | T-6758 B1：三端位置、边缘吸附与独立开关 |

## 106f. T-6758 悬浮球 B1：三端位置与生命周期接线（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 三端 controller | 完成 | `index.ts` 改为按 `desktop/sidebar/mobile` 管理 controller；桌面挂 body，侧栏通过 `resolveHost` 跟随宿主重绘，手机保留旧 `floatingBallUi`/`fabElement` 兼容别名；卸载统一 destroy 全部 surface | T-6759：首层动作面板 |
| 独立位置持久化 | 完成 | `persistFloatingBallPosition(surface, position)` 按端侧写入 `floatingBall.position[surface]`；归一化配置继续 clamp edge/yRatio；设置/数据变更与 layout ready 触发全端 reconciliation | T-6759 |
| 独立开关边界 | 完成 | 桌面、侧栏、手机分别读取 `floatingBall.enabled[surface]`；旧 `fabEnabled` 继续投影为 mobile；桌面/侧栏设置控件留到 B3，避免在 B1 复制设置页动作模型 | T-6760 |
| 生命周期回归 | 完成 | `tests/floating-ball-ui.test.cjs` 新增三端独立 portal 断言，定向 17/17 通过；此前生产图负向注入证据保持有效 | 全量门禁 |
| 本批状态 | 完成 | `pnpm test` 5894/5894；TypeScript、`pnpm build`、`pnpm repro:audit`、release/quality/integration audits、`release:check`、宿主质量 18/18 与三套 smoke 均通过；阶段性提交后保持不 push | T-6759 B2：动作面板 |

## 106g. T-6759 悬浮球 B2：共享动作 executor 与溢出面板（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 共享动作目录与执行器 | 完成 | 新增 `src/floating-ball-actions.js`；内建动作、adapter、dock、plugin command 统一归一为成功/不可用/失败结果；异步 provider rejection 被捕获，复用现有 registry、provider 和宿主回调，不复制第二套动作定义 | B3：悬浮球设置治理 |
| 首层与更多面板 | 完成 | 新增 `src/floating-ball-panel.js`：首层最多 6 项、固定 more 入口、unsupported 隐藏、unknown 禁用、空配置切换器回退；桌面/侧栏/手机共享 DOM 语义，外部 pointerdown 与 Escape 关闭，面板销毁清理监听器 | B3：端侧动作排序/启用/导入导出 |
| 三端生产接线 | 完成 | `index.ts` 为 desktop/sidebar/mobile controller 装配共享动作目录与 executor；拖动释放命中动作按钮，更多动作右键/Shift+F10/点击入口均可打开；动作失败复用现有 `showMessage`，弹层打开统一 suspend 全部悬浮球 | B4：闲置、全屏、无障碍与真机验收 |
| 样式与门禁 | 完成 | `_04-fab-fullscreen-soft.scss` 增加边缘内收布局、44px 热区、侧栏紧凑、手机底部抽屉、主题变量与 reduced-motion；生产图 59→61，负向注入 ceiling=60 精确失败后恢复；新增动作/面板测试 10 项 | 继续 B3；不 push |
| 本批收口 | 完成 | `pnpm test` **5904/5904**（218 文件）；`pnpm exec tsc --noEmit`、`pnpm build`、readiness snapshot、production graph 61/61、release/quality/integration audits、宿主质量 18/18、定向浮动球 27/27 通过 | T-6760 B3 |

## 106h. T-6760 悬浮球 B3：设置治理（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 版本化配置模型 | 完成 | 新增 `src/floating-ball-settings-model.js`：端侧动作 CRUD/排序/默认恢复、状态投影、512 KiB UTF-8 容量限制、旧版快捷动作数组迁移；位置与临时展开状态不进入导出，失败导入保持原配置 | B4：闲置、弹层让位与无障碍 |
| 设置页接入 | 完成 | 新增独立“悬浮球”标签；桌面/侧栏/手机独立开关、动作添加/排序/首层（最多 5 个配置动作 + More）、可用性状态、恢复默认、导入/导出与 `aria-busy`；保留旧 `fabEnabled` 同步 | B4 |
| 门禁与构建 | 完成 | 悬浮球定向 **54/54**；全量 `pnpm test` **5934/5934**（220 文件）；`pnpm exec tsc --noEmit`；生产图 62 模块（审计上限 62）；生产构建 `index.js` 849624、`index.css` 163289、`package.zip` 392986，压缩 `index.js` 230708；`repro:audit` 3/3 | B4；真实 Android/窄侧栏验收按 BLOCKERS 后置 |

## 106i. T-6761 悬浮球 B4：闲置、让位与无障碍收口（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 闲置与边缘呈现 | 完成 | `floating-ball-ui.ts` 以 pointer/focus/idle 生命周期管理闲置计时，支持可配置透明度、延迟和左右边缘半隐藏；hover/focus/拖动自动唤醒，destroy 清理计时器与监听器 | 继续真实端验收 |
| 全屏/后台让位 | 完成 | controller 监听 `fullscreenchange` 与 `visibilitychange`，按 fullscreen/visibility/manual 三类原因聚合隐藏；弹层打开时关闭已展开面板并 suspend 全部端侧悬浮球；`index.ts` 接入 `behavior.hideOnFullscreen` | 真实桌面/Android 后置 |
| 无障碍与不可用态 | 完成 | hidden/suspended root 设置 `aria-hidden`、`inert`，触发器移出 tab 顺序并清除后代焦点；unavailable/执行中按钮禁用且不接受 focus/activate | 宿主主题与读屏验收后置 |
| 自律线校准 | 完成 | raw bundle 自律线按用户要求由 832 KiB 调至 896 KiB（ADR 0067）；512 KiB 归档硬上限与 256 KiB 压缩条目线不变；负向验证覆盖临时低 ceiling 精确失败与字节级还原 | 后续超过 896 KiB 时按同程序复核 |
| 门禁与构建 | 完成 | 定向悬浮球 UI/设置 **20/20**；`pnpm exec tsc --noEmit`；生产构建 `dist/index.js` 854745、`dist/index.css` 163775、`package.zip` 394250、压缩 `index.js` 231837；全量 `pnpm test` **5941/5941**；`repro:audit`、release/quality/integration audits、`release:check` 与三套 smoke 待本轮最终跑完 | B5：真实桌面/侧栏/Android 验收 |

## 106j. T-6762 悬浮球 B5：宿主边界与窄侧栏自动化验收（2026-09-21）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 侧栏宿主边界 | 完成 | `FloatingBallUi` 增加可选 `resolveBounds()`；侧栏由 `index.ts` 传入宿主 `getBoundingClientRect()`，球体左右边距、垂直位置和拖动落点均按宿主矩形计算；桌面/手机未提供边界时保持 viewport 语义 | 真实最窄侧栏度量 |
| resize 生命周期 | 完成 | controller 复用既有环境生命周期监听 `resize`，只更新 transform/位置样式，无轮询；destroy 同步移除 resize/fullscreen/visibility 监听 | 真实窗口缩放与宿主重建 |
| 自动化验收夹具 | 完成 | `tests/floating-ball-ui.test.cjs` 新增窄侧栏 280px 与 184px 宿主矩形、resize 后位置安全区断言；面板焦点恢复/Tab 陷阱回归；定向 UI **12/12**、面板 **8/8**，全量 **5944/5944**，TypeScript 检查通过；夹具结果不计作真机证据 | 真实最窄侧栏度量与 Android |

## 106k. T-6763 悬浮球 B6：滚动显隐与生命周期统一治理（2026-09-22）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| controller 滚动状态 | 完成 | `floating-ball-ui.ts` 新增独立 `scroll` hidden reason 与 `setScrollHidden()`；滚动向下隐藏、向上恢复，继续沿用 `aria-hidden`、`inert`、焦点清理和 modal/fullscreen 原因聚合 | 真实宿主滚动方向验收 |
| 配置与监听生命周期 | 完成 | `hideOnScroll=false` 不绑定监听；运行中切换会清理监听并清除 scroll reason；监听按事件驱动、以每个滚动目标保存基线，不轮询；destroy 完整解绑 | 三端宿主滚动矩阵 |
| 生产接线 | 完成 | `index.ts` 三端 controller 均传递 `behavior.hideOnScroll`；移除旧手机 document 级 touchstart/touchmove 与遗留 class 操作，避免配置失效和监听残留 | 无 |
| 自动化验收 | 完成 | `tests/floating-ball-ui.test.cjs` 新增禁用开关、宿主向下/向上滚动、modal 原因独立、关闭与 destroy 清理回归；定向 UI **13/13**，TypeScript 检查通过 | 全量门禁与构建 |

## 106l. T-6764 悬浮球 B7：完整开发收口（2026-09-22）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 设计与配置边界 | 完成 | ADR 0068 明确修订 ADR 0066：三端共享外观/行为，独立开关/位置/动作；侧栏固定 44 px，桌面/手机 44~64 px；边距 0~32 px；schema v1 增量兼容 `marginPx` / `xRatio`，自由停放位置仍不导出；UI 规格同步实际行为 | 无 |
| 稳定拖动目标与命中 | 完成 | 新增 `floating-ball-layout.js`：半环/竖列/网格回退、至少 44 px 热区、最多 6 项、缩减保留更多；拖动开始测量一次，pointermove 使用缓存几何和球体 transform；命中动作保持原停靠位置，仅空白松手保存 | 真实宿主触控验收 |
| 更多动作与共享组件入口 | 完成 | 更多面板搜索/分组/来源/状态/启用/空态/管理页签；打开时隐藏首层并合并其键盘入口；共享 builtin `home` 打开组件面板，新默认含 4 项且不覆盖原用户列表；面板定向 **22/22** | 无 |
| 外观行为设置与预览 | 完成 | 停靠边、垂直比例、尺寸、边距、透明度、闲置延迟、吸附/半隐藏/滚动/全屏/弹层、8~12 px 高级阈值；滑杆 input 仅预览、change 才保存；确认恢复外观默认；设置 UI 定向 **17/17** | 无 |
| 生命周期与宿主边界 | 完成 | 滚动端侧隔离及边缘恢复按钮；1500 ms busy watchdog 和新旧 completion 隔离；嵌套弹窗中启停端侧/改变让位偏好按当前实例释放；visualViewport、安全区及手机底部 48 px 预留；低于宿主动态层级，不轮询 | 真实宿主验收按 B-004 / B-005 后置 |
| 几何及宿主动态证据 | 完成 | 新几何 **8/8**（含 100 组拖动数据），新宿主 **9/9**（AST 提取真实 index 方法、真实 executor/registry/面板）；含提供方注销/恢复、command/dock 缺失、拖动/更多前刷新及环境关闭面板，断言真实首层和 More 按钮状态；几何三轮负向均精确失败并字节恢复；宿主十一轮内存变异分别破坏尺寸/让位/home/当前实例释放/管理页签/三类提供方可用性/拖动与更多前刷新/面板关闭，均目标失败且生产文件 SHA-256 不变 | 无 |
| 生产图与容量 | 完成 | 生产依赖闭包 63，门禁上限 62→64；临时 ceiling=62 精确失败并字节还原。最终 `index.js` **885369**、`index.css` **171606**、`package.zip` **405921**、压缩 `index.js` **240657** bytes；896 KiB / 512 KiB / 256 KiB 三线均有余量，无需扩容，后续仍可按用户授权及实际增量校准 | 无 |
| Chromium 真实交互夹具 | 完成 | **22 场景**：亮暗主题、184 px 侧栏、手机横屏、可视区缩小、三端面板来源搜索/Tab/Escape；连续 **100 次真实指针拖动**每次恰好一次执行、无意外点击/位置保存、保持单例；空白松手只保存一次；浏览器发现并修复移动抽屉可视区越界及搜索动作文案冲突。20 px 热区与越界样式负向注入精确失败，移除后 CSS 字节一致、正向再次通过 | 真实宿主/设备验收 |
| 本批完整验收 | 完成 | 最终完整 `pnpm verify:release` 通过：**5990/5990**（222 文件）、TypeScript、生产构建、可复现构建 **3/3**、release:check、quality/integration 各 **50/50**、宿主质量 **18/18** 与四套 UI smoke；独立 release:audit **50/50**。触控阈值、1500 ms watchdog、默认动作、设置持久化/恢复、面板关闭/首层合并及发布计数契约均已负向验证并恢复原字节 | 本地阶段提交，不 push；悬浮球本地编码范围已完成，仅真实 SiYuan/Android 环境验收后置 |

本轮一次完整验证中，未修改的搜索缓存键性能采样曾为 3.09 倍（门禁上限 3 倍）；未调整实现或放宽断言，独立完整性能套件复核 **7/7**、该项 2.50 倍，随后最终完整 `verify:release` 通过。记录真实采样波动，不以单独复核替代完整链路结果。
