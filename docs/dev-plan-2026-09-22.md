# 2026-09-22 当期开发账本

> 本轮先完成侧滑研究；晚间起完成并行提交对齐与 P1 收口，并按用户指令启动设置页 UI 优化。

## T-6809 搜索过滤条（2026-09-23 开发完成，候补池首项落地）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 过滤条 chips | 完成 | 查询时结果区顶部"全部/页签/收藏·最近/文档"四枚 chips；dataset 可见性过滤（纯展示层，不重搜索）；空查询自动移除并重置；aria-selected/role=tablist | 真机观感随部署验证 |
| 门禁 | 完成 | 6060/6060 + tsc + build + 四套冒烟全绿；计数五处对齐 6060 | — |

## 批次对齐记录（并行提交合并）

| 事项 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 远端 v0.30.1 对齐 | 完成 | rebase 到 origin/main（Task Horizon 联动 4 提交），ROADMAP 冲突解决（保留 Task Horizon 段 + §8.0.9） | 按 §8.0.6 用户参与项推集市/邀请 |
| 撞号重编 | 完成 | 本地侧滑 ADR 0070→0071、T-6772~6776→T-6777~6781（远端已消费原号） | 引用已全量替换，零残留 |
| P1 通用动作收口 | 完成 | 修复 `actionSurface` 未定义编译错误；补 `tests/floating-ball-generic-actions.test.cjs` 9 项；契约测试按目录扩容更新（默认集不变） | 随下个版本发布 |
| 计数对齐 | 完成 | 重建产物 bundle 900149/zip 411999；6029/6029（224 文件）五处引用一致 | 持续保持 |

## T-6782 悬浮球设置页深度美化（用户指令，§8.0.10 U1）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 现状审计 | 完成 | `_02-settings.scss` 悬浮球段为功能优先的朴素样式：虚线框预览、平铺控件、无卡片层级；真实球视觉基准在 `_04-fab-fullscreen-soft.scss`（主色球体双层阴影、动作卡 12px 圆角浮层阴影） | 已按 R7 token 美化 |
| 预览舞台质感 | 完成 | 舞台改"设备屏幕"内凹渐变+inset 阴影；snap 开启时左右感应区渐变提示；新增端侧 chip 徽标（唯一 DOM 增量，aria-hidden 装饰）；预览球去描边、对齐真实球双层阴影与按压缩放 | 已入构建 |
| 三区卡片层级 | 完成 | 预览/外观/动作统一 `%sw-fb-panel` 卡片（12px 圆角+微阴影），标题主色竖条 `%sw-fb-heading`；端侧开关卡片化+hover；输出值改主色胶囊；range/checkbox 主题化 accent；高级折叠 hover；动作行 hover+序号圆徽 | 已入构建 |
| 契约保护 | 完成 | 全部 `data-control`/class 钩子保留；`pnpm test` 6029/6029、tsc、四套 UI 冒烟（含浮球 Chromium 22 场景）全绿；quick-actions 契约负向验证通过（注入错误目录→精确失败→恢复） | 真实桌面/主题验收随 T-6771 批后置 |

## T-6783 设置页其余标签页卡片语言跟进（已完成，见下方完成记录）


| 任务 | 计划内容 | 进入条件 | 退出证据 |
|---|---|---|---|
| T-6783 | 存储用量、文档集、导入导出等标签页复用 T-6782 的卡片语言 | T-6782 验收通过 | 各标签页样式统一、既有设置契约测试保持绿 |

## 自由停放与外拨展开评估（2026-09-22 用户需求，仅评估不实施）

用户需求：悬浮球可挪动至任意位置（不固定贴边）尝试一段时间（保证时间长度）；从球往外滑动可展开动作按钮，且滑动方向要有足够空间放下全部按钮。

### 评估结论：可行，且地基大半已在

| 需求点 | 结论 | 代码证据 |
|---|---|---|
| 挪动至任意位置 | ✅ 已随 B7 交付 | `floating-ball-model.js` 位置含可选 `xRatio`；`floating-ball-ui.ts positionFromPointer` 在 `snap=false` 时写入归一化 xRatio；位置经 `onPositionChange` 持久化，窗口 resize 按比例恢复；设置页"拖动后吸附边缘"开关已注明"关闭后可在可视区域自由放置"（i18n floatingBallSnapTip） |
| 保证一定时间长度 | ✅ 无需代码 | 自由位置本就长期持久化、重启保留，不存在"一会儿回弹"；"尝试一段时间"作为试验观察承诺登记（T-6784），不设自动回撤 |
| 滑动方向空间放下全部按钮 | ✅ 布局层已有降级链 | `floating-ball-layout.js`：`arcTargets/verticalTargets/gridTargets` + `fits()` 空间检查，放不下自动换排布；缺"滑动方向偏好"输入（当前方向只认左右 edge） |
| 往外滑动展开面板 | ⚠️ 新触发器，需开发 | 现状：拖动中按钮弹出+拖到按钮松手执行（已有）；但松手未命中时球落位、按钮消失——没有"松手后面板保持展开"（sticky targeting）态；无速度/方向判定 |

### 待办（T-6784～T-6787，未开工）

| 任务 | 内容 | 进入条件 | 退出证据 |
|---|---|---|---|
| T-6784 自由停放试验确认 | 登记试验观察项（不设回撤期限）；验证旋转/resize 后 xRatio 比例恢复、自由位置不触发半隐与边缘吸附、闲置降透明度在自由位置的观感 | 无（能力已在，纯验证+文档） | 验证清单勾选；设置文案如发现歧义再小修 |
| T-6785 方向感知布局 | `layoutFloatingBallActions` 增加 direction 偏好输入（上下左右），滑动方向优先，空间不足沿既有 fits 链自动降级 | T-6786 立项 | 纯函数门禁：各方向×各位置的排布矩阵；负向验证（删降级链注入精确失败） |
| T-6786 外拨展开触发器 | pointerup 速度窗口判定（约 80ms 均值）：快速外拨→球回原位并进入 sticky targeting（面板保持展开，点击按钮执行、外部点击/Escape 关闭）；慢速拖动→移动球（现状不变）。桌面先行 | T-6785 完成；**手机端进入条件=T-6778 `data-prevent-swipe` 已合入**（球贴边时手指起点可能落进宿主 3.8.5 的 12px 侧滑激活区，见 ADR 0071） | 判定器纯函数测试（速度/方向/位移边界）；Chromium 拖动回归扩展外拨场景；与点击/右键/拖动四路径互斥验证 |
| T-6787 设置与试验复盘 | 外拨展开设置开关（端侧独立）；试验期结束后按用户反馈复盘是否调整判定阈值或默认值 | T-6786 完成 | 设置项入设置页与导入导出；复盘记录 |

### 风险与缓解

- WebView pointermove 事件频率不稳 → 速度用时间窗口均值，不用最后两帧；
- 外拨与搬运误判 → 外拨成立时球回弹原位（复用 applyPosition）；位移与速度双条件判定，阈值进配置可调；
- sticky 态与滚动显隐/闲置降透明互斥 → targeting 态已在 hiddenReasons 豁免清单，沿用；
- 手机端宿主侧滑冲突 → 依赖 T-6778 先行（ADR 0071 结论），不抢跑。

## T-6788 悬浮球撤出侧栏端（2026-09-23 用户指令，ADR 0072）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 挂载收缩 | 完成 | `updateFloatingBallVisibility` 桌面只挂 desktop；`FLOATING_BALL_UI_SURFACES=["desktop","mobile"]` 新常量；旧 sidebar 控制器走既有清扫销毁 | 随下版发布 |
| 设置页双端化 | 完成 | 端侧开关与编辑表面选择器迭代 UI 清单；schema 保留三面（`FLOATING_BALL_SURFACES`）兼容旧配置/导入包 | — |
| 契约更新 | 完成 | host 契约=sidebar 永不挂载+旧控制器清扫；设置契约=2 开关+2 表面选项；负向验证双向通过（注回 sidebar 挂载→红、开关注回三面→红、还原→绿） | — |
| 文档 | 完成 | ADR 0072、ui-spec §2 侧栏行标记撤出 | 真机验收随 T-6771 批后置 |

## 悬浮球增量功能调研（2026-09-23 完成，报告 `docs/floating-ball-next-research-2026-09-23.md`）

| 事项 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 思源源码盘点 | 完成 | 克隆 `research-clones/siyuan` 核对内核路由/插件 API/命令系统/移动桥/手势契约；最大发现=3.8.3+ 官方 `globalCommand`（能力检测接入，不抬 minAppVersion） | T-6789~T-6792 近批 |
| 竞品分类学 | 完成 | FV/AssistiveTouch/OPPO/三星/小米华为 + 桌面参照，六维度聚合；不适配项（截屏/分屏/应用推荐/背面敲击）明确不做 | 候选池随反馈取用 |
| 任务分解 | 完成 | 近批 T-6789（命令底座）→T-6790（首批命令动作）→T-6791（一键同步）→T-6792（模板插入）；次批 T-6793~T-6795；评估池另记（ROADMAP §8.0.11） | 按批次推进 |

## T-6789/T-6790 宿主命令动作底座与首批命令（2026-09-23 开发完成，§8.0.11 近批）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 源码取证 | 完成 | 克隆 `research-clones/siyuan`：`globalCommand(command, app, range?)` 于 `plugin/API.ts` createAPI 导出（3.8.3+）；桌面分支 45+ 命令、MOBILE 分支仅 9 命令（fileTree/outline/bookmark/tag/inbox/backlinks/mainMenu/globalSearch/recentDocs） | 真机验证随 T-6305/T-6771 批 |
| 底座 | 完成 | 新动作种类 `global`：能力检测（无 globalCommand→unsupported，不抬 minAppVersion）、`runHostCommand` 桥、executor `invokeGlobal` 分支、picker 候选"思源命令"分组、设置/悬浮球目录接线 | — |
| 首批 9 命令 | 完成 | outline/bookmark/tag/inbox/backlinks/recentDocs（桌面+移动，MOBILE 分支实证）；recentClosed/riffCard/editReadonly（仅桌面） | 其余命令候补池 |
| 门禁 | 完成 | 目录形状+端侧声明断言、executor 派发/桥缺失降级断言；负向验证双注入（删 global 分派→2 红、恒 supported→1 红）后恢复全绿；6033/6033（224 文件）+ tsc + build + 浮球 Chromium 22 场景 | — |

## T-6791 一键同步（2026-09-23 开发完成，§8.0.11 近批）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 端点取证 | 完成 | 克隆 `router.go:367`：`/api/sync/performSync`（CheckAdminRole+CheckReadonly）；只读/发布模式与同步互斥由内核拒绝 | 真机同步语义随 T-6305 批 |
| 动作接线 | 完成 | builtin `sync-now`（桌面/侧栏/移动，mobileSafe）；executor `sync-now` 分支；宿主 `syncNow()` 直调内核（60s 超时适配大库）+ 完成/失败 toast；`fetchKernelJson` 增超时参数 | — |
| 契约夹具 | 完成 | 契约门禁拦截未登记端点；`gen-api-contract.cjs` 修复两处陈旧（NON_KERNEL 补 `/api/0/buckets`、上游根改由 schema 参数推导）后从 3.8.5 克隆重新生成（26 端点含 performSync） | — |
| 门禁 | 完成 | 目录契约更新（11 builtin，默认集不含 sync-now）+ executor 派发/降级断言；负向验证（注入恒 unavailable→1 红）后恢复；6034/6034 + 四套冒烟全绿 | — |

## 调研-吸收循环第 1 轮（2026-09-23 启动，用户指令，ROADMAP §8.0.12）

| 事项 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 循环立项 | 完成 | §8.0.12 固化五步流程（搜索→筛选→报告→立项开发→复盘下一轮）；与现役批次并行不阻塞 | R2 触发=R1-A 消化完毕 |
| R1 搜索 | 完成 | 三路并行返回：Obsidian 生态（AQS/Vertical Tabs/QuickAdd 等 9 插件深读）、思源集市（bazaar 索引 516 插件筛相关 15+，深读 5）、广域同类（浏览器/编辑器/OS 导航 5 方向） | 报告 `docs/research-cycle-r1-2026-09-23.md` |
| R1 吸收排名 | 完成 | 交叉去重后立项 R1-A 批 **T-6799~T-6804**（统一索引/数字槽+文档集快照/光标恢复/搜索语法/悬浮球场景预设/动态收藏组），ROADMAP §8.0.14；未采纳清单防反复 | 排在 §8.0.11 近批后消化 |

## 皮肤层方向（2026-09-23 用户指令，ADR 0073，ROADMAP §8.0.13）

| 事项 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 机制验证 | 完成 | 小驴打卡 `src/ui/tokens.scss`：根类作用域重定义 `--b3-*` 别名 + `data-appearance`/`data-palette` 多色板，已在集市插件实证 | 复用同机制 |
| 设计原型 | 完成 | `docs/design/skin-prototypes.html`：真实生产 DOM+dist/index.css 四栏对照（融合基线/苹果液态玻璃/暗夜玻璃/暖纸墨水），headless Edge 截图 `skin-prototypes.png`；三套皮肤仅换变量+质感层，零组件选择器改写 | 用户过目后定首批落地色板 |
| 立项 | 完成 | T-6796 架构 / T-6797 苹果风格 / T-6798 色板族，排在 §8.0.11 近批后 | 渐进开发 |

## T-6792 插入模板（2026-09-23 开发完成，§8.0.11 近批收官）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 源码取证 | 完成 | 克隆：清单=`/api/template/manage` {action:"list"}→`[{path,isDir}]`；渲染=`/api/template/render` {id:文档根块, path, mode:"editorInsert"}→`data.content`（hint/extend.ts:688 同参）；官方 `getActiveEditor`+`protyle.insert` 完成光标写入；两端点均在 apicontract/schema | 真机插入随 T-6305 批 |
| 动作接线 | 完成 | builtin `insert-template`（桌面+移动，侧栏无编辑器故排除）；executor 分支；宿主 `openTemplatePicker`（Dialog 列表）→ `insertHostTemplate`（render+insert）；无活动编辑器/空模板目录/渲染失败三路降级提示 | — |
| 契约与门禁 | 完成 | 契约夹具再生成（28 端点含 template/manage+render）；目录契约更新（12 builtin）+executor 派发/降级断言；负向验证（注入恒 unavailable→1 红）后恢复；6035/6035+四套冒烟全绿 | — |

## T-6799 切换器统一索引（2026-09-23 开发完成，§8.0.14 R1-A 首项；"只看有改动"过滤待下轮）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 纯函数层 | 完成 | `search-model.js` 新增 `buildUnifiedSections/scoreUnifiedTitle/normalizeUnifiedQuery`：计分（前缀 3>包含 2>分词 1）+ 分区限额 + 已开排除；并入同域模块不增生产图节点 | — |
| UI 接线 | 完成 | `applySearch` 挂 `renderUnifiedSections`：查询时收藏/最近关闭/文档集命中分区渲染于页签与全库结果之间（`.sw__unified`），空查询移除；激活分发 `activateUnifiedItem`（jumpToFavorite / openTab·mobileOpenDoc / restoreDocumentSetFromHome） | 键盘遍历随真机复核 |
| 门禁 | 完成 | 6 项纯函数测试并入 search-model.test.cjs；负向验证（注入计分恒 0→4 红）后恢复；6041/6041 + tsc + build + 浮球 Chromium 22 场景全绿；计数五处对齐 | 真机复核随 T-6305 批 |

## T-6800 文档集工作区切换（2026-09-23 开发完成，§8.0.14 R1-A）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 会话语义 | 完成 | settings 新增 `documentSetsAutoSave`（默认开）与 `documentSetsCurrentId`（≤64 字符有界）；恢复成功后标记当前集 | 真机切换体验随 T-6305 批 |
| 离开即快照 | 完成 | 切换目标集前把当前打开现场 `upsertDocumentSet` 回当前集（自动保存可关；无当前集/目标相同/无打开文档时静默跳过） | — |
| 循环切换 | 完成 | builtin `cycle-doc-set` 动作（桌面+移动）；纯函数 `pickNextDocumentSet`（环绕+单集/空列表拒绝，14 项 document-sets 测试通过） | — |
| 门禁 | 完成 | 契约更新（13 builtin；kernel-widgets 接线契约改钉"恢复成功即标记当前集"两行，负向验证：删标记行→1 红→恢复绿）；6044/6044 + 四套冒烟全绿；计数五处对齐 | — |

## T-6799b 最近"只看有改动"过滤（2026-09-23 开发完成，T-6799 收口）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 纯函数层 | 完成 | `recent-closed.js` 新增 `formatChangedWindowStart`（7 天窗口起点，14 位时间戳）与 `entryChangedWithin`（字符串比较；无更新信息的条目在过滤开启时隐藏——无法证明即不算） | — |
| UI 接线 | 完成 | 历史下拉顶部"只看有改动"开关（aria-pressed，下拉生命周期内缓存 updated 批量查询结果）；`fetchDocUpdatedMap` 按 32 条分块回查 blocks.updated | 真机过滤体验随 T-6305 批 |
| 门禁 | 完成 | 1 项纯函数测试；SQL 门禁新豁免登记（IN 列表 BLOCK_ID_RE 校验+32 条分块，天然有界）；6045/6045 + 四套冒烟全绿；计数五处对齐 | — |

## T-6801 重开滚动现场（2026-09-23 开发完成，§8.0.14 R1-A）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 捕获时机 | 完成 | 关闭页签前（handleCloseTab）与经历史切换离开前（openHistoryEntry）记录 `.protyle-content` 滚动比例；FIFO 50 份上限；会话级内存不持久化 | 跨会话浏览位置属宿主能力，不伪造 |
| 回卷 | 完成 | 桌面重开成功后有界等待渲染（30×50ms 自续 setTimeout）按比例回卷；纯比例计算 `computeScrollRatio/planScrollRestore` 入 recent-closed.js（19 项测试通过，短文档永不滚动） | 移动端回卷未验证，随真机批 |
| 门禁 | 完成 | 6046/6046 + 四套冒烟全绿；计数五处对齐；raw bundle 915415 距 896KiB 自律线仅余 ~2KB——**下批功能须先做包体复核/ADR 校准** | ⚠ 包体线告急 |

## ADR 0074 + T-6802 搜索语法与上次选择置顶（2026-09-23 开发完成，§8.0.14 R1-A）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| ADR 0074 包体校准 | 完成 | raw 自律线 896→960 KiB（+64KiB 步进，先例 0062/0067）；512KiB zip 硬上限与 256KiB 压缩条目线不变；`second-round-widgets` 常量钉死断言同步更新 | raw 实测 917491/983040，余量 ~65KB |
| 语法解析 | 完成 | `parseSearchQuery/formatCleanQuery/matchesParsedQuery` 入 search-model：`"精确短语"`（必须子串命中）、`-排除词`（命中即剔除）、普通词 AND；分词用 String.match（B-003 误报教训） | — |
| 三处应用 | 完成 | ①本地页签 filterCards 运算符匹配；②文档结果客户端预过滤 + 内核查询清洗（剔除排除项再发请求，缓存键用清洗后查询）；③统一索引分区 passesVeto | — |
| 上次选择置顶 | 完成 | `lastPickedByQuery`（会话级 FIFO 32）；`planDocResultsPage` 增 promoteId 参数置顶；分页契约测试同步更新 | — |
| 门禁 | 完成 | 6047/6047 + 四套冒烟全绿；计数五处对齐 | — |

## T-6803 悬浮球场景预设（2026-09-23 开发完成，§8.0.14 R1-A）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 模型层 | 完成 | `FLOATING_BALL_PRESET_MAX=8`；`normalizeFloatingBallPresets`（有界+去重+畸形丢弃）；`saveFloatingBallPreset`（同名覆盖，快照=主点击+两端动作列表）；`applyFloatingBallPreset`（拷贝布局，外观保持全局）；`removeFloatingBallPreset`（连带清 current 指向）；`pickNextFloatingBallPreset`（环绕） | 与文档集联动待反馈 |
| executor+宿主 | 完成 | builtin `cycle-ball-preset`（桌面+移动）；executor 分支；宿主 `cycleBallPreset`（环绕应用+气泡提示场景名） | — |
| 设置页 UI | 完成 | 悬浮球标签新增"场景预设"卡片：名称输入+保存当前；预设列表（当前集高亮/应用/删除）；presets 随配置导入导出与 512KiB 预算自然纳管 | — |
| 门禁 | 完成 | 模型 2 项新测试（覆盖/应用保外观/环绕/容量 8/畸形丢弃）+executor 派发断言；负向验证：6049/6049 + tsc + build + 浮球 Chromium 22 场景全绿；计数五处对齐 | — |

## T-6804 标签智能分组（2026-09-23 开发完成，§8.0.14 R1-A 收官）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 口径收敛 | 完成 | 执行口径"不引入任意 SQL"→ 放弃用户可写 SQL 的书签+模式，收敛为"标签智能分组"：用户只从内核 getTag 清单选标签，查询由插件参数化构造（LIKE 通配符/引号剥离，LIMIT 钳制 20） | 反链驱动组候补池 |
| 纯函数层 | 完成 | `favorite-actions.js`：`normalizeFavoriteSmartGroups`（4 组上限/同名去重/通配符剥离）、`buildTagSmartGroupQuery`、`projectTagSmartGroupEntries`（BLOCK_ID 校验+去重+限额） | — |
| 接线 | 完成 | settings 新增 `favoriteSmartGroups`；收藏面板动态分区（壳体先渲染+按需拉取+只读投影条目点击跳转）；设置页收藏标签智能分组管理器（名称+标签下拉来自 getTag，增删即改） | — |
| 门禁 | 完成 | 2 项纯函数测试（含消毒/边界/投影校验）；6051/6051 + 四套冒烟全绿；计数五处对齐 6051 | 真机分组体验随 T-6305 批 |

## T-6796~T-6798 皮肤层架构与三套皮肤（2026-09-23 开发完成，§8.0.13 全批收官）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6796 架构 | 完成 | `settings.skin`（`normalizeSkin` 白名单，默认 fusion）；body 只放 `data-sw-skin` 标记，变量覆盖经 `:is()` 限定在速切根容器；onload 应用/变更即刷/onunload 移除；`_10-skins.scss` 入清单（层叠最后） | 对比度采样随 T-6697 夹具扩展 |
| T-6797 苹果风格 | 完成 | 液态玻璃 Light：半透明表面+blur+saturate、iOS 蓝强调、渐变壁纸；结构自原型移植 | 用户真机观感反馈 |
| T-6798 色板族 | 完成 | 暗夜玻璃（Dark Glass）+ 暖纸墨水（Paper）同批落地；皮肤 id 白名单校验 | 反馈驱动扩展 |
| 门禁 | 完成 | skin 注册表白名单/默认断言；皮肤接线源码契约（body 标记/卸载清理/三皮肤作用域/变量覆盖）；6053/6053 + tsc + build + 四套冒烟全绿；计数五处对齐 | — |

## T-6793/T-6794 端侧体验扩展（2026-09-23 开发完成，§8.0.11 次批收官；T-6795 经复核由 B7 覆盖）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6793 抛独立窗口 | 完成 | builtin `throw-window`（仅桌面，openWindow 为桌面专属 API）；活动文档根块校验 + openWindow 能力检测；克隆 API.ts:53-82 签名取证 | 真机多窗口随 T-6305 批 |
| T-6794 收起键盘 | 完成 | builtin `hide-keyboard`（仅移动）；Android `JSAndroid.hideKeyboard` 守卫调用（keyboardToolbar.ts:1272 同款）；iOS 无桥如实不可用 | iOS 震动/回桌面候补池 |
| T-6795 复核 | 完成 | B7 已实现 visualViewport 边界重定位（冒烟"visual viewport"场景通过）——键盘引起的视口收缩已由同一机制覆盖；事件级 keyboard-show/hide 细化记候补池 | 关闭该子项 |
| 门禁 | 完成 | 目录契约更新（16 builtin，端侧声明断言）+ executor 派发断言；6054/6054 + 四套冒烟全绿；计数五处对齐 | — |

## 调研-吸收循环第 2 轮（2026-09-23 启动，R1-A 消化完毕触发）

| 事项 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| R2 启动 | 完成 | 三路并行：①CJK/拼音搜索专项 ②导航交互复扫 ③思源生态增量复查 | 汇成 `docs/research-cycle-r2-2026-09-23.md` |
| R2 吸收排名 | 完成 | 报告落档；**核心=拼音首字母/全拼匹配**（内核官方拒绝做=差异化空白；vendor tiny-pinyin MIT 3.5KB）；生态信号：番茄工具箱转专有（开源窗口）、熊猫导航跳转进原生历史 | 立项 §8.0.15：T-6805~T-6808 |
| T-6806 跳转栈后退/前进 | 完成 | 原生 backStack 集成取证为不可行（forwardStack 模块私有+活 protyle 契约）→ 降级为自有跳转栈：FIFO 50、suppress 抑制回跳自捕获、捕获点=历史/收藏/统一索引跳转离开前、空栈 toast；executor 派发/空栈断言；6059/6059 全绿 | 真机跳转连续性随 T-6305 批 |

| 吸收批 | R2-A 剩余两项完成 | **T-6807 零词条工作台**（空查询呈现场景预设/文档集/智能分组 chips，预设即点即用、文档集走既有恢复链、智能分组按需拉取；接线契约+负向验证）；**T-6808 frecency** 降级为候补池（"上次选择置顶"已覆盖其主要价值，频率加权待使用数据支撑） | R2 全部消化完毕 |

## T-6805 拼音辅助匹配（2026-09-23 开发完成，§8.0.15 R2-A 首项）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| vendor | 完成 | tiny-pinyin 1.3.2（MIT，Creeper 2017）dist 三文件+patcher+LICENSE 入 `src/vendor/tiny-pinyin/`（~15KB raw）；实测 convertToPinyin/parse API | 供应链钉死，不再跟 npm |
| 匹配引擎 | 完成 | `search-model.js`：懒加载引擎+4000 条表单缓存；`pinyinTitleHit` 逐字回溯（首字母 1 字符/全拼整段双分支，支持首字母+全拼混输如 cpin→产品路线图）；纯 ASCII 查询触发 | — |
| 接线 | 完成 | `filterOpenTabs` options.pinyinMatch（本地页签）；`filterCards` titleMatch 并列拼音命中（页签卡片）；settings `pinyinMatch` 默认开+行为标签开关+双语 | — |
| 门禁 | 完成 | 7 项行为测试（首字母/全拼/混输/开关关闭/汉字兜底/无关词不误配/空标题不抛错）；6058/6058 + tsc + build + 四套冒烟全绿；计数五处对齐 6058 | 真机输入法实测随 T-6305 批 |

## v0.31.0 发布准备（2026-09-23 完成，本地就绪；tag/推送待用户确认）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 版本 bump | 完成 | package.json + plugin.json 0.30.1→0.31.0；README 徽标/发布行（zh "正式发布"措辞满足 T-6744b）、ROADMAP 基线同步 | — |
| 双语 changelog | 完成 | v0.31.0 条目：悬浮球 7 项、切换器 4 项、最近过滤、文档集工作区、皮肤层、行为变更说明 | — |
| verify:release | 完成 | 全链通过：tsc、可复现构建审计、全量测试、release:check、quality/integration 审计、四套 UI 冒烟（浮球 Chromium 22 场景） | — |
| 待用户 | 等待 | tag `v0.31.0` + push + Release workflow + 集市同步 | 用户确认后执行 |
| 本地部署 | 完成 | v0.31.0 已复制部署至 `D:/小飞驴的SIYUAN` 与 `D:/思源测试` 双工作区插件目录（plugin.json 验证 0.31.0）；**需重启思源（或禁用再启用插件）后生效** | 用户真机体验：拼音匹配/统一索引/工作台/场景预设/皮肤层 |

## T-6783 设置页卡片语言统一（2026-09-23 开发完成，§8.0.10 U2 收官）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 卡片语言 | 完成 | `.sw-settings__panel` 施加统一卡片样式（12px 圆角+边框+微阴影）+ 分节标题主色竖条——纯 CSS，全部标签页零 DOM 改动；悬浮球标签豁免外层卡片（自带内层卡片避免双重边框） | 真机观感随部署验证 |
| 门禁 | 完成 | 6060/6060 + tsc + build + 冒烟全绿（settingsTitle 对比度采样不受影响） | — |

## T-6810 Essentials 常驻层（2026-09-23 开发完成，候补池高价值项落地）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 会话字段 | 完成 | settings `documentSetEssentials`（rootId 白名单校验、去重、≤10）；文档集标签新增管理器（添加当前打开页签/逐条移除/全部清空，自含重渲染） | 真机体验随部署 |
| 恢复链接入 | 完成 | restore 成功后 `openDocumentSetEssentials` 自动补开（已打开跳过，顺序打开保 tab 顺序） | — |
| 门禁 | 完成 | normalize 有界/去重/畸形断言；6063/6063 + tsc + build + 四套冒烟全绿 | — |

## v0.32.0 发布（2026-09-23 完成，tag+push+workflow 全绿）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 发布 | 完成 | main 1ae8b90→971e4c3 已推送；tag `v0.32.0`；Release workflow 1m24s success；[Release 资产](https://github.com/ai68298100/siyuan-speed-switch/releases/tag/v0.32.0) package.zip | 重启思源生效 |
| 本地部署 | 完成 | 双工作区已同步 v0.32.0 构建（plugin.json 验证 0.32.0） | 用户真机体验：拼音/过滤条/工作台/图标目录/Essentials/跳转栈/并排打开/场景联动/皮肤 |
| 遗留 | 候补池 | Essentials 管理器悬停预览（开发中撤回，重新实现时含浮层定位）；反链组；drag-to-target | 随反馈 |

## T-6777 思源 3.8.5 侧滑与悬浮球冲突复核


| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 上游源码锁定 | 完成 | 对照官方 tag `v3.8.5`（`60a4387cc1ce0cd0c7a61c2343e5a6a020758c0a`）与 `v3.8.4`（`9f775e8a12daef8255556097396f9b2739078892`），master 在检索时与 v3.8.5 同步 | 实现时保留 tag 链接和可复现命令 |
| 侧滑行为分析 | 完成 | `touch.ts` 普通页面激活距离 12 px；提交条件为屏宽三分之一或 32 px+0.3 px/ms；document 级 touch listeners；v3.8.5 发布说明列出 #19644/#19645 | 不改宿主阈值，先处理事件归属 |
| 原悬浮球方案评估 | 完成 | 当前 Pointer Events/slop/`touch-action:none` 只管理插件状态；portal 未设置 `data-prevent-swipe`，同一触摸可进入两套状态机 | 方案仍可行，补宿主 marker |
| 兼容边界 | 完成 | v3.8.1～v3.8.5 源码可见 `data-prevent-swipe`，v3.8.0 未发现该契约；项目最低版本仍为 3.8.0 | 先做 3.8.5/3.8.4 真机，再决定 3.8.0 局部兼容层或版本下限 |
| 文档与取舍 | 完成 | 新增研究报告、ADR 0071，并把推荐路线同步到 ROADMAP §8.0.9 | 用户确认方向后进入 T-6778 |
| 本轮实现 | 未开始 | 按用户要求没有修改 `src/`、没有构建或发布 | T-6778：只加 marker 的最小实现与契约测试 |

## 后续任务（仅计划，尚未开工）

| 任务 | 计划内容 | 进入条件 | 退出证据 |
|---|---|---|---|
| T-6778 | 移动 portal 根/触发按钮/恢复按钮加入 `data-prevent-swipe`；保留现有 Pointer Events、CSS 和配置 schema | 用户确认采用方案 A | 生产源码接线、静态/宿主契约测试、负向验证 |
| T-6779 | v3.8.5 touch 判定夹具：有 marker 时整轮不触发侧栏，无 marker 时保留宿主行为；验证动作面板竖向滚动和卸载清理 | T-6778 完成 | 夹具正向通过；删除接线、marker 改 class、不可达节点三种注入精确失败并字节恢复 |
| T-6780 | 真实思源桌面/Android 验收：左右边缘、旋转、键盘、更多面板、侧栏开关与连续拖动 | 需要用户设备/空闲时间，受 B-004/B-005 后置 | 设备版本、录屏/截图、动作计数和零误触记录；浏览器夹具不代替真机 |
| T-6781（条件） | 若 3.8.0 仍需支持且 marker 不生效，增加移动 portal 局部 touch 冒泡隔离，或提交抬升 `minAppVersion` 的独立决策 | T-6780 发现真实兼容缺口 | 长按、面板滚动、pointer/touch 双流、卸载重载全部通过；否则不合入 |

## 门禁口径

新增或修改契约测试时，按 `docs/gate-audit-checklist.md` 做外部状态检查和负向验证；只跑“改完仍通过”不算证据。实现前不扩大包体线、不新增依赖、不改变用户侧滑设置和悬浮球配置迁移语义。

## T-6811～T-6824 工作上下文导航路线（2026-09-23，ADR 0076）

| 任务 | 状态 | 具体步骤 | 退出证据 / 下一步 |
|---|---|---|---|
| T-6811 | 完成 | 将调研 R4 的 15 个方向映射到现有搜索、文档集、动作 registry、三端生命周期和 Agent 能力；固定五阶段路线与暂缓边界 | `ROADMAP.md` §8.0.16、ADR 0076、研究报告 R4；继续 T-6812 |
| T-6812 | 完成 | 在现有 `search-model.js` 增加 `buildNavigationResultModel`；按 tabs/unified/opened/global/actions 排序；统一来源、稳定键、分组数量、总量与截断计数；统一索引渲染经过该模型，执行仍走原有激活/registry | `tests/navigation-model.test.cjs` 三类纯函数测试；生产图不新增模块；继续 T-6813 |
| T-6813 | 计划 | 将搜索评分拆成可解释字段（来源、标题、路径、拼音、最近度、过滤命中），增加 health snapshot 和慢请求/降级原因 | 先复用 `search-session.js` 与现有搜索缓存；完成后再改结果 UI |
| T-6814 | 计划 | 取证 SiYuan 反链/提及/路径 API，建立 related-content 只读适配器和有界模型 | API 证据、权限/空数据/超时测试；无稳定 API 则记录阻塞 |
| T-6815 | 计划 | 将文档集、Essentials、跳转栈、场景预设组合成分层工作现场快照 | restored/skipped/failed 回执、旧配置保留、取消可恢复 |
| T-6816 | 计划 | 为搜索/关联结果增加只读首块预览和焦点回收 | 预览不持有编辑器写入权限，桌面先行，移动端后验收 |
| T-6817 | 计划 | 在既有标签智能分组之上增加参数化动态收藏组/查询型文档集 | 不开放任意 SQL，结果上限和导入迁移门禁先行 |
| T-6818 | 计划 | 快速捕获流程：输入→目标→预览→受控写入→回执 | 复用日记、模板、收集箱 bridge；每步声明副作用与失败原因 |
| T-6819 | 计划 | 将快捷动作 registry 固化为 provider protocol/SDK 元数据 | 保持未知提供方可见，展示与执行分离，卸载可恢复 |
| T-6820 | 计划 | 键盘优先：命令前缀、数字直达、最近/固定动作、结果类型快捷键 | 桌面先验收，移动端保留触控等价路径 |
| T-6821 | 计划 | 本地 deep link/剪贴板入口，接入快速捕获或指定文档打开 | URL/文本消毒、来源标记、用户确认；不保存凭据 |
| T-6822 | 计划 | 移动端独立布局、底部面板、侧滑所有权和首帧稳定壳继续收口 | T-6778/T-6779 后接 T-6780 真机；浏览器夹具不代替 Android |
| T-6823 | 计划 | 在 ADR 0073 皮肤层之上加入紧凑/舒适密度与三端布局档位 | 44px、焦点、对比度、reduced-motion 门禁不回退 |
| T-6824 | 计划 | 将工作区快照、动作目录、皮肤、动态组做版本化迁移包 | 导入失败原子回滚、冲突可解释、旧配置保留 |

本阶段执行顺序：先完成 T-6812，再做 T-6813；T-6814～T-6816 属于工作上下文批；T-6817～T-6821 属于动作生态批；T-6822～T-6824 属于表面和迁移批。D14 Agent/语义检索在这些证据完成前只保留评估，不进入默认入口。门禁只保留用户、宿主兼容、数据安全、无障碍/触控、回归和集市合规需要；模块数、测试数、包体自律线因合理增量触发时按实测校准。

## 调研-吸收循环第 5 轮（2026-09-23 完成，用户指令触发，ROADMAP §8.0.17）

| 事项 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| R5 三路调研 | 完成 | ①Obsidian 生态+Notion/Craft ②启动器/浏览器/编辑器/手机广域（含 R1 盲区：Kvaesitso 等 Android 启动器）③思源生态+新兴笔记软件；报告 `docs/research-cycle-r5-2026-09-23.md` | 立项 R5-A |
| 宿主 API 核实 | 完成 | 插件 RPC 网关（POST /api/plugin/rpc/:name + WS）、/api/history/ 域 13 端点、openTab 新选项（keepCursor/removeCurrentTab/afterOpen/doc.mode）、Plugin.addBreadcrumbButton 均对上游 master 源码逐项确认 | T-6826/T-6828/T-6831 有据可依 |
| 定位强化 | 完成 | ①生态出口：速切只读能力可经 RPC 网关被外部 Agent/MCP 消费（T-6832 评估池，依赖 D6/D14）②开源窗口：sy-quickswitch 停更闭源+番茄转专有，README 卖点候补 | 随下个版本窗口评估 |
| 竞品动态 | 完成 | Panda Navigation（2026-09 活跃）定位最重叠但其前进后退/滚动收起/预设已分别被 T-6806/B6/T-6803 先行交付，差异化成立不响应；3.8.6 反链强化→T-6814 立项时对齐差异化 | 持续观察 |
| R5-A 立项 | 完成 | T-6825 结果身份锚定（排 T-6813 后）、T-6826 openTab 新选项、T-6827 保存的搜索命令化、T-6828 history 域接入、T-6829 恢复版本历史、T-6830 打开策略层、T-6831 面包屑入口；T-6832 RPC 网关入评估池 | 按 §8.0.17 归位推进 |
| 执行原则落档 | 完成 | 承接 Codex 会话用户反馈：大胆推进北极星、数量类门禁只作观测、需真机项登记后置（R5 报告 §0） | 长期有效 |

## T-6813 搜索排序诊断与健康快照（2026-09-24 开发完成，§8.0.16 P0；收口 Codex 502 中断遗留 WIP）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 参数顺序 bug 修复 | 完成 | 前轮 Codex 发现 applySearch 与 runDocSearchFetch 参数错位（keyword 落入 version 位→远程搜索静默退出）+全文回退后缺过期检查；签名重排为 (…, keyword, version, onClose, filters, cacheKey, fetchQuery)，current() 收拢三项过期条件，缓存命中改走 applySearch 渲染+fetch 刷新页签层 | — |
| 评分解释 | 完成 | `buildSearchScoreBreakdown`：sourceWeight（opened 3/tabs 2/global 1）、titleScore、total（描述性合成，不参与排序）、title/path/pinyin/filter 命中、matchedFields；本次补齐 WIP 缺失的 sourceWeight/total 字段 | UI 呈现随 T-6825 批 |
| 健康快照 | 完成 | `buildSearchHealthSnapshot`：三来源计数/状态/延迟、slow/degraded 推导、≤8 个原因码（可序列化、不落宿主错误文本）；本次修复 loading 优先于 degraded 的推导顺序（慢请求在加载中也应报 degraded）；快照经 `updateDocSearchHealth` 挂 `scrollElement.dataset.swSearchState/swSearchReasons`，会话销毁即清（doc-search-state.health WeakMap） | 慢请求阈值进配置候补 |
| 行为测试 | 完成 | 新增 `tests/doc-search-fetch-behavior.test.cjs` 4 项：AST 提取真实函数体+依赖注入（无 fileName 转译——.js 后缀会让 TS 保留泛型产出非法 JS）；负向验证=注入旧参数顺序→fetch 未触发（测试会红）→恢复 | — |
| 契约对齐 | 完成 | 三处钉死旧形态的测试随签名重排更新：kernel-widgets-wiring（新调用顺序字面量+正向比较守卫）、doc-search-pagination-contract（fallback 调用点合并为"每处必须传上限"）、mobile-card-smoke（opened 探测带 report 回调+fetchText） | — |
| 杂项 | 完成 | index.ts 一处 GBK 乱码注释修复（"关键词为空"） | — |
| 门禁 | 完成 | 6074/6074 + tsc + build + 冒烟 70 PASS + Chromium 16 PASS | — |

## T-6825 搜索结果身份锚定（2026-09-24 开发完成，R5-A 首项，ROADMAP §8.0.17）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 纯模型层 | 完成 | `search-model.js` 新增 `pickDocViewportAnchor`（视口内首个文档条目为锚，fzf `--track` 语义）与 `planDocViewportRestore`（按同一条目回推 scrollTop；锚点/条目缺失保持现状不猜位置）；6 项单测（含锚定→钳制→恢复 roundtrip） | — |
| 渲染接线 | 完成 | `buildDocResultItem` 输出 `data-sw-doc-key` 稳定身份；`renderDocResults` 重建前捕获锚点，loading 清空期间暂存于 `docSearchState.docAnchors`（WeakMap，随表面销毁），results 装配完成后恢复视口；空态/清空路径同步清锚 | — |
| 契约 | 完成 | `doc-search-pagination-contract` 新增锚定接线契约；负向验证=注入移除恢复调用→红→字节恢复 | — |
| 杂项 | 完成 | doc-search-ui.ts 两处 GBK 乱码注释修复（"排除当前已打开的文档"/"空态：无可显示的搜索结果"）；构建后 readiness snapshot 重新生成（index.js 970875 / zip 437711） | — |
| 门禁 | 完成 | 6078/6078 + tsc + build + 冒烟 70 PASS + Chromium 16 PASS | — |

## T-6833 真实例 E2E 移植（2026-09-24 开发完成，R5-A 配套；对齐小驴打卡 T-1244~T-1249 方案）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 公开钩子 | 完成 | `window.siyuanSpeedSwitch`（`whenReady` 就绪信号 + `openSwitcher` 受控打开），onLayoutReady 挂载、onunload 移除；契约测试锁定挂载/清理/形态，负向验证（注入移除清理行→红→字节恢复）；生态出口（T-6832 RPC）与 E2E 共用此入口语义 | 随 provider 协议扩展 |
| E2E 基建 | 完成 | `scripts/e2e/lib.mjs`：本机内核探测、隔离工作区 `~/SiYuan-SpeedSwitch-E2E`（`swss-e2e.json` 标记防误指真实数据、回环地址硬校验、端口 6837/6838 避开用户实例）、dist 安装链（plugin.json.name 校验）、setBazaar+setPetalEnabled+loadPetals 启用链、内核日志落 `.artifacts/e2e/` | — |
| 双通道 | 完成 | `test:e2e`（可写）+ `test:e2e:readonly`（同工作区 `serve --readonly true`，前置自证内核拒绝 putFile）；Playwright 双配置串行 workers=1 | — |
| 冒烟 spec | 完成 | 桌面 2 项：切换器可开/可输入/Esc 可关且全程零未捕获异常、插件实例随内核下发且公开钩子可用；只读 1 项：readonly 角色下插件可用且切换器可开 | 移动 bundle spec 候补 |
| **真实验证** | 完成 | **本机思源 3.8.5 内核实测全绿：可写通道 2 passed（9.6s）、只读通道 1 passed（6.7s）——速切首次真实例宿主证据**。此前桌面真实宿主验收全部依赖用户手动，现可自动收口 | 后续真机项逐批转真实例证据（标注"真实内核+真实 web 前端"，不冒充 Android 真机） |
| 依赖与杂项 | 完成 | devDep 新增 `@playwright/test@1.63.0`（打卡同款，仅开发链）；`.gitignore` 增 `.artifacts/`；E2E i18n 断言按速切实际 `zh-CN.json` 命名适配 | — |
| 门禁 | 完成 | 6078/6078 + tsc + build + E2E 3/3（真实内核） | — |

## T-6826 openTab 新选项接入：恢复链不抢焦点（2026-09-24 开发完成，R5-A，ROADMAP §8.0.17）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 源码取证 | 完成 | v3.8.5 克隆实证：`keepCursor`（打开在新页签但不跳转，Wnd.addTab 参数）、`removeCurrentTab`、`afterOpen`、`doc.mode` 均在 3.8.5 已有；旧版宿主忽略未知选项=自然降级，无需运行时探测 | — |
| 选项透传 | 完成 | `openDocumentOnDesktop` 新增 keepCursor/mode/removeCurrentTab/afterOpen 透传（保持 position 兼容，命中块回退根文档时选项继续生效）；3 项单元测试（含回退场景） | — |
| 恢复链接线 | 完成 | 文档集恢复执行与 Essentials 常驻层改走 `keepCursor:true` 后台打开——恢复现场不再逐个抢焦点，当前页签保持；移动端语义不变 | 真机观感随 T-6305 批 |
| 真实打开链路 E2E | 完成 | 新增 `tests/e2e/open-flow.spec.mjs`：真实内核建笔记本+文档（createNotebook 契约=data.notebook 对象）→ 切换器搜索真实命中 → 单击打开真实 protyle 页签；3/3 全绿（本机 3.8.5） | — |
| 契约 | 完成 | kernel-widgets-wiring 新增恢复链 keepCursor 契约；负向验证（注入回旧抢焦点形态→红→字节恢复） | — |
| 门禁 | 完成 | 6081/6081 + tsc + build + 冒烟 70/16 PASS + E2E 3/3 | — |

### T-6828 重新评估（2026-09-24，账本决策记录）

`/api/history/` 域调研后认定：**"只看有改动"现有 SQL 批查（blocks.updated，32 条分块、IN 列表有界）在 3.8.x 上仍是正确实现**——history 域是版本快照域（按时间分页返回历史版本条目），用它判断"7 天内是否改动"需多页扫描+按文档聚合，一次往返与确定性都不如现有单查询，替换属负价值重构。**T-6828 收敛**：history 域保留给后续"文档版本查看/回滚"产品面（涉及破坏性回滚确认流+真机验收，单独立项）；本项不按原方案实施，R5 报告 §2.1 H2 的"数据源升级"表述据此修正。

## T-6827 保存的搜索（2026-09-24 开发完成，R5-A，ROADMAP §8.0.17）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 设置字段 | 完成 | `savedSearches`（≤16 条，id/名称≤40/查询≤120/可选笔记本≤64；同 id 或同名称+查询去重，畸形丢弃）入 normalizeSettings；8 项归一化单测（含容量/裁剪/去重/非数组） | 重命名 UI 候补 |
| 保存入口 | 完成 | 筛选菜单（有查询时）"保存当前搜索"：名称默认=查询文本（Electron 无 window.prompt，避免阻塞式命名），保存走 updateSettings 归一化持久化 + toast | — |
| 应用回放 | 完成 | `applySavedSearchFilters` 共享回放：查询写回输入框、笔记本筛选覆盖、筛选按钮徽标同步（filterButtonSync WeakMap 注册/清理）、立即执行搜索；筛选菜单"保存的搜索"子菜单与零词条工作台 chips 共用 | — |
| 工作台接入 | 完成 | 零词条工作台新增"保存的搜索"行：单击回放、右键确认删除并重建工作台；空态守卫补入 savedSearches（修复只有保存项时工作台不渲染） | — |
| 真实 E2E | 完成 | `saved-searches.spec.mjs`：真实内核菜单保存 → 清空查询 → 工作台 chip 出现 → 单击回放查询（E2E 工作区为英文前端，文案断言双语言正则）；E2E 4/4 + 只读 1/1 全绿（3.8.5） | — |
| 契约 | 完成 | kernel-widgets-wiring 新增保存回放链路契约；负向验证（移除工作台 saved 接线→红→字节恢复） | — |
| 门禁 | 完成 | 6083/6083 + tsc + build + 冒烟 70/16 PASS | 悬浮球/数字槽绑定候补 |

## T-6829/T-6830/T-6831 R5-A 三连收口（2026-09-24 开发完成，ROADMAP §8.0.17）

| 任务 | 内容 | 证据 | 下一步 |
|---|---|---|---|
| T-6829 文档集版本历史 | 覆盖保存自动留前一版（`DOCUMENT_SET_VERSION_MAX=3` FIFO，内容未变不留噪音版）；`rollbackDocumentSet` 纯函数（当前内容压回版本栈，回滚可逆）；设置页文档集行新增"回滚上一版 (n)"按钮（无版本禁用、confirm 确认）；normalizeSet 保留 versions 字段（旧数据 versions=[] 自然迁移） | document-sets 16 项单测（含容量 FIFO/相同内容不重复留版/回滚可逆）；契约负向验证（破坏留版逻辑→红→恢复） | 版本时间轴 UI 候补 |
| T-6830 打开策略层 | 设置新增 `reuseOpenTabs`（默认关=保持既有行为）；开启后搜索结果命中的文档已打开时 `activateTabForReuse`（按 rootId 归一查找已开页签，桌面/移动同源）聚焦复用而非新开，防重复页签；行为页新增开关 | settings-hardening 9 项（默认关/布尔校验）；契约负向验证（移除复用分支→红→恢复） | 按来源分策略候补 |
| T-6831 面包屑入口 | `Plugin.addBreadcrumbButton` 能力检测（3.8.5+；旧版宿主无此方法静默不挂载），按钮回调仅打开切换器（受控动作）；onunload `removeBreadcrumbButton` 拆除；i18n breadcrumbEntry | **真实内核实证：E2E 打开文档后 `[data-plugin-name="siyuan-speed-switch"]` 面包屑按钮真实挂载**；契约负向验证（移除拆除调用→红→恢复） | 按钮动作可配置候补 |
| E2E 稳定性 | 完成 | open-flow 每轮唯一后缀（唯一标题避免命中历史残留旧文档）+ 启动清理残留笔记本；此前连续失败根因=历史运行 removeNotebook 未达导致的旧文档命中，非新代码回归 | — |
| 门禁 | 完成 | 6089/6089 + tsc + build + 冒烟 70/16 + E2E 可写 4/4 + 只读 1/1（本机 3.8.5） | — |

## T-6814 关联内容只读适配（2026-09-24 开发完成，P0 工作上下文批，ADR 0076 D3；ROADMAP §8.0.16/§8.0.17）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 上游取证 | 完成 | v3.8.5 克隆实证 `/api/ref/getBacklink2`：请求 `{id, k(必传可为空), mk, includeMentions/includeBacklinks}`，响应 `backlinks[]/backmentions[]`（每项 id=来源文档根 ID、name、hPath、box）+ `linkRefsCount/mentionsCount` 总量；一次往返同时覆盖反链与提及；真内核探针实测块引索引有 2~3s 建立延迟 | — |
| 纯模型 | 完成 | 新增 `related-content-model.js`：`projectRelatedContent`（反链优先/跨类去重/限额 8/ID 形态校验/`truncated=shown<total` 截断可解释）+ `isRelatedCacheHit`（rootId 绑定 + TTL）；4 项单测（含畸形载荷安全） | — |
| 工作台接入 | 完成 | 零词条工作台新增"当前文档相关"行：活动文档 rootId → 官方端点 → 有界 chips（提及项带 ∼ 前缀），点击关闭切换器并跳转（openDocSearchResult 块定位降级语义）；空态守卫纳入 activeRootId；60s 会话缓存（FIFO ≤8）；首查为空时 2.5s×5 有界轮询（覆盖块引索引延迟），面板移除即放弃 | chip 观感随真机批 |
| 真实 E2E | 完成 | open-flow 扩展关联链路：文档 B 带块引指向 A → 打开 A 后再开切换器 → 工作台出现指向 B 的 chip → 点击真实打开 B；连续多轮 4/4 + 只读 1/1 | — |
| 契约 | 完成 | 契约夹具重新生成（29 端点含 getBacklink2）；kernel-widgets-wiring 新增关联接线契约；负向验证（移除关联行→红→恢复）；生产图上限按 ADR 0076 实测校准口径 64→66（related-content-model 入图 65，留痕注释） | — |
| 门禁 | 完成 | 6094/6094 + tsc + build + 冒烟 70/16 + E2E 多轮全绿（本机 3.8.5） | — |

## T-6815 恢复回执与分层快照（2026-09-24 开发完成，P0 工作上下文批，ADR 0076 D4；ROADMAP §8.0.16）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| Essentials 回执 | 完成 | `openDocumentSetEssentials` 返回 `{opened, failed, skipped}`（skipped=已打开跳过），不再 fire-and-forget；统一恢复摘要并入常驻层计数（`恢复: n/n · 常驻打开: +k`，失败计数独立呈现） | — |
| 分层快照 presetId | 完成 | 文档集条目新增可选 `presetId`（字符集与 setId 同规清洗）：场景应用成功后固化回集；恢复时优先按集内 presetId 应用场景、回退同名匹配（T-6810 语义保持）；覆盖保存保留上一版 presetId（分层不因覆盖丢失）；回滚保留 presetId | — |
| 旧配置保留 | 完成 | presetId 为可选字段（旧数据无此字段自然兼容）；normalize/覆盖/回滚/导入合并全链路测试通过（document-sets 17 项） | — |
| 契约 | 完成 | kernel-widgets-wiring 新增分层快照契约（presetId 优先联动/固化回写/回执调用/摘要并入）；负向验证（回退为无回执调用→红→恢复） | — |
| 门禁 | 完成 | 6096/6096 + tsc + build + 冒烟 70/16 + E2E（本机 3.8.5） | 取消可恢复语义已由现有 AbortController+报告导出覆盖 |

## T-6816 预览基础（2026-09-24 开发完成，P0 工作上下文批，ADR 0076 D8；ROADMAP §8.0.16）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 预览打开 | 完成 | Alt+点击文档结果 → `doc.mode="preview"` 预览页签（复用 T-6826 的 mode 透传；思源官方只读预览态 `.protyle-preview`）；预览路径跳过页签复用（用户明确要一个预览页签）；移动端不提供 | — |
| 可发现性 | 完成 | 文档结果 tooltip 追加预览提示（docSearchPreviewHint，中英双语） | 悬停预览/固定预览仍在候补池 |
| 真实 E2E | 完成 | open-flow 扩展预览链路：Alt+点击未打开的陪衬文档 → 断言 `.protyle-preview` 真实渲染；结果区"排除已打开文档"属设计语义，预览对象改用未打开文档后连续 3 轮 4/4 稳定 | — |
| 契约 | 完成 | kernel-widgets-wiring 新增预览契约（altKey→preview 选项→mode 透传→跳过复用）；负向验证（破坏 mode 透传→红→恢复） | — |
| 门禁 | 完成 | 6097/6097 + tsc + build + 冒烟 70/16 + E2E 可写 4/4 + 只读 1/1（本机 3.8.5） | — |

## T-6817 动态收藏组：参数化组合查询（2026-09-24 开发完成，P1 动作生态批，ADR 0076 D9；ROADMAP §8.0.16）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 组合条件 | 完成 | 智能分组在标签锚点之上叠加可选笔记本范围（`AND box=`）与更新时间窗（`AND updated >=`，白名单档位 7/30/90 天，拒绝任意天数）；`updated`（定长时间串）字典序比较，cutoff 由调用方注入 nowMs（纯函数可测） | — |
| 管理器扩展 | 完成 | 设置页智能分组新增"笔记本范围"下拉（任意/内核清单≤100）与"更新时间窗"下拉（任意/7/30/90，字面量 i18n 映射）；行内展示组合条件（`#tag @notebook ≤7d`） | — |
| 兼容与安全 | 完成 | 旧字符串入参兼容；LIKE 通配符/引号剥离、笔记本 ID 字符集清洗（与 setId 同规）、LIMIT 插件钳制不变；favorite-actions 6 项单测（含组合/清洗/白名单拒绝） | 查询型文档集候补 |
| 契约 | 完成 | kernel-widgets-wiring 新增动态组契约（整组透传/白名单/参数化 WHERE/管理器透传）；负向验证（白名单放宽 13 天→模型测试红→恢复） | — |
| 门禁 | 完成 | 6099/6099 + tsc + build + 冒烟 70/16 + E2E 4/4 + 只读 1/1 | — |

## T-6820 键盘优先第一片：数字直达与 chip 循环（2026-09-24 开发完成，P1 动作生态批，ADR 0076 D10；ROADMAP §8.0.16）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 数字直达 | 完成 | 切换器内按 1-9（无修饰键）打开第 n 个可见卡片；滚动容器外输入框中的数字不受影响；拒绝修饰键组合；与 Enter 共用 `activateCardByElement` 激活逻辑（单一事实来源） | 数字角标可视化候补 |
| chip 循环 | 完成 | Ctrl+←/→ 循环结果类型过滤条（all→tabs→unified→docs），空查询（无过滤条）时静默；复用 chipFilters 状态与 is-active/aria-selected 同步 | — |
| 契约 | 完成 | kernel-widgets-wiring 新增键盘优先契约（数字分支/共享激活/循环方法/绑定）；负向验证（移除数字直达→红→恢复） | — |
| 门禁 | 完成 | 6101/6101 + tsc + build + 冒烟 70/16 + E2E 4/4 + 只读 1/1（本机 3.8.5） | 命令前缀（`>` 动作模式）、link hints、marks 书签留在候补池 |

## T-6821 剪贴板入口/深链接（2026-09-24 开发完成，P1 动作生态批，ADR 0076 D13；ROADMAP §8.0.16）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 命令注册 | 完成 | 经 `safeRegisterPluginCommand` 注册 `clipboardEntry` 命令（命令面板白得发现性）；命令注册守卫契约 4→5 同步更新 | 全局热键绑定交由思源设置 |
| 解析与消毒 | 完成 | 剪贴板文本 trim 后严格锚定匹配 `siyuan://blocks/<id>`（ID 形态校验）→ 确认后走 openDocSearchResult 打开；普通文本预填快速捕获（截断 500 字）；异常/空剪贴板明确回执（clipboardEmpty） | — |
| 确认与来源 | 完成 | 链接打开前 `confirm` 用户确认（来源=剪贴板标记进文案）；不保存凭据；不落地任何持久化数据 | — |
| 契约 | 完成 | kernel-widgets-wiring 新增剪贴板契约（锚定匹配/确认/预填/回执）；负向验证（移除用户确认→红→恢复）；i18n ALLOWED_UNUSED 登记 clipboardEntry（langKey 动态查惯例） | — |
| 门禁 | 完成 | 6102/6102 + tsc + build + 冒烟 70/16 + E2E 4/4 + 只读 1/1（本机 3.8.5） | — |

## T-6824 第二片：配置包导出/导入接线（2026-09-24 开发完成，表面迁移批收官）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 主机方法 | 完成 | `exportConfigPack()`（版本化 JSON 导出）；`importConfigPack(payload)`（normalizeConfigPackImport 整体校验→settings 走 normalizeSettings 深校验→documentSets 走 normalizeDocumentSets 迁移门禁→校验全过后双键落盘，任一失败零写入） | — |
| 设置页 UI | 完成 | 存储页新增"导出/导入配置包"：导出下载 `siyuan-speed-switch-config-pack.json`；导入文件 ≤512KiB、JSON 解析、`confirm` 确认（覆盖提示）后原子应用；失败 toast 带原因 | — |
| 契约 | 完成 | kernel-widgets-wiring 新增配置包契约（导出/导入/确认/白名单/外来包拒绝/迁移门禁）；负向验证（移除导入确认→红→恢复） | — |
| 门禁 | 完成 | 6109/6109 + tsc + build + 冒烟 70/16 + E2E 4/4 + 只读 1/1（本机 3.8.5） | — |

## T-6824 版本时间轴 UI（2026-09-24 开发完成，表面迁移批）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 模型 | 完成 | `rollbackDocumentSet` 新增 `versionIndex` 选项（可回滚到任一历史版本；越界钳制；被选版本出栈、当前压入栈首仍可逆）；document-sets 18 项单测 | — |
| 设置页 UI | 完成 | 回滚按钮点击展开版本时间轴列表（`↩ 日期时间 (N 项)`），点选任一版本 confirm 后回滚并刷新 | — |
| 契约 | 完成 | kernel-widgets-wiring 契约更新（versionIndex 透传/时间轴展开）；负向验证（移除版本下标透传→红→恢复） | — |
| 门禁 | 完成 | 6115/6115 + tsc + build + 冒烟 70/16 + E2E 4/4 + 只读 1/1（本机 3.8.5） | — |

**P1+P4 搜索显示批（2026-09-24 开发完成）**

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| P4 统一索引窄列修复 | 完成 | **根因**：统一分区条目使用 `grid-template-columns: 32px minmax(0,1fr)`（为图标预留列），但渲染时缺少 `.sw__doc-icon` 元素——唯一的 `.sw__doc-copy` 被放入 32px 列，文本压成 2 字宽。修复=补齐图标元素 | — |
| P1 数字角标优化 | 完成 | 半透明 soft 底色改为主色实色底+反色字，位置从左上角内容区移到右上角外沿（`top:-6px; right:4px`），加投影增强可辨性 | — |
| 门禁 | 完成 | 6116/6116 + tsc + build + E2E 5/5 + 只读 1/1 | — |

## P2+P3 工作台体验批（2026-09-24 开发完成，合并提交 db3d371）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 停靠坞（P3） | 完成 | 工作台从 `.sw__scroll` 滚动流内部移至独立停靠区 `.sw__workbench-dock`（滚动区与快捷入口条之间），零查询时常驻首屏、页签再多也不被挤出视野；非查询时隐藏 | 手机端停靠坞适配随 T-6822 |
| SWR 缓存+骨架屏（P2） | 完成 | fillRelatedContent：骨架屏（aria-busy 脉冲标签）→ 数据到达后整行替换；终态重试耗尽移除空占位框；缓存 TTL 60s 会话内跨对话框复用 | 跨会话持久化缓存候补 |
| 移动端真实例 E2E | 完成 | `mobile-entry.spec.mjs`：移动前端 bundle + iPhone 13 视口，顶栏入口注入、切换器搜索、真实 protyle 打开；移动前端无 `.protyle-title`，断言以 protyle 内容为准 | — |
| 门禁 | 完成 | 6116/6116 + tsc + build + 冒烟 70/16 + E2E 5/5 + 只读 1/1（本机 3.8.5） | — |

## 手机悬浮球 × 思源 3.8.5 侧滑误触：多层叠加防御专项（2026-09-24 完成，T-6778/T-6779 收口，用户指令专项）

| 层 | 内容 | 证据 |
|---|---|---|
| 上游取证 | 宿主侧滑三个 touch 监听全部注册在 **document、bubble 相位**（mobile/index.ts:241-243）；touchstart 时 `hasClosestByAttribute(target,"data-prevent-swipe",null,true)` 命中即整轮放弃（move/end 均不处理侧栏）；块引索引延迟等行为差异已另行实证 | v3.8.5 克隆 touch.ts/apicontract |
| L1 官方契约 | 移动 portal 根 `data-prevent-swipe`（3.8.1+ 宿主 touchstart 即整轮让出）+ body 层恢复把手单独补标记 | T-6778（8ce2f04） |
| L2 事件拦截（新） | 球根 **capture 相位** stopPropagation 吞掉 touchstart/move/end/cancel——touch 事件流根本到不了 document bubble 监听，**对无 marker 契约的 3.8.0 旧宿主同样免疫** | floating-ball-ui.ts mobile 分支 |
| L3 CSS+滚动链 | `touch-action:none`（既有）+ 拖动/选态中 touchmove preventDefault（阻断滚动链）；更多面板滚动不受影响（非拖动态放行默认行为） | floating-ball-ui.ts |
| 决定性验收 | 真实 Chromium + CDP `Input.dispatchTouchEvent` 真实输入管线：页面内装"宿主侧滑状态机"模拟器（document bubble，与思源注册相位一致）——**①球上起触拖 40px：宿主 touchstarts=0、永不触发侧栏，球跟随手指 ✓ ②球外普通内容起触：宿主照常收到并 12px 阈值触发 ✓**（两层结论同屏证明：不误触 + 不影响宿主） | floating-ball-browser-smoke 新场景，23 场景全绿 |
| 门禁 | 完成 | 6115/6115 + tsc + build + 冒烟 70/16 + E2E 4/4 + 只读 1/1 | T-6780 Android 真机零误触录屏仍待设备 |

**设计说明**：三层互为冗余——marker 是 3.8.1+ 官方正道；capture 拦截层保证旧宿主同样免疫；touch-action/preventDefault 保证球拖动期间无滚动链干扰。三层都不影响思源自身左滑右滑（只作用于球根子树内的触摸），夹具第 ② 项即为此证明。

## T-6778 移动侧滑所有权：data-prevent-swipe（2026-09-24 开发完成，表面迁移批，ADR 0071/0076 T-6822a；§8.0.9）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 自主决策 | 完成 | 按「决策与验收长期授权」自主采用 R3/ADR 0071 推荐的方案 A（T-6778 原挂"待用户确认"） | — |
| 上游语义实证 | 完成 | `touch.ts:353`：`preventSwipe = hasClosestByAttribute(target, "data-prevent-swipe", null, true)` ——closest 向上匹配，标记 portal 根即覆盖全部子元素（触发按钮/动作面板） | — |
| 接线 | 完成 | `createPortal`：surface=mobile 时根节点 `dataset.preventSwipe="true"`；恢复把手挂 body 层（portal 根之外）单独补标记；桌面 portal 不标记（手势归属仅移动端问题） | 3.8.0 兼容性按 T-6781 条件项处理 |
| 契约 | 完成 | kernel-widgets-wiring 新增侧滑所有权契约（根标记/把手标记/dataset 写出）；负向验证（移除把手标记→红→恢复） | — |
| 门禁 | 完成 | 6110/6110 + tsc + build + 冒烟 70/16 + E2E 4/4 + 只读 1/1（本机 3.8.5） | **真机零误触验证仍归 T-6780（B-004/B-005）** |

## T-6820 数字直达角标（2026-09-24 开发完成，键盘优先可发现性）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 角标 | 完成 | `updateDigitBadges`：为前 9 个可见卡片写 `data-sw-digit`（CSS `::after content: attr()` 渲染，零 DOM 增量）；renderList 渲染后与 filterCards 筛选后两处刷新；第 9 个之后移除标记 | — |
| 契约 | 完成 | kernel-widgets-wiring 新增角标契约（独立方法/filterCards 刷新/9 上限/attr 渲染）；负向验证（移除筛选后刷新→红→恢复） | — |
| 门禁 | 完成 | 6111/6111 + tsc + build + 冒烟 70/16 + E2E 4/4 + 只读 1/1（本机 3.8.5） | — |

## marks 书签（2026-09-24 开发完成，R3 候补池首项，键盘优先配套）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 命令注册 | 完成 | `setSessionMark`（标记当前位置）/`jumpToSessionMark`（跳回最近标记）两条命令经 safeRegisterPluginCommand 注册，命令面板可发现、可绑快捷键；命令守卫 5→7 | — |
| 会话级存储 | 完成 | `sessionMarks` 内存 Map（rootId→滚动比例，FIFO ≤12），不持久化——跨会话位置属宿主能力不伪造（同 T-6801 口径）；比例计算复用 computeScrollRatio | — |
| 跳回链路 | 完成 | jumpToSessionMark：目标已活动则直接回卷，否则经 openDocSearchResult 聚焦后按比例回卷（planScrollRestore，30×50ms 有界等待）；空标记/无活动文档独立错误回执 | — |
| 契约与 i18n | 完成 | kernel-widgets-wiring 新增 marks 契约；负向验证（破坏容量→红→恢复）；i18n ALLOWED_UNUSED 登记两条命令 langKey | — |
| 门禁 | 完成 | 6112/6112 + tsc + build + 冒烟 70/16 + E2E 4/4 + 只读 1/1（本机 3.8.5） | — |

## T-6820 命令前缀 `>`（2026-09-24 开发完成，键盘优先批）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 命令模式 | 完成 | applySearch 识别 `>` 前缀 → renderCommandList 只列可执行动作（内建+宿主命令目录、限当前端、文本过滤、≤12 有界、空态提示）；点击经既有 executeQuickAction 执行（回执/失败原因同管线） | 移动端等价入口候补 |
| i18n | 完成 | commandModeLabel/commandModeEmpty 双语 | — |
| 契约 | 完成 | kernel-widgets-wiring 新增命令前缀契约；负向验证（移除前缀分支→红→恢复） | — |
| 门禁 | 完成 | 6112/6112 + tsc + build + 冒烟 70/16 + E2E 4/4 + 只读 1/1（本机 3.8.5） | — |

**T-6824 可迁移配置包全部收官——ADR 0076 表面迁移批 T-6823/T-6824 完成（T-6822 待用户确认 T-6778）。至此 ADR 0076 五阶段中 P0 两批+P1 动作生态+表面迁移主体全部落地，仅剩 P2 受控智能（T-6832 评估池）与 T-6822 移动侧侧滑所有权（等确认）。**

## T-6823 密度档位（2026-09-24 开发完成，P1/P2 表面迁移批首项，ADR 0076 D11；ROADMAP §8.0.16）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 设置与标记 | 完成 | `density`（comfortable 默认/compact，白名单外回落）；`applyDensity()` 仿 ADR 0073 皮肤机制：body 标记 + onload 应用 + 设置变更即刷 + onunload 移除 | — |
| SCSS | 完成 | `_10-skins.scss` 末尾紧凑档覆盖块：`body[data-sw-density="compact"]` + `:is()` 限定速切根容器，仅收窄卡片内边距/分组间距/文档行高，44px 触控与焦点不降级 | 密度×皮肤组合矩阵随真机批 |
| 设置页 | 完成 | 行为页"紧凑密度"开关（关闭即舒适） | — |
| 契约 | 完成 | kernel-widgets-wiring 新增密度接线契约；负向验证（破坏标记属性名→红→恢复） | — |
| 门禁 | 完成 | 6106/6106 + tsc + build + 冒烟 70/16 + E2E 4/4 + 只读 1/1（本机 3.8.5） | — |

## T-6819 provider 协议（2026-09-24 开发完成，P1 动作生态批收官，ADR 0076 D6；ROADMAP §8.0.16）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 协议元数据 | 完成 | `normalizeProvider` 升级：provider 级 `version`（≤32 有界）、动作级 `description`（≤200，展示与执行分离）；未知提供方 kind=adapter 保持可见的既有语义不变 | SDK 文档化随集市窗口 |
| 协议自描述 | 完成 | `describeProviderProtocol()`：协议版本（`QUICK_ACTION_PROVIDER_PROTOCOL_VERSION=1`）+ 字段 schema + 三条保证（展示/执行分离、卸载可恢复、未知提供方可见），可序列化供诊断/文档复用；与 T-6832 RPC 出口共享入口语义 | — |
| 测试 | 完成 | `tests/provider-protocol.test.cjs` 2 项：归一化（版本/描述/无效项丢弃）+ 注册表往返（新字段保留/卸载恢复/协议自描述有界） | — |
| 门禁 | 完成 | 6104/6104 + tsc + build + 冒烟 70/16 + E2E 4/4 + 只读 1/1（本机 3.8.5） | — |

**P1 动作生态批 T-6817~T-6821 全部收官。** ADR 0076 五阶段：P0 可靠性 ✅、P0 工作上下文 ✅、P1 动作生态 ✅（本节）、P1/P2 表面迁移（T-6822~6824）与 P2 受控智能（T-6832 评估池）未开始。

## 候补池登记（2026-09-24 盘点）

### 用户反馈问题清单（2026-09-24 起陆续收集，先分析立项、不直接开发）

| # | 问题 | 分析结论 | 候选方案（待定稿） | 状态 |
|---|---|---|---|---|
| P1 | 数字直达角标显示不清、与卡片内容重叠 | 角标 `::after` 定位在卡片左上角**内容区内**（页签卡为内容预览卡，无预留空位）；且用半透明 soft 底色，叠在深浅不可控的预览上对比度不足。触发逻辑（数字直达）本身正常，纯展示层缺陷 | ①角标移出内容区（卡片边缘挂牌式 / 底部标题灰条左端）；②半透明底改实色底+反色字；③可选"按键提示模式"（仅修饰键按下时显示）；④加描边/投影与留白 | 已登记，待立项 |
| P2 | 工作台（关联内容行）刷出不及时；希望常用内容缓存秒开、后台更新 | ①关联行数据走异步 getBacklink2：内核块引索引 2~3s 延迟 + 请求往返 + 失败轮询（2.5s×5），且缓存仅在实例内存，**重启后必冷启动**；②轮询耗尽无数据时**残留空占位 div**（截图"工作台"下方空白即此） | ①SWR 持久化缓存：关联投影写入插件存储（版本戳 key、有界），先显缓存标注"缓存"再后台刷新；②onLayoutReady 预热活动文档；③骨架屏占位；④终态失败移除空占位；⑤本地行永远优先，关联行可延后拉取 | 已登记，待立项 |
| P3 | 工作台需要固定首页保留空间：页签再多也在首屏可见 | 工作台盒当前插在 `.sw__scroll` **滚动流内部**（页签分组之后），位置由卡片数量决定——页签多时被推到折叠线以下（截图红框：工作台标题+空白在首屏底、内容全部不可见）。布局结构问题，与 P2 的"慢"相互独立 | ①**固定停靠坞（首选）**：工作台移出滚动流，放 `.sw__scroll` 与底部快捷条之间的独立容器（max-height+内部滚动+可折叠细条），零查询显示、输入隐藏，结构现成；②sticky 吸顶分区（持续占空间，长列表有损）；③桌面双栏（远期）；均需守住 44px 触控/焦点顺序/reduced-motion；与 P2 缓存组合=打开即满内容 | 已登记，待立项（建议与 P2 合并一个"工作台体验"批次） |
| P4 | 统一索引分区（收藏/最近关闭）条目文字被压成极窄列（每行约 2 字），片段截断过短；全库文档卡显示正常 | 问题限定在统一索引分区的容器/布局：全库网格 `.sw__doc-grid` 条目正常 → `.sw__doc-item` 基础样式无恙，是统一分区**未复用网格管线**或外层 flex 收窄；"聊三/个…"两字换行+省略号 = 文本元素被压到 min-content 级触发 ellipsis；片段渲染长度也与全库卡不一致 | ①统一索引条目复用全库网格容器/样式管线；②文本元素补 `min-width:0`+`flex:1`（flex 挤压经典修复）；③片段截断与全库卡对齐；④验收夹具=长标题+长片段+中英混排×宽窄两种弹窗宽度（含手机单列） | 已登记，待立项 |
| P5 | 悬浮球设置「添加动作」选择器：框过小、分类粗（只有 builtin）、宿主命令与第三方动作无分组、不能折叠 | ①选择器容器高度固定偏小（约 8 行截断），目录已扩到"16+ 内建+宿主命令+第三方适配器"，容量不匹配；②分组只按 kind 粗分——宿主命令无独立分组，第三方动作未按 `providerName`（适配器元数据已有该字段）聚合；③分组头无折叠能力 | ①容器扩容（min(60vh…) 或两列+内部滚动）；②三级分组：内建/思源命令/按插件聚合（providerName 同名合并）；③分组头折叠（状态记忆，默认展开或非内置折叠）；④搜索跨组过滤并自动展开命中组；⑤实现落在共享 `mountQuickActionPicker`（悬浮球/快捷入口设置多处受益）；⑥守住 44px 触控与键盘语义 | 已登记，待立项 |
| P6 | 真机反馈：靠边停靠仍易误触——手指瞄准半隐窄条时常偏移到球外边缘 12px 激活条，一滑就开思源侧栏。用户提议：非边缘位置停靠（偏下）+ 上滑呼出 | 三层防御只保护"落在球上"的触摸；半隐贴边形态下球的有效触面极窄，**落点偏移到球外的边缘条不受防御层保护**——形态与宿主"边缘=激活区"空间重叠，靠防御无法根治 | ①**离边停靠预设**：移动端新增"底部两侧上方/底部居中偏下"停靠档位（离开 12px 激活条，自由停放能力已有，补预设与引导）；②**上滑呼出手势**：球上快速上滑（速度+方向双判定，复用 T-6784 外拨速度窗口机）=打开切换器/动作面板，慢拖=移球——与左右侧滑正交零冲突；③手势分层可配置（短上滑=切换器、长上滑=更多面板）；④"视觉贴边、触点内移"补丁（edge 模式球体向内偏移半径）；三层防误触保持不变兜底 | 已登记，待立项（与 T-6784~6787 手势基建同族，建议合并批次） |
| P7 | 历史下拉「最近打开」被日记条目淹没（DailyNote 每天自动新增，含 Conflicted 副本），「最近关闭」被挤到不可见 | 结构性失衡：两分区共用连续滚动列表且**均无条目上限**；`YYYY-MM-DD` 日记标题辨识度低、回访价值分散，却按 MRU 与高价值条目平权混排；无分区折叠/上限/快速切换（「只看有改动」只管过滤不管篇幅） | ①分区折叠+计数（"最近打开（34）▾"，会话内记忆）；②每区默认展示前 8 条+「展开全部(n)」，保证最近关闭首屏可达；③日记智能聚合（连续 YYYY-MM-DD 条目聚成"9 月日记（12 篇）"簇）；④下拉内快速过滤 chips（全部/已打开/已关闭/有改动）；⑤下拉顶部双 Tab。建议 ①+② 最小闭环，③④⑤按反馈追加 | 已登记，待立项 |
| P8 | 组件面板「数据库」组件未按数据形态渲染：只显示"1 个数据库"+纯文本行，未按思源数据库视图（表格：列定义+行）展示 | 取数已通（块 ID 0 行→库 ID 两级取数；getAttributeView data.av 提供列定义+rowIds；renderAttributeView(库ID,viewID,pageSize=100) 实测 31 行），**渲染层是短板**：只做清单式投影（数量+首条摘要），未消费列定义与行数据；思源数据库多视图（表格/看板/画廊/日历）形态也未处理 | ①表格渲染（首选）：消费 columns+rows 渲染有界表格（列宽自适应/横向滚动/行上限+增量）；单元格按类型基础投影；②按当前视图渲染，其他视图降级表格+视图名标注；③组件面板保持只读，点击跳转源库；④pageSize 100 上限、显示前 N 行；av 端点结构化参数无 SQL 拼接面 | 已登记，待立项（**优先级中低，用户明示后置**） |
| P6 开发（2026-09-24 完成） | **离边停靠**：悬浮球设置新增"手机端离边停靠"开关（`behavior.edgeAvoidMobile`，默认关）；开启后 `edgeAvoidPx=12`——水平拖动钳制与 applyPosition 的 minX/maxX/right 均外推 12px，**球体与拖动范围离开宿主 12px 侧滑激活条**；半隐自动停用（半隐会把球推回激活条）。**上滑呼出**：docked 状态下采样指针轨迹（≤6 点），pointerup 时速度窗口判定（dy≤-24px 且 |dx|≤12 且速度≤-0.5px/ms）→ 打开更多动作面板并抑制合成 click | floating-ball-model 归一化默认关+布尔清洗；开关入悬浮球设置（仅手机端生效，桌面/侧栏不变）；契约（生效判定/水平钳制/半隐停用/模型归一化/设置开关）5 断言；**负向验证：全量回退 effectiveMarginX→红→恢复** | 真机手感阈值调优随 T-6780 |

| 项 | 说明 | 进入条件 |
|---|---|---|
| src/ GBK 乱码注释清扫 | index.ts/settings-sections.ts 等 R5a 时代文件存在十余处 GBK 乱码注释（如"鑾""閿""鎺"系）；零运行时影响但属瑕疵。需逐条按语境人工还原，不可机械正则替换 | 攒批一次清扫，每条对照上下文还原 |
| 版本时间轴 UI | 文档集 versions 字段已有数据层（T-6829），缺时间轴可视化 | 用户反馈需求 |
| 悬浮球/数字槽绑定保存的搜索 | 保存的搜索目前入口=筛选菜单+工作台 chips | 反馈驱动 |
| 命令前缀 `>` 动作模式 / link hints / marks 书签 | T-6820 剩余方向（marks 需新持久化 key，走 D-401 版本戳） | 反馈驱动 |

## T-6818 多目标快速捕获（2026-09-24 开发完成，P1 动作生态批，ADR 0076 D5；ROADMAP §8.0.16）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 目标切换 | 完成 | 捕获对话框新增目标段选：今日日记（默认）/ 当前文档（仅桌面且有活动编辑器）；role=tablist 语义 | 收集箱目标等 DockTomato 桥扩展 |
| 目的地预览 | 完成 | 预览行（aria-live=polite）：日记=笔记本名异步补齐+当日日期；当前文档=活动文档可见标题；当前目标不可写时 save 禁用并说明原因（quickCaptureNoTarget） | — |
| 受控写入+回执 | 完成 | 两条写入路径均为既有白名单端点（appendBlock）；成功回执含目标（当前文档路径显示《标题》）；失败分步呈现：空文本/无活动文档/日记创建失败/追加失败各自独立 i18n | — |
| 契约 | 完成 | kernel-widgets-wiring 新增捕获契约（目标段选/预览行/分步失败/双路径写入）；负向验证：移除当前文档写入分支→契约红→恢复 | — |
| 包体校准 | 完成 | raw 实测 984939 越过 960 KiB 线 → **ADR 0077**：自律线 960→1024 KiB（+64KiB 步进先例；zip 硬上限 512KiB 不变，实测 ~438KB）；常量钉死断言指向 0077 | — |
| 门禁 | 完成 | 6099/6099 + tsc + build + 冒烟 70/16 + E2E 4/4 + 只读 1/1 | — |

## T-6834 搜索结果关键词高亮（2026-09-24 开发完成，候补池 UX 项；ROADMAP §8.0 搜索体验）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 纯分段器 | 完成 | `buildKeywordHighlightSegments(text, query)`（search-model.js）：词条复用 parseSearchTerms includes（引号短语命中、排除词不高亮、大小写不敏感保原样）；命中区间每词条≤64 处有界、重叠/相邻合并；返回 `{text, hit}[]` 纯数据，join 恒等于原文 | 片段行（snippets）高亮候补 |
| UI 装配 | 完成 | buildDocResultItem 标题按分段装配 `createElement("mark")`/textNode——**零 innerHTML，无注入面**；query 经既有第 4 参透传，无新调用点 | — |
| 样式 | 完成 | `.sw__doc-title mark`：color-mix 主色 22% 透明底、继承字色、padding 0 1px，不撑行高不抢色（line-clamp 2 内正常） | — |
| 门禁 | 完成 | search-model 82/82（新增 8 条：单词条切分/大小写/多词条重叠合并/排除词不标/短语/空入参/join 恒等/64 处有界上限）；全套件 6124/6124 + verify:release 过；产物快照再生成（index.js 998261 / css 189841 / zip 449381） | — |

## T-6835 片段行关键词高亮 + 内核标记清洗（2026-09-24 开发完成，T-6834 延伸；ROADMAP §8.0 搜索体验）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 内核标记清洗 | 完成 | 新纯函数 `stripSnippetMarkup`（search-model.js）：只剥 `<mark>`/`</mark>`（含属性/大小写/截断残留开标签）——内核全文检索的命中包裹属宿主展示语义，投影只留纯文本；**故意不做宽泛 `<[^>]+>` 剥离**（会误食正文 "a < b and c > d" 字面文本）；normalizeSearchResult 内先剥后归一，全消费方（E2E/agent 投影）同源受益 | span/em 等遗留标记如有真机反馈再议 |
| 片段行装配 | 完成 | `.sw__doc-snippet` 与标题共用 buildKeywordHighlightSegments 分段装配（零 innerHTML）；样式=仅主色着色+加粗、无底色（比标题轻一档，视觉层级不倒挂） | — |
| 门禁 | 完成 | search-model 84/84（新增 2 条：包裹剥离含大小写/属性/截断残留；字面尖括号负向保护）；全套件 6126/6126 + build 过；产物快照再生成（index.js 998507 / css 190078 / zip 449474） | — |
| 真实 E2E | 完成 | open-flow 扩展高亮链路断言：搜索命中后 `.sw__doc-title mark` 真实渲染可见（本机 3.8.5 内核 5/5）；**负向验证：断言注入假选择器 mark.sw-negative-check → 该行精确红 → 还原** | 片段行 mark 非确定性（视内核返回片段而定）不入断言 |

## T-6836 行内关闭按钮：收敛不实施（2026-09-24 核查）

候补项（Chrome Tab Search 模式）核查结论=**能力已覆盖**：页签卡自带 `.sw__close` 右上角行内关闭（buildCardActions，桌面 hover 可见；点击走既有 handleCloseTab 闭环=页签关闭+卡片移除+分组计数+侧栏刷新）；手机端长按菜单含关闭。且全库文档网格按设计**排除已打开文档**（planDocResultsPage openRootIds，上半部分页签卡已表示），结果行无需 ×。同 T-6828 口径记收敛，不再建设。

## T-6837 键序直达第二片：文档结果行数字直达（2026-09-24 开发完成；ROADMAP §8.0 键盘优先）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 激活单一入口 | 完成 | 新增 `activateDocResultItem`（doc-search-ui.ts）：点击/数字直达共用；行身份 data-sw-doc-key、**块级锚定 data-sw-doc-hit 随行持久化**（原来 docSearchHitId 在点击/右键两处重复计算，现一次计算随行存储）；T-6802 置顶记账内置；T-6816 Alt 预览语义透传不变（对应 gate 断言同步新形态） | — |
| 键位接线 | 完成 | `activateDocItemByDigit`（index.ts）：查询词非空且第 n 条可见 → 优先直达结果行，未接管回退卡片直达；bindKeydown 控件守卫放行结果行上的数字键（Tab 聚焦按钮后直达可用；其余控件照旧让路）；卡片隐藏（搜索态）时数字键不再静默丢失 | 多字母 hint（f/j home-row）候补 |
| 角标 | 完成 | renderDocResults 给前 9 条可见结果行写 data-sw-digit（CSS attr 渲染零 DOM 增量）；`.sw__doc-item[data-sw-digit]` 样式=卡片角标同款缩小一号（右上角实色底反色字） | — |
| 门禁 | 完成 | kernel-widgets-wiring 新增 T-6837 契约（方法/优先接线/隐藏分支/守卫放行/单一入口/hit 随行/角标/CSS 8 断言）；**负向验证：直达调用改名 Broken → 门禁精确红 → 还原绿**；全套件 6127/6127 | — |
| 真实 E2E | 完成 | open-flow 打开路径改数字直达（读角标值聚焦按键，>9 条回退点击）：角标断言 + 真实按键直达 + protyle 渲染全链过，本机 3.8.5 内核 5/5 | — |

### 候补池勘误（2026-09-24）
- ~~版本时间轴 UI~~：**已覆盖**——T-6829 回滚按钮可展开版本列表（sw-setting__doc-set-versions），任选版本回滚+时间戳+条目数，非仅"缺可视化"。
- ~~搜索行内关闭按钮~~：见 T-6836，页签卡 `.sw__close` 已覆盖。
- marks 持久化：维持"不持久化"既有口径（跨会话浏览位置属宿主能力，T-6801 同款），继续反馈驱动。

## T-6838 结果行 ↑/↓ 行导航（2026-09-25 开发完成，键盘优先链路收口；ROADMAP §8.0）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 行导航 | 完成 | `moveDocItemFocus`（index.ts）：全库文档网格可见行间移动焦点+scrollIntoView 跟随；焦点行不在网格内（工作台/统一分区行）返回 false 不 preventDefault，**保留列表原生滚动**；有界钳制不回绕 | 左右键分组导航候补 |
| 守卫扩展 | 完成 | bindKeydown 控件守卫从"放行数字键"扩展为"数字键+↑/↓"（navKey 汇总判定），结果行上的方向键进入行导航分支；其余控件照旧让路 | — |
| 门禁 | 完成 | T-6837 契约同步扩展（守卫新形态/moveDocItemFocus/接管才 preventDefault）；**负向验证：行导航调用改名 X → 门禁精确红 → 还原绿**；全套件 6127/6127（首跑 1 红=已知 perf 微基准满载假红，单独复测恒过未动代码） | — |
| 真实 E2E | 完成 | open-flow 行导航链路：聚焦第 1 行 → ArrowDown 焦点落第 2 行（data-sw-doc-key 相等断言）→ ArrowUp 回第 1 行 → 按角标数字直达打开，本机 3.8.5 内核 5/5 | — |

**至此键盘优先链路完整**：Tab 进入结果区 → ↑/↓ 逐行扫视 → 1-9 直达或 Enter 激活 → Alt+点击/`>` 命令模式延伸；页签卡侧 1-9/方向键/Enter/Shift+F10 原有链路不变。

## R7 调研-吸收（2026-09-25 完成，报告=docs/research-r7-2026-09-25.md；无代码变更）

| # | 吸收项 | 去向 |
|---|---|---|
| 1 | **P9 常驻预览窗格规格草案**：桌面右分栏（~260px，窄宽隐藏）；零新增端点（getDocOutline 白名单内 + 有界 SQL 首段 ≤600 字）；同步钩子=既有行焦点（T-6838）；debounce 300ms + AbortController 竞态丢弃（T-6814 模式）；骨架+空态；手机不提供 | 候补（随反馈立项） |
| 2 | **T-6832 评估池升温**：思源内核自带 `POST /mcp`（3.7.2/3.7.4 已修鉴权 CVE-2026-66012 与 database_clean 穿越 CVE-2026-74798）；速切只读导航能力可经插件 RPC 路由被 MCP 侧 Agent 消费 | 评估池（等生态回应） |
| 3 | **关联内容锚文本排序**：3.8.6-alpha 上游已做反链按锚文本排序 | 候补（等 3.8.6 stable） |
| 4 | **集市分发协议提案**（latest.json + minisign 协议化）与 **3.8.6 插件发布资源目录声明**：发布管线潜在变化 | 观察项 |
| 5 | 上游确认 P8"按视图类型渲染"方向（3.8.5 数据库日历/列表视图落地），P8 维持用户明示后置；3.8.6 修复全局快捷键打断 onload——safeRegisterPluginCommand 防御保留双保险 | 无动作 |

## T-6839 常驻预览窗格（2026-09-25 开发完成，P9 规格落地；ROADMAP §8.0 搜索体验）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 纯模型 | 完成 | `buildDocPreviewSnapshot`（search-model.js）：大纲 ≤12 条（h1-h6 层级解析，异常归 1）、首段拼接 ≤600 字有界截断、双空 `empty` 标志；3 条单测（层级投影/有界/空态） | — |
| 窗格挂载 | 完成 | `mountDocPreviewPane`（doc-search-ui.ts）：`.sw__doc-results` 右分栏 260px 绝对定位（窄容器 <680px 与手机端不挂载）；随结果区 innerHTML 重建重挂；`focusin` 委托挂 box（行重建不丢钩子）；仅全库文档网格行触发 | 窗格 sticky 吸顶候补 |
| 取数管线 | 完成 | 300ms debounce + 代际计数丢弃过期回包（T-6814 同款竞态防线）；零新增端点（getDocOutline + /api/query/sql 均在白名单）；rootId 经 BLOCK_ID_RE 锚定校验后入 SQL 字面量；i18n 三键双语（预览标题/加载中/空态引导） | — |
| 门禁 | 完成 | kernel-widgets-wiring 新增 T-6839 契约 11 断言（挂载/守卫/debounce/focusin/rootId 校验/白名单/代际防线/纯模型/双有界/样式）；**负向验证：代际防线注入 `if (false)` → 门禁精确红 → 还原绿**；全套件 6131/6131 | — |
| 真实 E2E | 完成 | open-flow 陪衬文档改带标题+段落 markdown；行聚焦后断言 `.sw__doc-preview-heading` 真实渲染大纲锚文本（本机 3.8.5 内核 5/5） | — |

## 审查轮：性能×UI×功能全方位（2026-09-25 完成，报告=docs/audit-2026-09-25.md）

| 项 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 审查工具 | 完成 | `SWSS_AUDIT=1 pnpm run test:e2e` 截图切换器 7 态到 .tmp/audit/（工具入库 audit-screenshots.spec.mjs，常态零开销） | 随时可重放 |
| P-A 命令模式列表 | 修复 | 重做 quick pick 式行（图标+左对齐+↵ 提示）；根因=复用 .sw__doc-item 类但作用域外裸按钮态；截图复验 | 分组折叠候补 |
| P-B 标题高亮强度 | 修复 | 22%→30% 底+命中字主色；截图复验清晰 | — |
| P-C 预览窗格碎片感 | 修复 | 常驻头部+独立 body+min-height 200+空态引导 | sticky 吸顶候补 |
| **P-D 预览大纲缺级（功能 bug）** | 修复 | 探针实证内核契约：preview:false 恒空（须 true）；子项文本在 content 字段而非 name；层级在 depth。模型递归拍平+双字段；单测+契约锁死真实形状 | — |
| P-E 关联行 spinner | 修复 | 首轮空即显"暂无关联"弱提示（i18n workbenchRelatedEmpty），轮询继续 | — |
| P-F 底栏中英混排 | 待查 | 嫌疑=内建动作标签硬编码中文；中文界面无感，不阻塞 | i18n 化候补 |
| 性能走查 | 结论 | 输入防抖已有；重建式渲染由 T-6825 锚定补偿可接受；预览 debounce+代际防线复验；包体 1003641<1024KiB | — |

## §8.0.19 第一波=护城（2026-09-25 开发完成，三大支柱各立一件 P0 基石）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6847 页签管线性能门禁 | 完成 | 新 tests/tab-pipeline-perf.test.cjs：300 页签合成夹具压 sortItems×4 模式 + groupTabsByMode×3 模式（含组内 sortGroupItems），CPU 时间 best-of-3（T-6711 口径）。实测基线 sort 0.075ms / group 0.075ms / 管线 0.15ms；阈值按 ~20 倍余量校准（1.5/2/3.5ms）——O(n²) 退化（~36 倍，~5ms）必穿线。**负向验证：阈值收紧 0.01ms → 管线断言精确 FAIL → 还原绿**。既有 filterOpenTabs 300 页签门禁（search-filter-perf）不变，两者合计覆盖 §3.5 全预算 | 渲染层（DOM）帧预算归浏览器夹具，暂以模型层门禁+泄漏夹具组合覆盖 |
| T-6855 球面板泄漏夹具 | 完成 | floating-ball-browser-smoke 新增：EventTarget 补丁按目标记净增（WeakMap），**口径=持久节点（window/document/root）**——面板每轮重建的行元素随分离 DOM 被 GC 不计（首版全量净增 110 为过报假象，已修正口径）；开关 10 轮持久节点净增 0 断言 + openMore 同步装配+强制布局首帧预算（实测 3.2ms < 30ms）。**负向验证：注入孤儿 document 监听 → 审计报净增 1 → 移除归零**；--negative 块新增该场景 | T-6856 扩动作目录前的护栏就位 |
| T-6840 关联 SWR 持久化 | 完成 | ①模型：normalizeRelatedSwrStore（版本戳/7 天年龄上界/rootId 去重/有界 8/at 降序）+buildRelatedSwrStore，related-content-model 8 项单测（含 roundtrip 幂等）；②D-401 仪式：sw_related_swr 注册（constants PERSISTENT_KEYS 15→16、storage-migration HANDLED 13+处理器、agent 钳制 15→16、兼容矩阵文档 16 行、字面量总数断言同步）；③接线：onload 归一化载入、命中先渲染"·缓存"标注行（i18n workbenchRelatedCached）再后台刷新、终态清理守卫（已渲染行不误删）、成功取数后 FIFO≤8 落盘；④kernel-widgets-wiring 新增 SWR 契约 9 断言，**负向验证：终态守卫摘除 persisted 判断 → 精确红 → 还原绿** | 真机重启冷启动观感归 T-6305 |
| 门禁 | 完成 | 全套件 6139/6139（新增模型 5+契约 1+基准 1+冒烟 2 场景）+ build + E2E 5/1（备份工作区 b2；主工作区 .lock 陈旧句柄占用，进程清单核实无 E2E 内核，非用户主内核——主内核 21696 已核实 attach-ui 不动） | — |

## §8.0.19 第二波=恢复纵深（2026-09-25 开发完成，T-6841+T-6842~T-6844）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6844 合成名去重 | 完成 | dedupeSyntheticOutlineHeading（search-model）：内核合成文档名项与文档自带同名 h1 相邻重复时跳过合成项；异名不动。模型单测 2 断言（同名词去重+异名保持），90/90 | — |
| T-6843 预览悬停触发 | 完成 | mountDocPreviewPane 补 mouseover 委托，与 focusin 共用 300ms debounce 管线（同扫掠多次 hover 合并取末行）；触屏 tap 先 mouseover 后 click 无害 | — |
| T-6842 预览 sticky 吸顶 | 完成 | `.sw--with-preview` 从 absolute 覆盖层重构为 grid 两列（1fr+260px；label 跨两列/窗格 sticky top:0/加载更多 col1）；长列表滚动时窗格吸附视口顶部仍在场。审计截图复验布局正常 | — |
| T-6841 恢复报告 Essentials 明细+MD 导出 | 完成 | ①openDocumentSetEssentials 回执带逐项明细（rootId+status+失败原因，try/catch 捕获）；②buildDocumentSetRestoreReport 增补 options.essentials → report.essentials（counts+entries，非法条目剔除，无回执缺省不破坏旧消费方）；③documentSetRestoreReportToMarkdown 纯函数（条目行+失败原因码+Essentials 段）；④设置页恢复路径补跑 Essentials（对齐 T-6810"每次恢复"语义）并新增"导出 MD"按钮（与 JSON 同源）。document-sets 20/20 | — |
| 门禁 | 完成 | kernel-widgets-wiring 新增 wave-2 契约 9 断言；**负向验证：sticky 改回 absolute → 门禁精确红 → 还原绿**；全套件 6143/6143 + build + E2E 5/1（b2）+ 审计截图复验 | 真机导出文件观感归 T-6305 |

## §8.0.19 第三波=悬浮球扩充（2026-09-25 开发完成，T-6856+T-6845；包体余量复核 34.5KiB 无需 ADR）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6856 目录扩充 | 完成 | 内建 18→22（mark-set 标记位置/mark-jump 跳回标记/clipboard 剪贴板入口/close-tab 关闭当前页签，均复用既有命令实现）；global 9→12（fileTree/mainMenu/globalSearch，源码 global.ts 双分支核对）；executor 4 新 case + index 两处 executor 同步接线；closeActiveTabForAction 独立方法（无活动页签/失败明确回执）。**总目录 34 项** | T-6857 数字槽绑定 / T-6858 径向菜单 |
| T-6845 标签 i18n | 完成 | 目录全 34 条目携带 langKey；resolveQuickActionLabel 纯函数（i18n 命中→catalog 中文兜底，按 id 回查不依赖持久化字段）；三消费面接线（球面板 getFloatingBallActions/底栏/命令模式 catalog）——**P-F 中英混排收口**；sanitize 保留 langKey（受控字段，修复 sanitize 不动点）；i18n 门禁 action* 键入 ALLOWED_UNUSED（动态查表先例）+ 新增强校验"目录 langKey 必须双语存在" | — |
| 门禁 | 完成 | quick-actions 25/25（新增 2）、kernel-widgets-wiring 56/56（新增 wave-3 契约 11 断言）；**负向验证：executor clipboard 分发改名 → 门禁精确红 → 还原绿**；全套件 6146/6146 + build + E2E 5/1（b2）；包体 1013396 < 1024KiB（余 34.5KiB，无 ADR）；浮球宿主桩环境补 resolveQuickActionLabel 依赖 | — |

## §8.0.19 第四波=组件面板（2026-09-25 开发完成；含交接状态快照）

| 任务 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6850 P8 数据库视图形态 | 完成 | normalizeDatabaseListConfig 增 blockId（ID 形态校验）；index.ts database-list 绑定 blockId 时走 database-table 同款 AV 两级取数管线（块 ID 0 行 → getAttributeView 解析库 ID 重试）→ buildAvTableSnapshot 表格投影，**未绑定保持全库清单，库不可用回退清单不渲染空卡**；home-model configSchema 增 blockId 字段（database 选择器）；3 处门禁窗口/断言同步 | 真机绑库观感归 T-6305 |
| T-6851 面板状态持久化 | 完成 | settings-model 增 homeStore 字段（normalizeHomeStoreState：密度/视图模式白名单、sort 白名单、折叠分组 ≤32 条每条 ≤48 字去重）；home-store-ui 从设置载入初始值 + persistStoreState() 在排序/密度/视图模式/分组折叠/重置五处变更点回写；ISwSettings + DEFAULT_SETTINGS 同步 | — |
| T-6852/T-6854/T-6853 | 收敛 | 核查发现均已被覆盖：T-6854=失败卡内联重试按钮已存在（home-view.js:655 retry）；T-6852=组件卡配置入口已存在（home-view.js:268 onConfig 内联配置）；T-6853=商店分组计数徽标已存在（组标题 `${label} · ${groupCount}`）。同 T-6836 口径记收敛 | — |
| 门禁 | 完成 | 全套件 **6148/6148** + tsc --noEmit 干净 + build；kernel-widget-model 47/47（+2）、settings/widget 模型新测试；4 处过时正则锚点更新（home-store-contract/store-mobile-layout×3/store-ui-polish/store-rerender-focus——**修改 store UI 初始化或 handler 时这些契约用正则锚定源码，需同步**） | — |

## 交接状态快照（2026-09-25，交接给下一个 AI agent）

- **版本**：v0.36.0 已发布（releases/tag/v0.36.0）；本地领先 4 波开发提交未发版——**下一会话首选=v0.37.0 攒版发布**（流程见记忆/ledger：bump 双元数据→README 双语→ROADMAP 基线→CHANGELOG 双语→release-readiness 版本行→`pnpm exec tsc --noEmit` + `verify:release`→commit→push→tag→gh run watch→gh release view→E2E 部署验证）。
- **测试基线**：6148/6148 全绿；E2E 用备份工作区 `SWSS_E2E_WORKSPACE="C:\Users\sunku\SiYuan-SpeedSwitch-E2E-b2" SWSS_E2E_PORT=6839 pnpm run test:e2e`（主工作区 .lock 陈旧句柄占用，rm 报 busy，勿动用户主内核）。
- **验证顺序铁律**：改完先 `pnpm exec tsc --noEmit`（build 不做全量类型检查，CI 会拦）→ `pnpm test` → build → 涉发布再 verify:release。
- **§8.0.19 五波进度**：第一波护城 ✅（T-6847/6855/6840）、第二波恢复纵深 ✅（T-6841/6842/6843/6844）、第三波悬浮球扩充 ✅（T-6856/6845）、第四波组件面板 ✅（T-6850/6851 完成；6852/6854/6853 收敛）、**第五波交互升级 ⏸ 未开工**（T-6857 数字槽绑定保存的搜索、T-6858 径向快捷菜单、T-6848 页签卡更新时间/改动徽标、T-6849 首帧骨架评估——先测后做）。
- **候补/收敛口径**：P8 已随 T-6850 消化；marks 持久化/行内关闭/版本时间轴=已有覆盖或维持不实施；等外部证据项（T-6780 真机、3.8.6 stable 反链排序、T-6832 RPC/MCP 桥）不动。
- **工程红线**：Mimosa 拦 Bash 直写 src/tests（用 Edit）；`git add -A` 前确认无 .tmp-*；负向验证禁用 git checkout -- 回滚（用 Edit 反向替换）；性能门禁改阈值必须负向验证；契约测试用正则锚定源码片段，改源码结构时同步窗口/锚点。

## T-6859～T-6862 实验代码片段工作室（2026-09-25，继续开发）

| 任务ID | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6859 | 完成 | 固定 SiYuan v3.8.5 源码/schema；确认 `/api/snippet/getSnippet`、`/api/snippet/setSnippet`、`/api/setting/setSnippet`、`/api/ai/editor/chat` 的权限与请求体；ADR 0078 记录原生列表唯一来源、整表无 CAS、字体和包体分期 | 后续真实桌面宿主复核 |
| T-6860 | 完成（实验） | 桌面切换器工具栏入口；原生列表 store 串行读写、保存前选中行冲突比较、严格 `SnippetInput` 线投影、设置刷新通知、保存后回读；内置 5 条惰性 CSS、导入禁用草稿、导出/本地投稿包；`tests/snippet-studio-model.test.cjs`、`tests/snippet-studio-host.test.cjs` 通过 | 整表读写窗口仍无法做原子并发保护，已在 ADR 0078 明示 |
| T-6861 | 进行中 | CSS opaque-origin sandbox 预览；宽屏 4:1 工作室、筛选目录、响应式/键盘焦点/卸载清理；样式切片纳入 `index.scss`；同步 JS 执行存在 WebView 死循环冻结风险，执行按钮暂禁用；审计确认 Worker 无 DOM，不能冒充任意 SiYuan JS 预览 | 若继续实现只能先做受限模拟预览协议和负向测试，再补真实桌面截图 |
| T-6862 | 进行中 | `/api/ai/editor/chat` SSE 请求加入每次任务 `taskID`；生成/优化/解释/迭代、`history` 上限、UTF-8 上限、单代码块解析、取消/超时/过期回包、用户勾选同意和手动接受；`tests/snippet-studio-ai.test.cjs` 通过；官方没有无副作用的编辑器 AI 能力预探测 | 真实宿主 SSE 和更完整多轮体验待后续；不伪造能力预探测 |
| 门禁 | 完成（本轮本地） | `pnpm exec tsc --noEmit` 通过；完整 `pnpm test` **6184/6184**；专项与 bundle/layout 测试通过；生产构建产物 `dist/index.js` 1020120 B、`dist/index.css` 206024 B、`dist/snippet-studio.js` 44412 B、`package.zip` 478394 B；`pnpm run repro:audit` 3/3 一致；readiness snapshot、release-check、quality/integration audit 与四套 Chromium 冒烟均通过；窄屏选择器关闭焦点已加 `preventScroll` | 真实桌面/Android 宿主截图仍按 B-004/B-005 后置；T-6861/T-6862/T-6863 后续继续收口 |

## T-6863 片段工作室交互原型收口（2026-09-25）

| 任务ID | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6863 | 完成（实验原型） | 预览加入 iframe load 事件驱动的加载/就绪状态；详情卡显示当前名称、来源、启用状态、字节数和行数；比较按钮在没有保存基线时禁用；JS 执行按钮保留禁用原因；AI 增加模式说明、同意状态、结果空态与候选统计；选择器标记当前项并支持方向键/Home/End；移动端改为整页滚动，footer 不再覆盖编辑器和 AI；窄屏选择器固定于视口并恢复根/布局滚动位置；SCSS 复用全局动效 token。Chromium mock 交互审计 18/18，页面/控制台错误 0；截图见 `.tmp/snippet-ui-audit/` | 真实思源桌面截图与 Android 验收仍按 B-004/B-005 后置；JS 受限模拟预览另立任务，不在本任务解除安全边界 |

## T-6864 代码片段实验室二次调研（2026-09-25）

| 任务ID | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6864 | 完成（研究，无代码变更） | 报告 `docs/research-snippet-studio-2026-09-25.md`；复核当前 UI、model/host/AI 边界，并对比 TCOTC/snippets、siyuan-font-studio、Stylus、Raycast、VS Code、massCode/Gisto、Chrome DevTools。结论优先补 `disabledInPublish`、总开关引导、冲突 diff/另存为、全量备份恢复、顺序管理和编辑器基础能力；远程同步与任意 JS 执行后置 | 下一批按报告顺序立项；真实桌面/Android 仍按 B-004/B-005 后置 |

## T-6865 三大面板与统一切换平台再梳理（2026-09-25）

| 任务ID | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6865 | 完成（研究，无代码变更） | 报告 `docs/research-unified-switch-platform-2026-09-25.md`；复核第一面板、第二面板、悬浮球的源码、ADR、现行三大支柱账本，并对比 Quick Switch、Panda Navigation、VS Code、PowerToys、Obsidian、Raycast、Chrome Tab Search。定位升级为“思源笔记的统一切换与工作上下文平台”；下一步先统一对象/动作/现场/组件语义和能力状态，再补页签信息层级、组件健康回执与悬浮球平台回退入口 | 按 P0→P1 顺序另立实现任务；真实桌面/Android 仍按 B-004/B-005 后置 |

## T-6866 统一平台 UI、功能与交互设计（2026-09-25）

| 任务ID | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6866 | 完成（设计，无生产代码变更） | 设计规格 `docs/design-unified-platform-ui-2026-09-25.md` + ADR 0079；定义平台外壳、三表面构图、SurfaceContext/路由栈、对象/动作/状态协议、悬浮球平台入口、片段安全边界和桌面/移动端策略 | 先实现 P1 平台 token/公共原语与 PanelFrame 适配，再实现 P2 外壳路由；涉及契约门禁时按清单做负向验证，真实桌面/Android 继续按 B-004/B-005 后置 |

## T-6867 P1 统一视觉基础层与表面标记（2026-09-25）

| 任务ID | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6867 | 完成（P1-a，生产实现） | 新增 `src/styles/_platform-shell.scss` 和 `src/index.scss` manifest 接线；`--sw-platform-*` token 以宿主 `--b3-*`/既有 `--sw-*` 为来源，提供状态、动作、预览和无障碍原语；第一/第二/第三表面及 Dialog 增加静态标记。`pnpm exec tsc --noEmit`、Sass 编译、完整测试 **6184/6184**、生产构建、quality audit 50/50 通过 | P1-b 已由 T-6868 接入 |

## T-6868 统一平台表面导航与上下文条（2026-09-26）

| 任务ID | 状态 | 证据 | 下一步 |
|---|---|---|---|
| T-6868 | 完成（P1-b，生产实现） | `src/index.ts` 内联共享平台 DOM 适配器，提供 `SurfaceNav`、`ContextBar`、本地化文案和 `openPlatformSurface(surface, returnTo)`；切换器、工作台、片段实验室、侧栏和移动切换器挂载同一外壳，第二面板重绘后重新挂载；导航先销毁当前 Dialog，片段实验室的 `canClose()` 脏稿守卫、动态 chunk 和悬浮球挂起/恢复生命周期保持有效；`returnTo` 已贯穿桌面/移动切换器，工作台→切换器→片段实验室返回上下文正确；移动端只显示切换器/工作台。`pnpm exec tsc --noEmit`、完整测试 **6184/6184**、生产构建、`quality:audit` 50/50、`repro:audit` 3/3、release-check、生产图隔离 66/66、Chromium/浮球冒烟均通过 | P1-c 已由 T-6869 接入；真实桌面/Android 继续按 B-004/B-005 |

## T-6869 统一平台路由上下文、表面单例保护与悬浮球恢复（2026-09-26，P1-c 收口）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 纯模型层 | 完成 | 新增 `src/platform-surface-model.js`（零 DOM、零状态）：`PLATFORM_SURFACE_IDS` 冻结白名单、`normalizeSurfaceId` 回落、`normalizeSurfaceContext`（entry 白名单 toolbar/surface-nav/fab/back/command/breadcrumb/external/unknown + objectId ≤128/query ≤120 有界清洗，全空返回 null）、`resolveSurfaceReturnTarget`（上次表面在当前端可用清单内才恢复，否则回退切换器——覆盖"桌面开过实验室后到手机端"与会话外非法值）、`buildSurfaceContextCaption`（对象位恒为表面名、提示位有查询现场时显示查询） | — |
| 生产接线 | 完成 | `index.ts`：会话级 `lastPlatformSurface`/`lastPlatformContext` 记录（`notePlatformSurface`，`openPlatformSurface`/`showSwitcher`/`openSnippetStudio`/工作台 host 钩子四个入口全覆盖）；`openPlatformSurface(surface, returnTo, context?)` 上下文透传，表面导航统一 `{entry: "surface-nav"}`、工作室 Back `{entry: "back"}`、悬浮球 `{entry: "fab"}`、工具栏按钮 `{entry: "toolbar"}`；`getAvailablePlatformSurfaces()` 按端返回（移动端无实验室）；`mountPlatformChrome` 经 caption 投影渲染 ContextBar（snippet-studio-ui 透传 `platform.context`） | — |
| 单例保护 | 完成 | 桌面切换器 `platformSwitcherDialog`、移动切换器 `mobileSwitcherDialog`、工作台 `workbenchDialog` 三字段 + destroy-before-open 守卫（holder 模式在 destroyCallback 置空）——重复热键/悬浮球连点/多入口不再叠出多个平台弹窗；片段实验室保持既有 `snippetStudioDialog` 守卫与 `canClose()` 脏稿守卫（编辑现场由 `snippetStudioSession` 存续，重开自动恢复草稿） | — |
| 悬浮球恢复 | 完成 | `openPlatformFromBall()`：`resolveSurfaceReturnTarget(lastPlatformSurface, getAvailablePlatformSurfaces())` 决策后走 `openPlatformSurface`；仅悬浮球执行器（`executeFloatingBallSurfaceAction`）的 `onSwitcher` 改走恢复链，切换器工具栏/命令模式的共享执行器保持"打开切换器"语义不变 | — |
| 工作台编辑现场 | 完成 | `navigatePlatformSurface` 离开时 `workbenchResumeEditing = editing` 再销毁；`openSecondPanel` 打开时消费一次（仅表面导航离开的编辑态恢复，普通打开与 X 关闭保持查看态）；恢复时焦点放回工具栏布局开关（`preventScroll`） | — |
| 门禁 | 完成 | 生产图校准 66→67（`platform-surface-model` 入图，WIRED_SANITY 登记 + 注释留痕）；新 `tests/platform-surface-model.test.cjs` 4 项；kernel-widgets-wiring 新增 T-6869 契约 22 断言；**负向验证 5 注入全部精确红后字节还原**：①FAB 恢复接线回退 showSwitcher→T-6869 契约红 ②工作台单例守卫破坏→红 ③编辑现场记录改 false→红 ④生产图上限回退 66→图门禁红 ⑤模型可用性检查短路→模型测试红 | — |
| 全量验证 | 完成 | `pnpm exec tsc --noEmit` 干净；完整 `pnpm test` **6189/6189**（浮动球宿主桩按新依赖补 platform 模型导入 + 4 个平台路由方法后 15/15）；构建 `dist/index.js` 1028649 B（raw 线 1024 KiB 内余 ~19.9 KiB，无需 ADR）、`package.zip` 482831 B（<512 KiB）；readiness snapshot 回写、`quality:audit` 50/50、`integration:audit` 50/50、`repro:audit` 3/3、release-check、四套冒烟（浮球 24 场景）与 `verify:release` 全链 exit=0 | P2 跨表面对象/动作（统一对象描述、片段结果"在实验室打开"、objectId 级焦点恢复）；真实桌面/Android 按 B-004/B-005 后置 |

## T-6870 全插件统一 UI 原型与细节优化设计（2026-09-26，用户指令；实现分批立项 T-6871~）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 设计完成度盘点 | 完成 | 平台外壳+三表面+悬浮球=已有设计（ADR 0079/T-6866，P1-a/b/c 已实现）；**设置页 11 标签、对话框浮层、移动端页面此前无逐页原型**——本轮补齐 | — |
| 现代参考研究 | 完成 | Raycast/Linear（键盘优先 kbd 可见化、设置搜索）、Things/Craft（卡片层级留白）、macOS System Settings（分段控件/行分组）、Vercel Geist/shadcn（状态徽标/中性色阶）；提炼六条可应用模式（见设计文档 §1） | — |
| 原型图 | 完成 | `docs/design/platform-ui-redesign-2026-09-26.html` **26 屏**：平台表面 4（切换器/工作台/片段实验室/悬浮球）+ 设置页 11 标签全量 + 对话框浮层 6（捕获/历史/筛选/排序/组件配置/商店）+ 移动端 3 + 暗色对照 2；全部用**真实生产 DOM 类名 + dist/index.css** 渲染 + 提案层（RZ，全 `--b3-*` 变量） | 用户过目后分批实现 |
| 截图 | 完成 | `docs/design/redesign-2026-09-26/` 26 张 PNG（2x）；重放=`node scripts/design-screenshots.cjs`（联络图写 .tmp 不入库）；关键屏（切换器/设置外观/商店/移动切换器/悬浮球）目视复验 | — |
| 设计规格 | 完成 | `docs/design-unified-platform-ui-round2-2026-09-26.md`：逐屏设计说明 + 六条应用模式 + **细节优化清单立项 RZ-1~RZ-6（T-6871~T-6876：平台原语组件化/设置分组卡片/切换器细节/工作台商店细节/对话框统一/移动端细节）** | 按 RZ-1→RZ-6 顺序实现；每批独立可回滚；涉及契约按清单负向验证 |
| 无生产代码变更 | 完成 | 本轮纯设计（HTML/PNG/MD/截图脚本），零 src/ 改动 | — |

## T-6871 平台 UI 原语组件化（2026-09-26，RZ-1；T-6870 细节优化批第一件）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| SCSS 原语 | 完成 | `_platform-shell.scss` 补齐缺口原语（六态 `.sw-platform-status` 与 `.sw-platform-action` T-6867 已有）：kbd 芯片 `.sw-platform-kbd`（宿主代码字体+line token）、上下文栏 kbd 槽 `.sw-platform-context__kbd-hints`、分段控件 `.sw-platform-seg`/`__item`（激活态=bg 底+字重+微阴影）、胶囊 `.sw-platform-action--pill/--soft`、徽标语义色点 `::before`；coarse 指针下分段项 38px+容器 6px=44px 命中区 | — |
| DOM 助手 | 完成 | 新模块 `src/platform-dom.js`（零依赖、document 注入、生产图 67→68）：`createPlatformStatus`（六态白名单，非法回落中性不猜 ready）、`createPlatformKbd`、`createPlatformSegmented`（radiogroup/radio/aria-checked、空 value 剔除、非法 active 无高亮、同值不重复回调）、`createPlatformPillAction`；`tests/platform-dom.test.cjs` 5 项 jsdom 行为测试 | — |
| 首个消费点 | 完成 | 切换器上下文栏常驻键位提示组 `Tab/1-9/Enter/Alt 预览`（`kbdHints` 选项 + `platformKbdPreview` i18n 双语键）；空提示/空组不落 DOM | — |
| 对比度采样（真实达标修复） | 完成 | Chromium 冒烟新增平台原语采样点 5 个（ready/stale/error 徽标、kbd、seg 激活项）+ 夹具挂载块；**首次运行即抓到真实违规**：宿主 warning/error 原色做徽标小字 light-stale 2.9、dark-error 2.99 未达徽章线 3.0 → 徽标文字改语义色 68%/正文 32% 混合（保色相、过 3:1；采样解析器补 `color(srgb …)` color-mix 计算值格式）；解析修正使背景识别更准后 holiday 红 dark 2.91 同步混合修复（4.14）。终测 dark/light 全采样 PASS | — |
| 门禁与负向验证 | 完成 | 生产图 67→68（platform-dom 入图 + WIRED_SANITY + 注释留痕）；kernel-widgets-wiring 新增 T-6871 契约 16 断言（CSS 块级 declaresIn + 接线 + i18n）；**4 次注入全部精确红后还原**：①摘 kbdHints 守卫→契约红 ②分段切换去掉 is-active toggle→模型测试红 ③图上限回退 67→图门禁红 ④删 .sw-platform-kbd 块→契约红；对比度门禁的真实违规抓捕（2.9/2.99/2.91）即活体负向证据 | — |
| 全量验证 | 完成 | `pnpm exec tsc --noEmit` 干净；完整 `pnpm test` **6195/6195**；构建 `dist/index.js` 1030578 B（raw 线内余 ~18 KiB）、`package.zip` 483858 B；readiness snapshot 回写；四套冒烟全绿（浮球 24 场景、Chromium 含平台原语采样） | 下一批=RZ-2（T-6872 设置页分组卡片+行描述+枚举分段化） |

## T-6872 设置页分组卡片与枚举分段化（2026-09-26，RZ-2）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 宿主装配方法 | 完成 | index.ts 新增三个设置构建方法（SettingsSectionsHost 接口同步声明）：`settingGroupTitle`（组标题）、`settingGroupCard`（分组卡片容器）、`settingSegmented`（平台分段控件设置行，经 `createPlatformSegmented` 产出 radiogroup 语义） | — |
| 分段化（8 处） | 完成 | 2~4 个互斥取值的枚举全部改分段：皮肤(4)/面板尺寸模式(3)/dock 显示(3)/侧栏布局(2)/组件强调色(3)/工作台尺寸模式(4)/移动列数(3)/密度(舒适·紧凑 2)；列数(9)与排序(6)按设计保留下拉；写回语义不变（同 key 同值域，normalizeSettings 白名单兜底） | — |
| 分组卡片 | 完成 | 五个标签重组为语义分组：外观=主题与皮肤/切换器窗口/缩略图；行为=排序与密度/搜索与打开/受控能力；面板=列表与侧栏/左侧面板；组件面板=组件卡片/工作台窗口（custom 宽高条件卡）；移动端=卡片布局/兼容；`_02-settings.scss` 新增组标题（11px 大写字距 muted）与组卡片（10px 圆角边界+卡内行收敛负边距） | — |
| i18n | 完成 | 新增 15 键双语（settingsGroupTheme/Window/Thumbnails/SortDensity/SearchOpen/Agent/ListSidebar/Docks/Components/WorkbenchWindow/MobileLayout/Compatibility + densityLabel/densityComfortable/densityCompact）；删除被替换的 densityCompactLabel（唯一消费方已改）；两文件键集对齐 JSON 校验过 | — |
| 门禁与负向验证 | 完成 | kernel-widgets-wiring 新增 T-6872 契约（8 枚举分段断言+8 组标题断言+SCSS 块级 declaresIn+双语键+旧键删除）；T-6823 密度契约随分段形态更新（body 标记与写回值不变）；**2 次注入精确红后还原**（皮肤行标签拼接破坏分段匹配→红；删组卡圆角声明→红）；修复过程实录：i18n 锚点补丁曾覆盖 platformKbdPreview 键（行级替换用错锚点）→ 已补回并入契约防复发 | — |
| 全量验证 | 完成 | `pnpm exec tsc --noEmit` 干净；完整 `pnpm test` **6196/6196**（首跑 1 红=rss fixture 满载偶发，单独复测+全量复跑均过，与既有记录同型）；构建 `dist/index.js` 1031963 B（raw 线内余 ~16.2 KiB）、`package.zip` 484475 B；readiness snapshot 回写；四套冒烟全绿 | 设置页真实宿主逐标签截图对比归 SWSS_AUDIT 扩展/发布候选批；下一批=RZ-3（T-6873 切换器细节：数字角标 kbd 化+行图标容器+预览徽标） |

## T-6873 切换器细节：角标 kbd 化与预览状态徽标（2026-09-26，RZ-3）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 数字角标 kbd 化 | 完成 | 卡片与文档结果行角标（`data-sw-digit` attr() 渲染机制不变）皮肤改为 kbd 芯片：不透明 surface 底+hairline 边框+宿主代码字体+on-surface 文字——与 T-6871 键位提示同语言，不透明底在缩略图上依旧清晰（不回退 P1 用户反馈的可读性诉求） | — |
| 预览窗格状态徽标 | 完成 | doc-search-ui 引入 `createPlatformStatus`，新增 `setDocPreviewStatus`（头部徽标整体替换）：取数发起点=loading（docSearchPreviewStatusLoading），代际校验通过=ready（docSearchPreviewStatusReady）；空内容由既有提示行解释，徽标只陈述取数状态；头部改 flex 徽标右置（10px 小号）；i18n 两键双语 | — |
| 已覆盖项核查 | 完成 | 设计原型 01 的"行图标容器 28px"经核查已被现有 `.sw__doc-icon`（32px 圆角 8 瓦片+主色图标）覆盖，同 T-6836 口径记收敛 | — |
| 门禁与负向验证 | 完成 | kernel-widgets-wiring 新增 T-6873 契约（角标 attr()机制/surface 底/边框/代码字体块级断言 + 状态徽标 import/两态调用/头部 flex/右置/i18n 双语）；T-6820/T-6837 既有角标契约（attr() 渲染）不受影响继续通过；**2 次注入精确红后还原**（ready 调用改 loading→红；角标底色回退实色主色→红） | — |
| 全量验证 | 完成 | `pnpm exec tsc --noEmit` 干净；完整 `pnpm test` **6197/6197**；构建 `dist/index.js` 1032318 B（raw 线内余 ~16.3 KiB）、`package.zip` 484686 B；readiness snapshot 回写；四套冒烟全绿（Chromium 含对比度采样、浮球 24 场景） | 下一批=RZ-4（T-6874 工作台编辑横幅与商店动作胶囊） |

## T-6874 工作台编辑横幅与商店动作胶囊（2026-09-26，RZ-4）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 编辑横幅 | 完成 | second-panel-ui renderPanel 编辑态分支（工具栏之后）插入 `sw-home__edit-banner`：拖动提示文案（homeEditingHint 双语 i18n）+ 完成按钮（复用 homeDone，退出编辑态并重渲染，与工具栏开关同一 editing 状态源）；`_05-settings-widgets.scss` 新增横幅样式（主色 9% 软底+26% 边界+10px 圆角） | — |
| 商店动作胶囊 | 完成 | `_06-widgets-store.scss`：`__add` 改主色实底胶囊（pill 半径+primary 底+on-primary 字+hover 加深），`__configure` 改主色软底胶囊（12% 透明主色+primary 字）；**既有 store 契约全保留**（min-width 88/字重 600/焦点环/nowrap/touch-action，store-actions/store-focus 契约继续通过） | — |
| 门禁与负向验证 | 完成 | kernel-widgets-wiring 新增 T-6874 契约（横幅 edit 态分支/专用类/i18n/完成按钮写回+SCSS 块级断言+商店双胶囊+min-width 契约保留）；**2 次注入精确红后还原**（横幅分支条件恒假→红；add 胶囊底色回退 surface→红）；实录：readSourceText 剥注释，契约锚点不可落注释文本 | — |
| 全量验证 | 完成 | `pnpm exec tsc --noEmit` 干净；完整 `pnpm test` **6198/6198**；构建 `dist/index.js` 1032712 B（raw 线内余 ~15.9 KiB）、`package.zip` 484952 B；readiness snapshot 回写；四套冒烟全绿 | 健康徽标/回执条（需 home-controller 失败数据管道）拆 T-6874b 随反馈立项；下一批=RZ-5（T-6875 快速捕获对话框统一） |

## T-6875 快速捕获对话框统一（2026-09-26，RZ-5）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 目标段选分段化 | 完成 | `_08-home-store-cards.scss` 为 `sw-quick-capture__targets` 补分段控件外观（9px 圆角容器+透明项+激活 chip）；**修复真实视觉缺陷：`sw__target--active` 此前只有 JS 类切换、从无 CSS 规则，激活目标不可辨识**；T-6818 契约锚点（makeTargetButton/role=tab 结构）不动 | — |
| 保存胶囊+诚实 kbd 提示 | 完成 | save 按钮加 `sw-quick-capture__save` 专用类（主色实底胶囊，覆盖 outline 描边；disabled 半透明保留）；footer 新增键位提示组（createPlatformKbd：Ctrl+Enter=保存（既有 keydown 绑定如实呈现）、Esc=宿主关闭），只陈述真实绑定；actions 布局 hint 左置 | — |
| 门禁与负向验证 | 完成 | kernel-widgets-wiring 新增 T-6875 契约（targets 分段/激活 chip/保存胶囊/disabled 降透明/Ctrl+Enter 绑定存在/hints 槽+左置）；**2 次注入逐一精确红后还原**（hints 类名改写→红；激活 chip 删 font-weight→红） | — |
| 全量验证 | 完成 | `pnpm exec tsc --noEmit` 干净；完整 `pnpm test` **6199/6199**；构建 `dist/index.js` 1033069 B（raw 线内余 ~15.5 KiB）、`package.zip` 485242 B；readiness snapshot 回写；四套冒烟全绿 | 下一批=RZ-6（T-6876 移动端底栏堆叠与 sheet 审计） |

## T-6876 移动端底栏堆叠与 sheet 审计（2026-09-26，RZ-6，RZ 批次收口）

| 步骤 | 状态 | 证据 | 下一步 |
|---|---|---|---|
| 移动底栏堆叠 | 完成 | `_03-switcher-mobile.scss` 移动作用域 `.sw__quick-action` 改图标在上/10px 标签在下的堆叠布局（min-height 44px 命中区，图标 18px；仅 `.sw__mobile` 作用域，桌面底栏不变） | — |
| 收敛项审计 | 完成 | sheet 抓手（36×4 handle）/16px 顶圆角/safe-area 底padding/分区分隔线 经核查 `_05-settings-widgets.scss` 既有实现已完全覆盖设计目标（原型 24），同 T-6836 口径记收敛并以契约锁定防回退 | — |
| 门禁与负向验证 | 完成 | kernel-widgets-wiring 新增 T-6876 契约（堆叠 flex-direction/44px/10px 标签 + sheet 圆角与抓手收敛锁定）；**1 次注入精确红后还原**（删 flex-direction column→红） | — |
| 全量验证 | 完成 | `pnpm exec tsc --noEmit` 干净；完整 `pnpm test` **6200/6200**；构建 `dist/index.js` 1033069 B（raw 线内余 ~15.5 KiB）、`package.zip` 485275 B；readiness snapshot 回写；四套冒烟全绿 | **RZ-1~RZ-6 全部收口**；下一会话首选=v0.37.0 攒版发布（T-6869~T-6876 六批增量），其后=P2 跨表面对象/动作 |
