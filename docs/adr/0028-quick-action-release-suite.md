# ADR 0028: Quick Action Release Suite

The final provider release suite combines persistence separation, runtime
registry execution, surface capability boundaries, icon fallback, entry caps,
and malformed migration isolation. This suite is intentionally pure and fast;
real plugin-host execution remains a separate managed-host gate.
