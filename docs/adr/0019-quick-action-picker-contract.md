# ADR 0019: Quick Action Picker Contract

The settings picker preserves known core icons and assigns a safe plugin icon
fallback when a provider symbol is unavailable. Candidate filtering is stable,
case-insensitive, and independent from execution. Mobile capability hints must
distinguish unsupported entries from commands whose support is unknown.
