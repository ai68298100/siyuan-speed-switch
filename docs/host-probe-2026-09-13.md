# 真实宿主只读探测记录

探测日期：2026-09-13。

- 本机发现 `SiYuan-Kernel.exe` 进程，监听端口 6806 可建立 HTTP 连接。
- `GET /` 返回思源“访问授权”页面，说明当前端口没有可复用的已认证前端会话。
- 仓库 `tests/host/config.example.json` 的隔离测试端口为 6807；本次未启动测试实例，也未尝试猜测或提交认证信息。
- 因缺少授权上下文，未调用 `/api/filetree/listDocsByPath`，不把本次探测计入 T-107 的路径端点兼容证据。

后续真实桌面验收仍需在用户明确空闲、可提供合法测试会话时进行；探测只验证端口/认证状态，不改变宿主数据。
